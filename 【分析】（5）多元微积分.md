# 多元微积分

## 微分学

### 全微分

多元函数总可看成关于向量的一元映射。而向量不存在除法的概念，因此一元函数导数的定义无法迁移到多元函数。

但微分的概念对多元函数依然适用。以二元函数 $z=f(x,y)$ 为例，其图象为三维空间中一曲面，  
若将其某一点附近的图象无限放大，则可局部近似为平面，也就相应的在该点有唯一的切面。

<div align=center> <img src=./dvec0.png width=80%></div>

   $$\Downarrow\texttt{放大}$$

<div align=center> <img src=./dvec1.png width=40%></div>

相应的，对于 $n$ 元函数 $f(\vec{x})$，其图象为 $n+1$ 维空间中一 $n$ 维流形  
若将其某一点附近的图象无限放大，则可局部近似为 $n$ 维超平面。

综上，对于 $n$ 元函数 $y=f(\vec{x})$，若

$$\Delta y=f(\vec{x}_0+\Delta\vec{x})-f(\vec{x}_0)=\vec{a}^T\Delta\vec{x}+\omicron(\Delta\vec{x})\qquad(\Delta\vec{x}\rightarrow \vec{0})$$

则称

$$\mathrm{d}y=\vec{a}^T\Delta\vec{x}$$

为 $\vec{x}_0$ 处 $y$ 关于 $\vec{x}$ 的 **全微分**。所有点处的全微分构成的关于 $\vec{x}$ 的函数

$$\mathrm{d}y=\vec{a}^T(\vec{x})\Delta\vec{x}$$

称为 $y$ 关于 $\vec{x}$ 的全微分。相应地，记

$$\dfrac{\mathrm{d}y}{\mathrm{d}\vec{x}}=\vec{a}^T(\vec{x})$$

为 $y$ 关于 $\vec{x}$ 的 **全导数**，$\vec{a}(\vec{x})$ 称为 $f(\vec{x})$ 的 **梯度**，其就是多元函数在某一点的切面的梯度。

-------------------------------------------

标量关于列向量的导数为一行向量，列向量关于标量的导数为一列向量。  
由此猜测，列向量关于列向量的导数为一矩阵。

对于 $\R^m\rightarrow \R^n$ 的映射 $\vec{y}=\vec{f}(\vec{x})$，若

$$\Delta\vec{y}=\vec{f}(\vec{x}_0+\Delta\vec{x})-\vec{f}(\vec{x}_0)=A\Delta\vec{x}+\omicron(\Delta\vec{x})\qquad(\Delta\vec{x}\rightarrow \vec{0})$$

其中 $A$ 为 $n$ 行 $m$ 列矩阵，则记 

$$\mathrm{d}\vec{y}=A\Delta\vec{x}$$


为 $\vec{x}_0$ 处 $\vec{y}$ 关于 $\vec{x}$ 的 **全微分**。所有点处的全微分构成的关于 $\vec{x}$ 的函数

$$\mathrm{d}\vec{y}=A(\vec{x})\Delta\vec{x}$$

称为 $\vec{y}$ 关于 $\vec{x}$ 的全微分。

也就是说，我们将向量到向量的映射局部近似为线性映射。相应地，记

$$\dfrac{\mathrm{d}\vec{y}}{\mathrm{d}\vec{x}}=A(\vec{x})$$

为 $\vec{y}$ 关于 $\vec{x}$ 的 **全导数**。

事实上，对于任意满足完备性的赋范线性空间，均可定义其上的极限，进而定义其上映射的全微分。

#### 全微分的性质

通过全微分的定义，可证明全微分和全导数具有以下性质：

- 唯一性：若 $f(\vec{x})$ 可微，则 $\dfrac{\mathrm{d}f(\vec{x})}{\mathrm{d}\vec{x}}$ 是唯一的 
- 线性性：$\mathrm{d}(\vec{u}+\vec{v})=\mathrm{d}\vec{u}+\mathrm{d}\vec{v},\mathrm{d}(\lambda\vec{u})=\lambda\mathrm{d}\vec{u},\mathrm{d}(A\vec{u})=A\mathrm{d}\vec{u}$（$\lambda$ 为常数，$A$ 为常矩阵）
- 乘法法则：$\mathrm{d}(u\vec{v})=u\mathrm{d}\vec{v}+\vec{v}\mathrm{d}u,\mathrm{d}(\vec{u}^T\vec{v})=\vec{u}^T\mathrm{d}\vec{v}+\vec{v}^T\mathrm{d}\vec{u}$
- 链式法则（线性不变性）：$\dfrac{\mathrm{d}\vec{y}}{\mathrm{d}\vec{x}}=\dfrac{\mathrm{d}\vec{y}}{\mathrm{d}\vec{u}}\dfrac{\mathrm{d}\vec{u}}{\mathrm{d}\vec{x}}$
- 方向导数：设 $\vec{e}$ 为一单位向量，若 $f(\vec{x})$ 存在全微分，则 $f(\vec{x})$ 沿 $\vec{e}$ 方向的变化率（方向导数）为 $\dfrac{\mathrm{d} f}{\mathrm{d}\vec{x}}\vec{e}$ 。

证明与一元函数的求导法则类似，故从略。

#### 方向导数与等势面

设 $\vec{e}$ 为一单位向量，则可定义 $f(\vec{x})$ 沿 $\vec{e}$ 的 **方向导数**

$$
\lim_{\lambda\rightarrow 0}\dfrac{f(\vec{x}+\lambda\vec{e})-f(\vec{x})}{\lambda}
$$

由全微分的定义及其几何意义，若 $f(\vec{x})$ 的全微分存在，则其方向导数为

$$\dfrac{\mathrm{d}f}{\mathrm{d}\vec{x}}\vec{e}$$

我们称 $f(\vec{x})=C$（$C$ 为常数）为 $f(\vec{x})$ 图像上的一个 **等势面**。  
由全微分与方向导数的关系可知，**等势面上，多元函数的梯度总与该等势面垂直**。

### 偏导数

多元函数的 **偏导数** 即将其他自变量视为常数，求某一自变量的导数。例如二元函数 $z=f(x,y)$ 的偏导数

$$f_x(x,y)=\dfrac{\partial z}{\partial x}=\lim_{h\rightarrow 0}\dfrac{f(x+h,y)-f(x,y)}{h}$$

$$f_y(x,y)=\dfrac{\partial z}{\partial y}=\lim_{h\rightarrow 0}\dfrac{f(x,y+h)-f(x,y)}{h}$$

相应地，将其他自变量视为常数，求某一自变量的微分称为 多元函数的 **偏微分**。  
例如，$z$ 关于 $x$ 的偏微分记为 $\partial_x z$。

#### 与全微分/全导数的关系

由多元函数极限的定义，在多元函数的全微分定义

$$\Delta y=\vec{a}^T\Delta\vec{x}+\omicron(\Delta\vec{x})\qquad(\Delta\vec{x}\rightarrow \vec{0})$$

里，$\Delta\vec{x}\rightarrow \vec{0}$ 的过程中，$\Delta\vec{x}$ 的某几个分量是可以为 $0$ 的，只要保证所有分量不同时为 $0$ 即可。  
因此令某一个分量非 $0$，其余分量均为 $0$，可得

$$\Delta y=\sum_k\dfrac{\partial y}{\partial x_k}\Delta x_k+\omicron\left(\sqrt{\sum_k \Delta x_k^2}\right)\qquad(\sum_k \Delta x_k^2\rightarrow 0)$$

$$=\begin{bmatrix}
	\dfrac{\partial y}{\partial x_1}&\dfrac{\partial y}{\partial x_2}&\cdots&\dfrac{\partial y}{\partial x_n}
\end{bmatrix}\Delta\vec{x}+\omicron(\Delta\vec{x})\qquad(\Delta\vec{x}\rightarrow 0)$$

因此若多元函数在某一点的全微分存在，则其在该点的偏导数均存在，且

$$\dfrac{\mathrm{d}y}{\mathrm{d}\vec{x}}=\begin{bmatrix}
	\dfrac{\partial y}{\partial x_1}&\dfrac{\partial y}{\partial x_2}&\cdots&\dfrac{\partial y}{\partial x_n}
\end{bmatrix}$$

相应地，对于 $\R^m\rightarrow \R^n$ 的映射 $\vec{y}=\vec{f}(\vec{x}),\vec{y}$，若其在某一点的全微分存在，  
则 $\forall i\in[1,n]\cap\Z,j\in[1,m]\cap\Z,\dfrac{\partial y_i}{\partial x_j}$ 均存在，且

$$
\dfrac{\mathrm{d}\vec{y}}{\mathrm{d}\vec{x}}=\begin{bmatrix}\dfrac{\partial y_1}{\partial x_{1_{}}}&\dfrac{\partial y_1}{\partial x_2}&\cdots&\dfrac{\partial y_1}{\partial x_m}\\\dfrac{\partial y_2}{\partial x_1}&\dfrac{\partial y_2}{\partial x_2}&\cdots&\dfrac{\partial y_2}{\partial x_m}\\\vdots&\vdots&\ddots&\vdots\\\dfrac{\partial y_n}{\partial x_1}&\dfrac{\partial y_n}{\partial x_2}&\cdots&\dfrac{\partial y_n}{\partial x_m}\end{bmatrix}
$$

该矩阵称为 **雅各比矩阵**。

--------------------------------------------

但以上结论反之并不成立。也就是说，偏导数存在只是全微分存在的充要条件。不过我们有

> （充分条件）：若多元函数 $y=f(\vec{x})$ 在某一点的偏导数均存在且连续，则其在该点的全微分存在。

以上讨论实际上给出了用偏导数计算全微分的方法，以及用全微分计算多元复合函数的偏导数的方法。

#### 高阶偏导数

与一元函数的高阶导数相同，可定义多元函数的高阶偏导数。以二元函数 $z=f(x,y)$ 为例，其二阶偏导数为

$$f_{xx}(x,y)=\dfrac{\partial z}{\partial x^2}=\dfrac{\partial}{\partial x}\left(\dfrac{\partial z}{\partial x}\right),f_{xy}(x,y)=\dfrac{\partial z}{\partial x\partial y}=\dfrac{\partial}{\partial x}\left(\dfrac{\partial z}{\partial y}\right)$$
$$f_{yx}(x,y)=\dfrac{\partial z}{\partial y\partial x}=\dfrac{\partial}{\partial y}\left(\dfrac{\partial z}{\partial x}\right),f_{yy}(x,y)=\dfrac{\partial z}{\partial y^2}=\dfrac{\partial}{\partial y}\left(\dfrac{\partial z}{\partial y}\right)$$

> 求证：若区域 $D$ 中 $\dfrac{\partial z}{\partial x\partial y},\dfrac{\partial z}{\partial y\partial x}$ 均存在且连续，则二者在 $D$ 内相等。
>

这一结论推广到多元函数也是成立的。换言之，连续的高阶偏导数各个自变量的求导次序可以交换。

### 多元函数微分学的应用

#### 隐函数的导数

对于两个变量 $x,y$，有时候我们并不能直接得到二者的函数关系，而只能通过方程

$$f(x,y)=0$$

来隐式确定 $x,y$ 的函数关系，这称为 $y$ 关于 $x$ 的 **隐函数**。

$f(x,y)=0$ 是 $f(x,y)$ 图像上的等势线，$f(x,y)$ 在其上的梯度总与该等势线垂直。

因此若 $f(x,y)$ 在点 $(x_0,y_0)$ 存在全微分，设 $f(x,y)=0$ 在该点的方向向量为 $\vec{e}$，则

$$\begin{bmatrix}f_x(x_0,y_0),f_y(x_0,y_0)\end{bmatrix}\vec{e}=0$$

于是隐函数在该点的导数为

$$\dfrac{\mathrm{d}y}{\mathrm{d}x}=-\dfrac{f_x}{f_y}$$

这就是隐函数的导数公式。其在形式上相当于对等式两边同时求微分

$$f_x\mathrm{d}x+f_y\mathrm{d}y=0$$

然后计算两个微分相除。

----------------------------------------------

显然，以上公式可以推广到具有三个及以上变量

$$f(x_1,x_2,\cdots,x_n)=0$$

的情形，此时有

$$\dfrac{\partial x_i}{\partial x_j}=-\dfrac{f_{x_i}}{f_{x_j}}$$

其在形式上相当于对原方程两边同时求全微分

$$\sum_{k}f_{x_k}\mathrm{d}x_k=0$$

然后将其中某一个变量的全微分用其他变量的全微分表示。



#### 极值与条件极值

同一元函数的极值，对于定义域为 $n$ 维区域 $D$ 的 $n$ 元函数 $y=f(\vec{x})$，若其在 $\vec{x}_0$ 处取极值且存在偏导数，则有

$$\forall k\in[1,n]\cap\Z,f_{x_k}(\vec{x}_0)=0$$

这是极值存在的必要条件。可以通过在该点对多元函数作泰勒展开，进一步判断其是否为极值点。

-----------------------------------

除了一般意义下的极值，另一重要问题是所谓 **条件极值**，即我们希望求 $n$ 维区域 $D$ 内 $n$ 元函数 $y=f(\vec{x})$ 在

$$g(\vec{x})=0$$

若 $f(\vec{x}),g(\vec{x})$ 在区域 $D$ 内均存在全微分，在某一点取条件极值需保证   
$f(\vec{x})$ 在该点沿等势面 $g(\vec{x})=0$ 上任意方向的变化率均为 $0$，即 $f(\vec{x})$ 的梯度与等势面 $g(\vec{x})=0$ 垂直。

又等势面 $g(\vec{x})=0$ 上 $g(\vec{x})$ 的梯度亦与该等势面垂直，因此 $f(\vec{x})$ 与 $g(\vec{x})$ 的梯度平行，即

$$\exist\lambda\in\R,\dfrac{\mathrm{d}f(\vec{x})}{\mathrm{d}\vec{x}}+\lambda \dfrac{\mathrm{d}g(\vec{x})}{\mathrm{d}\vec{x}}=\vec{0}^T$$

将该方程与 $g(\vec{x})=0$ 联立，即可解得所有可能的条件极值点。

也就是说，构造所谓的 **拉格朗日函数**

$$
L\begin{pmatrix}
	\vec{x}\\\hline\lambda
\end{pmatrix}=f(\vec{x})+\lambda g(\vec{x})
$$

则 $L$ 全导数为 $\vec{0}^T$ 的点就是 $f(\vec{x})$ 在 $g(\vec{x})=0$ 条件下的可能极值点。  
该方法称为 **拉格朗日乘子法**。解是否为极值点可根据具体情况来判定。

显然拉格朗日乘子法可推广到多个约束条件

$$\vec{g}(\vec{x})=\vec{0}$$

的情形，此时要构造的拉格朗日函数为

$$
L\begin{pmatrix}
	\vec{x}\\\hline\vec{\lambda}
\end{pmatrix}=f(\vec{x})+\vec{\lambda}^T \vec{g}(\vec{x})$$

#### 梯度、散度与旋度*

以三元函数为例，定义 **Nabla** 算子

$$
\nabla=\begin{bmatrix}
	\dfrac{\partial}{\partial x_{}}\\
	\dfrac{\partial^{^{^{}}}}{\partial y_{}}\\
	\dfrac{\partial^{^{^{}}}}{\partial z}
\end{bmatrix}
$$

那么对于三元函数 $f(x,y,z)$，我们有所谓 **梯度**

$$
\operatorname{grad} f=\nabla f=\begin{bmatrix}
	\dfrac{\partial f}{\partial x_{}}\\
	\dfrac{\partial f^{^{^{}}}}{\partial y_{}}\\
	\dfrac{\partial f^{^{^{}}}}{\partial z}
\end{bmatrix}
$$

其意义已在【模】（7）计算几何中说明，不再赘述。

而对于 $\R^3\rightarrow\R^3$ 的映射 $\vec{f}(x,y,z)$，我们有所谓 **散度**

$$
\operatorname{div}\vec{f}=\nabla\cdot\vec{f}=
\begin{bmatrix}
	\dfrac{\partial f_1}{\partial x_{}}\\
	\dfrac{\partial f_2^{^{^{}}}}{\partial y_{}}\\
	\dfrac{\partial f_3^{^{^{}}}}{\partial z}
\end{bmatrix}
$$

其物理意义是，若将 $\vec{f}(x,y,z)$ 视为三维空间中稳定流动的不可压缩流体在某一点的流速，则

- $\operatorname{div}\vec{f}>0\Leftrightarrow$ 流体从该点向外发散
- $\operatorname{div}\vec{f}<0\Leftrightarrow$ 流体向该点汇聚

最后，还有所谓 **旋度**

$$
\operatorname{rot} f=\nabla\times\vec{f}=\begin{bmatrix}
	\dfrac{\partial f_3}{\partial y}-\dfrac{\partial f_2}{\partial z_{_{_{}}}}\\
	\dfrac{\partial f_1}{\partial z}-\dfrac{\partial f_3}{\partial x_{_{}}}\\
	\dfrac{\partial f_2}{\partial x}-\dfrac{\partial f_1}{\partial y}
\end{bmatrix}
$$

其几何意义是，若刚体绕原点 $O$ 转动，其某一点的速度为 $\vec{v}$，角速度为 $\vec{\omega}$，则

$$\vec{v}=2\vec{\omega}$$

其他维度的空间是类似的。

## 积分学

### 曲线积分

#### 第二类曲线积分

对于 $\R^n$ 中一力场 $\vec{F}(\vec{r})$ 和一有向曲线 $L$，考虑质点沿 $L$ 运动时 $\vec{F}$ 所做的功。  
将 $L$ 分割为若干小段，每段视为直线，$\vec{F}$ 在每一小段视为恒力，在分割得足够精细时，$\vec{F}$ 所做的功

$$W=\int_L\vec{F}(\vec{r})\cdot\mathrm{d}\vec{r}=\int_L\vec{F}^T(\vec{r})\mathrm{d}\vec{r}=\int_L \sum_k F_k\mathrm{d}x_k$$

称为 $\vec{F}$ 沿 $L$ 的 **曲线积分**。其称为 **第二类曲线积分** 或 **对坐标的曲线积分**。  
特别地，若 $L$ 为闭合曲线，积分号一般用 $\displaystyle\oint$ 而非 $\displaystyle\int$ 来表示。

第二类曲线积分可以借助参数方程来计算，具体地，若有向曲线 $L$ 为多元函数 $\vec{r}(t)$ 的图象，  
$\vec{r}(t_0),\vec{r}(t_1)$ 分别为 $L$ 的起点、终点，则 

$$\int_L\vec{F}^T(\vec{r})\mathrm{d}\vec{r}=\int_{t_0}^{t_1}\vec{F}^T(\vec{r})\vec{r}'(t)\mathrm{d}t$$

-----------------------------------------------

全微分与对坐标的曲线积分是一对互逆的运算。具体地，若存在 $W(\vec{r})$ 使得 $\vec{F}^T=\dfrac{\mathrm{d}W}{\mathrm{d}\vec{r}}$，

且 $\vec{r}_0,\vec{r}_1$ 分别为 $L$ 的起点、终点，则

$$\int_L\vec{F}^T(\vec{r})\mathrm{d}\vec{r}=W(\vec{r}_1)-W(\vec{r}_0)$$

这就是 **曲线积分的基本定理**。

特别地，如果 $L$ 为闭合曲线，那么在上述情况下，$L$ 上的第二类曲线积分

$$\oint_L\vec{F}^T\mathrm{d}\vec{r}=0$$

该结论等价于曲线积分只与起点、终点有关，与路径无关。

#### 第一类曲线积分

与曲线积分相关的问题中，常见的是由曲线形物体 $L$ 的线密度 $\mu(\vec{r})$ 求其质量 $m$ 的问题。

同样地，将 $L$ 分割为若干小段，每段视为直线，$\mu(\vec{r})$ 视为恒量。在分割得足够精细时，物体质量

$$m=\int_L\mu(\vec{r})\mathrm{d}s$$

称为 $\mu(\vec{x})$ 沿 $L$ 的 **第一类曲线积分** 或 **对弧长的曲线积分**。

第一类曲线积分同样可以借助参数方程来计算。即

$$\int_L\mu(\vec{r})\mathrm{d}s=\int_{t_0}^{t_1}\mu(\vec{r})\left|\dfrac{\mathrm{d}\vec{r}}{\mathrm{d}t}\right|\mathrm{d}t$$

此外，其与第二类曲线积分的联系为

$$\int_L\vec{F}^T\mathrm{d}\vec{r}=\int_L\left(\vec{F}^T\vec{\tau}\right)\mathrm{d}s$$

其中 $\tau(\vec{r})$ 为沿 $L$ 的单位切向量。

### 流形上的积分

#### 重积分与三维空间的曲面积分

对于 $\R^n$ 中一块物体 $\Omega$，设其密度为 $\rho(\vec{x})$，将其分割成若干小块，每一小块的密度视为常数，  
则在分割得足够精细时，该物体的质量

$$m=\int_\Omega\rho\mathrm{d}x_1\mathrm{d}x_2\cdots\mathrm{d}x_n$$

称为 $\rho(\vec{x})$ 在闭区域 $\Omega$ 上的 **$n$ 重积分**。例如

- 二重积分：$\displaystyle\iint_{D_{}}\mu(x,y)\mathrm{d}x\mathrm{d}y=\iiint_D\mu(x,y)\mathrm{d}\sigma$
- 三重积分：$\displaystyle\iiint_\Omega\rho(x,y,z)\mathrm{d}x\mathrm{d}y\mathrm{d}z=\iiint_\Omega\rho(x,y,z)\mathrm{d}V$

重积分内各个自变量是相互独立的，因此可以化为累次积分计算

$$\int_\Omega\rho\mathrm{d}x_1\mathrm{d}x_2\cdots\mathrm{d}x_n=\int_{l_n}^{r_n}\left(\int_{l_{n-1}(x_n)}^{r_{n-1}(x_n)}\left(\cdots\left(\int_{l_1(x_1,x_2,\cdots,x_n)}^{r_1(x_1,x_2,\cdots,x_n)}\rho\mathrm{d}x_1\right)\cdots\right)\mathrm{d}x_{n-1}\right)\mathrm{d}x_n$$

#### 微分形式及其积分*

#### 斯托克斯公式
对于 定向分段光滑 $k$ 维流形 $S$ ，设其边界为 $\partial S$，  
若对于其中的微分形式 $\omega$，其外微分 $\mathrm{d}\omega$ 存在，则有 **一般的斯托克斯公式**

$$\int_S\mathrm{d}\omega=\int_{\partial S}\omega$$

其中 $S$ 与 $\partial S$ 的定向相同。  
也就是说，流形上的积分和流形边界上的积分可以相互转化。  
一维空间中，直线的边界就是两个点，因此一般的斯托克斯公式是牛顿-莱布尼兹公式的推广。

该公式在具体的流形上的积分的表示如下

- 格林公式（二维空间曲线积分与重积分）：$\displaystyle\iint_D\left(\dfrac{\partial Q}{\partial x}-\dfrac{\partial P}{\partial y}\right)\mathrm{d}x\mathrm{d}y=\oint_{\partial D}P\mathrm{d}x+Q\mathrm{d}y$
- 高斯公式（三维空间曲面积分与重积分）：$\displaystyle\iiint_\Omega (\nabla\cdot\vec{f})\mathrm{d}V=\oiint_{\partial\Omega}\vec{f}\cdot\mathrm{d}\vec{S}$
- 斯托克斯公式（三维空间曲线积分与曲面积分）：$\displaystyle\iint_{\Sigma}(\nabla\times\vec{f})\cdot\mathrm{d}\vec{S}=\oint_{\partial\Sigma}\vec{f}\cdot\mathrm{d}\vec{r}$

此外，该公式的重要推论是闭合流形的积分为 $0$ 的条件，以及用全微分求曲线积分的条件。

> 求证：若 $\vec{f}(\vec{r})$ 各分量在区域 $D$ 内存在一阶连续偏导数，则区域 $D$ 内，   
> $\qquad\quad\nabla\times\vec{f}(\vec{r})=\vec{0}$ 恒成立 是 存在 $W(\vec{r})$ 使得 $\dfrac{\mathrm{d}\vec{W}}{\mathrm{\mathrm{d}\vec{r}}}=\vec{f}^T$ 的充要条件。