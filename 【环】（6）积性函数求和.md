## 积性函数求和

在得到了贝尔级数的情况下，可以用 $\Theta(n)$ 的线性筛求出积性函数 $f$ 的前 $n$ 项。

然而，对于积性函数的前 $n$ 项和

$$S_f(n)=\sum_{k=1}^nf(k)$$

其求解的时间复杂度可以低于 $\Theta(n)$ 。例如，  
幂函数的前 $n$ 项和有对应的通项公式（详见【分析】有限微积分与无限微积分）。

积性函数求和问题的核心是通过狄利克雷卷积，将原积性函数的求和转化为幂函数的求和，下面将讨论这一问题。

### 整除分块（求解 $S_{f*g}$）

对于数论函数 $f,g$，整除分块允许我们从 $S_f,S_g$ 导出 $S_{f*g}$：  

$$S_{f*g}(n)=\sum_{k=1}^n(f*g)(n)=\sum_{k=1}^n\sum_{ij=k}f(i)g(j)$$
$$=\sum_{i=1}^nf(i)\sum_{j=1}^{\left\lfloor\frac{n}{i}\right\rfloor}g(j)=\sum_{i=1}^nf(i)S_g\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

由此解决了 $\sigma_k$ 的求和问题：

$$\sigma_k=\mathrm{id}_k*I,S_{\sigma_k}(n)=\sum_{i=1}^n \mathrm{id}_k(i)S_I\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=\sum_{i=1}^n i^k\left\lfloor\frac{n}{i}\right\rfloor$$

对于多个数论函数的狄利克雷卷积的前缀和，由带余除法的性质 1. 可知，  
计算过程中始终只涉及前缀和在 $\left\lfloor\dfrac{n}{i}\right\rfloor$ 处的取值，故**可用哈希表记忆化来优化计算**。      


> 求证：若求解 $S_f(n),S_g(n),S_{h}(n)$ 的复杂度均为 $\Theta(1)$，则记忆化下整除分块求解 $S_{f*g*h}(n)$ 的复杂度为 $\Theta(n^{\frac{3}{4}})$ 。  
> 证明*：我们需要用哈希表记忆化
> $$S_{f*g}\left(\left\lfloor\dfrac{n}{1}\right\rfloor\right),S_{f*g}\left(\left\lfloor\dfrac{n}{2}\right\rfloor\right),\cdots,S_{f*g}\left(\left\lfloor\dfrac{n}{\lfloor\sqrt{n}\rfloor}\right\rfloor\right),$$
> $$S_{f*g}(\lfloor\sqrt{n}\rfloor),S_{f*g}(\lfloor\sqrt{n}\rfloor-1),\cdots,S_{f*g}(1)$$
> $\qquad\quad$ 其中 $S_{f*g}(t)$ 的求解枚举了 $S_f$ 和 $S_g$ 的 $2\lfloor\sqrt{t}\rfloor$ 个位置的值。故总的时间复杂度为
> $$\sum_{i=1}^{\lfloor\sqrt{n}\rfloor}\left(\Theta\left(\sqrt{i}\right)+\Theta\left(\sqrt{\dfrac{n}{i}}\right)\right)=\Theta\left(\sum_{i=1}^{\lfloor\sqrt{n}\rfloor}\sqrt{i}+\sqrt{n}\sum_{k=1}^{\lfloor\sqrt{n}\rfloor}\dfrac{1}{\sqrt{i}}\right)$$
> $\qquad\quad$ 用积分估算求和，时间复杂度即
> $$\Theta\left(\int_0^{\sqrt{n}}\sqrt{x}\mathrm{d}x+\sqrt{n}\int_0^{\sqrt{n}}\dfrac{\mathrm{d}x}{\sqrt{x}}\right)=\Theta\left(\dfrac{2}{3}(\sqrt{n})^{\frac{3}{2}}+\sqrt{n}\times2(\sqrt{n})^{\frac{1}{2}}\right)=\Theta(n^{\frac{3}{4}})$$

记忆化保证了整除分块在此情形下的亚线性时间复杂度。但我们还可以作如下的**折半优化**：  
分成 $x\le k$ 和 $x\ge k$ 两段，$x\le k$ 部分的 $S_{f*g*h}(x)$ 直接线性筛求解，  
$x\ge k$ 部分的 $S_{f*g*h}(x)$ 再使用整除分块求解。

> 求证：在使用记忆化和折半优化的情况下，若求解 $S_f(n),S_g(n),S_{h}(n)$ 的复杂度均为 $\Theta(1)$，  
> $\qquad\quad$ 则 $k=n^{\frac{2}{3}}$ 时，计算 $S_{f*g*h}$ 的最优复杂度为 $\Theta(n^{\frac{2}{3}})$ 。  
> 证明*：由于使用了折半优化，只要计算 $k\le\left\lfloor\dfrac{n}{i}\right\rfloor\le n$ 的 $S_{f*g}\left(\left\lfloor\dfrac{n}{i}\right\rfloor\right)$。  
> $\qquad\quad$ 由仅使用记忆化下时间复杂度的证明可知 $k\le\sqrt{n}$ 时复杂度没有优化，$k>\sqrt{n}$ 时复杂度变为
> $$\Theta(k)+\sum_{i=1}^{\lfloor\frac{n}{k}\rfloor}\Theta\left(\sqrt{\dfrac{n}{i}}\right)=\Theta(k)+\Theta\left(\sqrt{n}\int_{0}^{\frac{n}{k}}\dfrac{\mathrm{d}x}{\sqrt{x}}\right)$$
> $$=\Theta(k)+\Theta\left(\sqrt{n}\times2\sqrt{\dfrac{n}{k}}\right)=\Theta\left(k+\dfrac{n}{\sqrt{k}}\right)$$
> $\qquad\quad$ 故 $k=\dfrac{n}{\sqrt{k}}$ 时，即 $k=n^{\frac{2}{3}}$ 时，取最优复杂度 $\Theta(n^{\frac{2}{3}})$ 。

三个数论函数的情况显然可以推广到多个数论函数，故省略。

### 杜教筛

整除分块允许我们从 $S_f,S_g$ 导出 $S_{f*g}$，在此基础上，**杜教筛** 允许我们从 $S_f,S_g$ 导出 $S_{f^{-1}*g}$ 。

首先，由 $g=f*(f^{-1}*g)$ 得

$$S_g(n)=\sum_{i=1}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=f(1)S_{f^{-1}*g}(n)+\sum_{i=2}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

$f^{-1}$ 存在，故 $f(1)\not =0$，于是 

$$S_{f^{-1}*g}(n)=\dfrac{1}{f(1)}\left(S_g(n)-\sum_{i=2}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)\right)$$

该式是一个 $S_{f^{-1}*g}(n)$ 向 $S_{f^{-1}*g}\left(\left\lfloor\dfrac{n}{i}\right\rfloor\right)$ 递归的式子，  
同样可用记忆化和折半优化来优化计算。因此：

- 在仅使用记忆化的情况下，杜教筛的复杂度为 $\Theta(n^{\frac{3}{4}})$
- 在同时使用记忆化和折半优化的情况下，杜教筛的复杂度为 $\Theta(n^{\frac{2}{3}})$

-----------------------------------------------------
若 $f$ 为积性函数，则 $f(1)=1$，上式即

$$S_{f^{-1}*g}(n)=S_g(n)-\sum_{i=2}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

由此解决了 $\mu,\varphi$ 的求和问题：

$$S_{\mu}(n)=S_\varepsilon(n)-\sum_{i=2}^n I(i)S_{\mu}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=1-\sum_{i=2}^nS_{\mu}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

$$S_{\varphi}(n)=S_\mathrm{id}(n)-\sum_{i=2}^n I(i)S_{\varphi}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=\dfrac{n(n+1)}{2}-\sum_{i=2}^n S_{\varphi}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

使用整除分块和杜教筛求积性函数前缀和时，我们需要根据贝尔级数，将积性函数分解成

$$\mathrm{id}_0^{c_0}*\mathrm{id}_1^{c_1}*\mathrm{id}_2^{c_2}*\cdots\quad(c_0,c_1,c_2,\cdots\in\Z)$$

的形式，正次项和负次项分别用整除分块合并，再用杜教筛合并二者。

### Powerful Number 筛

并不是所有的积性函数都可分解成 $\mathrm{id}_i^{c_i}$，但当积性函数 $f$ 满足 $f(p)$ 为关于 $p$ 的低次多项式时，  
**Powerful Number 筛**（简称 **PN 筛**）允许我们将其前缀和的求解转化为该情况。

首先另找一积性函数 $g$，使得 $g(p)=f(p)$  。  
设 $h=f*g^{-1}$，则 $h$ 为积性函数，且 $f=h*g$，可得
$$f(p)=h(p)g(1)+h(1)g(p)=h(p)+g(p)$$
$$h(p)=f(p)-g(p)=0$$

故 $h$ 在质数处取值为 $0$ 。  

> 求证：设满足 $h(n)\not =0$ 的 $n$ 为 **Powerful Number**，则其一定可以表示成 $a^2b^3\ (a,b\in\N_+)$ 的形式。  
> 证明：将 $n$ 质因数分解，设 
> $$n=p_1^{c_1}p_2^{c_2}\cdots p_k^{c_k} \quad(p_1,p_2,\cdots,p_k\in\mathbb{P},c_1,c_2,\cdots,c_k\in\N_+)$$
> $\qquad\quad$ 则由积性函数定义得
> $$h(n)=h(p_1^{c_1})h(p_2^{c_2})\cdots h(p_k^{c_k})$$
> $\qquad\quad h$ 在质数处取值为 $0$，而 $h(n)\not=0$，故
> $$c_1,c_2,\cdots,c_k\ge 2$$ 
> $\qquad\quad$ 若 $c_k$ 为偶数，设 $c_k=2d_k$，则 $p_k^{c_k}=(p_k^{d_k})^2$；  
> $\qquad\quad$ 否则，设 $c_k=2d_k+1$，则 $p_k^{c_k}=\left(p_k^{d_k-1}\right)^2p_k^3$ 。  
> $\qquad\quad$ 总上可得，$p_k^{c_k}$ 总可以表示成 $a^2b^3$ 的形式，故 $n$ 亦同样可以表示成 $a^2b^3$ 的形式。

由以上讨论可得，$n$ 以下 Powerful Number 的个数为

$$\sum_{a=1}^{\sqrt{n}}\sqrt[3]{\frac{n}{a^2}}=\Theta\left(\sqrt[3]{n}\int_{0}^{\sqrt{n}}x^{-\frac{2}{3}}\mathrm{d}x\right)=\Theta(\sqrt{n})$$

又由整除分块可知

$$S_f(n)=S_{h*g}(n)=\sum_{i=1}^n h(i)S_g\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

$$=\sum_{\substack{i\in[1,n]\\i\texttt{ is Powerful Number}} } h(i)S_g\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

由此将 $S_f$ 的求解转化为 $S_g$ 的求解和 $h(i)$ 的求解。对于 $h(i)$ 的求解，可以直接根据其贝尔级数，  
也可同狄利克雷卷积逆一样递推，即

$$h(p^k)=f(p^k)-\sum_{i=0}^{k-1}h(p^i)g(p^{k-i})$$

Powerful Number 的质因子小于 $\sqrt{n}$，因此用后一种方式求解 $h(i)$ 所需总时间有一个很宽松的上界

$$\Omicron\left(\frac{\sqrt{n}}{\ln\sqrt{n}}\log^2 n\right)=\Omicron(\sqrt{n}\log n)$$

--------------------------

如何根据 $f$ 来选取合适的 $g$？首先，在 $\pmod{x^2}$ 意义下，有

$$\dfrac{1}{1-p^kx}\equiv 1+p^kx\pmod{x^2}$$

$$(1+ax)(1+bx)\equiv 1+(a+b)x\pmod{x^2}$$

故若 $f(p)=\displaystyle\sum_{k=0}^{m}a_kp^k$，则

$$f_p(x)\equiv 1+\left(\sum_{k=0}^{m}a_kp^k\right)x\equiv\left(\dfrac{1}{1-x}\right)^{a_0}\left(\dfrac{1}{1-px}\right)^{a_1}\cdots\left(\dfrac{1}{1-p^mx}\right)^{a_m}\pmod{x^2}$$

因此所要选取的 $g$ 为

$$\mathrm{id}^{a_0}*\mathrm{id}^{a_1}*\cdots*\mathrm{id}_m^{a_m}$$

综合整除分块、杜教筛和 PN 筛 所得的所有讨论，所得的筛法又称 **冷群筛**。

- 若不使用折半优化，总复杂度为 $\Theta(n^{\frac{3}{4}}\sum_i\log c_i)$ 。
- 若使用折半优化，由于需要线性筛 $n^{\frac{2}{3}}$ 以内的项，而在 $n$ 以下，素数的个数约为 $\dfrac{n}{\ln n}$，  
  $p^k\ (p\in\mathbb{P},k\ge 2)$ 均为 Powerful Number，个数小于 $\Theta(\sqrt{n})$，故总复杂度为 
  $$\Theta\left(n^{\frac{2}{3}}\left(1+\dfrac{\sum_i\log c_i}{\ln n}\right)\right)$$ 

------------------------------------------------------------
### Min_25 筛

冷群筛在求解复杂的积性函数时常数巨大。
