# 常微分方程

## 一阶微分方程

### 可分离变量的微分方程

$$y'=f(x)g(y)$$

根据导数的链式法则（即微分的形式不变性），我们有

$$\dfrac{\mathrm{d}y}{\mathrm{d}x}=f(x)g(y)$$

$$\dfrac{\mathrm{d}y}{g(y)}=f(x)\mathrm{d}x$$

$$\int\dfrac{\mathrm{d}y}{g(y)}=\int f(x)\mathrm{d}x$$

从而消除 $y'$ 。

### 齐次方程

$$\dfrac{\mathrm{d}y}{\mathrm{d}x}=f\left(\dfrac{y}{x}\right)$$

设 $y=ux$，则

$$\dfrac{\mathrm{d}(ux)}{\mathrm{d}x}=f(u)$$

$$x\dfrac{\mathrm{d}u}{\mathrm{d}x}+u=f(u)$$

$$\dfrac{\mathrm{d}u}{\mathrm{d}x}=\dfrac{f(u)-u}{x}$$

由此转化为可分离变量的微分方程。因此

$$\int\dfrac{\mathrm{d}u}{f(u)-u}=\int\dfrac{\mathrm{d}x}{x}$$

解得 $u$ 的通解后，回代即可求得 $y$ 的通解。

----------------------------------------------

有些方程是非齐次的，但可通过变量代换化为齐次。例如

$$\dfrac{\mathrm{d}y}{\mathrm{d}x}=\dfrac{ax+by+p}{cx+dy+q}\qquad(|p|+|q|\not =0,|b|+|d|\not =0)$$

若 $\begin{vmatrix}a&b\\ c&d\end{vmatrix}\not =0$，设 $x=X+h,y=Y+k$（$h,k$ 为常数），则

$$\dfrac{\mathrm{d}Y}{\mathrm{d}X}=\dfrac{ax+by+p}{cx+dy+q}=\dfrac{\mathrm{d}y}{\mathrm{d}x}=\dfrac{aX+bY+ah+bk+p}{cX+dY+ch+dk+q}$$

求解关于 $h,k$ 的二元一次方程

$$
\begin{cases}
ah+bk+p=0\\
ch+dk+q=0
\end{cases}
$$

即可找到合适的 $h,k$，将原方程化为关于 $Y$ 关于 $X$ 的齐次方程。

若 $\begin{vmatrix}a&b\\ c&d\end{vmatrix}=0$，则直接令 $Y=cx+dy+q$，亦可化为关于 $Y$ 关于 $X$ 的齐次方程。

### 一阶线性微分方程

$$y'+p(x)y=q(x)$$

若 $q(x)=0$（齐次），这就是可分离变量的微分方程，解得

$$y=Ce^{-\int p(x)\mathrm{d}x}$$

$q(x)\not =0$ 时（非齐次），考虑拆函数，令 $y=uv$，得

$$uv'+u'v+p(x)uv=q(x)$$

$$u(v'+p(x)v)+u'v=q(x)$$

若 $v'+p(x)v=0$，则可解得

$$u=\int\dfrac{q(x)}{v}\mathrm{d}x$$

而 $v'-p(x)v=0$ 就是已经解决的 $q(x)=0$ 时的情况。于是

$$v=Ce^{\int p(x)\mathrm{d}x}$$

综上，对于非齐次的情况，设

$$y=ue^{-\int p(x)\mathrm{d}x}$$

即将对应齐次方程通解中的常数改为函数。将其代入原方程，即可求得原方程的通解

$$y=e^{-\int p(x)\mathrm{d}x}\left(\int q(x)e^{\int p(x)\mathrm{d}x}\mathrm{d}x+C\right)$$

这就是解决非齐次方程问题的 **常数变易法**。

### 伯努利方程

$$y'+p(x)y=q(x)y^n\qquad(n\not =0,1)$$

两边同除以 $y^n$，得

$$y^{-n}y'+p(x)y^{1-n}=q(x)$$

$$\dfrac{(y^{1-n})'}{1-n}+p(x)y^{1-n}=q(x)$$

于是转化成 $y^{1-n}$ 关于 $x$ 的一阶线性微分方程。

### 全微分方程

$$P(x,y)\mathrm{d}x+Q(x,y)\mathrm{d}y=0$$

此处需要【分析】（5）多元微积分 的内容。若存在函数 $u(x,y)$，使得

$$\mathrm{d}u(x,y)=P(x,y)\mathrm{d}x+Q(x,y)\mathrm{d}y$$

那么显然，可通过

$$\dfrac{\partial u}{\partial x}=P(x,y),\dfrac{\partial u}{\partial y}=Q(x,y)$$

求得 $u(x,y)$，从而得到原方程的隐式通解

$$u(x,y)=C$$

由【分析】（5）多元微积分中的讨论，若 $P,Q$ 均存在一阶连续偏导数，则这样的 $u(x,y)$ 存在的充要条件为

$$\dfrac{\partial P}{\partial y}=\dfrac{\partial Q}{\partial x}$$

## 高阶微分方程

### 可降阶的高阶微分方程

对于形如

$$y''=f(x,y')$$

的方程，令 $p=y'$，即可化为关于 $p$ 的一阶微分方程

$$p'=f(x,p)$$

解出 $p$ 的通解后积分即可得到 $y$ 的通解。

---------------------------------------

而对于形如

$$y''=f(y,y')$$

的方程，同样令 $p=y'$，则

$$y''=\dfrac{\mathrm{d}p}{\mathrm{d}x}=\dfrac{\mathrm{d}p}{\mathrm{d}y}\dfrac{\mathrm{d}y}{\mathrm{d}x}=p\dfrac{\mathrm{d}p}{\mathrm{d}y}$$

从而将原方程化为 $p$ 关于 $y$ 的一阶微分方程

$$p\dfrac{\mathrm{d}p}{\mathrm{d}y}=f(y,p)$$

解出来的通解就是 $y$ 关于 $x$ 的一阶微分方程。

### 高阶线性微分方程

$$\sum_{k=0}^np_k(x)y^{(k)}=f(x)$$

其不存在通用的通解求法，但我们可以分析其解的结构。

函数可以视为向量，“求导”和“左乘一个函数”可以视为矩阵。

### 常系数线性微分方程

$$\sum_{k=0}^np_ky^{(k)}=f(x)$$

函数可以视为向量，求导可以视为矩阵。故设微分算子 $\mathcal{D}=\dfrac{\mathrm{d}}{\mathrm{d}x}$，则

$$\sum_{k=0}^np_k\mathcal{D}^ky=f(x)$$

$$\left(\sum_{k=0}^np_k\mathcal{D}^k\right)y=f(x)$$

$$y=\dfrac{f(x)}{\displaystyle\sum_{k=0}^np_k\mathcal{D}^k}$$

复数域上，对等式的右边作有理分式分解，即可化为一阶线性微分方程

$$y=\dfrac{f(x)}{\mathcal{D}-k}\Leftrightarrow (\mathcal{D}-k)y=f(x)\Leftrightarrow y'-ky=f(x)$$

从而得到复数域上的通解，运用欧拉公式即可化为实数域上的通解。

### 常系数线性微分方程组

$$A(\mathcal{D})\vec{y}=\vec{f}(x)$$

其中 $A(\mathcal{D})$ 为关于 $\mathcal{D}$ 的多项式构成的矩阵。那么

$$\vec{y}=A^{-1}(\mathcal{D})\vec{f}(x)$$

这样求得的通解中各常数直接可能存在依赖关系，需要代回原方程找出这种依赖关系。

### 欧拉方程

$$\sum_{k=0}^n p_kx^ky^{(k)}=f(x)$$

复数域内，设 $x=e^t,\mathcal{D}=\dfrac{\mathrm{d}}{\mathrm{d}t}$，则可归纳得

$$x^ky^{(k)}=\mathcal{D}^{\underline{k}}y$$

于是

$$\sum_{k=0}^np_k\mathcal{D}^{\underline{k}}y=f(e^t)$$

从而化为 $y$ 关于 $t$ 的常系数线性微分方程。

实数域内设 $t=\ln|x|$ 也可得到相同的结果。