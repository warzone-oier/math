## 线性映射与线性变换
有了数域，就要研究数域上的函数；有了线性空间，就要研究线性空间上的映射。

我们特别关注的是线性空间上的 **线性映射**：

对于同一域 $P$ 上的线性空间 $V,W$，若映射 $T:V\rightarrow W$ 满足
- $\forall \vec{u},\vec{v}\in V,T(\vec{u}+\vec{v})=T(\vec{u})+T(\vec{v})$
- $\forall\lambda\in P,\vec{a}\in V,T(\lambda\vec{v})=\lambda T(\vec{v})$

则称 $A$ 为 $V\rightarrow W$ 的 **线性映射**。 特别地，当 $V=W$ 时，$T$ 称为 $V$ 上的 **线性变换**。

例如，求导和积分都是典型的线性变换。

对于 $V$ 上的某个基底 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n,T$ 为 $V\rightarrow W$ 的线性映射等价于

$$\forall x_1,x_2,\cdots,x_n\in P,T(x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n)\\
=x_1T(\vec{a}_1)+x_2T(\vec{a}_2)+\cdots+x_nT(\vec{a}_n)$$

证明显然，套用前一种定义可得后一种，将 $\vec{u},\vec{v}$ 用基表示后，套用后一种定义可得前一种。

由后一种定义，$T(\vec{a}_1),T(\vec{a}_2),\cdots,T(\vec{a}_n)$ 可确定整个线性映射 $T$，体现其与坐标的相对性；  
又由前一种定义，线性映射 $T$ 本身与基底具体的选取无关，体现其空间上的绝对性。

与线性空间的线性映射类似的，我们可以定义自由模的 **模同态**。

## 矩阵

矩阵用来表示线性映射。对于线性映射 $T:V\rightarrow W$ 和 $V$ 上的基 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$，  
若 $T(\vec{a}_k)=\vec{a}'_k$ 在 $W$ （的某一个基底（若 $V=W$ 则也为 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$））下的坐标为 $\begin{bmatrix}a'_{k,1}\\a'_{k,2}\\\vdots\\a'_{k,m}
\end{bmatrix}$，则 $T$ 可表示为

$$A=\begin{bmatrix}
	a'_{1,1}&a'_{2,1}&\cdots&a'_{n,1}\\
	a'_{1,2}&a'_{2,2}&\cdots&a'_{n,2}\\
	\vdots&\vdots&\ddots&\vdots\\
	a'_{1,n}&a'_{2,n}&\cdots&a'_{m,n}
\end{bmatrix}$$

$A$ 称为 $T$ 在基 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 下的 **矩阵**。

例如，$A=\begin{bmatrix}3&1&2\\-1&0&4\end{bmatrix}$ 表示

$$T\left(\begin{bmatrix}1\\0\\0\end{bmatrix}\right)={3\brack-1},
T\left(\begin{bmatrix}0\\1\\0\end{bmatrix}\right)={1\brack 0},
T\left(\begin{bmatrix}0\\0\\1\end{bmatrix}\right)={2\brack 4}$$

### 矩阵乘法

**矩阵乘向量** 就是将向量代入线性映射中求值，即

$$A\vec{v}=A(\vec{v})$$

例如，$\begin{bmatrix}3&1&2\\-1&0&4\end{bmatrix}\begin{bmatrix}1\\3\\2
\end{bmatrix}=1\displaystyle{3\brack -1}+3{1\brack 0}+2{2\brack4}={10\brack 7}$

**矩阵乘法** 就是线性映射的复合，即

$$B=[\vec{B}_1|\vec{B}_2|\cdots|\vec{B}_n]$$
$$AB=[A\vec{B}_1|A\vec{B}_2|\cdots|A\vec{B}_n]$$

因此显然，矩阵乘法满足结合律，不满足交换律，  
且 $AB$ 存在当且仅当 $A$ 的列数（外层映射定义域维数）等于 $B$ 的行数（内层映射值域维数）。

就元素而言，矩阵乘法也可写成“左行乘右列”，也即

$$(AB)_{i,j}=\sum_kA_{i,k}B_{k,j}$$


### 矩阵的加法和数乘

对于线性映射 $F:V\rightarrow W$ 和 $G:V\rightarrow W$，我们可以验证 $F+G,\lambda F$ 也是线性映射。

于是可以定义矩阵的加法和数乘：矩阵的加法即对应位置元素相加，矩阵的数乘即所有元素同乘上同一个数。

矩阵的加法和数乘满足向量加法和数乘的所有运算律，此外还满足

- 数乘与矩阵乘法的相容性：$\lambda(AB)=(\lambda A)B=A(\lambda B)$
- 矩阵乘法关于矩阵加法的分配律：$(A+B)C=AC+BC,A(B+C)=AB+AC$

可见，向量可以是为只有一行或一列的矩阵，矩阵可以视为一组列向量或行向量，反映了二者的同一性。

### 矩阵的转置

由“左行乘右列”可知，矩阵可以右乘列向量，也可以左乘行向量。

这反映了数学上一些重要的对应关系。例如在二维平面内，

$$\texttt{两条直线确定一点}\left(\begin{bmatrix}A_1&B_1\\\hline A_2&B_2\end{bmatrix}{x\brack y}={C\brack C}\right)\Leftrightarrow$$
$$\texttt{两点确定一条直线}\left(\begin{bmatrix}A&B\end{bmatrix}\left[\begin{array}{c|c}x_1&x_2\\y_1&y_2\end{array}\right]=\begin{bmatrix}C&C\end{bmatrix}\right)$$

由此定义矩阵的 **转置**：所谓转置，就是将矩阵的行列互换

$$(A^T)_{i,j}=A_{j,i}$$

那么有

$$(AB)^T=B^TA^T$$

$$(A+B)^T=A^T+B^T,(\lambda A)^T=\lambda A^T$$