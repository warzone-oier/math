# 集合幂级数

对于环 $R$ 和 $U=\{1,2,\cdots,n\}$，我们将映射 $F:2^U\rightarrow R$ 称为环 $R$ 上的 **集合幂级数**，记为

$$F=\sum_{S\subseteq U} F(S)x^S$$

与多项式环一样，此处的 $x^S$ 只是一个形式符号，一个系数位置的标识符，  
且易定义集合幂级数的加法和数乘为

$$(F+G)(S)=F(S)+G(S)$$

$$(cF)(S)=cF(S)\quad(c\in R)$$


## 位运算卷积

接下来，我们定义集合幂级数的乘法。

集合有交（与）、并（或）、对称差（异或）三种运算，  
基于这三种运算，我们定义集合幂级数的 **与卷积**、**或卷积** 和 **异或卷积** 分别为

$$(F\times_\cap G)(S)=\sum_{A\cap B=S}F(A)G(B)$$

$$(F\times_\cup G)(S)=\sum_{A\cup B=S}F(A)G(B)$$

$$(F\times_\Delta G)(S)=\sum_{A\Delta B=S}F(A)G(B)$$

不难发现，以上三种卷积均能使集合幂级数构成环，且 $R$ 为交换环时这三种卷积亦满足交换律。  
并且，当 $R$ 为幺环时，与卷积的存在单位元 

$$[S=U]=1x^U$$

或卷积与异或卷积均存在单位元

$$[S=\varnothing]=1x^{\varnothing}$$

### FWT

我们希望通过与 FFT 类似的方法实现位运算卷积，称为 **快速沃尔什变换（FWT）**。  

下面我们从蝴蝶变换的角度推导 FWT 的异或形式 $\mathcal{F}_\Delta$。首先，其至少满足：  

$$F\times_\Delta G=F^{-1}_\Delta (\mathcal{F}_\Delta (F)\cdot \mathcal{F}_\Delta (G))$$  

其中 $\cdot$ 表示系数对应位置相乘。

已知 $H=F\times_\Delta G$，设三个序列奇偶分段后分别为 $F_0,F_1,G_0,G_1,H_0,H_1$，  
则根据异或卷积的定义可得

$$H_0=F_0\times_\Delta G_0+F_1\times_\Delta G_1$$
$$H_1=F_1\times_\Delta G_0+F_0\times_\Delta G_1$$

又由异或卷积的分配律得

$$(F_0+F_1)\times_\Delta (G_0+G_1)=F_0\times_\Delta G_0+F_1\times_\Delta G_1+F_0\times_\Delta G_1+F_1\times_\Delta G_0$$
$$(F_0-F_1)\times_\Delta (G_0-G_1)=F_0\times_\Delta G_0+F_1\times_\Delta G_1-F_0\times_\Delta G_1-F_1\times_\Delta G_0$$

显然，逆变换时只要对如上两式加减消元即可。  
于是可得 $\mathcal{F}_\Delta$ 为：

$$
\begin{matrix}
F_0 & F_1 & G_0 & G_1\\
\Downarrow & \Downarrow &
\Downarrow & \Downarrow \\
F_0+ F_1 & F_0-F_1 & 
G_0+ G_1 & G_0-G_1
\end{matrix}
$$

向下递归，计算出卷积 
$$P_0=(F_0+F_1)\times_\Delta (G_0+G_1)$$
$$P_1=(F_0-F_1)\times_\Delta (G_0-G_1)$$
回溯时，逆变换为
$$
\begin{matrix}
\dfrac{1}{2}(P_0+P_1) & \dfrac{1}{2}(P_0-P_1) \\
\Uparrow & \Uparrow \\
P_0 & P_1
\end{matrix}
$$
注意到系数 $\dfrac{1}{2}$ 可以提出来，到最后再除，因此可得到 

$$\mathcal{F}^{-1}_\Delta (F)=2^{-n}\mathcal{F}_\Delta (F)$$

这样便实现了在 $\Theta(n2^n)$ 的时间复杂度下计算异或卷积。

---------------

类似的，易推得 FWT 的与形式 $\mathcal{F}_\cap$ 和或形式 $\mathcal{F}_\cup$：

$$
\begin{matrix}
 & F_0 & F_1 &  & F_0-F_1 & F_1\\
\mathcal{F}_\cap: & \Downarrow & \Downarrow & \mathcal{F}^{-1}_\cap: & \Uparrow & \Uparrow \\
 & F_0+ F_1 & F_1 & & F_0 & F_1
\end{matrix}
$$
$$
\begin{matrix}
 & F_0 & F_1 & & F_0 & F_1-F_0\\
\mathcal{F}_\cup: & \Downarrow & \Downarrow & \mathcal{F}^{-1}_\cup: & \Uparrow & \Uparrow \\
 & F_0 & F_1+ F_0 & & F_0 & F_1
\end{matrix}
$$

与 FFT 一样，FWT 也是有对应的封闭形式的。  
根据以上三种位运算卷积对应的 FWT 的蝴蝶变换，不难得

$$(\mathcal{F}_\cap(F))(S)=\sum_{T\supseteq S} F(T),(\mathcal{F}^{-1}_\cap(F))(S)=\sum_{T\supseteq S} (-1)^{|S\Delta T|}F(T)$$

$$(\mathcal{F}_\cup(F))(S)=\sum_{T\subseteq S} F(T),(\mathcal{F}^{-1}_\cup(F))(S)=\sum_{T\subseteq S} (-1)^{|S\Delta T|}F(T)$$

$$(\mathcal{F}_\Delta(F))(S)=\sum_{T}(-1)^{|S\cap T|}F(T),(\mathcal{F}^{-1}_\Delta(F))(S)=2^{-n}(\mathcal{F}_\Delta(F))(S)$$

> 练习：
> 1. 试用蝴蝶变换证明
>    $$\mathcal{F}^{-1}_\Delta(\mathcal{F}_\Delta(F))=\mathcal{F}_\Delta(\mathcal{F}^{-1}_\Delta(F))=F$$
>    在此基础上，证明
>    $$\mathcal{F}_\Delta(F\times_\Delta G)=\mathcal{F}_\Delta (F)\cdot \mathcal{F}_\Delta (G)$$
> 2. 试用 FWT 的封闭形式证明
>    $$\mathcal{F}_\Delta(F+G)=\mathcal{F}_\Delta(F)+\mathcal{F}_\Delta(G)$$
>    $$\mathcal{F}_{\Delta}(cF)=c\mathcal{F}_{\Delta}(F)\quad(c\in R)$$

### 异或卷积的意义与 $k$ 进制 FWT

和与卷积及或卷积相比，异或卷积有其特殊意义。  
具体地，任意集合幂级数 $F$ 总唯一对应一个 $n$ 元，且每个元的次数最高为 $1$ 的多项式

$$f(x_1,x_2,\cdots,x_n)=\sum_{S\subseteq U}F(S)\prod_{k\in S}x_k$$

那么，两个集合幂级数作异或卷积，即二者对应的多项式作长度为 $2$ 的循环卷积

$$H=F\times_{\Delta} G\Leftrightarrow h=(\cdots(((f\times g)\bmod{(x_1^2-1)})\bmod{(x_2^2-1)})\cdots)\bmod{(x_n^2-1)}$$

这表明，**FWT 的异或形式就是 $n$ 维且每一维的长度均为 $2$ 的 FFT**。

这使得我们可以推广 FWT 的异或形式。  
具体地，对于 $a,b\in\N$，定义 **$k$ 进制异或** $a\oplus_k b$ 为 $k$ 进制不进位加法。   
对于长度为 $m^k$ 的序列 $f_0,f_1,\cdots,f_{m^k-1},g_0,g_1,\cdots,g_{m^k-1}$，定义 **$k$ 进制异或卷积**

$$(f\times_{\oplus _k} g)_n=\sum_{a\oplus_k b=n}f_ag_b$$

也就是说，设

$$f(x_0,x_1,\cdots,x_m)=\sum_{n=0}^{m^k-1}f_n\prod_{i=0}^{+\infty}x_i^{\left\lfloor\frac{n}{k^i}\right\rfloor\bmod k}$$

则 $k$ 进制异或卷积就是 $m$ 维，每一维长度为 $k$ 的循环卷积 

$$f\times_{\otimes _k} g=(\cdots(((f\times g)\bmod{(x_1^2-1)})\bmod{(x_2^2-1)})\cdots)\bmod{(x_m^2-1)}$$

那么，$k$ 进制异或卷积可用 $k$ 进制 FWT 来计算，其就是 $m$ 维，每一维长度为 $k$ 的 FFT

$$\mathcal{F}_{\oplus_k}(f)_n=f(\omega_k^{n\bmod k},\omega_k^{\left\lfloor\frac{n}{k}\right\rfloor\bmod k},\cdots,\omega_k^{\left\lfloor\frac{n}{k^{m-1}}\right\rfloor\bmod k})$$

$$\mathcal{F}^{-1}_{\oplus_k}(f)_n=\dfrac{1}{m^k}f(\omega_k^{-\left(n\bmod k\right)},\omega_k^{-\left(\left\lfloor\frac{n}{k}\right\rfloor\bmod k\right)},\cdots,\omega_k^{-\left(\left\lfloor\frac{n}{k^{m-1}}\right\rfloor\bmod k\right)})$$

## 子集卷积

异或卷积是 $n$ 维，每一维长度为 $2$ 的循环卷积。  
与之相对应的就是 $n$ 维，每一维长度为 $2$ 的非循环卷积，称为 **子集卷积**

$$(F\times G)(S)=\sum_{\substack{A\cup B=S\\A\cap B=\varnothing}} F(A)G(B)=\sum_{T\subseteq S}f(T)G\left(\complement_ST\right)$$

$$H=F\times G\Leftrightarrow h=(\cdots(((f\times g)\bmod{x_1^2})\bmod{x_2^2})\cdots)\bmod{x_m^2}$$

当提到集合幂级数的乘法时，默认指子集卷积。此时，集合幂级数环就是 

$$R[x_1,x_2,\cdots,x_n]/(x_1^2,x_2^2,\cdots,x_n^2)$$

子集卷积可以转化为 或卷积/异或卷积 计算。具体地，

$$(F\times G)(S)=\sum_{\substack{A\cup B=S\\|A|+|B|=|S|}} F(A)G(B)=\sum_{\substack{A\Delta B=S\\|A|+|B|=|S|}} F(A)G(B)$$

故设 $F_k(S)=F(S)[|S|=k]$，则

$$(F\times G)(S)=\sum_{k=0}^{|S|}(F_k\times_\cup G_{|S|-k})(S)=\sum_{k=0}^{|S|}(F_k\times_\Delta G_{|S|-k})(S)$$

即可用 $\Theta(n^22^n)$ 的时间和 $\Theta(n2^n)$ 的空间计算子集卷积。

### 半在线卷积

集合幂级数也存在半在线卷积。对于递推

$$F(S)=\sum_{T\subsetneq S}L(T,F(T))G\left(\complement_ST\right)+H(S)$$


注意到整个递推是从 $|T|$ 较小的 $F(T)$ 贡献到 $|S|$ 较大的 $F(S)$，故整个递推可分为 $n$ 轮，  
第 $i$ 轮确定 $F_i(S)=F(S)[|S|=i]$，然后用 或卷积/异或卷积 计算 $F_i$ 向 $F_k\ (k>i)$ 的贡献。  
显然时间复杂度与子集卷积相同，为 $\Theta(n2^n)$。

### 乘法逆元

$R$ 为幺环时，子集卷积的单位元

$$1=[S=\varnothing]$$

于是便可定义子集卷积的逆元，即集合幂级数的乘法逆元。  
同形式幂级数的乘法逆元，易推得集合幂级数的乘法逆元存在递推式

$$F^{-1}(\varnothing)=\dfrac{1}{F(\varnothing)},F^{-1}(S)=-\dfrac{1}{F(\varnothing)}\sum_{T\subsetneq S}F^{-1}(T)G\left(\complement_ST\right)\quad(S\not =\varnothing)$$

该递推式符合半在线卷积的形式，故可通过半在线卷积 $\Theta(n^22^n)$ 计算。


### 形式偏导数

集合幂级数对应多元而非一元幂级数。对于多元幂级数，我们一般讨论的是对某个特定变量的偏导数。

故定义集合幂级数 $F$ 的 $k$ - 形式偏导数

$$\mathcal{D}_k F(S)=[k\notin S]F(S\cup\{k\})$$

则

$$G=\mathcal{D}_k F\Leftrightarrow g=\dfrac{\partial f}{\partial x_k}$$

### 左复合形式幂级数

集合幂级数可以左复合上形式幂级数

$$g\circ F=\sum_{k=0}^{+\infty} g_kF^k$$

与形式幂级数一样，我们规定 $g\circ F$ 存在当且仅当 $g$ 为有限项或 $F(\varnothing)=0$，  
这样就不涉及环 $R$ 上的极限了。

$g\circ F$ 可以通过形式偏导数计算。具体地，设 $H=g\circ F$，则 $h\equiv g\circ f\pmod{x_1^2,x_2^2,\cdots,x_n^2}$，于是

$$h\equiv[x_n^0](g\circ f)+x_n\dfrac{\partial (g\circ f)}{\partial x_n}=(g\circ [x_n^0]f)+x_n(g'\circ f)\dfrac{\partial f}{\partial x_n}$$

$$\equiv(g\circ [x_n^0]f)+x_n(g'\circ [x_n^0]f)[x_n^1]f\pmod{x_1^2,x_2^2,\cdots,x_n^2}$$

问题便从求解 $g\circ f$ 转化为求解 $(g\circ [x_n^0]f)$ 和 $(g'\circ [x_n^0]f)$ 这两个规模为 $n-1$ 的子问题。  
这是一个动态规划问题，所有的中间状态为

$$(g^{(i)}\circ [x_{n-k+1}^0x_{n-k+2}^0\cdots x_n^0]f)\quad(0\le i\le k\le n)$$

故总的时间复杂度为

$$\Theta\left(\sum_{k=0}^n(n-k)k^22^k\right)=\Theta(n^22^n)$$

### $\exp,\ln$ 及其组合意义


$$e^F=\sum_{m=0}^{+\infty}\dfrac{F^m}{m!}$$

$$-\ln(1-F)=\sum_{k=1}^{+\infty}\dfrac{F^k}{k}$$

$\exp,\ln$ 都是形式幂级数。因此，可以在 $\Theta(n^22^n)$ 的时间内计算集合幂级数左复合上二者的结果

$$e^f=e^{[x_n^0]f}(1+x_n[x_n^1]f)$$

$$\ln f=[x_n^0]\ln f+x_n\dfrac{[x_n^1]f}{[x_n^0]f}$$

-------------------------------

将集合幂级数的 $\exp$ 按照定义展开，即可得到其组合意义


$$(e^F)(S)=\sum_{m=0}^{+\infty}\dfrac{1}{m!}\sum_{\substack{T_1\cup T_2\cup \cdots\cup T_m=S\\\\T_1,T_2,\cdots,T_m\texttt{ 两两不交}}}\prod_{k=1}^mF(T_k)$$

组合意义为，设 $F(S)$ 为集合 $S$ 的权值，则 $(e^F)(S)$ 为

1. 将 $S$ 划分为若干无标号非空集合 $T_1,T_2,\cdots,T_m$（$\dfrac{1}{m!}$ 表示去掉标号）
2. 定义划分方案 $T_1,T_2,\cdots,T_m$ 的权值为方案中各个集合的权值之积 $\displaystyle\prod_{k=1}^mF(T_k)$
3. 将所有的划分方案得到的权值求和

$\ln$ 同样也有组合意义，只是相对于 $\exp$ 更难理解

$$(-\ln(1-F))(S)=\sum_{m=1}^{+\infty}\dfrac{1}{m}\sum_{\substack{T_1\cup T_2\cup \cdots\cup T_m=S\\\\T_1,T_2,\cdots,T_m\texttt{ 两两不交}}}\prod_{k=1}^mF(T_k)$$

其组合意义为，设 $F(S)$ 为集合 $S$ 的权值，则 $(-\ln(1-F))(S)$

1. 将 $S$ 划分为若干有标号非空集合 $T_1,T_2,\cdots,T_m$，并将这些非空集合串成环  
   （$\dfrac{1}{m}$ 表示环的第一个元素有 $m$ 种选择，每种选择得到的环是一样的）
2. 定义整个环的权值为方案中各个集合的权值之积 $\displaystyle\prod_{k=1}^mF(T_k)$
3. 将所有的划分方案得到的权值求和

> 练习：试说明 $(1-F)^{-1}=\displaystyle\sum_{k=0}^{+\infty} F^k$ 的组合意义。