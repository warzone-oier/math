# 有限微积分

## 差分

$$\Delta f(x)=f(x+1)-f(x)$$

意思是，$\Delta f(x)$ 是一个新的函数，它等于 $f(x)$ 的差。

### 差分的运算律

若 $u,v$ 的差分均存在，则有以下运算律成立：

- 线性性：$\Delta(au+bv)=a\Delta u+b\Delta v\quad(a,b\in\mathbb{C})$
  > 证明：$\Delta(af(x)+bg(x))=af(x+1)+bg(x+1)-af(x)-bg(x)$  
  > $\qquad=a(f(x+1)-f(x))+b(g(x+1)-g(x))=a\Delta f(x)+b\Delta g(x)$
- 乘法法则：$\Delta(uv)=u\Delta v+\mathrm{E}v\Delta u$  
  其中 $\mathrm{E}f(x)=f(x+1)$。
  > 证明：$\Delta(f(x)g(x))=f(x+1)g(x+1)-f(x)g(x)$  
  > $\qquad=f(x+1)g(x+1)-f(x)g(x+1)+f(x)g(x+1)-f(x)g(x)$  
  > $\qquad=(f(x+1)-f(x))g(x+1)+f(x)(g(x+1)-g(x))$  
  > $\qquad=f(x)(g(x+1)-g(x))+(f(x+1)-f(x))g(x+1)$  
  > $\qquad=f(x)\Delta g(x)+\mathrm{E}g(x)\Delta f(x)$

### 基本差分公式与下降幂

指数函数的差分由差分的定义可得

$$\Delta(a^x)=(a-1)a^x$$

对于幂函数的差分，我们定义所谓的 **下降幂**

$$x^{\underline{n}}=\begin{cases}
	x(x-1)\cdots(x-n+1)&(n>0)\\
	1&(n=0)\\
	\dfrac{1}{(x+1)(x+2)\cdots(x+(-n))}&(n<0)
\end{cases}$$

则

$$\Delta(x^{\underline{n}})=nx^{\underline{n-1}}$$

下降幂多项式可以用多项式乘法唯一转化为普通幂多项式，  
而普通幂多项式亦可以用多项式除法唯一转化为下降幂多项式。

### 高阶差分与牛顿插值

我们定义所谓的 **高阶差分**

$$\Delta^0 f=f,\Delta^n f=\Delta(\Delta^{n-1}f)\quad(n\in\N_+)$$

由线性代数的知识可知，函数可以视为向量，$\Delta,\mathrm{E}$ 可视为矩阵，故

$$\Delta=\mathrm{E}-1$$

$$\Delta^nf(x)=(\mathrm{E}-1)^nf(x)=\sum_{k=0}^n\dbinom{n}{k}(-1)^{n-k}\mathrm{E}^kf(x)$$

$$\Delta^nf(x)\sum_{k=0}^n\dbinom{n}{k}(-1)^{n-k}f(x+k)$$

因此我们可以 $\Theta(n)$ 求出函数的 $n$ 阶差分的某一项。

对于下降幂级数 $f(x)=\displaystyle\sum_{k=0}^{+\infty} a_kx^{\underline{k}}$，我们有

$$\Delta^nf(0)=n!a_n$$

因此

$$f(x)=\sum_{k=0}^{+\infty}\dfrac{\Delta^kf(0)}{k!}x^{\underline{k}}=\sum_{k=0}^{+\infty}\dfrac{\Delta^kf(0)}{k!}x^{\underline{k}}=\sum_{k=0}^{+\infty}\dbinom{x}{k}\Delta^kf(0)$$

对该式作一下平移，得

$$f(x)=\sum_{k=0}^{+\infty}\dbinom{x-x_0}{k}\Delta^kf(x_0)$$

我们称形如 $\displaystyle\sum_{k=0}^{+\infty}b_k\dbinom{x}{k}$ 的级数为 **牛顿级数**，而上式被称为 **等间距牛顿插值公式**。

### 差分的应用

应用差分，我们能对数列的单调性进行分析。

- $\forall n\in[l,r)\cap\Z,\Delta a_n>0\Rightarrow$ 数列 $a$ 在 $[l,r]\cap\Z$ 单调递增。
- $\forall n\in[l,r)\cap\Z,\Delta a_n<0\Rightarrow$ 数列 $a$ 在 $[l,r]\cap\Z$ 单调递减。

通过分析数列的差分，我们可以得到数列的单调区间，进而得到数列的 **极值**：

若 $a_n \ge a_{n+1},a_{n-1}$，则称 $n$ 为数列 $a$ 的一个 **极大值点**，$a_n$ 为 $a$ 的一个 **极大值**。  
极小值和极小值点同理。极大值和极小值统称 **极值**，极值点同理。

显然，得到数列的极值后，我们就能得到数列的最值。

-------------------------------------------------

除了分析数列的增减性，差分还可用于分析数列的凹凸性：若

$$\forall a<b<c\quad(a,b,c\in[l,r]\cap\Z),(c-a)s_b\le (c-b)s_a+(b-a)s_c$$

则称数列 $s$ 在 $[l,r]\cap\Z$ 内是 **凹（下凸）** 的。  
将定义中 $\le$ 分别换成 $<,\ge,>$，得到的分别是 **严格凹（严格下凸）**、**凸（上凸）**、**严格凸（严格上凸）** 的定义。

易证明，可以通过二阶差分来获取数列的凹凸性：

- $\forall n\in[l,r-2]\cap\Z,\Delta^2 s_n\ge 0\Rightarrow$ 数列 $s$ 在 $[l,r]\cap\Z$ 内是凹（下凸）的。
- $\forall n\in[l,r-2]\cap\Z,\Delta^2 s_n>0\Rightarrow$ 数列 $s$ 在 $[l,r]\cap\Z$ 内是严格凹（严格下凸）的。
- $\forall n\in[l,r-2]\cap\Z,\Delta^2 s_n\le 0\Rightarrow$ 数列 $s$ 在 $[l,r]\cap\Z$ 内是凸（上凸）的。
- $\forall n\in[l,r-2]\cap\Z,\Delta^2 s_n< 0\Rightarrow$ 数列 $s$ 在 $[l,r]\cap\Z$ 内是凸（严格上凸）的。

### Stolz 定理

差分除了能用来分析数列，还可用于求解数列极限。

当极限的形式为 $\dfrac{\infty}{\infty}$ 或 $\dfrac{0}{0}$ 时，极限的值无法直接通过分子、分母的极限决定。  
此时，我们可以用 **Stolz 定理** 将比的极限转化为差分之比的极限。

> **Stolz 定理（$\dfrac{\cdot}{\infty}$ 型）**：对于实数数列 $a,b$，若 $b$ 严格单调且发散，则
> $$\lim_{n\rightarrow+\infty}\dfrac{\Delta a_n}{\Delta b_n}=L\ (L\in[-\infty,+\infty])\Rightarrow\lim_{n\rightarrow+\infty}\dfrac{a_n}{b_n}=L$$
> 证明*：此处仅证明 $b$ 单调递增的情况，单调递减的情况可通过取相反数转为单调递增的情况。

> **Stolz 定理（$\dfrac{0}{0}$ 型）**：对于实数数列 $a,b$，若 $b$ 严格单调，且 $\displaystyle\lim_{n\rightarrow+\infty}a_n=\lim_{n\rightarrow +\infty} b_n=0$，则
> $$\lim_{n\rightarrow+\infty}\dfrac{\Delta a_n}{\Delta b_n}=L\ (L\in[-\infty,+\infty])\Rightarrow\lim_{n\rightarrow+\infty}\dfrac{a_n}{b_n}=L$$

## 和式

### 定和式

$${\sum}_{a}^bf(x)\delta x=\sum_{k=a}^{b-1}f(k)=f(a)+f(a+1)+\cdots+f(b-1)$$

其与差分是一对互逆的运算，具体的，有 **有限微积分基本定理**

$${\sum}_{a}^b\Delta f(x)\delta x=f(b)-f(a)$$

除此之外，定和式还满足以下性质：

- $\displaystyle{\sum}_{a}^bf(x)\delta x=-{\sum}_{b}^af(x)\delta x$
- $\displaystyle{\sum}_{a}^{b^{^{^{}}}}f(x)\delta x+{\sum}_{b}^cf(x)\delta x={\sum}_{a}^cf(x)\delta x$
- 线性性：$\displaystyle{\sum}_{a}^b(cf(x)+dg(x))\delta x=c{\sum}_{a}^bf(x)\delta x+d{\sum}_{a}^bg(x)\delta x\quad(c,d\in\R)$

有了有限微积分基本定理，我们求出某个函数的差分，就能导出相应的和。

练习：试证明等比数列求和公式、等差数列求和公式、平方和公式、立方和公式。

### 不定和式与分部求和

我们希望运用差分的乘法法则来计算定和式。于是我们定义所谓 **不定和式**

$$\sum\Delta f(x)\delta x=f(x)+C$$

其中常数 $C$ 表示差分相同的数列具有不同的首项。

将差分的乘法法则移项，再取和式，即可得到所谓 **分部求和公式**

$$\sum u\Delta v=uv-\sum Ev\Delta u$$

使用时切记是 $\mathrm{E}v$ 而非 $\mathrm{E}\Delta v$。

分部求和具有“一升一降”的效果。对于指数函数与幂函数之积的求和，  
我们一般取指数函数为 $v$、幂函数函数为 $u$，从而实现对幂函数的降次。

### 调和数

$$H_n={\sum}_{0}^nx^{\underline{-1}}\delta x=\sum_{k=1}^n\dfrac{1}{k}$$

尽管有限微积分可以帮我们求出大部分常见数列的和，对于调和数，其不存在初等的通项公式。

# 无限微积分

无限微积分与有限微积分是类似的，区别只是将有限的差与和替换成了无穷小的差（微分学）与和（积分学）

## 微分学

### 导数

$$f'(x)=\dfrac{\mathrm{d}f(x)}{\mathrm{d}x}=\lim_{h\rightarrow 0}\dfrac{f(x+h)-f(x)}{h}$$

其中 $h$ 一般记为 $\Delta x$。  
$x=x_0$ 时，得到的极限 $f'(x_0)$ 称为 $f(x)$ 在 $x_0$ 处的 **导数**，  
在所有位置的导数构成的函数 $f'(x)$ 称为 $f(x)$ 的 **导函数**。 


从几何意义上看，导数表示 $f(x)$ 图象的切线的斜率；  
从物理意义上看，对于关于时间的函数 $g(t)$，其导数 $g'$ 表示 $g$ 的“瞬时变化率”。  
例如，速度是位移关于时间的导数，加速度是速度关于时间的导数。

#### 基本求导法则

若 **$f(x),g(x)$ 均可导**，则有以下基本求导法则成立：

- 线性法则：$(af+bg)'=af'+bg'\quad(a,b\in\mathbb{C})$
  > 证明：$(af(x)+bg(x))'=\displaystyle\lim_{h\rightarrow 0}\dfrac{(af(x+h)+bg(x+h))-(af(x)+bg(x))}{h}$
  > $$=\lim_{h\rightarrow 0}\left(a\dfrac{f(x+h)-f(x)}{h}+b\dfrac{g(x+h)-g(x)}{h}\right)$$
  > $$a\lim_{h\rightarrow 0}\dfrac{f(x+h)-f(x)}{h}+b\lim_{h\rightarrow 0}\dfrac{g(x+h)-g(x)}{h}=af'(x)+bg'(x)$$
- 乘法法则：$(fg)'=f'g+fg'$
  > 证明：$(f(x)g(x))'=\displaystyle\lim_{h\rightarrow 0}\dfrac{f(x+h)g(x+h)-f(x)g(x)}{h}$
  > $$=\lim_{h\rightarrow 0}\dfrac{f(x+h)g(x+h)-f(x)g(x+h)+f(x)g(x+h)-f(x)g(x)}{h}$$
  > $$=\lim_{h\rightarrow 0}\left(\dfrac{f(x+h)-f(x)}{h}g(x+h)+f(x)\dfrac{g(x+h)-g(x)}{h}\right)$$
  > $\qquad\quad$ 可导一定连续（详见下文 导数与极限），因此
  > $$(f(x)g(x))'=\left(\lim_{h\rightarrow 0}\dfrac{f(x+h)-f(x)}{h}\right)\lim_{h\rightarrow 0}g(x+h)+f(x)\lim_{h\rightarrow 0}\dfrac{g(x+h)-g(x)}{h}$$
  > $$=f'(x)g(x)+g'(x)f(x)$$

- 链式法则：$(f\circ g)'=(f'\circ g)\times g'$

练习：

1. 除法法则：已知 $\left(\dfrac{1}{x}\right)'=-\dfrac{1}{x^2}$。试用乘法法则和链式法则证明，若 $f(x),g(x)$ 均可导且 $g'(x)\not =0$，则
$$\left(\dfrac{f(x)}{g(x)}\right)'=\dfrac{f'(x)g(x)-f(x)g'(x)}{g^2(x)}$$ 
2. 反函数求导：试用链式法则证明，若 $f,g$ 反函数，且 $f$ 可导，则
   $$g'=\dfrac{1}{f'\circ g}$$

#### 基本初等函数的导数

- $(e^x)'=e^x$
  > 证明：
  > $$(e^x)'=\lim_{h\rightarrow 0}\dfrac{e^{x+h}-e^x}{h}=e^x\lim_{h\rightarrow 0}\dfrac{e^h-1}{h}$$
  > 设 $t=e^h-1$，则 $h=\ln(t+1)$，于是
  > $$(e^x)'=e^x\lim_{t\rightarrow 0}\dfrac{t}{\ln(t+1)}=\dfrac{e^x}{\displaystyle\lim_{t\rightarrow 0}\ln((1+t)^\frac{1}{t})}$$
  > 使用重要极限 $\displaystyle\lim_{x\rightarrow\infty}\left(1+\dfrac{1}{x}\right)^x=e$，可得
  > $$(e^x)'=\dfrac{e^x}{\displaystyle\lim_{t\rightarrow\infty}\ln\left(\left(1+\dfrac{1}{t}\right)^t\right)}=\dfrac{e^x}{\ln e}=e^x$$
  推论：$(a^x)'=a^x\ln a\qquad(a^x=e^{x\ln a})$
- $\ln' x=\dfrac{1}{x}$
  > 证明：$e^x,\ln x$ 互为反函数，由反函数求导得
  > $$\ln' x=\dfrac{1}{(e^x)'\circ \ln x}=\dfrac{1}{e^x\circ\ln x}=\dfrac{1}{x}$$
  $\dfrac{1}{x}$ 在 $x<0$ 时仍有定义。实数域内，我们将 $\ln x$ 扩充为偶函数 $\ln|x|$，可得
  $$(\ln |x|)'=\dfrac{1}{x}$$
  推论：$\log_a' x=\dfrac{1}{x\ln a}\qquad(\log_a x=\dfrac{\ln x}{\ln a})$
- $(x^n)'=nx^{n-1}\quad(n\in\mathbb{C})$
  > 证明：$(x^n)'=(e^{n\ln x})'=(e^x\circ n\ln x)'=((e^x)'\circ n\ln x)\times(n\ln x)'$  
  > $\qquad\qquad=(e^x\circ n\ln x)\times\dfrac{n}{x}=x^n\times\dfrac{n}{x}=nx^{n-1}$
- $\sin'x=\cos x,\cos'x=-\sin x$
  > 证明：以 $\sin'$ 为例，有
  > $$\sin' x=\displaystyle\lim_{h\rightarrow 0}\dfrac{\sin(x+h)-\sin x}{h}=\lim_{h\rightarrow 0}\dfrac{\sin x\cos h+\cos x\sin h-\sin x}{h}$$
  > $$=\left(\lim_{h\rightarrow 0}\dfrac{\cos h-1}{h}\right)\sin x+\left(\lim_{h\rightarrow 0}\dfrac{\sin h}{h}\right)\cos x$$
  > 使用重要极限 $\displaystyle\lim_{x\rightarrow 0}\dfrac{\sin x}{x}=1,\lim_{x\rightarrow 0}\dfrac{1-\cos x}{x}=0$，得
  > $$\sin' x=0\times \sin x+1\times\cos x=\cos x$$
  > $\cos'$ 是类似的。  

  推论：$\tan' x=\dfrac{1}{\cos^2 x}\qquad(\tan x=\dfrac{\sin x}{\cos x})$
- $\arcsin'x=\dfrac{1}{\sqrt{1-x^2}},\arccos'x=-\dfrac{1}{\sqrt{1-x^2}},\arctan' x=\dfrac{1}{1+x^2}$
  > 证明：以 $\arcsin'$ 为例，由反函数求导得
  > $$\sin' x=\dfrac{1}{\arcsin'\sin x}=\cos x=\sqrt{1-\sin^2 x}$$
  > $$\arcsin' x=\dfrac{1}{\sqrt{1-x^2}}$$
  > $\arccos',\arctan'$ 是类似的。

结合基本求导法则和基本初等函数的导数，可求任意初等函数的导数。

练习：试求解 $x^x$ 的导数。

#### 高阶导数

我们定义所谓的 **二阶导数** 和 **三阶导数** 为

$$y''=\dfrac{\mathrm{d}^2y}{\mathrm{d}x^2}=(y')'$$

$$y'''=\dfrac{\mathrm{d}^3y}{\mathrm{d}x^3}=(y'')'$$

由此拓展到所谓的 **$n$ 阶导数**

$$y^{(0)}=\dfrac{\mathrm{d}^0y}{\mathrm{d}x^0}=f(x),y^{(n)}=\dfrac{\mathrm{d}^ny}{(\mathrm{d}x)^n}=(y^{(n-1)})'\quad(n\in\N_+)$$

$n$ 阶导数依然满足线性法则。而对于乘法法则，可由组合意义得到所谓的 **莱布尼兹公式**

$$(uv)^{(n)}=\sum_k\dbinom{n}{k}u^{(n-k)}v^{(k)}$$

#### 导数的应用

同样地，我们可以用导数对函数的单调性进行分析：

- $\forall x\in(l,r),f'(x)>0\Rightarrow f(x)$ 在 $(l,r)$ 单调递增。
- $\forall x\in(l,r),f'(x)<0\Rightarrow f(x)$ 在 $(l,r)$ 单调递减。

同样地，若 $f(x)$ 在 $x_0$ 附近的取值总比 $f(x_0)$ 小，则称 $x_0$ 为 $f(x)$ 的一个 **极大值点**，$f(x_0)$ 称为 $f(x)$ 的一个 **极大值**。  
极小值和极小值点同理。极大值和极小值统称 **极值**，极值点同理。

我们同样可以通过单调性得到函数的极值。此外还有
> 费马引理：若 $f(x)$ 在 $x_0$ 处可导，那么 $x_0$ 为 $f(x)$ 极值点 $\Rightarrow f'(x_0)=0$

有了极值，我们就可求得函数的最值。

--------------------------------------------------------------------------

函数同样有凹凸性的概念。区间 $I$ 内，若 

$$\forall x_1,x_2\in I,\lambda\in(0,1),f(\lambda x_1+(1-\lambda)x_2)\le\lambda f(x_1)+(1-\lambda)f(x_2)$$

则称 $f(x)$ 在区间 $I$ 内是  **凹（下凸）** 的。若 $f(x)$ 连续，该定义等价于

$$\forall x_1,x_2\in I,\lambda\in(0,1),f\left(\dfrac{x_1+x_2}{2}\right)\le\dfrac{f(x_1)+f(x_2)}{2}$$

将定义中 $\le$ 分别换成 $<,\ge,>$，得到的分别是 **严格凹（严格下凸）**、**凸（上凸）**、**严格凸（严格上凸）** 的定义。  
我们同样可以用二阶导数获取函数的凹凸性：

- $\forall x\in I,f''(x)\ge 0\Rightarrow f(x)$ 在区间 $I$ 内是凹（下凸）的。
- $\forall x\in I,f''(x)> 0\Rightarrow f(x)$ 在区间 $I$ 内是严格凹（严格下凸）的。
- $\forall x\in I,f''(x)\le 0\Rightarrow f(x)$ 在区间 $I$ 内是凸（上凸）的。
- $\forall x\in I,f''(x)<0\Rightarrow f(x)$ 在区间 $I$ 内是严格凸（严格上凸）的。

> 注意：单调性、极值、凹凸性的定义与函数的可导性无关。

#### 导数与极限

首先讨论函数的可导性与其连续性的关系。

由 $|x|$ 可知，函数在某一点连续则不一定在该点可导。实际上，  
可以构造处处连续处处不可导的函数：魏尔斯特拉斯函数

$$f(x)=\sum_{n=0}^{+\infty}a^n\cos(b^n\pi x)\quad\left(a<1,b=2c+1(c\in\Z),ab>1+\dfrac{3\pi}{2}\right)$$

那么反之是否成立？答案是肯定的。

> 求证：$f(x)$ 在某一点可导则一定在该点连续。  
> 证明：若 $f(x)$ 在 $x_0$ 可导，则
> $$\lim_{h\rightarrow 0}\dfrac{f(x_0+h)-f(x_0)}{h}=f'(x_0)$$
> $$\lim_{h\rightarrow 0}\dfrac{f(x_0+h)-f(x_0)-f'(x_0)h}{h}=0$$
> $$f(x_0+h)-f(x_0)-f'(x_0)h=\omicron(h)\quad(h\rightarrow 0)$$
> $$f(x_0+h)=f(x_0)+f'(x_0)h+\omicron(h)\quad(h\rightarrow 0)$$
> $$\lim_{h\rightarrow 0} f(x_0+h)=\lim_{h\rightarrow 0}(f(x_0)+f'(x_0)h+\omicron(h))=f(x_0)$$
> 因此 $f(x)$ 在 $x_0$ 连续。

> 注：实际上，从微分的几何意义（下文将说明）上看，这一结论是显然的。

---------------------------------------------
接下来，与 Stolz 定理相同，我们可以借助导数求解函数极限。我们有

> **洛必达法则**，若 $\displaystyle\lim_{\mathcal{B}}$

------------------------------------

最后，讨论导函数的连续性。我们有

> 导数极限定理：若 $f(x)$ 在 $x_0$ 及其附近连续，在 $x_0$ 附近可导，则 $f(x)$ 在 $x_0$ 可导，且
> $$\lim_{t\rightarrow x_0}f'(t)=f'(x_0)$$

### 微分

微分源自于导数的莱布尼兹记号 $\dfrac{\mathrm{d}y}{\mathrm{d}x}$ 。该记号相当直观，例如导数的链式法则可用莱布尼兹记号表示为

$$\dfrac{\mathrm{d}y}{\mathrm{d}x}=\dfrac{\mathrm{d}y}{\mathrm{d}u}\dfrac{\mathrm{d}u}{\mathrm{d}x}$$

此时我们依然将 $\dfrac{\mathrm{d}y}{\mathrm{d}x}$ 视为一个整体。我们希望将其分子和分母拆开使用，即

$$\mathrm{d}y=y'\mathrm{d}x$$

显然，$\mathrm{d}y$ 与 $\Delta y$ 的意义并不相同：后者的意义就是 $y$ 的变化量，而前者的意义由上式可知，  
是将函数在某一点附近的图象 **近似为直线** 后 $y$ 的变化量。

如何理解 “近似为直线”？函数某一点的切线有两种理解方式：

1. 割线的极限，即导数  
  <div align=center> <img src=./d_0.png width=70%></div>

   $$\Downarrow Q\rightarrow P$$
   <div align=center> <img src=./d0.png width=70%></div>
2. 将函数在该点附近的图象无限放大，得到的局部图象越来越接近直线   
   <div align=center> <img src=./d0.png width=70%></div>

   $$\Downarrow\texttt{放大}$$
   <div align=center> <img src=./d1.png width=70%></div>

   $$\Downarrow\texttt{放大}$$
   <div align=center> <img src=./d2.png width=70%></div>

$\quad$这就是对 “近似为直线” 的直观理解，也就是微分的几何意义。

综上，对于变量 $y=f(x)$，若在 $x_0$ 处有

$$\Delta y=f(x_0+\Delta x)-f(x_0)=A\Delta x+\omicron(\Delta x)\quad(\Delta x\rightarrow 0)$$

其中 $A$ 为与 $\Delta x$ 无关的量，则称

$$\mathrm{d}y=A\Delta x\qquad(\Delta x\rightarrow 0)$$

为 $x_0$ 处变量 $y$ 对变量 $x$ 的 **微分**。所有点处的微分构成的关于 $x$ 的函数

$$\mathrm{d}y=A(x)\Delta x\qquad(\Delta x\rightarrow 0)$$

称为 变量 $y$ 对变量 $x$ 的微分。

特别地，由定义，自变量对自身的微分 $\mathrm{d}x=\Delta x$。

> 求证：可导与可微等价，且
> $$\mathrm{d}f(x)=f'(x)\mathrm{d}x$$
> 证明：$\displaystyle\lim_{\Delta x\rightarrow 0}\dfrac{f(x+\Delta x)-f(x)}{\Delta x}=A(x)\Leftrightarrow \lim_{\Delta x\rightarrow 0}\dfrac{f(x+\Delta x)-f(x)-A(x)\Delta x}{\Delta x}=0$
> $$\Leftrightarrow f(x+\Delta x)-f(x)-A(x)\Delta x=\omicron(\Delta x)\quad(\Delta x\rightarrow 0)$$
> $$\Leftrightarrow f(x+\Delta x)-f(x)=A(x)\Delta x+\omicron(\Delta x)\quad(\Delta x\rightarrow 0)$$
> $$\Leftrightarrow \mathrm{d}f(x)=A(x)\Delta x\quad(\Delta x\rightarrow 0)$$

这导数与微分的等价性说明，二者是对同一对象（切线）的不同表述（割线的极限和函数图象的局部近似）。   
导数的运算法则和基本初等函数的导数均可迁移到微分。

> 求证：导数的链式法则，即
> $$\dfrac{\mathrm{d}y}{\mathrm{d}x}=\dfrac{\mathrm{d}y}{\mathrm{d}u}\dfrac{\mathrm{d}u}{\mathrm{d}x}$$
> 证明：一个显然的想法是
> $$\lim_{\Delta x\rightarrow 0}\dfrac{\Delta y}{\Delta x}=\lim_{\Delta x\rightarrow 0}\dfrac{\Delta y}{\Delta u}\dfrac{\Delta u}{\Delta x}$$
> $\qquad$ 遗憾地是，$\Delta x\rightarrow 0$ 时 $\Delta u$ 可能等于 $0$ 。  
> $\qquad$ 不过我们在处理渐进时已经解决了这一问题，于是我们使用微分。设
> $$\Delta y=A\Delta u+\omicron(\Delta u)\qquad(\Delta u\rightarrow 0)$$
> $$\Delta u=B\Delta x+\omicron(\Delta x)\qquad(\Delta x\rightarrow 0)$$
> $\qquad$ 则
> $$\Delta y=A(B\Delta x+\omicron(\Delta x))+\omicron(B\Delta x+\omicron(\Delta x))$$
> $$=AB\Delta x+A\omicron(\Delta x)+\omicron(B\Delta x+\omicron(\Delta x))$$
> $$=AB\Delta x+\omicron(A\Delta x)+\omicron(B\Delta x)$$
> $$=AB\Delta x+\omicron(\Delta x)\qquad(\Delta x\rightarrow 0)$$
> $\qquad$ 因此
> $$\dfrac{\mathrm{d}y}{\mathrm{d}u}=A,\dfrac{\mathrm{d}u}{\mathrm{d}x}=B\Rightarrow \dfrac{\mathrm{d}y}{\mathrm{d}x}=AB$$

导数的链式法则在微分中等价于

$$\mathrm{d}(f(g(x)))=f'(g(x))\mathrm{d}g(x)$$

也就是说，复合函数不论对哪个中间变量作微分，微分在形式上是不变的，这称为 **微分的形式不变性**。  
因此，我们可以直接称 $\mathrm{d}y$ 为变量 $y$ 的微分，而无须说明是对哪个自变量的微分。

#### 参数方程与隐函数的导数

对于参数方程

$$\begin{cases}
	x=x(t)\\
	y=y(t)\\
\end{cases}$$

我们将 $y$ 对 $x$ 的导数直接看成两个微分相除，即

$$\dfrac{\mathrm{d}y}{\mathrm{d}x}=\dfrac{\mathrm{d}y(t)}{\mathrm{d}x(t)}=\dfrac{y'(t)\mathrm{d}t}{x'(t)\mathrm{d}t}=\dfrac{y'(t)}{x'(t)}$$

如果我们只知道 $x,y$ 的关系，例如椭圆的方程

$$\dfrac{x^2}{a^2}+\dfrac{y^2}{b^2}=0$$

其隐式给出了 $x,y$ 的函数关系，称为 **隐函数**，我们对等式两边同时取微分，得到

$$\mathrm{d}\left(\dfrac{x^2}{a^2}+\dfrac{y^2}{b^2}\right)=0$$

$$\dfrac{2x}{a^2}\mathrm{d}x+\dfrac{2y}{b^2}\mathrm{d}y=0$$

$$\dfrac{\mathrm{d}y}{\mathrm{d}x}=-\dfrac{b^2x}{a^2y}$$

这就是 **隐函数求导** 的方法。

> 思考：如何通过参数方程或隐函数求解 $\dfrac{\mathrm{d}^2y}{\mathrm{d}x^2}$？（提示：$\dfrac{\mathrm{d}^2y}{\mathrm{d}x^2}=\dfrac{\mathrm{d}\left(\dfrac{\mathrm{d}y}{\mathrm{d}x}\right)}{\mathrm{d}x}$）

## 积分学

### 定积分

### 不定积分

#### 换元积分法

#### 分部积分法

#### 有理函数积分