# 环
**环** 是一个集合 $R$ 及其上的二元运算 $+:R\times R\rightarrow R$ 和 $\cdot:R\times R\rightarrow R$   
所组成的代数结构 $(R,+,\cdot)$，满足以下三条性质：

1. $(R,+)$ 成交换群，其单位元记为 $0,a\in R$ 的逆元记为 $-a$。
2. $(R,\cdot)$ 构成半群。
3. $\cdot$ 关于 $+$ 的分配律：$\forall a,b,c\in G,(a+b)\cdot c=a\cdot c+b\cdot c,a\cdot(b+c)=a\cdot b+a\cdot c$

无歧义时，$(R,+,\cdot)$ 可简记为 $R$。

- 若 $(R,+)$ 仅成交换幺半群而非交换群，则称 $R$ 为 **半环**。
- 若 $\cdot$ 满足交换律，则称 $R$ 为 **交换环**。
- 若 $(R,\cdot)$ 构成幺半群，则称 $R$ 为 **幺环**，其乘法单位元记为 $1$。
  - 在此基础上，若 $a\in R$ 存在乘法逆元，则称 $a$ 为 **可逆元**，并将 $a$ 的乘法逆元记为 $a^{-1}$。
  - 若 $\complement_R\{0\}$ 中所有元素均存在乘法逆元，则称 $R$ 为 **除环**。
- 交换除环称为 **域**，一般用 $P$ 表示。

-----------------------------------------------
由环的定义，我们可以立即推得以下性质：

1. >$\forall a\in R,0\cdot a=a\cdot 0=0$  
   >证明： $0\cdot a+0\cdot a=(0+0)\cdot a=0\cdot a$，故由 $(R,+)$ 的消去律得
   >$$0\cdot a=0$$
2. >$\forall a,b\in R,(-a)\cdot b=a\cdot (-b)=-(a\cdot b)$  
   >证明：由分配律和性质 1. 得
   >$$a\cdot b+(-a)\cdot b=(a-a)\cdot b=0\cdot b=0$$
   >$\qquad\quad$ 故 $(-a)\cdot b=-(a\cdot b)$。  
   >$\qquad\quad$ 同理， $a\cdot (-b)=-(a\cdot b)$，于是
   >$$(-a)\cdot b=a\cdot (-b)=-(a\cdot b)$$
   > 证毕。因此以上三者均可写成 $-a\cdot b$。

环的性质 1. 的逆定理（$a\cdot b=0$ 则 $a=0$ 或 $b=0$）未必成立（例如 $2\times 2=0\pmod{4}$），  
当其成立（即 $\complement_R\{0\}$ 关于环乘法封闭）时，称 $R$ 为 **无零因子环**。    
无零因子交换幺环称为 **整环**。

> 求证：一个环是无零因子环的充要条件为乘法的左右消去律均成立，即
> $$c\not=0,a\cdot c=b\cdot c\Rightarrow a=b$$  
> $$c\not=0,c\cdot a=c\cdot b\Rightarrow a=b$$  
> 证明：先证必要性。若环为无零因子环且 $c\not =0$，$a\cdot c=b\cdot c$，由分配律和性质 2. 得
> $$(a-b)\cdot c=a\cdot c-b\cdot c=0$$
> $\qquad\quad$ 因为性质 1. 的逆定理成立且 $c\not=0$，故 $a-b=0$，即 $a=b$。  
> $\qquad\quad$ 因此乘法的左消去律成立。同理可证得乘法的右消去律也成立。  
> $\qquad\quad$ 再证充分性，若乘法的左右消去律均成立，设 $a\cdot b=0$，若 $b\not =0$，则
> $$a\cdot b=0\cdot b$$
> $\qquad\quad$ 故由乘法的右消去律可得 $a=0$。同理，若 $a\not=0$，则由乘法的左消去律可得 $b=0$。  
> $\qquad\quad$ 因此性质 1. 的逆定理成立，即环为无零因子环。


除环一定满足乘法的消去律，故除环一定是无零因子环。

# 整数环

对于环，我们着重研究的是整环，尤其是整环的数论性质。

下面介绍整数环 $\Z$ 的数论性质及算法，同时以此为例介绍环论的各种概念。

## 理想和同余

与群类似的，我们可定义子环、子环 $R'$ 的陪集 $r+R'$、环上的同余关系和环同态。

群同态的核对应正规子群。相应地，对于环同态 $\phi:R\rightarrow R'$，易验证 $\ker\phi$ 是 $R$ 的子环，且

$$\phi(r_0)=0_{R'}\Rightarrow\forall r\in R,\phi(r\cdot r_0)=0_{R''}$$

于是我们定义，若 $R$ 的子环 $I$，满足

$$\forall r\in R,r\cdot I\subseteq I\texttt{ 且 }I\cdot r\subseteq I$$

则称 $I$ 为 $R$ 的 **理想**，于是相应的得到了同余关系 $\equiv\pmod{I}$、**商环** $R/I$。

> 注：若将 $S\cdot T$ 直接理解为 $S,T$ 中元素相乘，则
> $$(a+I)\cdot(b+I)\subseteq a\cdot b+I$$
> 虽然这保证了 $\equiv\pmod{I}$ 下乘法是良定义的，但无法保证 $R/I$ 中乘法良定义，故强制定义陪集的乘法为
> $$(a+I)\cdot(b+I)=a\cdot b+I$$  
> 为保证上下文明确，$I$ 作为自身的陪集时必须写成 $0+I$。

并且，与群完全一样，有

> **环同态基本定理/第一同构定理**：对于任意环同态 $\phi:R\rightarrow R'$，$\ker\phi$ 均为 $R$ 的理想，且
> $$R/\ker\phi\cong\operatorname{Im}\phi$$  
> $\qquad$ 其同构映射 $f:R/\ker\phi\rightarrow\operatorname{Im}\phi$ 为 $f(g*\ker\phi)=\phi(g)$。  
> $\qquad$ 同时，对于环 $R$ 的任意理想 $I$，均存在同态 $\phi:R\rightarrow R/I$
> $$\phi(r)=r+I$$
> $\qquad$ 称为 $R\rightarrow R/I$ 的自然同态。

> **第二同构定理**：对于环 $R$ 的理想 $I$ 和子环 $R'$，$I\cap R'$ 是 $R'$ 的理想，  
> $\qquad R'+I$ 是 $R$ 的子环，$I$ 是 $R'+I$ 的理想，且
> $$R'/(I\cap R')\cong (R'+I)/I$$
> $\qquad$ 其同构映射 $\phi(r'+(I\cap R'))=r'+I$ 。

> **第三同构定理**：若 $I,J$ 均为 $R$ 的理想，且 $J$ 为 $I$ 的子环，则 $J$ 为 $I$ 的理想，且
> $$(R/J)/(I/J)\cong R/I$$
> $\qquad$ 其同构映射 $\phi((r+J)+(I/J))=r+I$。   
> $\qquad$ 反之，若 $J$ 是 $R$ 的理想，$J'$ 是 $R/J$ 的理想，设 $I=\displaystyle\bigcup_{j\in J}j$，  
> $\qquad$ 则 $I$ 是 $R$ 的理想，$J$ 是 $I$ 的子环且 $I/J=J'$。

整数环上的同余 $\pmod{m}$ 和一般环上的同余有一定的区别，  
其对应的是由单个生成元所生成的理想（即包含生成元的最小理想）

$$\left(a\right)=\left\{na+r'_0\cdot a+a\cdot r''_0+\sum_{k=1}^mr'_k\cdot a\cdot r''_k|n,m\in\Z,\forall k\in[0,m]\cap\N,r'_k,r''_k\in R\right\}$$

称为环 $R$ 的 **主理想**。

我们之后会证明，整数环 $\Z$ 上的理想全部为主理想，即一定能写成

$$m\Z=\{mk|k\in\Z\}$$

的形式。于是整数环上的同余就一定能写成 $\pmod{m}$。

## 整除和唯一分解

我们可以定义整环上的 **整除** 的概念。对于 $a,b\in R$，若存在 $r\in R$ 满足

$$a=b\cdot r$$

则称 $a$ 被 $b$ 整除 或 $b$ 整除 $a$，记为 $b|a$。
若 $a,b$ 互相整除对方，则称 $a,b$ **相伴**。  
显然整除是整环上的一个预序关系（定义详见【域】（2）有序域与博弈），   
且 $a,b$ 相伴当且仅当存在可逆元 $r$ 使得 $a=b\cdot r$。

对于整数环 $\Z$，在整除的基础上，定义了**约数**、**倍数**、**质数**和**合数**，且有 

> **唯一分解定理**：任意正整数 $n$ 均可唯一地写成 
> $$\displaystyle n=\prod_{k=1}^mp_k^{c_k}$$
> 的形式，其中 $p_1,p_2,\cdots$ 为质数，$c_1,c_2,\cdots,c_k\in\N$。

对于一般的整环，我们也有约数、倍数及与质数相对应的 **不可约元** 的概念，但未必满足唯一分解定理。 

满足唯一分解定理的整环被称为 **唯一分解整环**，即环中所有非零元素 $r$ 在相伴意义下均可唯一地写成

$$r=\prod_{k=1}^mp_k^{c_k}$$

的形式，其中 $p_1,p_2,\cdots$ 为不可约元，$c_1,c_2,\cdots,c_k\in\N$。

整数环上可用 Miller-Rabin 算法判素，并用 Pollad-Rho 算法分解质因数。$n$ 以下的素数大约有 $\dfrac{n}{\ln n}$ 个。

------------------------------------------------

同时，我们从理想的角度来讨论整除和唯一分解的问题。

注意到，整环中 $a$ 被 $b$ 整除和 $(a)\subseteq(b)$ 从定义上看是完全等价的，  
也就是说，**整除关系就是主理想的包含关系**（并且二者相反，$a$ 比 $b$ “大”但 $(a)$ 比 $(b)$ “小”）。

对于唯一分解，将一般环 $R$ 的两个理想 $I,J$ 的积 $I\cdot J$ 的定义改为

$$I\cdot J=\left\{a_1\cdot b_1+a_2\cdot b_2+\cdots+a_n\cdot b_n|a_1,a_2,\cdots,a_n\in I,b_1,b_2,\cdots,b_n\in J\right\}$$

即，不仅将 $I,J$ 中元素两两相乘，还通过有限次求和使之封闭。

这样一来，理想的乘积便仍是理想，且对于交换环有

$$(a)\cdot(b)=(a\cdot b)$$

读者还可用定义主动验证理想的乘积满足以下性质，  
从类比的角度看，它们是极为自然的：

- $I\cdot J\subseteq I$ 且 $I\cdot J\subseteq J$（整除就是主理想的包含关系）
- 结合律：$(I_1\cdot I_2)\cdot I_3=I_1\cdot (I_2\cdot I_3)$
- 交换律（对于交换环）：$I_1\cdot I_2=I_2\cdot I_1$
- 单位元（对于幺环）：$I\cdot R=R\cdot I=I$（幺环必有 $R=(1)$）

于是，元素的唯一分解等价于主理想的唯一分解。

## 带余除法、欧几里得

在因数和倍数的基础上，可定义两元素的 **公约数**、**公倍数**，  
进而定义 **最大公约数** $\gcd(a,b)$（是 $a,b$ 的公约数，且是 $a,b$ 所有公约数的倍数）  
和 **最小公倍数** $\operatorname{lcm}(a,b)$（是 $a,b$ 的公倍数，且是 $a,b$ 所有公倍数的约数）。

整数环 $\Z$ 中，唯一分解定理确定了二者的存在性

$$\gcd\left(\prod_kp_k^{c_k},\prod_kp_k^{d_k}\right)=\prod_{k}p_k^{\min\left(c_k,d_k\right)}$$

$$\operatorname{lcm}\left(\prod_kp_k^{c_k},\prod_kp_k^{d_k}\right)=\prod_{k}p_k^{\max\left(c_k,d_k\right)}$$

这就将 $\gcd,\operatorname{lcm}$ 与自然数的 $\min,\max$ 联系起来了。  
特别地，$0$ 被所有整数整除，因此可以认为 $0=\displaystyle\prod_{k}p_k^{+\infty}$。

类似的，环论中，也易证明对于唯一分解整环，  
两个元素的最大公因数和最小公倍数均存在且在相伴意义下唯一。

此外，整数的最大公约数和最小公倍数还满足 **欧几里得算法**

$$\gcd(a,b)=\gcd(b,a\bmod b)$$

其时间复杂度为 $\Theta(\log n)$。可以看到，欧几里得算法的基础是整数的 **带余除法**。

我们称定义了带余除法的整环称为 **欧几里得整环**。具体地，存在映射 $f:R\rightarrow\N$，满足

- $f(0)=0$。
- 带余除法：对于任意的 $a,b\in R$ 且 $b\not =0$，均存在 $q,r\in R$，  
  使得 $a=q\cdot b+r$，且 $r=0$ 或 $f(r)<f(b)$。  
  此时记 $q=\left\lfloor\dfrac{a}{b}\right\rfloor,r=a\pmod b$。

此时称 $f$ 为 $R$ 的 **范数**。整数环 $\Z$ 的范数就是绝对值函数 $|x|$。

可见，带余除法的过程中，元素的范数在单调地下降。  
因此，欧几里得整环中可用欧几里得算法计算最大公约数。

> 练习：
> - 试解释，对于整环，$\gcd,\operatorname{lcm}$ 均满足交换律、结合律，且单位元分别为 $0,1$。
> - 试解释，对于整环，$\gcd(r,1)=1,\operatorname{lcm}(r,0)=0$。
> - 试解释，对于唯一分解整环，环乘法 $\cdot$ 关于 $\gcd,\operatorname{lcm}$ 均满足分配律
> - 试解释，对于唯一分解整环，$ab=\gcd(a,b)\operatorname{lcm}(a,b)$

## 扩展欧几里得（exgcd）

整数环中，在欧几里得算法的基础上，可以讨论二元一次不定方程

$$ax+by=c\quad(a,b,c\in\N,x,y\in\Z,a^2+b^2\not=0)$$

的解的情况。

首先，若求出了该方程的一组特解 $(x_0,y_0)$，显然方程的通解为

$$\left(x_0+\dfrac{kb}{\gcd(a,b)},y_0-\dfrac{ka}{\gcd(a,b)}\right)\quad(k\in\Z)$$
至于解的存在性，我们有

>**裴蜀（Bézout's）定理**：$ax+by=c\quad(a,b,c\in\N,x,y\in\Z,a^2+b^2\not=0)$ 有解的充要条件为 $\gcd(a,b)|c$ 。  
>证明：显然 $ax+by$ 是 $\gcd(a,b)$ 的倍数。因此 $\gcd(a,b)|c$ 是有解的必要条件。  
>$\qquad\quad$ 欲证 $\gcd(a,b)|c$ 是有解的充要条件，设 $c=k\gcd(a,b)$，只要给出
> $$ax+by=\gcd(a,b)$$
> $\qquad\quad$ 的一组特解，然后扩大 $k$ 倍即可得到 $ax+by=c$ 的一组特解。  
> $\qquad\quad 1^\circ\quad a\bmod b=0$ 时，$\gcd(a,b)=b$，显然有特解 $x=0,y=1$。  
> $\qquad\quad 2^\circ\quad a\bmod b\not=0$ 时，设
> $$bx_0+(a\bmod b)y_0=\gcd(a,b)$$
> $\qquad\qquad$ 则
> $$bx_0+\left(a-\left\lfloor\dfrac{a}{b}\right\rfloor b\right)y_0=\gcd(a,b)$$
> $$ay_0+b\left(x_0-\left\lfloor\dfrac{a}{b}\right\rfloor y_0\right)=\gcd(a,b)$$
> $\qquad\qquad$ 故原方程有特解 $x=y_0,y=x_0-\left\lfloor\dfrac{a}{b}\right\rfloor y_0$ 。  
> $\qquad\quad$ 综上，我们在用欧几里得算法求出最大公约数后，一步步递归，即可求出  
> $\qquad\quad ax+by=\gcd(a,b)$ 的一组特解，命题得证。

以上所使用的求解 $ax+by=\gcd(a,b)$ 的算法称为 **扩展欧几里得算法**。对于其求出的特解，我们有

> 定理：扩展欧几里得算法所得的特解必有 $|x|\le b,|y|\le a$  
> 证明：$a\bmod b=0$ 时，显然 $|0|\le b,|1|\le a$  
> $\qquad\quad$ 否则，若 $|x_0|\le (a\bmod b),|y_0|\le b$，由 $x=y_0,y=x_0-{\left\lfloor\dfrac{a}{b}\right\rfloor}y_0$ 得  
>   $$|x|=|y_0|\le b,|y|\le|x_0|+\left|{\left\lfloor\dfrac{a}{b}\right\rfloor}y_0\right|\le a-{\left\lfloor\dfrac{a}{b}\right\rfloor}b+{\left\lfloor\dfrac{a}{b}\right\rfloor}|y_0|$$  
>   $$\le a-{\left\lfloor\dfrac{a}{b}\right\rfloor}(b-|y_0|)\in\left[a-{\left\lfloor\dfrac{a}{b}\right\rfloor}b,a\right]=[a\bmod b,a]$$  

由此彻底解决了整数环上二元一次不定方程的求解问题。

--------------------------------------------

从环论的角度看，二元一次不定方程的实质是  
求解两个主理想 $(a),(b)$ 的和 $(a)+(b)$。

易验证，一般环中两个理想 $I_1,I_2$ 的和仍是理想，且是同时包含 $I_1,I_2$ 的最小理想，  
即 $I_1,I_2\subseteq I_1+I_2$ 且对于 $R$ 的任意理想 $I$，有

$$I_1,I_2\subseteq I\Leftrightarrow I_1+I_2\subseteq I$$

整除关系就是主理想的包含关系，于是理想的加法便起到了取最大公约数的作用。  

但两个主理想的和 $(a)+(b)$ 不能保证是主理想，  
不过根据刚才的讨论，若 $(a)+(b)=(d)$ ，则必有 $d=\gcd(a,b)$。

故定义，若一个整环的所有理想均为主理想，则称为 **主理想整环**。  
于是裴蜀定理对主理想整环成立。

而在欧几里得整环中，同样可用扩展欧几里得算法给出

$$a\cdot x+b\cdot y=\gcd(a,b)$$

的一组特解。且有

> 求证：欧几里得整环一定是主理想整环。  
> 证明：对于欧几里得整环的任意理想 $I$，若 $I=\{0\}$，显然 $I$ 为主理想。  
> $\qquad$ 否则，因为 $\N$ 的任意非空子集均存在最小值，取 $I$ 中范数最小的非零元素 $d$，  
> $\qquad$ 则根据带余除法的定义，
> $$\forall a\in I,\exist q,r\in R,a=q\cdot d+r$$
> $\qquad$ 由理想的定义，$r=q\cdot d-a\in I$。又因为 $d$ 是 $I$ 中范数最小的非零元素，故 $r=0$。于是
> $$I=\{q\cdot d|q\in R\}=(d)$$


还可进一步证明，主理想整环一定是唯一分解整环。

## 中国剩余定理与笛卡尔积

整数环上，欲合并两个模数不同的同余方程
$$\begin{cases}
	x\equiv x_1\pmod{m_1}\\
	x\equiv x_2\pmod{m_2}
\end{cases}$$

设 $x=x_1+k_1m_1=x_2+k_2m_2$，则只要求出二元一次不定方程
$$m_1k_1-m_2k_2=x_2-x_1$$

的一组特解 $(k_1,k_2)$，即可合并为同余方程

$$x\equiv x_1+k_1m_1\equiv x_2+k_2m_2\pmod{\operatorname{lcm}(m_1,m_2)}$$

这就是 **扩展中国剩余定理（exCRT）**。

实际应用中，最常用的是 $m_1,m_2$ 互质的情况。设 
$$\begin{cases}
	m_2t_1\equiv 1\pmod{m_1}\\
	m_1t_2\equiv 1\pmod{m_2}
\end{cases}$$

则
$$x\equiv x_1m_2t_1+x_2m_1t_2\pmod{m_1m_2}$$
这就是 **中国剩余定理（CRT）**。其重要意义在于其给出了同构

$$\Z/nm\Z\cong\Z/n\Z\times\Z/m\Z\quad(\gcd(n,m)=1)$$

从而将模一般合数的同余分解为模 $p^k$（$p$ 为质数）的同余。

CRT 与 exCRT 均可推广到多个同余方程的情况。例如对于 CRT：

$$\begin{cases}
	x\equiv x_1\pmod{m_1}\\
	x\equiv x_2\pmod{m_2}\\
	\cdots\\
	x\equiv x_n\pmod{m_n}
\end{cases}\\
m_1,m_2,\cdots,m_n\texttt{ 两两互质}
$$

设 $M=\displaystyle\prod_{i=1}^n m_i,M_k=\dfrac{M}{m_k},M_kt_k\equiv 1\pmod{m_k}$，则
$$x\equiv\sum_{k=1}^nx_kM_kt_k\pmod{M}$$

CRT 与 exCRT 的时间复杂度均为 $\Theta(n\log\mathop{\operatorname{lcm}}_{k=1}^n m_k)$。

---------------------------------------

接下来，我们考虑将以上讨论推广到一般的交换幺环。  
与群论中的讨论一样，两个理想 $I_1,I_2$ 的交 $I_1\cap I_2$ 仍是 $R$ 的理想。对于环 $R$ 上的同余方程组

$$
\begin{cases}
	x\equiv x_1\pmod{I_1}\\
	x\equiv x_2\pmod{I_2}
\end{cases}
$$

我们有

> 一般环上的扩展中国剩余定理：对于环 $R$ 的理想 $I_1,I_2$，  
> $\qquad$ 存在单同态 $\phi:R/(I_1\cap I_2)\rightarrow R/I_1\times R/I_2$
> $$\phi(r\bmod(I_1\cap I_2))=(r\bmod I_1,r\bmod I_2)$$
> $\qquad$ 其中 $\operatorname{Im}\phi=\{(x_1\bmod I_1,x_2\bmod I_2)\in R/I_1\times R/I_2|x_1-x_2\in I_1\cap I_2\}$

此时扩展欧几里得只是帮助我们求解理想的交的手段。  
因为 $I_1\cap I_2$ 是同时被 $I_1,I_2$ 包含的最大理想，  
于是和理想的加法一样，理想的交集运算也起到了取最小公倍数的作用。因此同理，

- $I\cdot J\subseteq I\cap J$
- 整环中若 $(a)\cap (b)=(d)$，则 $d=\operatorname{lcm}(a,b)$

而对于 CRT，与群论中的讨论一样，$I_1+I_2=R$ 时，  
扩展中国剩余定理给出的单同态是双射，有


$$R/(I_1\cap I_2)\cong R/I_1\times R/I_2$$

但仅有该式还不方便推广到多个理想的情况。

类比整数互质的定义，定义理想 $I_1,I_2$ **互素** 当且仅当 $I_1+I_2=R$。此时同样有

> 求证：交换幺环 $R$ 中，若 $I_1,I_2$ 互素，则 $I_1\cdot I_2=I_1\cap I_2$   
> 证明*：因为 $1\in R=I_1+I_2$，故设 $1=i_1+i_2\ (i_1\in I_1,i_2\in I_2)$，则 $\forall i\in I_1\cap I_2$，
> $$i=i\cdot 1=i\cdot(i_1+i_2)=i\cdot i_1+i\cdot i_2=i_1\cdot i+i\cdot i_2\in I_1\cdot I_2$$
> $\qquad$ 故 $I_1\cap I_2\subseteq I_1\cdot I_2$。我们已经验证了 $I_1\cdot I_2\subseteq I_1\cap I_2$，故 $I_1\cdot I_2=I_1\cap I_2$。    

> 求证：交换幺环 $R$ 中，若 $I_1$ 和 $I_2,I_3$ 均互素，则 $I_1$ 和 $I_2\cdot I_3$ 互素   
> 证明*：因为 $1\in R=I_1+I_2$，故设 $1\equiv i_2\pmod{I_1}$（$i_2\in I_2$），  
> $\qquad$ 又因为 $R=I_1+I_3$，故 $\forall r\in R$，设 $r=i_3$（$i_3\in I_3$），则
> $$r=1\cdot r\equiv i_2\cdot i_3\pmod{I_1}\Rightarrow r\in I_1+i_2\cdot i_3\subseteq I_1+I_2\cdot I_3$$
> $\qquad$ 因此 $R=I_1+I_2\cdot I_3$，即 $I_1$ 和 $I_2\cdot I_3$ 互素。

于是，即可归纳得

> **一般环上的中国剩余定理**：若交换幺环 $R$ 中 $I_1,I_2,\cdots,I_n$ 两两互素，则
> $$R/(I_1\cdot I_2\cdot\cdots\cdot I_n)=R/(I_1\cap I_2\cap\cdots\cap I_n)\cong R/I_1\times R/I_2\times\cdots\times R/I_n$$

## BSGS 与 exBSGS

整数模 $n$ 乘法群 $\Z_n^{\times}$ 存在原根，在此基础上我们可以求解模意义下的 **离散对数**。

设模数为 $m$，底数为 $a$，真数为 $N$，离散对数即求解 

$$a^x\equiv N\pmod{m}$$

的最小自然数解。

$a,m$ 互质时，$N\in[0,\varphi(m))\cap\N$，一个显然的暴力算法是将 $x$ 拆分为

$$x=s+t\lceil\sqrt{\varphi(m)}\rceil\quad(s,t\in[0,\lceil\sqrt{\varphi(m)}\rceil)\cap\N)$$

于是

$$a^{s+t\lceil\sqrt{\varphi(m)}\rceil}\equiv N\pmod{m}$$
$$a^{t\lceil\sqrt{\varphi(m)}\rceil}\equiv a^{-s}N\pmod{m}$$

于是将所有的 $Na^{-s}$ 存入一个哈希表，对于所有的 $a^{t\lceil\sqrt{\varphi(m)}\rceil}$，依次查询与之对应的 $a^{-s}N$ 即可。    
这就是所谓 **大步小步（BSGS）算法**。

$a,m$ 不互质时，设 $d=\gcd(a,m)$，则

- $d\nmid b$ 时，当且仅当 $b=1$ 时有解 $x=0$。
- 否则，原问题等价于
  $$\dfrac{a}{d}a^{x-1}\equiv \dfrac{N}{d}\pmod{\dfrac{m}{d}}$$
  若此时仍有 $\gcd\left(a,\dfrac{m}{d}\right)\not =1$，继续尝试同除以 $\gcd\left(a,\dfrac{m}{d}\right)$，直到得到以下方程
  $$\dfrac{a^k}{D}a^{x-k}\equiv\dfrac{N}{D}\pmod{\dfrac{m}{D}},\gcd\left(a,\dfrac{m}{D}\right)\nmid N\texttt{ 或 }\gcd\left(a,\dfrac{m}{D}\right)=1$$
  首先 $\Theta(k)$ 求出 $x\le k$ 的解。  
  若 $\gcd\left(a,\dfrac{m}{D}\right)\not=1$，显然 $x> k$ 范围内无解。   
  否则，可 BSGS 求出所有 $x> k$ 的解。

这就是 **扩展大步小步算法（exBSGS）**。

BSGS 与 exBSGS 的时间复杂度均为 $\Theta(\sqrt{m})$。  
BSGS算法可推广到一般群的对数问题。

## Lucas 与 exLucas

对于组合数取模

$$\dbinom{n}{m}\bmod p$$

当 $n,m\ge p$ 时，是不能通过组合数的定义式求解的。  
但 $p$ 为素数时，我们有如下定理：

> **卢卡斯（Lucas）定理**：$p$ 为素数时，$\dbinom{n}{m}\equiv\dbinom{\lfloor\frac{n}{p}\rfloor}{\lfloor\frac{m}{p}\rfloor}\dbinom{n\bmod p}{m\bmod p}\pmod{p}$  
> 证明*：首先 $\forall k\in[1,p-1]\cap\N$，由吸收恒等式（详见【组合】（1）二项式系数）得
> $$\dbinom{p}{k}=\dfrac{p}{k}\dbinom{p-1}{k-1}$$
> $\qquad\quad$ 其中由 $p$ 为素数，可知 $k$ 为 $\dbinom{p-1}{k-1}$ 的约数，故
> $$\dbinom{p}{k}\bmod p=\dfrac{p}{k}\dbinom{p-1}{k-1}\bmod p=0\quad(k\in[1,p-1]\cap\N)$$
> $\qquad\quad$ 于是在 $\Z_p$ 上的多项式环 $\Z_p[x]$ 上，由二项式定理得
> $$(1+x)^p=\sum_{k=0}^p\dbinom{p}{k}x^k=\dbinom{p}{0}+\dbinom{p}{p}x^p+\sum_{k=1}^{p-1}\dbinom{p}{k}x^k \equiv 1+x^p\pmod{p}$$
> $\qquad\quad$（详见【环】（2）多项式环 和 【组合】（1）二项式系数）  
> $\qquad\quad$ 在此基础上，有
> $$\dbinom{n}{m}\equiv [x^m](1+x)^n\equiv[x^m](1+x)^{p\left\lfloor\frac{n}{p}\right\rfloor}(1+x)^{n\bmod p}$$
> $$\equiv[x^m](1+x^p)^{\left\lfloor\frac{n}{p}\right\rfloor}(1+x)^{n\bmod p}\equiv\sum_{k=0}^{+\infty}\dbinom{\lfloor\frac{n}{p}\rfloor}{k}\dbinom{n\bmod p}{m-pk}\pmod{p}$$
> $\qquad\quad$ 欲使 $\dbinom{n\bmod p}{m-pk}$ 非 $0$，则
> $$0\le m-pk\le n\bmod p<p$$
> $$\Rightarrow\dfrac{m}{p}-1<k\le \dfrac{m}{p}$$
> $\qquad\quad$ 故仅有 $k=\left\lfloor\dfrac{m}{p}\right\rfloor$ 满足要求，此时 $m-pk=m\bmod p$，于是
> $$\dbinom{n}{m}\equiv\dbinom{\lfloor\frac{n}{p}\rfloor}{\lfloor\frac{m}{p}\rfloor}\dbinom{n\bmod p}{m\bmod p}\pmod{p}$$

有了 Lucas 定理，对于 $p$ 是质数的情况，只要对 $n,m$ 作 $p$ 进制分解即可。时间复杂度 $\Theta((n+m)\log p)$。

但该做法不具有一般性。根据中国剩余定理，一般模数的情况至多简化为问题 
 
$$\dbinom{n}{m}\bmod p^k$$

其中 $p$ 为素数。

对于该问题的求解，设 $V(n)$ 为质因子 $p$ 在 $n!$ 中的次数，$f(n)=\dfrac{n!}{p^{V(n)}}\bmod p$，则

$$\dbinom{n}{m}\equiv\dfrac{f(n)}{f(m)f(n-m)}p^{V(n)-V(m)-V(n-m)}\pmod{p^k}$$

问题转化为求解 $V(n)$ 和 $f(n)$。设

$$g(n)=\dfrac{n!}{\displaystyle\prod_{i=1}^{\left\lfloor\frac{n}{p}\right\rfloor}pi}$$

将阶乘展开，可得

$$V(0)=1,V(n)=\left\lfloor\dfrac{n}{p}\right\rfloor+V\left(\left\lfloor\dfrac{n}{p}\right\rfloor\right)$$

$$f(0)=1,f(n)\equiv f\left(\left\lfloor\dfrac{n}{p}\right\rfloor\right)g^{\left\lfloor\frac{n}{p^k}\right\rfloor}\left(p^k-1\right)g(n\bmod p^k)\pmod{p^k}$$

$\forall t\in[0,p^k)$，预处理 $g(t)$，即可递归求解 $V(n)$ 和 $f(n)$。 

这就是 **扩展卢卡斯定理（exLucas）**，其时间复杂度为 $\Theta(\sum_i p_i^{k_i})-\Theta(l\log n)$。

## $n$ 次剩余

在原根和 BSGS 的基础上，可以求解模意义下的开根问题。

对于方程 $x^n\equiv k\pmod{m}$，若其有解，则称 $k$ 为模 $m$ 意义下的 **$n$ 次剩余**。

练习：试根据 BSGS 和 $\Z_m^\cdot$ 的结构，给出 $n$ 次剩余的一般求解算法。

### 模奇素数意义下的二次剩余

一般的 $n$ 次剩余算法不适合多组数据的快速判定与求解。  
在模奇素数意义下，有更快的判定开根可行性的算法：

> **欧拉判别准则**：若 $p$ 为奇素数，设 $k=\gcd(n,p-1)$，则 $m$ 为模 $p$ 意义下的 $n$ 次剩余当且仅当 
> $$m^\frac{p-1}{k}\equiv 1\pmod{p}$$    
>$\qquad\qquad$ 且 $x^n\equiv t\pmod{p}$ 有解时，其解数为 $k$。  
> 通过原根和 exgcd 即可证明。留给读者作为练习。

在欧拉判别准则的基础上，对于**模奇素数意义下的二次剩余**，  
我们知道若 $x^2\equiv a\pmod{p}$ 有解则其解数一定为 $2$，且有判别开根可行性的 **勒让德符号**：

$$
\left(\dfrac{a}{p}\right)=\begin{Bmatrix}
	0&(a\equiv 0)\\
	1&(a\not\equiv 0,\texttt{且 } x^2\equiv a\pmod{p}\texttt{ 有解})\\
	-1&(x^2\equiv a\pmod{p}\texttt{ 无解})
\end{Bmatrix}\equiv a^{\frac{p-1}{2}}\pmod{p}$$

勒让德符号满足以下性质：

- 完全积性：$\left(\dfrac{a}{p}\right)\left(\dfrac{b}{p}\right)=\left(\dfrac{ab}{p}\right)$
- **二次互反律**：若 $p,q$ 均为奇素数，则 $\left(\dfrac{q}{p}\right)\left(\dfrac{p}{q}\right)=(-1)^{\frac{(p-1)(q-1)}{4}}$

接下来，可以通过二次剩余的判别来求解开根。

对于方程 $x^2\equiv n\pmod{p}$，首先找到一个 $a$ 满足 $a^2-n$ 为非二次剩余，显然期望 $2$ 次找到这样一个 $a$ 。  

接下来，正如从实数扩张到复数那样，考虑类似的扩域：

> 求证：设虚数 $i^2\equiv a^2-n\pmod{p}$，则必有 $(a+i)^{p+1}\equiv n\pmod{p}$。  
> 证明*：$i^p\equiv i(i^2)^\frac{p-1}{2}\equiv i(a^2-n)^{\frac{p-1}{2}}\equiv-i\pmod{p}$ 。  
> $\qquad$ 在证明 Lucas 定理时已经证明了 $(1+x)^p\equiv 1+x^p\pmod{p}$，故  
> $\qquad$ 因此 $(a+i)^{p+1}\equiv(a^p+i^p)(a+i)\equiv(a-i)(a+i)\equiv n\pmod{p}$。

由此我们得到了一个解 $x_0\equiv (a+i)^{\frac{p+1}{2}}\pmod{p}$。
> 求证：$x_0$ 的虚部一定为 $0$。  
> 证明：假设存在 $B\not\equiv 0,(A+Bi)^2\equiv n\pmod{p}$，  
> $\qquad$ 则 $A^2+B^2(a^2-n)-n\equiv -2ABi\pmod{p}$。  
> $\qquad$ 因此 $AB\equiv 0\pmod{p}$，又已知 $B\not\equiv 0\pmod{p}$，于是 $A\equiv 0\pmod{p}$。  
> $\qquad$ 代入得 $i^2=nB^{-2}$，也就是说 $i^2$ 为二次剩余，与 $i^2$ 为非二次剩余矛盾，证毕。

该算法即求解模奇素数下二次剩余的 **Cipolla 算法**，其时间复杂度为 $\Theta(\log p)$