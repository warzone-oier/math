## 模、半模与线性空间
对于域 $P$ 及集合 $V$，若

-  定义向量加法 $+:V\times V\rightarrow V$，使得 $(V,+)$ 构成交换群，  
   其单位元记为 $\vec{0},\vec{a}\in V$ 的逆元记为 $-\vec{a}$ 。
- 定义数乘 $P\times V\rightarrow V$，使得
  - 数乘的单位元：$\forall\vec{a}\in M,1\vec{a}=\vec{a}$
  - 数乘与域乘法的相容性：$\forall\lambda,\mu\in P,\vec{a}\in V,(\lambda\mu)\vec{a}=\lambda(\mu\vec{a})$
  - 数乘关于域加法的分配律：$\forall \lambda,\mu\in P,\vec{a}\in V,(\lambda+\mu)\vec{a}=\lambda\vec{a}+\mu\vec{a}$
  - 数乘关于向量加法的分配律：$\forall \lambda\in P,\vec{a},\vec{b}\in V,\lambda(\vec{a}+\vec{b})=\lambda\vec{a}+\lambda\vec{b}$

则称 $V$ 为域 $P$ 上的 **线性空间**（或 **向量空间**），其元素称为 **向量**。

如果将定义中的域 $P$ 改为一般的幺环（或幺半环） $R$，则称对应的集合 $M$ 为 $R-$**模**（或 $R-$**半模**）。



线性空间是欧几里得空间向量的推广。如：

- 具有相同周期的正弦函数 $\{A\sin(\omega x+\varphi)|A,\varphi\in\R\}$ 为 $\R$ 上的线性空间。
- 二进制数为 $\{0,1\}$ 上的线性空间，其中乘法为与，加法为异或。

模和半模则是向量空间的推广，如：

- 幺环 $R$ 上的多项式环 $R[x]$，形式幂级数环 $R[[x]]$ 均为 $R$ 上的模。
- 任意交换群均为整数环 $\Z$ 上的模，标量乘法即群的乘方。
- $[-\infty,+\infty]$ 上的半模，其中加法为 $\min$ 或 $\max$，乘法为实数的加法。

## 基、维数与坐标

在两向量共线的充要条件、平面向量基本定理（三向量共面的充要条件）、空间向量基本定理的基础上，

我们定义所谓的 **线性相关**：

对于 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n\in V$，若 $\forall x_1,x_2,\cdots,x_n\in P$，当且仅当 $x_1=x_2=\cdots=x_n=0$ 时，有

$$x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n=\vec{0}$$

则称 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ **线性无关**，否则称 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ **线性相关**。

显然，$n=2$ 时，线性相关就是两向量共线；$n=3$ 时，线性相关就是三向量共面。

在此基础上，我们定义一般线性空间 $V$ 的 **基底**:

若 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性无关，且 $\forall\vec{p}\in V,\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n,\vec{p}$ 线性相关。  
则称 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 为 $V$ 的一组 **基底**（或 **基**）。


> 求证：线性空间基的定义等价于，$\forall\vec{p}\in V,\vec{p}$ 总可由 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 唯一 **表示**，即  
> $\qquad$ 总存在唯一的 $x_1,x_2,\cdots,x_n\in P,\vec{p}=x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n$。  
> 证明：
> > 必要性：首先证明存在性。由基的原定义，$\vec{p},\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性相关，即
> > $$\exist y_0,y_1,\cdots,y_n\in P\texttt{ 不同时为 }0,y_0\vec{p}+y_1\vec{a}_1+y_2\vec{a}_2+\cdots+y_n\vec{a}_n=\vec{0}$$
> > $\qquad$ 若 $y_0=0$，则
> > $$y_1\vec{a}_1+y_2\vec{a}_2+\cdots+y_n\vec{a}_n=\vec{0}$$
> > $\qquad$ 由基的原定义，$\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性无关，因此
> > $$y_1=y_2=\cdots=y_n=0$$
> > $\qquad$ 这与 $y_0,y_1,\cdots,y_n$ 不同时为 $0$ 矛盾。故 $y_0\not =0$，于是
> > $$\vec{p}=\dfrac{y_1}{-y_0}\vec{a}_1+\dfrac{y_2}{-y_0}\vec{a}_2+\cdots+\dfrac{y_n}{-y_0}\vec{a}_n$$
> > $\qquad$ 因此存在 $x_1=\dfrac{y_1}{-y_0},x_2=\dfrac{y_2}{-y_0},\cdots,x_n=\dfrac{y_n}{-y_0}\vec{a}_n,\vec{p}=x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n$。  
> > $\qquad$ 然后证明唯一性。设  
> > $$\vec{p}=x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n=x_1'\vec{a}_1+x_2'\vec{a}_2+\cdots+x_n'\vec{a}_n$$
> > $\qquad$ 则
> > $$(x_1-x_1')\vec{a}_1+(x_2-x_2')\vec{a}_2+\cdots+(x_n-x_n')\vec{a}_n=\vec{0}$$
> > $\qquad$ $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性无关，故
> > $$x_1-x_1'=x_2-x_2'=\cdots=x_n-x_n'=0$$
> > $$x_1=x_1',x_2=x_2',\cdots,x_n=x_n'$$
> > $\qquad$ 由此证明 $x_1,x_2,\cdots,x_n$ 是唯一的。
>
> > 充分性：$\forall \vec{p}\in V,\exist$ 唯一的 $x_1,x_2,\cdots,x_n\in P,\vec{p}=x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n$，即  
> > $$x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n-\vec{p}=\vec{0}$$
> > $\qquad -1\not =0$，故 $x_1,x_2,\cdots,x_n$ 不同时为 $0$，即 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n,\vec{p}$ 线性相关。  
> > $\qquad$ 而当 $\vec{p}=\vec{0}$ 时，有  
> > $$\exist\texttt{ 唯一的 }x_1,x_2,\cdots,x_n\in P,x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n=\vec{0}$$
> > $\qquad$ 由于 $0\vec{a}_0+0\vec{a}_1+\cdots+0\vec{a}_n=\vec{0}$ 恒成立，可得
> > $$x_1=x_2=\cdots=x_n=0$$
> > $\qquad$ 也就是说，当且仅当 $x_1=x_2=\cdots=x_n=0$ 时，$x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n=\vec{0}$，  
> > $\qquad$ 即 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性无关。  
> > $\qquad$ 综上，$\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性无关，且 $\forall\vec{p}\in V,\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n,\vec{p}$ 线性相关。

一般模上，这两种定义不是等价的，我们取后一种作为其基底的定义。  

基底是否一定存在呢？研究表明，一般模不一定存在基底，而对于线性空间，  
**“任意线性空间都存在基” 等价于选择公理**。

存在基底的模称为 **自由模**。

欧几里得空间中，**维度** 的概念十分重要，而对于一般的线性空间，我们可以证明，  
同一线性空间 $V$ 的不同基底所含向量的个数相等，这称为线性空间 $V$ 的 **维数**，记为 $\dim V$。

-----------------------------------------

在基底的第二种定义的基础上，可以定义向量 $\vec{p}$ 在基底 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 下的 **坐标**：若

$$\vec{p}=p_1\vec{a}_1+p_2\vec{a}_2+\cdots+p_n\vec{a}_n$$

则称 $\vec{p}$ 在基 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 下的 **坐标** 为

$$
\begin{bmatrix}
	p_1\\
	p_2\\
	\vdots\\
	p_n
\end{bmatrix}
$$

一般的线性空间中，我们只定义了向量的线性运算，而不涉及长度和正交性。  
因此，该定义下的坐标对我们的研究便已经足够。

在不加说明的情况下，我们会忽略具体的基，而将向量与其坐标视为等价的。

## 同构、线性子空间、仿射空间

我们同样可以定义线性空间 **同构** 的概念：  
同一域 $P$ 上的两个线性空间 $V,W$ 同构，当且仅当存在双射 $\varphi:V\rightarrow W$，使得
$$\forall\vec{a},\vec{b}\in V,\varphi(\vec{a}+\vec{b})=\varphi(\vec{a})+\varphi(\vec{b})\quad(1)$$

$$\forall\lambda\in P,\vec{a}\in V,\varphi(\lambda\vec{a})=\lambda\varphi(\vec{a})\quad(2)$$

则我们显然有以下结论：

1. > 求证：同一域 $P$ 上两个线性空间 $V,W$ 同构。若 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n\in V$ 是 $V$ 的一组基底，  
   > $\qquad$ 则 $\varphi(\vec{a}_1),\varphi(\vec{a}_2),\cdots,\varphi(\vec{a}_n)$ 是 $W$ 的一组基底。  
   > 证明：由式 $(1)(2)$，可得
   > $$\varphi(x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n)=x_1\varphi(\vec{a}_1)+x_2\varphi(\vec{a}_2)+\cdots+x_n\varphi(\vec{a}_n)\quad(3)$$
   > $\qquad$ 若 $\varphi(\vec{a}_1),\varphi(\vec{a}_2),\cdots,\varphi(\vec{a}_n)$ 线性相关，则
   > $$x_1\varphi(\vec{a}_1)+x_2\varphi(\vec{a}_2)+\cdots+x_n\varphi(\vec{a}_n)=\vec{0}_W$$
   > $\qquad$ 有至少两组解，由 $(3)$ 以及 $\varphi$ 的双射性质，这也即
   > $$\varphi(x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n)=\vec{0}_W$$
   > $$x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n=\vec{0}_V$$
   > $\qquad$ 有至少两组解，这与 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性无关矛盾，故 $\varphi(\vec{a}_1),\varphi(\vec{a}_2),\cdots,\varphi(\vec{a}_n)$ 线性无关。  
   > $\qquad$ 同样地，$\forall b\in W$，我们有
   > $$x_1\varphi(\vec{a}_1)+x_2\varphi(\vec{a}_2)+\cdots+x_n\varphi(\vec{a}_n)+x_{n+1}\vec{b}=\varphi(x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n+x_{n+1}\varphi^{-1}(\vec{b}))\quad(4)$$
   > $\qquad\quad\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n,\varphi^{-1}(\vec{b})$ 总是线性相关的，即  
   > $$x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n+x_{n+1}\varphi^{-1}(\vec{b})=\vec{0}_V$$
   > $\qquad\quad$ 有至少两组解，由 $(4)$ 以及 $\varphi$ 的双射性质，这也即
   > $$x_1\varphi(\vec{a}_1)+x_2\varphi(\vec{a}_2)+\cdots+x_n\varphi(\vec{a}_n)+x_{n+1}\vec{b}=\vec{0}_W$$
   > $\qquad\quad$ 有至少两组解，故 $\varphi(\vec{a}_1),\varphi(\vec{a}_2),\cdots,\varphi(\vec{a}_n),\vec{b}$ 总是线性相关的。  
   > $\qquad\quad$ 综上，$\varphi(\vec{a}_1),\varphi(\vec{a}_2),\cdots,\varphi(\vec{a}_n)$ 是 $W$ 的一组基底。

2. > 求证：同一域 $P$ 上，若两个线性空间 $V,W$ 的维数均存在，则 $V,W$ 同构当且仅当它们的维数相等。  
   > 证明：性质 1. 已证充分性，下证必要性。  
   > $\qquad V,W$ 维数相等时，设二者维数均为 $n$，  
   > $\qquad$ 分别取 $V$ 的一组基底 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 和 $W$ 的一组基底 $\vec{b}_1,\vec{b}_2,\cdots,\vec{b}_n$，构建映射
   > $$\varphi(x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n)=x_1\vec{b}_1+x_2\vec{b}_2+\cdots+x_n\vec{b}_n$$
   > $\qquad$ 显然这是 $V\rightarrow W$ 的一个双射，且满足式 $(1)(2)$，故 $V,W$ 同构。 

----------------------------------------------------

同样的，我们可以定义所谓 **线性子空间** 的概念：  

若 $V$ 为域 $P$ 上的线性空间，则 $W$ 为 $V$ 的线性子空间当且仅当 $W\subseteq V$，  
且 $W$ 也为域 $P$ 上的线性空间。

线性子空间的定义可以弱化为 $W\subseteq V$ 且 $W$ 关于向量加法和数乘封闭，  
因为向量加法的交换律、结合律，以及数乘的单位元、相容性、分配律在封闭性成立的条件下显然成立，  
且我们可以用数乘构造向量加法的单位元和逆元

$$\vec{a}\in W,\vec{0}=0\vec{a}\in W$$

$$\forall \vec{a}\in W,-\vec{a}=-1\vec{a}\in W$$

在线性子空间的基础上，我们可以定义 **仿射空间** 的概念：

若 $W$ 为 $V$ 的线性子空间，$\vec{b}\in V$，则 $V$ 的一个仿射空间为

$$\vec{b}+W=\{\vec{b}+\vec{\delta}|\vec{\delta}\in W\}$$

欧几里得空间的仿射空间又叫 **线性流形**，它们就是我们熟知的点、直线、平面等几何对象。

## 赋范空间与内积空间*

绝大部分时候，我们只需要线性性，即加法与数乘，线性空间的概念便已经足够。

但有时，我们需要长度、角度等概念，这需要相应地引入模长与内积。

-------------------------------------

我们定义域 $P$ 上的 **绝对值** 为满足以下条件的函数 $|\cdot|:P\rightarrow\R$ ：

1. 正定性：$|x|=0$ 当且仅当 $x=0$
2. 齐次性：$|xy|=|x||y|$
3. 三角形不等式：$|x+y|\le|x|+|y|$

性质 3. 取 $y=-x$ 可证明绝对值是非负的。  
取 $|\cdot|$ 的值域为 $\R$ 是因为一切可完备化的有序域均为 $\R$ 的子域。

在绝对值的基础上，对于 $P$ 上的线性空间 $V$，定义 $V$ 上的 **范数** 为满足以下条件的映射 $||\cdot||:V\rightarrow\R$：

1. 正定性：$||\vec{a}||=0$ 当且仅当 $\vec{a}=\vec{0}$
2. 齐次性：$||\lambda\vec{a}||=|\lambda|||\vec{a}||$
3. 三角形不等式：$||\vec{a}+\vec{b}||\le||\vec{a}||+||\vec{b}||$

范数是模长在一般线性空间的推广，定义了范数的线性空间称为 **赋范线性空间**，简称 **赋范空间**。

范数的重要意义在于其导出了距离的概念。对于集合 $X$ 和映射 $d:X\times X\rightarrow\R$，若 $d$ 满足

1. 正定性：$d(x_1,x_2)=0$ 当且仅当 $x_1=x_2$
2. 对称性：$d(x_1,x_2)=d(x_2,x_1)$
3. 三角形不等式：$d(x_1,x_3)\le d(x_1,x_2)+d(x_2,x_3)$

则称 $d$ 为 $X$ 上的 **度量**，$(X,d)$ 为 **度量空间**。

由上可知，任意赋范线性空间均存在一个自然的度量

$$d(\vec{a},\vec{b})=||\vec{a}-\vec{b}||$$

其在满足度量空间的公理的同时，还满足

1. 平移不变性：$d(\vec{a}+\vec{c},\vec{b}+\vec{c})=d(\vec{a},\vec{b})$
2. 齐次性：$d(\lambda\vec{a},\lambda\vec{b})=|\lambda|d(\vec{a},\vec{b})$

以下是常见的范数和度量：

- 闵可夫斯基距离 $L_p(p\ge 1)$：$||\vec{x}||_p=\left(\displaystyle\sum_{k}|x_k|^p\right)^{\frac{1}{p}}$  
  $p=2$ 时即我们所熟知的欧几里得距离，$p=1$ 时称为曼哈顿距离。
- 切比雪夫距离 $L_{\infty}$：$||\vec{x}||=\displaystyle\max_k|x_k|$  
  取对数后使用洛必达法则，可知切比雪夫距离是 $p\rightarrow\infty$ 时的闵可夫斯基距离。  
  （详见【分析】（3）无限微积分）

--------------------------------------------

范数定义了线性空间中的长度概念，而内积定义了线性空间中的角度概念。

设域 $P$ 为 $\R$ 或 $\mathbb{C}$，对于 $P$ 上的线性空间 $V$，  
其上的 **内积** 为满足以下条件的函数 $\cdot:V\times V\rightarrow P$，满足

1. 共轭对称性：$\forall\vec{a},\vec{b}\in V,\vec{a}\cdot\vec{b} =\overline{\vec{b}\cdot\vec{a}}$
2. 线性：$\forall\vec{a},\vec{b},\vec{c}\in V,(\vec{a}+\vec{b})\cdot\vec{c}=\vec{a}\cdot\vec{c}+\vec{b}\cdot\vec{c}$  
   $\qquad\quad\forall\lambda\in P,\vec{a},\vec{b}\in V,(\lambda\vec{a})\cdot\vec{b}=\lambda(\vec{a}\cdot\vec{b})$
3. 非负性：$\forall\vec{a}\in V,\vec{a}\cdot\vec{a}\ge 0,$，当且仅当 $\vec{a}=\vec{0}$ 时取等号。

定义了内积的线性空间称为 **内积空间**。

结合线性和共轭对称性，可得内积对于右半边满足

$$\forall\vec{a},\vec{b},\vec{c}\in V,\vec{a}\cdot(\vec{b}+\vec{c})=\vec{a}\cdot\vec{b}+\vec{a}\cdot\vec{c}$$
$$\forall\lambda\in P,\vec{a},\vec{b}\in V,\vec{a}\cdot(\lambda\vec{b})>=\overline{\lambda}(\vec{a},\vec{b})$$


在内积空间中，我们定义范数 $||\vec{a}||=\sqrt{\vec{a}\cdot\vec{a}}$，其满足

> **柯西-施瓦兹不等式：** $\left|\vec{a}\cdot\vec{b}\right|\le||\vec{a}||||\vec{b}||$，当且仅当 $\vec{a},\vec{b}$ 共线时取等号。  
> 证明：$\vec{a}=\vec{0}$ 或 $\vec{b}=\vec{0}$ 时，由内积的非负性，上述不等式显然成立。  
> $\qquad\quad\vec{a},\vec{b}\not=\vec{0}$ 时，设
> $$\vec{e}_{\vec{a}}=\dfrac{\vec{a}}{||\vec{a}||},\vec{e}_{\vec{b}}=\dfrac{\vec{b}}{||\vec{b}||},\lambda=\vec{e}_{\vec{a}},\vec{e}_{\vec{b}}$$
> $\qquad$ 则 $||\vec{e}_{\vec{a}}||=||\vec{e}_{\vec{b}}||=1$，且由内积性质得
> $$(\vec{e}_{\vec{a}}-\lambda\vec{e}_{\vec{b}})\cdot(\vec{e}_{\vec{a}}-\lambda\vec{e}_{\vec{b}})=\vec{e}_{\vec{a}}\cdot\vec{e}_{\vec{a}}+(-\lambda\vec{e}_{\vec{b}})\cdot\vec{e}_{\vec{a}}+\vec{e}_{\vec{a}}\cdot(-\lambda\vec{e}_{\vec{b}})+(-\lambda\vec{e}_{\vec{b}})\cdot(-\lambda\vec{e}_{\vec{b}})$$
> $$=1-\lambda\overline{\lambda}-\overline{\lambda}\lambda+\lambda\overline{\lambda}=1-\overline{\lambda}\lambda=1-|\lambda|^2\ge 0$$
> $\qquad\quad$ 因此
> $$\dfrac{\left|\vec{a}\cdot\vec{b}\right|}{||\vec{a}||||\vec{b}||}=\left|\dfrac{\vec{a}}{||\vec{a}||}\cdot\dfrac{\vec{b}}{||\vec{b}||}\right|=|\vec{e}_{\vec{a}}\cdot\vec{e}_{\vec{b}}|=|\lambda|\le 1$$
> $$\left|\vec{a}\cdot\vec{b}\right|\le||\vec{a}||||\vec{b}||$$

在此基础上，即可证明由此定义的范数的三角形不等式。

> 证明：
> $$||\vec{a}+\vec{b}||^2=(\vec{a}+\vec{b})\cdot(\vec{a}+\vec{b})=\vec{a}\cdot\vec{a}+\vec{b}\cdot\vec{b}+\vec{a}\cdot\vec{b}+\vec{b}\cdot\vec{a}$$
> $$=||\vec{a}||^2+||\vec{b}||^2+\vec{a}\cdot\vec{b}+\overline{\vec{a}\cdot\vec{b}}=||\vec{a}||^2+||\vec{b}||^2+2\operatorname{Re}(\vec{a}\cdot\vec{b})$$
> $\qquad\quad$ 于是由柯西-施瓦兹不等式得
> $$||\vec{a}+\vec{b}||^2\le ||\vec{a}||^2+||\vec{b}||^2+2\left|\vec{a}\cdot\vec{b}\right|\le ||\vec{a}||^2+||\vec{b}||^2+2||\vec{a}||||\vec{b}||=(||\vec{a}||+||\vec{b}||)^2$$

柯西-施瓦兹不等式确定了内积的取值范围，于是由此可定义两向量的夹角

$$\theta=\arccos\dfrac{\operatorname{Re}(\vec{a}\cdot\vec{b})}{||\vec{a}||||\vec{b}||}$$

特别地，$\vec{a},\vec{b}=0$ 时，两向量夹角为 $90^\circ$，此时称两向量 **正交** 。

> 求证：若 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n\in V$ 均非 $\vec{0}$ 且两两正交，则它们线性无关。   
> 证明：假设 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性相关，即
> $$\exist x_1,x_2,\cdots,x_n\in P\texttt{ 不同时为 }0,x_1\vec{a}_1+x_2\vec{a}_2+\cdots+x_n\vec{a}_n=\vec{0}$$
> $\qquad\quad x_1,x_2,\cdots,x_n$ 不同时为 $0$，故不妨设 $x_1\not =0$，因为 $\vec{a}_1\not =0$，故
> $$x_2\vec{a}_2+x_3\vec{a}_3+\cdots+x_n\vec{a}_n=-x_1\vec{a}_1$$
> $$x_2\vec{a}_2+x_3\vec{a}_3+\cdots+x_n\vec{a}_n,-x_1\vec{a}_1=-\vec{x}_1\vec{a}_1,-x_1\vec{a}_1=||-x_1\vec{a}_1||\not =0\qquad(5)$$
> $\qquad\quad$ 又 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n\in V$ 两两正交，因此
> $$(x_2\vec{a}_2+x_3\vec{a}_3+\cdots+x_n\vec{a}_n)\cdot(-x_1\vec{a}_1)=(x_2\vec{a}_2)\cdot(-x_1\vec{a}_1)+(x_3\vec{a}_3)\cdot(-x_1\vec{a}_1)+\cdots+(x_n\vec{a}_n)\cdot(-x_1\vec{a}_1)$$
> $$=-\overline{x_1}x_2(\vec{a}_2\cdot\vec{a}_1)-\overline{x_1}x_3(\vec{a}_3\cdot\vec{a}_1)-\cdots-\overline{x_1}x_n(\vec{a}_n\cdot\vec{a}_1)=0\qquad(6)$$
> $\qquad\quad(5)(6)$ 式矛盾，故 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$ 线性相关。

于是，我们可以取两两正交的非 $\vec{0}$ 向量作为内积空间的基底。  
若取两两正交的单位向量作为基底，那么

$$
\begin{bmatrix}
	x_1\\
	x_2\\
	\vdots\\
	x_n
\end{bmatrix}\cdot
\begin{bmatrix}
	y_1\\
	y_2\\
	\vdots\\
	y_n
\end{bmatrix}=x_1\overline{y_1}+x_2\overline{y_2}+\cdots+x_n\overline{y_n}
$$

这就是复向量内积的定义。

> 练习（傅里叶级数）：对于周期为 $2\pi$ 的连续周期函数，定义其内积为
> $$f\cdot g=\dfrac{1}{4\pi}\int_{-\pi}^{\pi}f(x)\overline{g(x)}\mathrm{d}x$$
> 1. 试证明 $\cdots,e^{-2ix},e^{-ix},1,e^{ix},e^{2ix},\cdots$ 为两两正交的单位向量。
> 2. 对于傅里叶级数
> $$f(x)=\sum_{k=-\infty}^{+\infty}a_ke^{ikx},g(x)=\sum_{k=-\infty}^{+\infty}b_ke^{ikx}\qquad(\forall k\in\Z,a_k,b_k\in\mathbb{C})$$
> $\qquad\quad$ 试证明 $f\cdot g=\displaystyle\sum_{k=-\infty}^{+\infty}a_k\overline{b_k}$ 。

> 注：为何范数和内积的概念未考虑到超现实数等无法完备化的有序域，是一个亟待解决的历史问题。


## 外代数与楔积*

叉积反映了 $k$ 维有向体积所具有的性质，但无法体现不同维有向体积间的换算关系，如

$$\texttt{长度}\times\texttt{面积}=\texttt{体积}$$

这是因为叉积不满足结合律。

为什么叉积不满足结合律？原因其一是不同维的向量无法作叉积，其二是虽然 $\dbinom{n}{k}=\dbinom{n}{n-k}$，  
即 $k$ 维有向体积和 $n-k$ 维有向体积组成的线性空间是同构的，但二者在这种换算关系中是不同的。

------------------------------------------------------------------------

综上，对于域 $P$ 上的线性空间 $V$，设 $\bigwedge^0 V,\bigwedge^1 V,\bigwedge^2 V,\cdots$ 为   
$P$ 上相互独立的 $\dbinom{n}{0},\dbinom{n}{1},\dbinom{n}{2},\cdots$ 维线性空间，特别地，令

$${\bigwedge}^0V=P,{\bigwedge}^1V=V$$

定义 $\bigwedge^0 V,\bigwedge^1 V,\bigwedge^2 V,\cdots$ 组合成的 $2^n$ 维线性空间

$$\bigwedge V=\{\mathbf{v}_0+\mathbf{v}_1+\mathbf{v}_2+\cdots|\forall k\in\N,\mathbf{v}_k\in{\bigwedge}^kV\}$$

为 $V$ 上的 **外积空间**、**楔积空间** 或 **外代数**，其零向量 $\mathbf{0}$ 同时是 $\bigwedge^0 V,\bigwedge^1 V,\bigwedge^2 V,\cdots$ 的零向量，  
并称 $\bigwedge^k V$ 中的元素为 $V$ 的 $k$ 维有向体积。

之后，定义其上的 **楔积** $\wedge:\bigwedge^aV\times\bigwedge^bV\rightarrow\bigwedge^{a+b}V$，满足

1. 与数乘的相容性：$\forall\lambda\in P,\mathbf{a,b}\in\bigwedge V,\lambda\mathbf{a}\wedge\mathbf{b}=\mathbf{a}\wedge\lambda\mathbf{b}=\lambda(\mathbf{a}\wedge\mathbf{b})$
2. 结合律：$\forall\mathbf{a,b,c}\in\bigwedge V,\mathbf{(a\wedge b)\wedge c=a\wedge(b\wedge c)}$
3. 分配律：$\forall\mathbf{a,b,c}\in\bigwedge V,\mathbf{(a+b)\wedge c=a\wedge c+b\wedge c,a\wedge(b+c)=a\wedge b+a\wedge c}$
4. 反交换律：$\forall\vec{a},\vec{b}\in V,\vec{a}\wedge\vec{b}=\mathbf{0}$ 当且仅当 $\vec{a},\vec{b}$ 线性相关。
5. 单位元：$\forall \mathbf{a}\in\bigwedge V,1 \wedge\mathbf{a}=\mathbf{a}\wedge 1=\mathbf{a}$ 

由外代数的定义，可推得

1. $\forall\lambda\in P,\mathbf{a}\in\bigwedge V,\lambda\wedge\mathbf{a}=\lambda\mathbf{a}$（标量与外代数中元素的楔积与 数乘/标量乘法 等价）
2. $\forall \mathbf{a}\in\bigwedge V,\mathbf{0\wedge a=a\wedge 0=a\wedge a=0}$
3. $\forall\vec{v}_1,\vec{v}_2,\cdots,\vec{v}_k\in V,\vec{v}_1\wedge\vec{v}_2\wedge\cdots\wedge\vec{v}_k=\mathbf{0}$ 当且仅当 $\vec{v}_1,\vec{v}_2,\cdots,\vec{v}_k$ 线性相关
4. 若 $V$ 中存在一组基底 $\vec{a}_1,\vec{a}_2,\cdots,\vec{a}_n$，则 $\bigwedge^k V\ (k>0)$ 中存在一组基底
   $$\{\vec{a}_{b_1}\wedge\vec{a}_{b_2}\wedge\cdots\wedge\vec{a}_{b_k}|1\le b_1<b_2<\cdots<b_k\le n\}$$

5. $\forall\mathbf{a}\in\bigwedge^iV,\mathbf{b}\in\bigwedge^j V,\mathbf{a\wedge b}=(-1)^{i+j}\mathbf{b\wedge a}$