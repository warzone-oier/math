以下介绍获取生成函数的第二种方法：通过递推式得到生成函数。

对于一维递推，我们可以直接列出关于生成函数的函数方程（乃至微分方程），  
进而手工求解或用牛顿迭代/半在线卷积等求解。

# 线性递推

## 常系数齐次线性递推

**常系数齐次线性递推数列** 是满足如下递推关系的递推数列：

$$f_n=\sum_{i=1}^{k}p_if_{n-i}\quad(n\ge k)$$

例如，斐波那契数列

$$f_0=f_1=1,f_n=f_{n-1}+f_{n-2}\quad(n\ge 2)$$

就是一种常见的常系数齐次线性递推数列。

### 生成函数

注意到常系数齐次线性递推关系是一个卷积式子，将其写成生成函数方程的形式

$$f(x)=\sum_{i=1}^kp_kx^kf(x)+g(x)$$

其中 $g(x)$ 是一个 $k-1$ 次多项式。于是

$$\left(1-\sum_{i=1}^kp_ix^i\right)f(x)=g(x)$$

设 $p(x)=\displaystyle 1-\sum_{i=1}^kp_ix^i$，则

$$f(x)=\dfrac{g(x)}{p(x)}$$

因此，**一个数列是常系数齐次线性递推数列当且仅当其生成函数是有理真分式**。  
我们可以直接利用生成函数 $\Theta(n\log n)$ 求得常系数齐次线性递推数列的前 $n$ 项，  
或者用递推初值 $f_0,f_1,\cdots,f_{k-1}$ 求得递推数列生成函数的分子

$$g(x)\equiv p(x)f(x)\pmod{x^k}$$



### 通项公式

注意到

$$[x^n]\dfrac{1}{(1-ax)^{k}}=\dbinom{-k}{n}(-a)^n=\dbinom{n+k-1}{n}a^n=\dbinom{n+k-1}{k-1}a^k\quad(n\ge 0,k\ge 1)\qquad(1)$$

因此对于常系数齐次线性递推数列，如果我们能将它的生成函数**有理分式分解**成

$$\sum_{i}\dfrac{f_{i}(x)}{(1-a_ix)^{k_i}}$$

的形式，即可得到它的通项公式。

具体地，对于常系数齐次线性递推数列，我们称其生成函数分母 $p(x)$ 翻转后的多项式 $p_R(x)$ 为该数列的 **特征多项式**，方程

$$p_R(x)=0$$

称为该数列的 **特征方程**。设特征方程的解为 $x_1,x_2,\cdots$，重根数分别为 $c_1,c_2,\cdots$，则
$$p_R(x)=\prod_{i}(x-x_i)^{c_i},p(x)=\left(\prod_{i}(x-x_i)^{c_i}\right)_R=\prod_{i}(x-x_i)^{c_i}_R=\prod_{i}(1-x_ix)^c_i$$

于是

$$f(x)=\dfrac{g(x)}{\prod_{i}(1-x_ix)^{c_i}}$$

之后，用扩展欧几里得作有理分式分解，即可得到通项公式。

但 $g(x)$ 的求解与扩展欧几里得都相对复杂，我们可以跳过这两个步骤：  
根据式 $(1)$，常系数齐次线性递推数列的通项公式一定满足

$$f_n=\sum_ih_i(n)x_i^n$$

的形式，其中 $h_i(n)$ 为 $c_i-1$ 次多项式。  
因此，求出特征方程的解后，利用 $f_0,f_1,\cdots,f_k$，使用待定系数法列方程即可求得通项公式。 

练习：试求得斐波那契数列的通项公式。

### 求单项

利用矩阵快速幂，我们可以在 $\Theta(k^3\log n)$ 的时间内求得常系数齐次线性递推数列的第 $n$ 项：

$$\begin{bmatrix}
	a_1&a_2&\cdots&a_{k-1}&a_k\\
	1&0&\cdots &0&0\\
	0&1&\cdots&0&0\\
	\vdots&\vdots&\ddots&\vdots&\vdots\\
	0&0&\cdots&1&0
\end{bmatrix}
\begin{bmatrix}
	f_{n-1}\\ f_{n-2}\\\vdots\\ f_{n-k}
\end{bmatrix}=\begin{bmatrix}
	f_{n}\\ f_{n-1}\\\vdots\\ f_{n-k+1}
\end{bmatrix}
$$

> 注*：可以发现，转移矩阵的特征多项式与数列的特征多项式是相同的。

---------------------------------------------------------
但这一做法在 $k$ 较大时不可行。我们考虑另一种思路：将高次项用低次项展开。

例如对于斐波那契数列：

$$\mathrm{Fib}_5=\mathrm{Fib}_4+\mathrm{Fib}_3=2\mathrm{Fib}_3+\mathrm{Fib}_2=3\mathrm{Fib}_2+2\mathrm{Fib}_1=5\mathrm{Fib}_1+3\mathrm{Fib}_0$$

不难发现，这与 $x^5$ 对 $\mathrm{Fib}$ 的特征多项式—— $x^2-x-1$ 取模的过程是等价的：

$$
\begin{array}{lr} 
& x^3+\ \ x^2+2x+3\\ 
x^2-x-1\!\!\!\!\!&\overline{)x^5\ \ \ \qquad\qquad\qquad\qquad\qquad\quad} \\ 
& \underline{x^5-x^4-\ \ x^3\ \ \ \qquad\qquad\qquad} \\ 
&  x^4+\ \ x^3\ \ \ \qquad\qquad\qquad\\
& \underline{x^4-\ \ x^3-\ \ x^2\qquad\qquad} \\ 
& 2x^3+\ \ x^2\qquad\qquad\\
&\underline{2x^3-2x^2-2x\quad\ \ \ }\\
&3x^2+2x\quad\ \ \ \\
&\underline{3x^2-3x-3}\\
&5x+3\end{array}
$$

因此对于一般的常系数齐次线性递推数列，求解

$$h(x)=x^n\bmod p_R(x)$$

即可将 $f_n$ 展开为 $f_{0},f_1,\cdots,f_{k-1}$

$$f_n=\sum_{i=0}^{k-1}h_if_i$$

暴力取模时间复杂度 $\Theta(k^2\log n)$，使用 FFT 时间复杂度 $\Theta(k\log k\log n)$，均优于矩阵快速幂。

>注*：使用哈密顿-凯莱定理也可得到相同的结果。

上述算法的优势在于无须求解递推数列具体的生成函数。  
在已知递推数列的生成函数时，使用 Bostan-Mori Algorithm 也可 $\Theta(k\log k\log n)$   
得到常系数齐次线性递推数列的第 $n$ 项，常数更小。

## 常系数非齐次线性递推

$$f_n=\sum_{i=1}^{k}p_if_{n-i}+g_n\quad(n\ge k)$$

其中 $\displaystyle\sum_ng_nx^n$ 为有理分式。同齐次的情况，设

$$p(x)=1-\sum_{i=1}^kp_ix^i,g(x)=(f(x)p(x)\bmod x^k)+\sum_{n\ge k} g_nx^n$$

则

$$f(x)=\sum_{i=1}^k p_ix^if(x)+g(x)$$

$$f(x)=\dfrac{g(x)}{p(x)}$$

于是 $f(x)$ 也是有理分式，就可以将非齐次的情况转换成齐次的情况。

有理分式关于加减乘除、复合、求导等运算的封闭性极大地方便我们处理线性递推数列。  
除此之外，有理分式还满足

> 求证：若 $f,g$ 是有理分式，则 $f\cdot g$ 也是有理分式。  
> 证明：从通项公式的角度看是显然的，但为了绕过特征方程直接求出 $f\cdot g$ 满足的递推式，  
> $\qquad$ 我们从递推式的角度证明这一点，即证明两个常系数齐次线性递推数列的乘积仍是常系数齐次线性递推数列。设
> $$f_{n+k}=\sum_{i=0}^{k-1}p_{k-i}f_{n+i}\quad(n\ge 0)$$
> $$g_{n+k}=\sum_{i=0}^{k-1}q_{k-i}g_{n+i}\quad(n\ge 0)$$
> $\qquad$ 则对于任意正整数 $n$，$f_{x+n}$ 总能表示成 $f_x,f_{x+1},\cdots,f_{x+k-1}$ 的线性组合，即
> $$f_{x+n}=\sum_{i=0}^{k-1}a_{n,i}f_{x+i}$$
> $\qquad$ 于是 $f_{x+n}g_{x+n}$ 总能唯一表示成
> $$f_{x+n}g_{x+n}=\sum_{i=0}^{k-1}\sum_{j=0}^{k-1}u_{n,i,j}f_{x+i}g_{x+j}$$
> $\qquad$ 的形式。对 $f_xg_x,f_{x+1}g_{x+1},\cdots,f_{x+k^2}g_{x+k^2}$ 的以上线性表示高斯消元，  
> $\qquad$ 即可求得 $f\cdot g$ 满足长度为 $k^2$ 的线性递推式。


# 非线性递推 

## 卡特兰数 

卡特兰数起源于 **翻折容斥** 问题：

> 从 $(0,0)$ 走到 $(2n,0)$ 每次可以从 $(x,y)$ 走到 $(x+1,y+1)$ 或 $(x+1,y-1)$，  
> 且中途必须保证 $x\ge 0$，有多少种可能的路径？

显然，一条路径由若干个从 $x$ 轴走到 $x>0$，再从 $x>0$ 走回 $x$ 轴的“山峰”组成。  
削掉山峰最底下 $y\in[0,1]$ 的部分，得到的是一条满足要求，但长度更短的路径。  
于是设答案的组合类为 $\mathcal{C}$，则

$$\mathcal{C}=\mathrm{SET}(\mathcal{Z_\bullet}\times\mathcal{C})$$

即答案的生成函数满足方程

$$C(x)=\dfrac{1}{1-xC(x)}$$

解得 $C(x)=\dfrac{1\pm\sqrt{1-4x}}{2x}$，由于 $[x^{-1}]C(x)=0$，故

$$C(x)=\dfrac{1-\sqrt{1-4x}}{2x}$$

这就是卡特兰数的生成函数，于是由二项式定理和吸收恒等式得

$$C_n=[x^n]\dfrac{1-\sqrt{1-4x}}{2x}=-\dfrac{1}{2}[x^{n+1}]\sqrt{1-4x}=-\dfrac{1}{2}[x^{n+1}](1-4x)^{\frac{1}{2}}$$

$$=-\dfrac{1}{2}\dbinom{\frac{1}{2}}{n+1}(-4)^{n+1}=\dfrac{1}{n+1}\dbinom{-\frac{1}{2}}{n}(-4)^n=\dfrac{1}{n+1}\prod_{k=0}^{n-1}\dfrac{\left(-\frac{1}{2}-k\right)\times(-4)}{k+1}$$

$$=\dfrac{1}{n+1}\prod_{k=0}^{n-1}\dfrac{2(2k+1)}{k+1}=\dfrac{1}{n+1}\prod_{k=0}^{n-1}\dfrac{(2k+2)(2k+1)}{(k+1)^2}=\dfrac{1}{n+1}\dbinom{2n}{n}$$

## 伯努利数

有限微积分给出了自然数幂和的下降幂公式，  
而 **伯努利数** $B_n$ 是雅各布·伯努利在观察自然数幂和的普通幂公式时发现的。
其定义为

$$\sum_{k=0}^n\dbinom{n+1}{k}B_k=[n=0]$$

### 指数生成函数

由伯努利数的定义得

$$\sum_{k=0}^{n+1}\dbinom{n+1}{k}B_k=B_{n+1}+[n=0]$$

$$\sum_{k=0}^{n+1}\dfrac{1}{(n+1-k)!}\times\dfrac{B_k}{k!}=\dfrac{B_{n+1}}{(n+1)!}+\dfrac{[n=0]}{(n+1)!}$$

$$\sum_{k=0}^{n}\dfrac{1}{(n-k)!}\times\dfrac{B_k}{k!}=\dfrac{B_{n}}{n!}+\dfrac{[n=1]}{n!}\quad(n\ge 1)$$

由定义可求得 $B_0=1$，因此

$$e^x\hat{B}(x)=\hat{B}(x)+x$$

$$\hat{B}(x)=\dfrac{x}{e^x-1}$$

这就是伯努利数的指数生成函数。于是可以 $\Theta(n\log n)$ 求得伯努利数的前 $n$ 项。

### 求单项

注意到

$$\hat{B}(x)=\dfrac{\ln(1+x)}{x}\circ (e^x-1)$$

$$=\left(\sum_{k=0}^{+\infty}\dfrac{(-1)^kx^k}{k+1}\right)\circ(e^x-1)=\sum_{k=0}^{+\infty}\dfrac{(1-e^x)^k}{k+1}$$

于是

$$B_n=n![x^n]\hat{B}(x)=n![x^n]\sum_{k=0}^n\dfrac{(1-e^x)^k}{k+1}=n![x^n]\sum_{k=0}^n\dfrac{1}{k+1}\sum_i\dbinom{k}{i}(-1)^ie^{ix}$$

$$=n!\sum_{k=0}^n\dfrac{1}{k+1}\sum_i\dbinom{k}{i}(-1)^ii^n=n!\sum_{i=0}^n(-1)^ii^n\sum_{k=0}^n\dbinom{k}{i}\dfrac{1}{k+1}$$

设 $f_{n,i}=\displaystyle\sum_{k=0}^n\dbinom{k}{i}\dfrac{1}{k+1}$。注意到

$$f_{n,i}=\sum_{k=0}^n\left(\dbinom{k+1}{i+1}-\dbinom{k}{i+1}\right)\dfrac{1}{k+1}=\sum_{k=0}^n\dbinom{k+1}{i+1}\dfrac{1}{k+1}-\sum_{k=0}^n\dbinom{k}{i+1}\dfrac{1}{k+1}$$

$$=\dfrac{1}{i+1}\sum_{k=0}^n\dbinom{k}{i}-f_{n,i+1}=\dfrac{1}{i+1}\dbinom{n+1}{i+1}-f_{n,i+1}$$

于是可以 $\Theta(n)$ 递推出 $f_{n,n},f_{n,n-1},\cdots,f_{n,0}$。线性筛出 $1^n,2^n,\cdots,n^n$ 后，  
即可 $\Theta(n)$ 求得伯努利数的一项

$$B_n=n!\sum_{i=0}^n(-1)i^nf_{n,i}$$

### 与自然数幂和的关系

我们考虑一列而非一行自然数幂和的指数生成函数

$$\hat{f}_n(x)=\sum_{m=0}^{+\infty}\dfrac{x^m}{m!}\sum_{k=0}^{n-1}k^m$$

于是

$$\hat{f}_n(x)=\sum_{k=0}^{n-1}\sum_{m=0}^{+\infty}\dfrac{k^mx^m}{m!}=\sum_{k=0}^{n-1}e^{kx}=\dfrac{e^{nx}-1}{e^x-1}$$

由此得到

$$\hat{f}_n(x)=\hat{B}(x)\dfrac{e^{nx}-1}{x}$$

$$[x^m]\hat{f}_n(x)=\sum_{k=0}^m\left([x^k]\hat{B}(x)\right)\left([x^{m-k}]\dfrac{e^{nx}-1}{x}\right)$$

$$\dfrac{1}{m!}\sum_{k=0}^{n-1}k^m=\sum_{k=0}^m\dfrac{B_k}{k!}\dfrac{n^{m-k+1}}{(m-k+1)!}$$

$$\sum_{k=0}^{n-1}k^m=\dfrac{1}{m+1}\sum_{k=0}^m\dbinom{m+1}{k}B_kn^{m+1-k}$$

这就是伯努利数与自然数幂和的关系。其给出了自然数幂和的普通幂公式。

## 二次非线性递推

二次非线性递推是满足如下递推关系的递推数列：

$$f_{n+1}=af_n^2+bf_n+c\quad(a\not =0)$$

设 $g_n=af_n+\dfrac{b}{2},C=ac-\dfrac{b^2}{4}+\dfrac{b}{2}$，则原递推数列等价于

$$g_{n+1}=g_n^2+C$$

$C=0$ 时，显然 $\{g_n\}$ 有通项公式

$$g_n=g_0^{2^n}$$

$C\not =0$ 时，若 $g_0,C$ 均为正整数，[可以证明](https://spaces.ac.cn/archives/3064)，存在实数 $k$，使得

$$g_n=\lfloor k^{2^n}\rfloor$$

精确计算 $k$ 需要知道所有的 $\{g_n\}$ 所有的项，而在 $k$ 存在误差的情况下，  
$g_n$ 的误差将会随 $n$ 的增加爆炸式增长。这一性质称为 **混沌性**。

二次非线性递推的混沌性是 Pollad_Rho 算法的基础之一。

## P-递归与整式递推

**P-递归数列** 是满足如下递推关系的数列

$$f_n=\sum_{k=1}^mp_k(n)f_{n-k}\quad(n\ge d)$$

其中 $d\ge m,p_1(x),p_2(x)\cdots p_m(x)$ 均为有理分式。

阶乘就是一种典型的 P-递归数列。

### 微分有限函数

考虑 P-递归数列的生成函数，首先等式两边同乘以 $p_1(x),p_2(x)\cdots p_m(x)$ 的公分母，得

$$\sum_{k=0}^mq_k(n)f_{n-k}=0\quad(n\ge d)$$

根据欧拉方程的求解，设

$$q_k(x+k)=\sum_{i}a_{k,i}x^{\underline{i}}$$

则

$$\sum_{k=0}^m\sum_ia_{k,i}(n-k)^{\underline{i}}f_{n-k}=0\quad(n\ge d)$$

$$\sum_{k=0}^m\sum_ia_{k,i}x^{k+i}\dfrac{\mathrm{d}^if(x)}{\mathrm{d}x^i}=g(x)\quad(\deg g<d)\quad(2)$$

$(2)$ 式是一个非齐次线性微分方程，但等式两边同时求 $d$ 阶导后即可化为齐次的。

由此定义，若生成函数 $f(x)$ 满足齐次线性微分方程

$$\sum_{k=0}^na_k(x)f^{(k)}(x)=0\quad(a_n(x)\not =0)$$

且 $a_0(x),a_1(x),\cdots,a_n(x)$ 均为多项式，则称 $f(x)$ 为 **微分有限函数**。

那么，**一个数列是 P-递归 的当且仅当其生成函数为微分有限函数**。

--------------------

大多数情况下，我们并不能通过微分方程求解出对应的微分有限函数的封闭形式。  
但这并不妨碍我们处理微分有限函数。与有理函数类似，有

1. 若 $f,g$ 是微分有限函数，则 $\alpha f+\beta g\quad(\alpha,\beta\in\mathbb{C})$ 也是微分有限函数。
   > 证明：我们同时从生成函数和递推式两个角度证明这一点。  
   > $\qquad$ 从生成函数的角度看，设
   > $$\sum_{k=0}^na_k(x)f^{(k)}(x)=\sum_{k=0}^mb_k(x)g^{(k)}(x)=0$$
   > $$(n,m\in\N_+,a_n(x),b_m(x)\not=0,a_0,a_1,\cdots,a_n,b_0,b_1,\cdots,b_m\in\mathbb{C}[x])$$
   > $\qquad$ 则
   > $$f^{(n)}(x)=\sum_{k=0}^{n-1}\dfrac{a_k(x)}{a_n(x)}f^{(k)}(x)$$
   > $\qquad$ 也就是说，考虑有理分式域 $\mathbb{C}(x)$ 上的线性空间，则对于任意非负整数 $k$，  
   > $\qquad f^{(k)}$ 可视为向量，且总能表示成 $f^{(0)},f^{(1)},\cdots,f^{(n-1)}$ 的线性组合。  
   > $\qquad g^{(k)}$ 同理。故对于 $h=\alpha f+\beta g,h^{(k)}$ 总能表示成
   > $$h^{(k)}(x)=\sum_{i=0}^{n-1}u_{k,i}(x)f^{(i)}(x)+\sum_{i=0}^{m-1}u_{k,n+i}(x)g^{(i)}(x)$$
   > $\qquad$ 的形式。由于 $\vec{u}_k(x)$ 是一个 $n+m$ 维向量，故高斯消元即可得到 $h$ 满足 $n+m$ 阶微分方程
   > $$\sum_{k=0}^{n+m}c_k(x)h^{(k)}(x)=0\quad(c_0(x)\not =0)$$
   > --------------------------------------------
   > $\qquad$ 从递推式的角度看，设
   > $$f_k=\sum_{i=1}^np_i(k)f_{k-i},g_k=\sum_{i=1}^mq_i(k)g_{k-i}$$
   > $$(p_1(x),p_2(x),\cdots,p_n(x),q_1(x),q_2(x),\cdots,q_m(x)\in\mathbb{C}(x))$$
   > $\qquad$ 则同样考虑有理分式域 $\mathbb{C}(x)$ 上的线性空间，对于任意正整数 $k$，  
   > $\qquad f_{x+k}$ 可以表示成 $f_x,f_{x+1},\cdots,f_{x+n-1}$ 的线性组合。$g$ 同理。  
   > $\qquad$ 故对于 $h=\alpha f+\beta g$，$h_{x+k}$ 总能表示成
   > $$h_{x+k}=\sum_{i=0}^{n-1}u_{k,i}(x)f_{x+i}+\sum_{i=0}^{m-1}u_{k,n+i}(x)g_{x+i}$$
   > $\qquad$ 的形式，于是同样高斯消元即可得到 $h$ 满足 P-递归关系。
2. 若 $f,g$ 是微分有限函数，则 $f\times g$ 也是微分有限函数。  
   > 证明：与微分有限函数线性性的生成函数角度证明类似。设 $f,g$ 满足的微分方程阶数分别为 $n,m$，  
   > $\qquad$ 根据高阶导数的莱布尼兹公式，对于 $h=f\times g$ ，$h^{(k)}$ 总能表示成
   > $$h^{(k)}(x)=\sum_{i=0}^{n-1}\sum_{j=0}^{m-1}u_{k,i,j}(x)f^{(i)}(x)g^{(j)}(x)$$
   > $\qquad$ 的形式。高斯消元即可得到 $h$ 满足 $nm$ 阶微分方程。
3. 若 $f,g$ 是微分有限函数，则 $f\cdot g$ 也是微分有限函数。$\cdot$ 表示系数对应位置相乘。
   > 证明：与微分有限函数线性性的递推式角度证明类似。设
   > $$f_k=\sum_{i=1}^np_i(k)f_{k-i},g_k=\sum_{i=1}^mq_i(k)g_{k-i}$$
   > $$(p_1(x),p_2(x),\cdots,p_n(x),q_1(x),q_2(x),\cdots,q_m(x)\in\mathbb{C}(x))$$
   > $\qquad$ 则对于 $h=f\cdot g$，$h_{x+k}$ 总能表示成
   > $$h_{x+k}=\sum_{i=0}^{n-1}\sum_{j=0}^{m-1}u_{k,i,j}(x)f_{x+i}g_{x+j}$$
   > $\qquad$ 的形式。高斯消元即可得到 $h$ 满足 P-递归 关系。
4. 若 $f(x)$ 是微分有限函数，则 $f'(x),\displaystyle\int f(x)\mathrm{d}x$ 也是微分有限函数。
   > 证明：设 $\displaystyle\sum_{k=0}^na_k(x)f^{(k)}(x)=0\quad(a_n(x)\not =0),g(x)=f'(x),h(x)=\int f(x)\mathrm{d}x$，则显然有
   > $$\sum_{k=0}^na_k(x)h^{(k+1)}(x)=0$$
   > $\qquad$ 若 $a_0(x)=0$，显然 $\displaystyle\sum_{k=1}^na_k(x)g^{(k-1)}(x)=0$；否则
   > $$f(x)+\sum_{k=1}^n\dfrac{a_k(x)}{a_0(x)}f^{(k)}(x)=0$$
   > $$\left(f(x)+\sum_{k=1}^n\dfrac{a_k(x)}{a_0(x)}f^{(k)}(x)\right)'=0$$
   > $$g(x)+\sum_{k=1}^n\left(\dfrac{a_k'(x)a_0(x)-a_k(x)a_0'(x)}{a_0^2(x)}g^{(k-1)}(x)+\dfrac{a_k(x)}{a_0(x)}g^{(k)}(x)\right)=0$$
   > $$a_0^2(x)g(x)+\sum_{k=1}^n((a_k'(x)a_0(x)-a_k(x)a_0'(x))g^{(k-1)}(x)+a_k(x)a_0(x)g^{(k)}(x))=0$$


5. 若 $f(x)$ 是微分有限函数，则对于任意非常数的有理分式 $g$，$f\circ g$ 也是微分有限函数。
   > 证明：设 $\displaystyle\sum_{k=0}^na_k(x)f^{(k)}(x)=0\quad(a_n(x)\not =0)$，作换元 $x=g(t)$，则
   > $$\sum_{k=0}^na_k(g(t))\left(\dfrac{\mathrm{d}}{\mathrm{d}g(t)}\right)^kf(g(t))=0$$
   > $$\sum_{k=0}^na_k(g(t))\left(\dfrac{1}{g'(t)}\times\dfrac{\mathrm{d}}{\mathrm{d}t}\right)^k(f\circ g)(t)=0$$
   > $\qquad$ 由于有理分式的导数仍是有理分式，根据导数的乘法法则，上式最终将变成以下形式
   > $$\sum_{k=0}^nb_k(t)\dfrac{\mathrm{d}^k(f\circ g)(t)}{\mathrm{d}x^k}=0$$
   > $\qquad$ 等式两边同乘以 $b_0,b_1,\cdots,b_n$ 的公分母，即可得到 $f\circ g$ 是满足 $n$ 阶微分方程的微分有限函数。

> 练习：试证明调和数是 P-递归 数列，并给出其生成函数满足的齐次线性微分方程。

### 整式递推

P-递归 数列的递推式不能写成矩阵快速幂的形式，但依然可以写成矩阵连乘积的形式

$$
\begin{bmatrix}
f_n\\f_{n-1}\\ f_{n-2}\\\vdots\\ f_{n-m+1}
\end{bmatrix}=\begin{bmatrix}
	p_1(n)&p_2(n)&\cdots&p_{m-1}(n)&p_m(n)\\
	1&0&\cdots&0&0\\0&1&\cdots&0&0\\
	\vdots&\vdots&\ddots&\vdots&\vdots\\0&0&\cdots&1&0

\end{bmatrix}\begin{bmatrix}
f_{n-1}\\ f_{n-2}\\\vdots\\ f_{n-m}
\end{bmatrix}
$$

让我们考虑一个更一般的问题，即递推式

$$\vec{a}_n=A(n)\vec{a}_{n-1}$$

其中 $A(x)$ 为由关于 $x$ 的有理分式组成的矩阵，称为 **整式递推**。  

如果 $\vec{a}_n$ 的维数和 $A(x)$ 中元素分子分母的次数都很小，整式递推是可以在 $\Theta(\sqrt{N})$ 内求得一项的。  
首先提取出 $A(x)$ 中元素的公分母，设

$$A(x)=\dfrac{B(x)}{p(x)}$$

其中多项式 $p(x)$ 的次数不大于 $d$，$B(x)$ 是由次数不大于 $d$ 的多项式组成的 $m\times m$ 矩阵。则

$$\vec{a}_N=\dfrac{B(N)B(N-1)\cdots B(1)}{p(N)p(N-1)\cdots p(1)}\vec{a}_0$$

$p(N)p(N-1)\cdots p(1)$ 的求解是可以通过连续点值平移在 $\Theta(\sqrt{N}\operatorname{polylog}(N))$ 内完成的。设

$$\deg p\le d,f_n(x)=p(nx+n)p(nx+n-1)\cdots p(nx+1)$$

则 $f_n(x)$ 是一个 $nd$ 次多项式，其可以由 $nd+1$ 个连续点值

$$f_n(0),f_n(1),\cdots,f_n(nd)$$

唯一确定。且

$$f_n(nd)f_n(nd-1)\cdots f_n(0)=p(n^2d+n)p(n^2d+n-1)\cdots p(1)$$

考虑用倍增来求得 $f_n(0),f_n(1),\cdots,f_n(nd)$，得

$$f_{2n}(x)=f_n(2x+1)f_n(2x)$$

故在已知 $f_n(0),f_n(1),\cdots,f_n(nd)$ 的基础上，通过连续点值平移求得

$$f_n(0),f_n(1),\cdots,f_n(4nd+1)$$

即可在 $\Theta(nd\log (nd))$ 的时间内求得 $f_{2n}(0),f_{2n}(1),\cdots,f_{2n}(2nd)$。  
于是倍增求解 $f_n(0),f_n(1),\cdots,f_n(nd)$ 的总时间复杂度由主定理得为

$$T(n)=T\left(\dfrac{n}{2}\right)+\Theta(nd\log(nd))=\Theta(nd\log(nd))$$

这样便在 $\Theta(\sqrt{Nd}\log(Nd))$ 的复杂度内求得了 $p(N)p(N-1)\cdots p(1)$。  
$B(N)B(N-1)\cdots B(1)$ 的求解是类似的，设 $\deg B\le d$，则求解的时间复杂度为

$$\Theta(\sqrt{Nd}(m^3+m^2\log(Nd)))$$

这就是求解整式递推第 $N$ 项的时间复杂度。