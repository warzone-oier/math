以下介绍获取生成函数的第二种方法：通过递推式得到生成函数。

对于一维递推，我们可以直接列出关于生成函数的函数方程（乃至微分方程），  
进而手工求解或用牛顿迭代/半在线卷积求解。

# 线性递推

## 常系数齐次线性递推

**常系数齐次线性递推数列** 是满足如下递推关系的递推数列：

$$f_n=\sum_{i=1}^{k}p_if_{n-i}\quad(n\ge k)$$

例如，斐波那契数列

$$f_0=f_1=1,f_n=f_{n-1}+f_{n-2}\quad(n\ge 2)$$

就是一种常见的常系数齐次线性递推数列。

### 生成函数

注意到常系数齐次线性递推关系是一个卷积式子，将其写成生成函数方程的形式

$$f(x)=\sum_{i=1}^kp_kx^kf(x)+g(x)$$

其中 $g(x)$ 是一个 $k-1$ 次多项式。于是

$$\left(1-\sum_{i=1}^kp_ix^i\right)f(x)=g(x)$$

设 $p(x)=\displaystyle 1-\sum_{i=1}^kp_ix^i$，则

$$f(x)=\dfrac{g(x)}{p(x)}$$

因此，常系数齐次线性递推数列的生成函数是有理函数。  
我们可以直接利用生成函数 $\Theta(n\log n)$ 求得常系数齐次线性递推数列的前 $n$ 项。



### 通项公式

注意到

$$[x^n]\dfrac{1}{(1-ax)^{k}}=\dbinom{-k}{n}(-a)^n=\dbinom{n+k-1}{n}a^n=\dbinom{n+k-1}{k-1}a^k\quad(n\ge 0,k\ge 1)\qquad(1)$$

因此对于常系数齐次线性递推数列，如果我们能将它的生成函数**有理分式分解**成

$$\sum_{i}\dfrac{f_{i}(x)}{(1-a_ix)^{k_i}}$$

的形式，即可得到它的通项公式。

具体地，对于常系数齐次线性递推数列，我们称其生成函数分母 $p(x)$ 翻转后的多项式 $p_R(x)$ 为该数列的 **特征多项式**，方程

$$p_R(x)=0$$

称为该数列的 **特征方程**。设特征方程的解为 $x_1,x_2,\cdots$，重根数分别为 $c_1,c_2,\cdots$，则
$$p_R(x)=\prod_{i}(x-x_i)^{c_i},p(x)=\left(\prod_{i}(x-x_i)^{c_i}\right)_R=\prod_{i}(x-x_i)^{c_i}_R=\prod_{i}(1-x_ix)^c_i$$

于是

$$f(x)=\dfrac{g(x)}{\prod_{i}(1-x_ix)^{c_i}}$$

之后，用扩展欧几里得作有理分式分解，即可得到通项公式。

但 $g(x)$ 的求解与扩展欧几里得都相对复杂，我们可以跳过这两个步骤：  
根据式 $(1)$，常系数齐次线性递推数列的通项公式一定满足

$$f_n=\sum_ih_i(n)x_i^n$$

的形式，其中 $h_i(n)$ 为 $c_i-1$ 次多项式。  
因此，求出特征方程的解后，利用 $f_0,f_1,\cdots,f_k$，使用待定系数法列方程即可求得通项公式。 

练习：试求得斐波那契数列的通项公式。

### 求单项

利用矩阵快速幂，我们可以在 $\Theta(k^3\log n)$ 的时间内求得常系数齐次线性递推数列的第 $n$ 项：

$$\begin{bmatrix}
	a_1&a_2&\cdots&a_{k-1}&a_k\\
	1&0&\cdots &0&0\\
	0&1&\cdots&0&0\\
	\vdots&\vdots&\ddots&\vdots&\vdots\\
	0&0&\cdots&1&0
\end{bmatrix}
\begin{bmatrix}
	f_{n-1}\\ f_{n-2}\\\vdots\\ f_{n-k}
\end{bmatrix}=\begin{bmatrix}
	f_{n}\\ f_{n-1}\\\vdots\\ f_{n-k+1}
\end{bmatrix}
$$

> 注*：可以发现，转移矩阵的特征多项式与数列的特征多项式是相同的。

---------------------------------------------------------
但这一做法在 $k$ 较大时不可行。我们考虑另一种思路：将高次项用低次项展开。

例如对于斐波那契数列：

$$\mathrm{Fib}_5=\mathrm{Fib}_4+\mathrm{Fib}_3=2\mathrm{Fib}_3+\mathrm{Fib}_2=3\mathrm{Fib}_2+2\mathrm{Fib}_1=5\mathrm{Fib}_1+3\mathrm{Fib}_0$$

不难发现，这与 $x^5$ 对 $\mathrm{Fib}$ 的特征多项式——$x^2-x-1$ 取模的过程是等价的：

$$
\begin{array}{lr} 
& x^3+\ \ x^2+2x+3\\ 
x^2-x-1\!\!\!\!\!&\overline{)x^5\ \ \ \qquad\qquad\qquad\qquad\qquad\quad} \\ 
& \underline{x^5-x^4-\ \ x^3\ \ \ \qquad\qquad\qquad} \\ 
&  x^4+\ \ x^3\ \ \ \qquad\qquad\qquad\\
& \underline{x^4-\ \ x^3-\ \ x^2\qquad\qquad} \\ 
& 2x^3+\ \ x^2\qquad\qquad\\
&\underline{2x^3-2x^2-2x\quad\ \ \ }\\
&3x^2+2x\quad\ \ \ \\
&\underline{3x^2-3x-3}\\
&5x+3\end{array}
$$

因此对于一般的常系数齐次线性递推数列，求解

$$h(x)=x^n\bmod p_R(x)$$

即可将 $f_n$ 展开为 $f_{0},f_1,\cdots,f_{k-1}$

$$f_n=\sum_{i=0}^{k-1}h_if_i$$

暴力取模时间复杂度 $\Theta(k^2\log n)$，使用 FFT 时间复杂度 $\Theta(k\log k\log n)$，均优于矩阵快速幂。

>注*：使用哈密顿-凯莱定理也可得到相同的结果。

## 常系数非齐次线性递推

$$f_n=\sum_{i=1}^{k}p_if_{n-i}+g(n)\quad(n\ge k)$$

其中 $g$ 为一个 $m$ 次多项式。


# 非线性递推 

## 卡塔兰数 

卡塔兰数

## 伯努利数

有限微积分给出了自然数幂和的下降幂公式，  
而 **伯努利数** $B_n$ 是雅各布·伯努利在观察自然数幂和的普通幂公式时发现的。
其定义为

$$\sum_{k=0}^n\dbinom{n+1}{k}B_k=[n=0]$$

### 指数生成函数

由伯努利数的定义得

$$\sum_{k=0}^{n+1}\dbinom{n+1}{k}B_k=B_{n+1}+[n=0]$$

$$\sum_{k=0}^{n+1}\dfrac{1}{(n+1-k)!}\times\dfrac{B_k}{k!}=\dfrac{B_{n+1}}{(n+1)!}+\dfrac{[n=0]}{(n+1)!}$$

$$\sum_{k=0}^{n}\dfrac{1}{(n-k)!}\times\dfrac{B_k}{k!}=\dfrac{B_{n}}{n!}+\dfrac{[n=1]}{n!}\quad(n\ge 1)$$

由定义可求得 $B_0=1$，因此

$$e^x\hat{B}(x)=\hat{B}(x)+x$$

$$\hat{B}(x)=\dfrac{x}{e^x-1}$$

这就是伯努利数的指数生成函数。

### 与自然数幂和的关系

我们考虑一列而非一行自然数幂和的指数生成函数

$$f_n(x)=\sum_{m=0}^{+\infty}\dfrac{x^m}{m!}\sum_{k=0}^{n-1}k^m$$

于是

$$f_n(x)=\sum_{k=0}^{n-1}\sum_{m=0}^{+\infty}\dfrac{k^mx^m}{m!}=\sum_{k=0}^{n-1}e^{kx}=\dfrac{e^{nx}-1}{e^x-1}$$

由此得到

$$f_n(x)=\hat{B}(x)\dfrac{e^{nx}-1}{x}$$

$$[x^m]f_n(x)=\sum_{k=0}^m\left([x^k]\hat{B}(x)\right)\left([x^{m-k}]\dfrac{e^{nx}-1}{x}\right)$$

$$\dfrac{1}{m!}\sum_{k=0}^{n-1}k^m=\sum_{k=0}^m\dfrac{B_k}{k!}\dfrac{n^{m-k+1}}{(m-k+1)!}$$

$$\sum_{k=0}^{n-1}k^m=\dfrac{1}{m+1}\sum_{k=0}^m\dbinom{m+1}{k}n^{m+1-k}$$

这就是伯努利数与自然数幂和的关系。其给出了自然数幂和的普通幂公式。

## 二次非线性递推

二次非线性递推是满足如下递推关系的递推数列：

$$f_{n+1}=af_n^2+bf_n+c\quad(a\not =0)$$

设 $g_n=af_n+\dfrac{b}{2},C=ac-\dfrac{b^2}{4}+\dfrac{b}{2}$，则原递推数列等价于

$$g_{n+1}=g_n^2+C$$

$C=0$ 时，显然 $\{g_n\}$ 有通项公式

$$g_n=g_0^{2^n}$$

$C\not =0$ 时，[可以证明](https://spaces.ac.cn/archives/3064)，存在实数 $k$，使得

$$g_n=\lfloor k^{2^n}\rfloor$$

精确计算 $k$ 需要知道所有的 $\{g_n\}$ 所有的项，而在 $k$ 存在误差的情况下，  
$g_n$ 的误差将会随 $n$ 的增加爆炸式增长。这一性质称为 **混沌性**。

二次非线性递推的混沌性是 Pollad_Rho 算法的基础之一。

## 整式递推

