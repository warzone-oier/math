# 多项式环
对于环 $(R,+,\times)$ 我们可以定义其上的 **多项式** 为

$$f=\left<f_0,f_1,f_2,\cdots,f_n\right> =f_0+f_1x+f_2x^2+\cdots+f_nx^n=\sum_{k=0}^nf_kx^k\quad(n=0\text{ 或 }f_n\not =0)$$
其中的 $x$ 只是一个形式符号，一个对于系数位置的标识符。  
$n$ 称为多项式的 **度** 或次数，记为 $\deg f$（特别地，$\deg 0=-\infty$）。

$R$ 上的所有多项式构成 **多项式环** $R[x]$。

如果我们还允许无穷个项的存在，即

$$f=\left<f_0,f_1,f_2,\cdots\right> =f_0+f_1x+f_2x^2+\cdots=\sum_{k=0}^{+\infty}f_kx^k$$

则称其为 $R$ 上的 **形式幂级数**，其构成 **形式幂级数环** $R[[x]]$。

$f_k$ 也可记为 $[x^k]f(x)$。形式幂级数 $f(x)$ 称为序列 $\left<f_0,f_1,f_2,\cdots\right>$ 的 **生成函数**。

### 多项式函数和形式多项式

我们在初等数学、微积分中接触的多项式，其本质是一个 **函数**，  
两个函数相等当且仅当它们的函数值相等，即 **多项式函数**。

多项式环中的多项式是 **形式多项式**，其本质是系数序列，**不能视为一个函数**。

在一般的环和域上，一个形式多项式仅对应一个多项式函数，而一个多项式函数可以对应多个形式多项式。  
例如对于多项式函数，显然有

$$1+x\equiv 1+x^2\pmod{2}$$

二者的系数序列显然不同，但二者的函数值相同。

同时，幂级数要考虑其点值的敛散性，而形式幂级数不需要。如

$$\sum_{k=0}^{+\infty} x^n=\dfrac{1}{1-x}$$

等式左边仅在 $|x|<1$ 收敛，而等式右边有意义只需要 $x\not=1$。

### 加法和乘法

$$(f+g)_n=f_n+g_n$$

$$(f\times g)_n=\sum_{i+j=n}f_ig_j=\sum_{k=0}^nf_kg_{n-k}$$

其中乘法对应的序列变换称为 **序列卷积**。

练习 1：试证明多项式环和形式幂级数环满足环公理。  
练习 2：试讨论多项式环和形式幂级数环的乘法交换律和乘法单位元。  
练习 3：试证明 $R[x]$ 和 $R[[x]]$ 为整环当且仅当 $R$ 为整环。


### 复合

我们可以定义 $R[[x]]$ 中元素的乘方，并在此基础上定义 $f,g\in R[[x]]$ 的复合

$$(f\circ g)(x)=f(g(x))=\sum_{k=0}^{+\infty}f_kg^k(x)$$

我们规定 $f\circ g$ 存在当且仅当 $f$ 为有限项或 $g_0=0$，这样就不涉及 $R$ 的极限了。

$\circ$ 满足结合律（$(f\circ g)\circ h$ 和 $(f\circ (g\circ h))$ 均存在时），不满足交换律。  
$R$ 为幺环时 $\circ$ 存在单位元 $1\times x$（简记为 $x$）。


## 多项式环的数论性质

### 拉格朗日插值

一个感性上显然的事实是，任意域上，$n$ 个不同位置的点值 $(x_0,y_0),(x_1,y_1),\cdots,(x_{n-1},y_{n-1})$   
可唯一确定一个 $n-1$ 次多项式 $f(x)$，因为我们可以籍此得到关于系数的一个 $n$ 元一次方程组。

事实上，可以构造所谓的 **拉格朗日插值公式**，从而由点值直接导出多项式：设

$$f(x)=y_0f_0(x)+y_1f_1(x)+\cdots+y_{n-1}f_{n-1}(x)$$

其中 $f_i(x)\quad(i\in\{0,1,\cdot,n-1\})$ 均为多项式，满足

$$f_i(x_i)=1,f_i(x_j)=0\quad(i\not =j)$$

欲满足 $f_i(x_j)=0\quad(i\not =j)$，故在 $f_i(x)$ 中添加因式 $\displaystyle\prod_{j\not =i}(x-x_j)$；  
又欲满足 $f_i(x_i)=1$，故 $f_i(x)$ 整体除以 $\displaystyle\prod_{j\not =i}(x_i-x_j)$。于是

$$f_i(x)=\prod_{j\not=i}\dfrac{x-x_j}{x_i-x_j}$$

由此可得最终的公式为

$$f(x)=\sum_{i=0}^{n-1}y_i\prod_{j\not=i}\dfrac{x-x_j}{x_i-x_j}$$

### 带余除法

对于域上的多项式环，我们可以定义 **多项式的带余除法**：

$$A(x)=B(x)P(x)+Q(x)\ (\deg Q<\deg B\text{ 或 } Q(x)=0)$$
$$\Leftrightarrow P(x)=\left\lfloor\frac{A(x)}{B(x)}\right\rfloor,Q(x)=A(x)\bmod B(x)$$

其可以用如下竖式计算：

$$
\begin{array}{lr} 
& x-4 \\ 
x^2+5x+6  \!\!\!\!\!\! & \overline{)x^3 +\ \ x^2+\ \ 7x +\ \  9} \\ 
& \underline{x^3+5x^2+\ \ 6x\quad\quad\ } \\ 
&  -4x^2+\quad x+\ \ 9\\
& \underline{-4x^2-20x-24} \\ 
& 21x+33 \end{array}
$$

对于一般的多项式环，我们无法定义带余除法，但环上依然可以定义同余关系。

域一定是整环，其上的多项式有带余除法，因此，**域上的多项式环为欧几里得整环**。  
于是初等数论中的结论可以推广到域上的多项式：
- 与质数相对应的概念是 **不可约多项式**。
- 与质因数分解相对应的概念是 **因式分解**。
- 与最大公因数、最小公倍数相对应的概念是 **最大公因式** 和 **最小公倍式**。符号同样为 $\gcd$ 和 $\operatorname{lcm}$。
- 欧几里得算法、扩展欧几里得算法、中国剩余定理、BSGS 对于域上的多项式同样适用。

练习：（有理分式分解）对于域上的有理分式 $\dfrac{R(x)}{P(x)Q(x)}$，其中 $P(x),Q(x)$ 互质，  
  $\qquad$证明一定存在多项式 $F(x),G(x)$，使得 $\dfrac{F(x)}{P(x)}+\dfrac{G(x)}{Q(x)}=\dfrac{R(x)}{P(x)Q(x)}$。

### 代数基本定理

不可约多项式有哪些？一次因式的情形可以用点值来讨论：
> 求证：对于幺环 $R$ 上的多项式 $f(x)\in R[x]$ 和 $x_0\in R,f(x_0)=0$ 当且仅当 $(x-x_0)|f(x)$。  
> 证明：$x_0=0$ 时，$f(0)=f_0=0$ 当且仅当 $x|f(x)$，命题显然成立。  
> $\qquad\quad x_0\not=0$ 时，考虑换元，设 $g(x)=f(x+x_0)$，则
> $$f(x_0)=0\Leftrightarrow g(x)=0\Leftrightarrow x|g(x)\Leftrightarrow (x-x_0)|f(x)$$

而对于复数域 $\mathbb{C}$，有
> **代数基本定理**：复数域上的一元 $n$ 次多项式方程 $f(x)=0$ 至少有一根。 

对于域上的多项式 $f(x)$，若 $f(x_0)=0$，则 $\dfrac{f(x)}{x-x_0}$ 依然为该域上的多项式，归纳推得：  
复数域上，一元 $n$ 次多项式方程 $f(x)=0$ 有 $n$ 个根（重根按重数计）。  
即复数域上，不可约多项式 $=$ 一次多项式。

实数域 $\R$ 上，由一元二次方程可知不可约多项式不止有一次多项式。但我们有

> **虚根成对定理**：对于复数域上的实系数多项式 $f(x),f(x_0)=0\Leftrightarrow f(\overline{x_0})=0$  
> 证明：共轭满足以下运算律：
> - 若 $x\in\R$，则 $\overline{x}=x$
> - $\overline{a+b}=\overline{a}+\overline{b}$
> - $\overline{a\times b}=\overline{a}\times\overline{b}$
> 
> $\qquad\quad$ 因此对于实系数多项式 $f(x)=\displaystyle\sum_{k}f_kx^k$，有
> $$\overline{f(x)}=\overline{\sum_{k}f_kx^k}=\sum_k\overline{f_kx^k}=\sum_{k}\overline{f_k}\overline{x}^k=\sum_{k}f_k\overline{x}^k=f(\overline{x})$$
> $\qquad\quad$ 于是 $f(x)=0\Rightarrow \overline{f(x_0)}=\overline{0}\Rightarrow f(\overline{x_0})=0$，即 $f(x_0)=0\Leftrightarrow f(\overline{x_0})=0$。

$(x-a+bi)(x-a-bi)=(x-a)^2+b^2$ 为实系数多项式，  
故若实系数多项式 $f(x)$ 有虚根 $z$，则 $\dfrac{f(x)}{(x-z)(x-\overline{z})}$ 仍为实系数多项式，归纳推得：  
实系数多项式方程 $f(x)=0$ 的虚根成对存在，且 **重数相同**。  
即实数域上，不可约多项式即一次多项式和无实根的二次多项式。


## 形式幂级数环的分析性质

### 形式导数和不定积分

一般的环上未必存在极限，但我们可以定义形式幂级数的 **形式导数** 为

$$\left(\sum_{k=0}^{+\infty}f_kx^k\right)'=\sum_{k=0}^{+\infty}kf_kx^k$$

其中

$$kf_k=\underbrace{f_k+f_k+\cdots+f_k}_{k\texttt{ 个 }f_k}$$

基本求导法则——加法法则、乘法法则、链式法则（复合允许的情况下）依然是正确的。

对于除环，可以相应定义形式幂级数的 **形式不定积分**。

### 泰勒展开

对于形式幂级数 $f(x)$，总可以定义 $f(0)=f_0$。于是

$$f^{(k)}(0)=k!f_k$$

由此可得

$$f(x)=\sum_{k=0}^{+\infty}\dfrac{f^{(k)}(0)}{k!}x^k$$

该式称为 $f(x)$ 的 **麦克劳林展开**。

而对于非形式的幂级数，有

> **拉格朗日中值定理**：若 $\R$ 上函数 $f(x)$ 在 $[a,b]$ 连续，在 $(a,b)$ 可导，则
> $$\exists\xi\in(a,b),f'(\xi)=\dfrac{f(b)-f(a)}{b-a}$$
> 证明：首先证明 $f(a)=f(b)$ 的情况，即罗尔中值定理。
> > 证明：因为 $f(x)$ 连续，故其在 $[a,b]$ 内一定有最大值和最小值，分别记为 $M$ 和 $m$。  
> > $\qquad\quad 1^\circ\quad M>f(a)$ 或 $m<f(a)$ 时，$f(x)$ 一定在 $(a,b)$ 内取最大值或取最小值，  
> > $\qquad\qquad$ 在取最值的点必有 $f'(x)=0$。  
> > $\qquad\quad 2^\circ\ M=m=f(a)$ 时，$f(x)$ 在 $(a,b)$ 内为常函数，恒有 $f'(x)=0$。   
> > $\qquad$ 综上，一定存在 $\xi\in(a,b),f'(\xi)=0$，证毕。 
>  
> $\qquad$ 在此基础上，构造函数
> $$g(x)=f(x)-\dfrac{f(b)-f(a)}{b-a}(x-a)$$
> $\qquad$ 则 $g(a)=g(b)=f(a)$，由罗尔中值定理得
> $$\exists\xi\in(a,b),g'(\xi)=f'(\xi)-\dfrac{f(b)-f(a)}{b-a}=0$$
> $\qquad$ 即 $\exist\xi\in(a,b),f'(\xi)=\dfrac{f(b)-f(a)}{b-a}$，证毕。

于是若 $\R$ 上函数 $f(x)$ $n$ 阶可导，对 $f(x)$ 求 $n-1$ 阶导，使用拉格朗日中值定理后再积分，可得

$$f(x)=\sum_{k=0}^{n-1}\dfrac{f^{(k)}(x_0)}{k!}(x-x_0)^k+\dfrac{f^{(n)}(\xi)}{n!}(x-x_0)^n\quad(x_0\le\xi\le x\texttt{ 或 }x\le\xi\le x_0)$$

该式称为 $f(x)$ 在 $x_0$ 处的 $n$ 阶 **泰勒展开**。复变函数是类似的。

以下是常见初等函数的麦克劳林展开式：

1. $(1+x)^r=\displaystyle\sum_{k=0}^{+\infty}\dbinom{r}{k}x^k\quad(|x|<1\texttt{ 或 }r\in\N)$（二项式定理）
2. $\dfrac{1}{1-x}=\displaystyle\sum_{k=0}^{+\infty}x^k\quad(|x|<1)$（1. 的推论）
3. $\dfrac{1}{(1-x)^2}=\displaystyle\sum_{k=0}^{+\infty}(k+1)x^k\quad(|x|<1)$（2. 的导数）
4. $-\ln(1-x)=\displaystyle\sum_{k=0}^{+\infty}\dfrac{x^k}{k}\quad(|x|<1)$（2. 的积分）
5. $e^x=\displaystyle\sum_{k=0}^{+\infty}\dfrac{x^k}{k!}\quad(x\in\mathbb{C})$
6. $\sin x=\displaystyle\sum_{k=0}^{+\infty}\dfrac{(-1)^kx^{2k+1}}{(2k+1)!}\quad(x\in\mathbb{C})$
7. $\cos x=\displaystyle\sum_{k=0}^{+\infty}\dfrac{(-1)^kx^{2k}}{(2k)!}\quad(x\in\mathbb{C})$

我们可以用 $e^x,\ln,\sin,\cos$ 的麦克劳林展开式证明其所满足的运算律。

### 乘法逆元

由 $\dfrac{1}{1-x}=\displaystyle\sum_{k=0}^{+\infty}x^k$ 可知，形式幂级数是有可能存在倒数，即乘法逆元的。

对于形式幂级数 $f$，定义其乘法逆元 $f^{-1}$ 为满足

$$f^{-1}\times f=1$$

的形式幂级数。将定义式写成卷积形式，得到

$$\sum_{k=0}^nf^{-1}_kf_{n-k}=[n=1]$$

- $n=0$ 时，$f^{-1}_0f_0=1$，即 $f^{-1}_0=\dfrac{1}{f_0}$。

- $n>0$ 时，可得

  $$f^{-1}_nf_0+\sum_{k=0}^{n-1}f^{-1}_kf_{n-k}=0$$

  $$\Rightarrow f_n^{-1}=-\dfrac{1}{f_0}\sum_{k=0}^{n-1}f^{-1}_kf_{n-k}$$

  该式给出了形式幂级数乘法逆元的 $\Theta(n^2)$ 递推。

综合以上二式，形式幂级数乘法逆元存在的充要条件是 $f_0$ 存在乘法逆元。

### 复合逆

**复合逆** 即反函数概念在形式幂级数环上的推广。

对于满足 $f_0=0$ 且 $f_1\not =0$ 的形式幂级数 $f$，其复合逆为满足 $g(f(x))=f(g(x))=x$ 的形式幂级数 $g$，记为 $f^{\left<-1\right>}$。

对于复合逆的求解，我们有

> **拉格朗日反演**：对于域上的形式幂级数环 $P[[x]]$，若 $f\in P[[x]]$ 存在复合逆 $g\in P[[x]]$，则 
> $$n[x^n]f^k(x)=k[x^{n-k}]\left(\dfrac{x}{g(x)}\right)^n\quad(n,k\in\Z)$$  
> 证明*：考虑在 $P[[x]]$ 中引入有限个负次项，形成 **形式洛朗级数环** $P[[x]][x^{-1}]$ 
> $$f=\left<f_{-t},f_{-t+1},\cdots\right> =\sum_{k=-t}^{+\infty}f_kx^k$$
> $\qquad$ 此时拉格朗日反演的形式为
> $$n[x^n]f^k(x)=k[x^{-k}]g^{-n}(x)\quad(n,k\in\Z)$$
> $\qquad$ 接下来证明如下引理：
> > $$[x^{-1}]f^k\times f'=[k=-1]\quad(k\in\Z)$$
> > 证明：$k\not =-1$ 时，$f^k\times f'=\left(\dfrac{f^{k+1}}{k+1}\right)'$，故 $[x^{-1}]f^k\times f'=0\times[x^0]\dfrac{f^{k+1}}{k+1}=0$   
> > $\qquad\quad k=-1$ 时，$[x^{-1}]\dfrac{f'}{f}=[x^{-1}]\dfrac{\displaystyle\sum_{k=0}^{+\infty}(k+1)f_{k+1}x^k}{\displaystyle\sum_{k=1}^{+\infty}f_kx^k}=[x^{-1}]\dfrac{\displaystyle\sum_{k=0}^{+\infty}(k+1)f_{k+1}x^k}{x\displaystyle\sum_{k=0}^{+\infty}f_{k+1}x^k}$  
> > $\qquad\qquad\qquad\qquad\qquad=[x^{0}]\dfrac{\displaystyle\sum_{k=0}^{+\infty}(k+1)f_{k+1}x^k}{\displaystyle\sum_{k=0}^{+\infty}f_{k+1}x^k}=\left(\dfrac{\displaystyle\sum_{k=0}^{+\infty}(k+1)f_{k+1}x^k}{\displaystyle\sum_{k=0}^{+\infty}f_{k+1}x^k}\right)(0)=\dfrac{(0+1)f_1}{f_1}=1$
> 
> $\qquad$ 在该引理的基础上，有
> $$f^k(g(x))=x^k$$
> $$(f^k)'(g(x))g'(x)=kx^{k-1}$$
> $$\sum_{i}i([x^i]f^k(x))g^{i-1}(x)g'(x)=kx^{k-1}$$
> $$\sum_{i}i([x^i]f^k(x))g^{i-1-n}(x)g'(x)=kx^{k-1}g^{-n}(x)$$
> $$[x^{-1}]\sum_{i}i([x^i]f^k(x))g^{i-1-n}(x)g'(x)=[x^{-1}]kx^{k-1}g^{-n}(x)$$
> $$n[x^k]f^k(x)=k[x^{-k}]g^{-n}(x)$$
> $\qquad\quad$ 证毕。

拉格朗日反演一般写成如下形式，便于计算:

> **扩展拉格朗日反演**：在拉格朗日反演基础上，对任意 $h\in P[[x]]$，有
> $$[x^n](h\circ f)(x)=\dfrac{1}{n}[x^{n-1}]h'(x)\left(\dfrac{x}{g(x)}\right)^n$$
> 证明：$[x^n](h\circ f)(x)=[x^n]\displaystyle\sum_k h_kf^k(x)=\dfrac{1}{n}\sum_{k}nh_k[x^n]f^k(x)$  
> $\qquad\quad\displaystyle=\dfrac{1}{n}\sum_{k}kh_k[x^{n-k}]\left(\dfrac{x}{g(x)}\right)^n=\dfrac{1}{n}[x^{n-1}]h'(x)\left(\dfrac{x}{g(x)}\right)^n$

## 多元多项式环

一元幂级数的讨论可以迁移到多元幂级数。如 形式偏导数、泰勒展开、乘法逆元、多元拉格朗日反演。

> 练习：试证明二元形式幂级数 $g(x,y)$ 的第二个变量在一元形式幂级数 $f(x)$ 处的泰勒展开
> $$g(x,y)=\sum_{k=0}^{+\infty}\dfrac{\dfrac{\partial^k g}{\partial y^k}(x,f(x))}{k!}(y-f(x))^k$$

但一元多项式的讨论不能完全迁移到多元多项式，    
因为域 $P$ 上的多项式环 $P[x]$ 是欧几里得整环而不是域，    
欧几里得整环 $R$ 上的多项式环 $R[x]$ 也不一定是欧几里得整环，  
因此二元多项式环 $P[x,y]$ 不是欧几里得整环，无法定义其上的带余除法。

--------------------------------------------

不过，尽管没有带余除法，多元多项式环依然满足其他的一些更弱的性质。    
首先，由于 $R[x]$ 是整环当且仅当 $R$ 为整环，$R[x,y]$ 为整环亦当且仅当 $R$ 为整环。

其次，类比整数环 $\Z$ 向有理数域 $\mathbb{Q}$ 的扩张，对于整环 $(R,+,\cdot)$，定义 $R\times\complement_R\{0\}$ 上元素的关系 $\equiv$

$$(a,b)\equiv(c,d)\Leftrightarrow a\cdot d=b\cdot c$$

易验证 $\equiv$ 是 $R\times\complement_R\{0\}$ 上的等价关系。例如对于传递性，由整环中乘法的消去律得

$$
\begin{rcases}
	\begin{rcases}
		a_1\cdot b_2=a_2\cdot b_1\\
		a_2\cdot b_3=a_3\cdot b_2
	\end{rcases}\Rightarrow a_1\cdot b_2\cdot b_3=a_2\cdot b_1\cdot b_3=a_3\cdot b_1\cdot b_2\\
	b_2\not =0
\end{rcases}\Rightarrow a_1\cdot b_3=a_3\cdot b_1

$$

之后，定义 $R\times\complement_R\{0\}$ 中元素的加法和乘法

$$
(a,b)+(c,d)=(a\cdot d+b\cdot c,b\cdot d)\\
(a,b)\cdot(c,d)=(a\cdot c,b\cdot d)
$$

则易验证 $\equiv$ 关于这两个运算均是良定义的。设 $\equiv$ 的所有等价类构成集合 $F$，  
则易验证 $(F,+,\cdot)$ 构成域，称为 $R$ 的 **分式域**，其中

- $(a,b)\in R\times\complement_R\{0\}$ 对应的等价类记为 $\dfrac{a}{b}$
- 分式域的加法单位元 $0=\dfrac{0}{b}$，乘法单位元 $1=\dfrac{a}{a}$，  
  $\dfrac{a}{b}$ 的加法逆元为 $-\dfrac{a}{b}-\dfrac{-a}{b}$，乘法逆元为 $\left(\dfrac{a}{b}\right)^{-1}=\dfrac{b}{a}$

- $\left\{\dfrac{r}{1}|r\in R \right\}$ 为 $F$ 的子域，且与 $R$ 同构。

整数环 $\Z$ 的分式域就是有理数域 $\mathbb{Q}$，  
整环 $R$ 上的多项式环 $R[x]$ 的分式域称为 $R$ 的 **有理分式域**，记为 $R(x)$。

整环 $R$ 向有理分式域 $F$ 的扩张，使得我们能通过 $F[x]$ 的性质来研究 $R[x]$ 的性质。具体地，有

> **高斯引理**：对于唯一分解整环 $R$ 及其分式域 $F$，若 $f(x)\in R[x]$ 在 $F[x]$ 中可约
> $$f(x)=g(x)h(x)\quad(g(x),h(x)\in F[x])$$
> $\qquad$ 则存在 $\lambda,\mu\in F$ 使得
> $$\lambda g(x),\mu h(x)\in R[x],\lambda\mu=1$$
> $$f(x)=(\lambda g(x))(\mu h(x))$$
> $\qquad$ 即，$f(x)$ 在 $R[x]$ 中也是可约的。  
> 证明：设 $g(x)$ 所有系数分母的最小公倍数为 $r_1$，$h(x)$ 所有系数分母的最小公倍数为 $r_2$，则
> $$r_1g(x),r_2h(x)\in R[x],f(x)=\dfrac{(r_1g(x))(r_2h(x))}{r_1r_2}$$
> $\qquad$ 将 $r=r_1r_2$ 因式分解，对于 $r$ 的一个质因子 $p$，易验证，$R/(p)$  
> $\qquad$（即 $\pmod p$ 意义下的 $R$） 是整环，否则与唯一分解定理矛盾
> $$
> \begin{rcases}
> a=\displaystyle\prod_k p_k^{c_k}&(\forall k\in\N,p_k\not =p)\\
> b=\displaystyle\prod_k p_k^{d_k}&(\forall k\in\N,p_k\not =p)
> \end{rcases}\Rightarrow ab=\prod_kp_k^{c_k+d_k}\not\equiv 0\pmod{p}
> $$
> $\qquad R/(p)$ 是整环，故 $(R/(p))[x]$ 也是整环，于是对多项式的系数取模，由整环的消去律得
> $$f(x)=\dfrac{(r_1g(x))(r_2h(x))}{r_1r_2}\in R[x]\Rightarrow (r_1g(x))(r_2h(x))\equiv 0\pmod{p}$$
> $$\Rightarrow r_1g(x)\equiv 0\pmod{p}\texttt{ 或 }r_2h(x)\equiv 0\pmod{p}$$
> $\qquad$ 因此 $r_1g(x),r_2h(x)$ 至少有一个系数能被 $p$ 整除，这就证明了该引理。

由高斯引理，$R[x]$ 上的因式分解与 $F[x]$ 上的因式分解是一一对应的。  
因此，**$R[x]$ 是唯一分解整环当且仅当 $R$ 是唯一分解整环**，  
由此进一步得到，$R[x,y]$ 是唯一分解整环当且仅当 $R$ 是唯一分解整环。

高斯引理还提供了一般的唯一分解整环 $R$ 上多项式的最大公约式、  
最小公倍式的计算方法——在 $R$ 的分式域 $F$ 上使用欧几里得算法计算最大公约式。  
这给出了多元有理分式的通分方法。