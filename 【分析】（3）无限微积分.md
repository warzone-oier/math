
# 无限微积分

## 微分学

### 导数

$$f'(x)=\frac{\mathrm{d}f(x)}{\mathrm{d}x}=\lim_{h\rightarrow 0}\frac{f(x+h)-f(x)}{h}$$

其中 $h$ 一般记为 $\Delta x$。  
$x=x_0$ 时，得到的极限 $f'(x_0)$ 称为 $f(x)$ 在 $x_0$ 处的 **导数**，  
在所有位置的导数构成的函数 $f'(x)$ 称为 $f(x)$ 的 **导函数**。 


从几何意义上看，导数表示 $f(x)$ 图象的切线的斜率；  
从物理意义上看，对于关于时间的函数 $g(t)$，其导数 $g'$ 表示 $g$ 的“瞬时变化率”。  
例如，速度是位移关于时间的导数，加速度是速度关于时间的导数。

#### 基本求导法则

若 **$f(x),g(x)$ 均可导**，则有以下基本求导法则成立：

- 线性法则：$(af+bg)'=af'+bg'\quad(a,b\in\mathbb{C})$
  > 证明：$(af(x)+bg(x))'=\displaystyle\lim_{h\rightarrow 0}\frac{(af(x+h)+bg(x+h))-(af(x)+bg(x))}{h}$
  > $$=\lim_{h\rightarrow 0}\left(a\frac{f(x+h)-f(x)}{h}+b\frac{g(x+h)-g(x)}{h}\right)$$
  > $$a\lim_{h\rightarrow 0}\frac{f(x+h)-f(x)}{h}+b\lim_{h\rightarrow 0}\frac{g(x+h)-g(x)}{h}=af'(x)+bg'(x)$$
- 乘法法则：$(fg)'=f'g+fg'$
  > 证明：$(f(x)g(x))'=\displaystyle\lim_{h\rightarrow 0}\frac{f(x+h)g(x+h)-f(x)g(x)}{h}$
  > $$=\lim_{h\rightarrow 0}\frac{f(x+h)g(x+h)-f(x)g(x+h)+f(x)g(x+h)-f(x)g(x)}{h}$$
  > $$=\lim_{h\rightarrow 0}\left(\frac{f(x+h)-f(x)}{h}g(x+h)+f(x)\frac{g(x+h)-g(x)}{h}\right)$$
  > $\qquad\quad$ 可导一定连续（详见下文 导数与极限），因此
  > $$(f(x)g(x))'=\left(\lim_{h\rightarrow 0}\frac{f(x+h)-f(x)}{h}\right)\lim_{h\rightarrow 0}g(x+h)+f(x)\lim_{h\rightarrow 0}\frac{g(x+h)-g(x)}{h}$$
  > $$=f'(x)g(x)+g'(x)f(x)$$

- 链式法则：$(f\circ g)'=(f'\circ g)\times g'$

练习：

1. 除法法则：已知 $\left(\dfrac{1}{x}\right)'=-\dfrac{1}{x^2}$。试用乘法法则和链式法则证明，若 $f(x),g(x)$ 均可导且 $g'(x)\not =0$，则
$$\left(\frac{f(x)}{g(x)}\right)'=\frac{f'(x)g(x)-f(x)g'(x)}{g^2(x)}$$ 
2. 反函数求导：试用链式法则证明，若 $f,g$ 反函数，且 $f$ 可导，则
   $$g'=\frac{1}{f'\circ g}$$

#### 基本初等函数的导数

- $\ln' x=\dfrac{1}{x}$
  > 证明：
  > $$\ln' x=\displaystyle\lim_{h\rightarrow 0}\frac{\ln(x+h)-\ln x}{h}=\lim_{h\rightarrow 0}\frac{\ln\left(\frac{x}{h}+1\right)}{h}=\frac{1}{x}\lim_{h\rightarrow 0}\frac{x}{h}\ln\left(1+\frac{x}{h}\right)=\frac{1}{x}\lim_{h\rightarrow 0}\ln\left(\left(1+\frac{x}{h}\right)^{\frac{x}{h}}\right)$$
  > $\qquad$ 使用重要极限 $\displaystyle\lim_{x\rightarrow\infty}\left(1+\frac{1}{x}\right)^x=e$，可得
  > $$\ln'x=\frac{1}{x}\lim_{t\rightarrow\infty}\ln\left(\left(1+\frac{1}{t}\right)^t\right)=\frac{1}{x}\ln e=\frac{1}{x}$$
   $\frac{1}{x}$ 在 $x<0$ 时仍有定义。实数域内，我们将 $\ln x$ 扩充为偶函数 $\ln|x|$，可得
  $$(\ln |x|)'=\frac{1}{x}$$
   复数域内则直接使用其多值函数定义，详见【域】（1）复数域。  
  推论：$\log_a' x=\frac{1}{x\ln a}\qquad(\log_a x=\frac{\ln x}{\ln a})$
- $(e^x)'=e^x$
  > 证明：$e^x,\ln x$ 互为反函数，由反函数求导得
  > $$(e^x)'=\frac{1}{\ln' \circ\ e^x}=\frac{1}{\frac{1}{e^x}}=e^x$$
  推论：$(a^x)'=a^x\ln a\qquad(a^x=e^{x\ln a})$
- $(x^n)'=nx^{n-1}\quad(n\in\mathbb{C})$
  > 证明：$(x^n)'=(e^{n\ln x})'=(e^x\circ n\ln x)'=((e^x)'\circ n\ln x)\times(n\ln x)'$  
  > $\qquad\qquad=(e^x\circ n\ln x)\times\frac{n}{x}=x^n\times\frac{n}{x}=nx^{n-1}$
- $\sin'x=\cos x,\cos'x=-\sin x$
  > 证明：以 $\sin'$ 为例，有
  > $$\sin' x=\displaystyle\lim_{h\rightarrow 0}\frac{\sin(x+h)-\sin x}{h}=\lim_{h\rightarrow 0}\frac{\sin x\cos h+\cos x\sin h-\sin x}{h}$$
  > $$=\left(\lim_{h\rightarrow 0}\frac{\cos h-1}{h}\right)\sin x+\left(\lim_{h\rightarrow 0}\frac{\sin h}{h}\right)\cos x$$
  > 使用重要极限 $\displaystyle\lim_{x\rightarrow 0}\frac{\sin x}{x}=1,\lim_{x\rightarrow 0}\frac{1-\cos x}{x}=0$，得
  > $$\sin' x=0\times \sin x+1\times\cos x=\cos x$$
  > $\cos'$ 是类似的。  

  推论：$\tan' x=\dfrac{1}{\cos^2 x}\qquad(\tan x=\dfrac{\sin x}{\cos x})$
- $\arcsin'x=\dfrac{1}{\sqrt{1-x^2}},\arccos'x=-\dfrac{1}{\sqrt{1-x^2}},\arctan' x=\dfrac{1}{1+x^2}$
  > 证明：以 $\arcsin'$ 为例，由反函数求导得
  > $$\sin' x=\frac{1}{\arcsin'\sin x}=\cos x=\sqrt{1-\sin^2 x}$$
  > $$\arcsin' x=\frac{1}{\sqrt{1-x^2}}$$
  > $\arccos',\arctan'$ 是类似的。

结合基本求导法则和基本初等函数的导数，可求任意初等函数的导数。

练习：试求解 $x^x$ 的导数。

#### 高阶导数

我们定义所谓的 **二阶导数** 和 **三阶导数** 为

$$y''=\frac{\mathrm{d}^2y}{\mathrm{d}x^2}=(y')'$$

$$y'''=\frac{\mathrm{d}^3y}{\mathrm{d}x^3}=(y'')'$$

由此拓展到所谓的 **$n$ 阶导数**

$$y^{(0)}=\frac{\mathrm{d}^0y}{\mathrm{d}x^0}=y,y^{(n)}=\frac{\mathrm{d}^ny}{(\mathrm{d}x)^n}=(y^{(n-1)})'\quad(n\in\N_+)$$

$n$ 阶导数依然满足线性法则。而对于乘法法则，可由组合意义得到所谓的 **莱布尼兹公式**

$$(uv)^{(n)}=\sum_k\dbinom{n}{k}u^{(n-k)}v^{(k)}$$

#### 导数的应用

同样地，我们可以用导数对函数的单调性进行分析：

- $\forall x\in(l,r),f'(x)>0\Rightarrow f(x)$ 在 $(l,r)$ 单调递增。
- $\forall x\in(l,r),f'(x)<0\Rightarrow f(x)$ 在 $(l,r)$ 单调递减。

同样地，若 $f(x)$ 在 $x_0$ 附近的取值总比 $f(x_0)$ 小，则称 $x_0$ 为 $f(x)$ 的一个 **极大值点**，$f(x_0)$ 称为 $f(x)$ 的一个 **极大值**。  
极小值和极小值点同理。极大值和极小值统称 **极值**，极值点同理。

我们同样可以通过单调性得到函数的极值。此外还有
> 费马引理：若 $f(x)$ 在 $x_0$ 处可导，那么 $x_0$ 为 $f(x)$ 极值点 $\Rightarrow f'(x_0)=0$

有了极值，我们就可求得函数的最值。

--------------------------------------------------------------------------

函数同样有凹凸性的概念。区间 $I$ 内，若 

$$\forall x_1,x_2\in I,\lambda\in(0,1),f(\lambda x_1+(1-\lambda)x_2)\le\lambda f(x_1)+(1-\lambda)f(x_2)$$

则称 $f(x)$ 在区间 $I$ 内是  **凹（下凸）** 的。若 $f(x)$ 连续，该定义等价于

$$\forall x_1,x_2\in I,\lambda\in(0,1),f\left(\frac{x_1+x_2}{2}\right)\le\frac{f(x_1)+f(x_2)}{2}$$

将定义中 $\le$ 分别换成 $<,\ge,>$，得到的分别是 **严格凹（严格下凸）**、**凸（上凸）**、**严格凸（严格上凸）** 的定义。  
我们同样可以用二阶导数获取函数的凹凸性：

- $\forall x\in I,f''(x)\ge 0\Rightarrow f(x)$ 在区间 $I$ 内是凹（下凸）的。
- $\forall x\in I,f''(x)> 0\Rightarrow f(x)$ 在区间 $I$ 内是严格凹（严格下凸）的。
- $\forall x\in I,f''(x)\le 0\Rightarrow f(x)$ 在区间 $I$ 内是凸（上凸）的。
- $\forall x\in I,f''(x)<0\Rightarrow f(x)$ 在区间 $I$ 内是严格凸（严格上凸）的。

> 注意：单调性、极值、凹凸性的定义与函数的可导性无关。

#### 导数与极限

首先讨论函数的可导性与其连续性的关系。

由 $|x|$ 可知，函数在某一点连续则不一定在该点可导。实际上，  
可以构造处处连续处处不可导的函数：魏尔斯特拉斯函数

$$f(x)=\sum_{n=0}^{+\infty}a^n\cos(b^n\pi x)\quad\left(a<1,b=2c+1(c\in\Z),ab>1+\frac{3\pi}{2}\right)$$

那么反之是否成立？答案是肯定的。

> 求证：$f(x)$ 在某一点可导则一定在该点连续。  
> 证明：若 $f(x)$ 在 $x_0$ 可导，则
> $$\lim_{h\rightarrow 0}\frac{f(x_0+h)-f(x_0)}{h}=f'(x_0)$$
> $$\lim_{h\rightarrow 0}\frac{f(x_0+h)-f(x_0)-f'(x_0)h}{h}=0$$
> $$f(x_0+h)-f(x_0)-f'(x_0)h=\omicron(h)\quad(h\rightarrow 0)$$
> $$f(x_0+h)=f(x_0)+f'(x_0)h+\omicron(h)\quad(h\rightarrow 0)$$
> $$\lim_{h\rightarrow 0} f(x_0+h)=\lim_{h\rightarrow 0}(f(x_0)+f'(x_0)h+\omicron(h))=f(x_0)$$
> 因此 $f(x)$ 在 $x_0$ 连续。

> 注：实际上，从微分的几何意义（下文将说明）上看，这一结论是显然的。

---------------------------------------------
接下来，与 Stolz 定理相同，我们可以借助导数求解函数极限。我们有

> **洛必达法则（$\dfrac{0}{0}$ 型）**：若 $\R$ 上函数极限 $\displaystyle\lim_{x\rightarrow t^+}f(x)=\lim_{x\rightarrow t^+} g(x)=0$，则
> $$\lim_{x\rightarrow t^+}\dfrac{f'(x)}{g'(x)}=L\Rightarrow \lim_{x\rightarrow t^+}\dfrac{f(x)}{g(x)}=L$$
> 证明：首先证明
> > 柯西中值定理：若 $\R$ 上 $f(x),g(x)$ 在 $[a,b]$ 连续，在 $(a,b)$ 可导，且 $\forall x\in(a,b),g'(x)\not=0$，则
> > $$\exists\xi\in(a,b),\dfrac{f(b)-f(a)}{g(b)-g(a)}=\dfrac{f'(\xi)}{g'(\xi)}$$
> > 柯西中值定理是拉格朗日中值定理（详见【环】（2）多项式环）的推广。  
> > 证明：首先，$g(a)\not =g(b)$，这是因为若 $g(a)=g(b)$，由罗尔中值定理  
> > $\qquad$（拉格朗日中值定理 $f(a)=f(b)$ 的情况），存在 $\xi\in(a,b)$ 使得 $g'(\xi)=0$,  
> > $\qquad$ 与题意 $\forall x\in(a,b),g'(x)\not=0$ 相矛盾。接下来，构造函数
> > $$F(x)=(f(b)-f(a))(g(x)-g(a))-(g(b)-g(a))(f(x)-f(a))$$
> > $\qquad$ 显然 $F(a)=F(b)=0$，同样由罗尔中值定理得
> > $$\exist\xi\in(a,b),F'(\xi)=(f(b)-f(a))g'(\xi)-(g(b)-g(a))f'(\xi)=0$$
> > $$\exist\xi\in(a,b),\dfrac{f(b)-f(a)}{g(b)-g(a)}=\dfrac{f'(\xi)}{g'(\xi)}$$
> 在此基础上，假设 $f(t)=g(t)=0$，显然这不影响定理的正确性。此时结合题意 $\displaystyle\lim_{x\rightarrow t^+}\dfrac{f'(x)}{g'(x)}=L$，    
> 必存在 $\delta>0$ 使得 $f(x),g(x)$ 在 $[t,t+\delta)$ 连续，在 $(t,t+\delta)$ 可导，且 $\forall x\in(t,t+\delta),g'(x)\not =0$。  
> 于是对于任意的 $x\in(t,t+\delta)$，$[t,x]$ 符合柯西中值定理的使用条件，存在 $\xi\in(t,x)$ 使得
> $$\dfrac{f(x)}{g(x)}=\dfrac{f(x)-f(t)}{g(x)-g(t)}=\dfrac{f'(\xi)}{g'(\xi)}$$
> 因此若 $\displaystyle\lim_{x\rightarrow t^+}\dfrac{f'(x)}{g'(x)}=L$，则
> $$\lim_{x\rightarrow t^+}\dfrac{f(x)}{g(x)}=\lim_{x\rightarrow t^+}\dfrac{f(\xi)}{g(\xi)}=\lim_{x\rightarrow t^+}\dfrac{f'(x)}{g'(x)}$$

> **洛必达法则（$\dfrac{\cdot}{\infty}$ 型）**：若 $\R$ 上函数极限 $\displaystyle\lim_{x\rightarrow t^+} g(x)=\infty$，则
> $$\lim_{x\rightarrow t^+}\dfrac{f'(x)}{g'(x)}=L\Rightarrow \lim_{x\rightarrow t^+}\dfrac{f(x)}{g(x)}=L$$
> 证明*：由于 $\displaystyle\lim_{x\rightarrow t^+} g(x)=\infty,\lim_{x\rightarrow t^+}\dfrac{f'(x)}{g'(x)}=L$，必存在 $\delta>0$ 使得 $f(x),g(x)$ 在 $(t,t+\delta)$ 可导且
> $$\forall x\in(t,t+\delta),g'(x)\not =0,g(x)\not =0$$
> $\qquad\quad$ 因此对于任意的 $[x,y]\subseteq(t,t+\delta)$，由柯西中值定理得存在 $\xi\in(x,y)$，使得
> $$\dfrac{f(y)-f(x)}{g(y)-g(x)}=\dfrac{f'(\xi)}{g'(\xi)}$$
> $\qquad\quad$ 也即
> $$f(y)-f(x)=\dfrac{f'(\xi)}{g'(\xi)}(g(y)-g(x))$$
> $$\dfrac{f(x)}{g(x)}=\dfrac{f(y)}{g(x)}+\dfrac{f'(\xi)}{g'(\xi)}\left(1-\dfrac{g(y)}{g(x)}\right)\quad(1)$$
> $\qquad\quad$ 固定 $y$，则显然有 $\displaystyle\lim_{x\rightarrow t^+}\dfrac{f(y)}{g(x)}=\lim_{x\rightarrow t^+}\dfrac{g(y)}{g(x)}=0$，也就是说，  
> $\qquad\quad$ 对于任意的 $\epsilon_0>0,y\in(t,t+\delta)$，总存在 $x_0\in(t,y)$，使得
> $$\forall x\in(t,x_0),\dfrac{|f(y)|}{|g(x)|}<\epsilon_0,\dfrac{|g(y)|}{|g(x)|}<\epsilon_0$$
> $\qquad\quad$ 若 $L$ 为有限值，则根据 $\displaystyle\lim_{x\rightarrow t^+}\dfrac{f'(x)}{g'(x)}=L$ 的定义，对于任意的 $\epsilon_1>0$，总存在 $y\in(t,t+\delta)$，使得
> $$\forall\xi\in(t,y),\dfrac{f'(\xi)}{g'(\xi)}\in(L-\epsilon_1,L+\epsilon_1)$$
> $\qquad\quad$ 于是对于 $\epsilon_1$ 指定相应的 $y$，再由 $y,\epsilon_0$ 指定相应的 $x_0$，得 $(1)$ 式的范围为
> $$(L-\epsilon_1-(|L|+1)\epsilon_0-\epsilon_1\epsilon_0,L+\epsilon_1+(|L|+1)\epsilon_0+\epsilon_1\epsilon_0)$$
> $\qquad\quad$ 因此对于任意的 $\epsilon>0$，令
> $$\epsilon_0=\epsilon_1=\dfrac{\sqrt{(|L|+2)^2+4\epsilon}-|L|-2}{2}$$ 
> $\qquad\quad$ 即可使得 $\epsilon_1+(|L|+1)\epsilon_0+\epsilon_1\epsilon_2=\epsilon$，进而根据 $\epsilon_0,\epsilon_1$ 确定 $y$ 和 $x$ 的范围 $x_0$，  
> $\qquad\quad$ 使得 $(1)$ 式落在 $(L-\epsilon,L+\epsilon)$ 内。由极限的定义，这就是
> $$\lim_{x\rightarrow t^+}\dfrac{f(x)}{g(x)}=L$$ 
> $\qquad\quad L$ 不为有限值的情况也是类似的。即根据 $\dfrac{f(x)}{g(x)}$ 的范围 $\epsilon$ 确定 $\dfrac{f(y)}{g(x)},\dfrac{g(y)}{g(x)}$ 的范围 $\epsilon_0$ 以及  
> $\qquad\quad\dfrac{f'(\xi)}{g'(\xi)}$ 的范围 $\epsilon_1$，进而确定相应的 $y,x_0$。

通过右复合 $-x,\dfrac{1}{x}$ 的方法可知，将 $x\rightarrow t^+$ 换成 $\R$ 上的其余 $5$ 种趋近方式（$x\rightarrow t^-,t,\pm\infty,\infty$），  
依然有相应的洛必达法则成立。

$0\cdot \infty,0^0,1^{\infty},\infty^0,+\infty-\infty$ 等类型的极限均可通过取倒数，对数、指数  
等方法化为以上两种极限，进而通过洛必达法则求解。

> 注意：
> 1. 使用洛必达法则前一定要检查是否为 $\dfrac{0}{0}$ 或 $\dfrac{\cdot}{\infty}$ 。
> 2. 若洛必达法则能求得极限，则极限一定存在；若洛必达法则无法求得极限，则极限不一定不存在。如
> $$\lim_{x\rightarrow 0}\dfrac{x^2\sin\dfrac{1}{x}}{\sin x},\lim_{x\rightarrow\infty}\dfrac{x+\sin x}{x}$$

------------------------------------

最后，讨论导函数的连续性。导函数不一定是连续的，例如

$$
f(x)=\begin{cases}
	x^2\sin\left(\dfrac{1}{x^2}\right)&(x\not =0)\\
	0&(x=0)
\end{cases}\\
f'(x)=\begin{cases}
	2x\sin\left(\dfrac{1}{x^2}\right)-\dfrac{2}{x}\cos\left(\dfrac{1}{x^2}\right)&(x\not =0)\\
	\displaystyle\lim_{h\rightarrow 0}h\sin\left(\dfrac{1}{h^2}\right)=0&(x=0)
\end{cases}
$$

显然 $\displaystyle\lim_{x\rightarrow 0}f'(x)$ 发散。以上例子也说明导函数在闭区间上不一定存在最值。

但是，导函数依然和连续函数一样满足介值性，即

> **达布中值定理**：若 $f(x)$ 在 $[a,b]$ 可导，则 $f'(x)$ 在 $(a,b)$ 可取在 $f'(a)$ 和 $f'(b)$  
> $\qquad\qquad\quad$（不包括 $f'(a),f'(b)$）之间的任何值。  
> 证明：以下证明 $f'(a)<f'(b)$ 的情况，$f'(a)>f'(b)$ 的情况是类似的。  
> $\qquad$ 设 $f'(a)<\eta<f'(b)$，构造函数 $g(x)=f(x)-\eta x$，则
> $$g'(x)=f'(x)-\eta$$
> $\qquad\quad$ 由于 $g(x)$ 在 $[a,b]$ 可导，故 $g(x)$ 在 $[a,b]$ 连续，其在 $[a,b]$ 必然存在最大值和最小值。又因为
> $$g'(a)=\lim_{x\rightarrow a}\dfrac{g(x)-g(a)}{x-a}=f'(a)-\eta<0$$
> $$g'(b)=\lim_{x\rightarrow b}\dfrac{g(x)-g(b)}{x-b}=f'(b)-\eta>0$$
> $\qquad\quad$ 因此 $[a,b]$ 上，在 $a$ 附近有 $x>a,g(x)<g(a)$，在 $b$ 附近有 $x<b,g(x)<g(b)$，   
> $\qquad\quad$ 故 $g(x)$ 在 $[a,b]$ 的最小值只可能在 $(a,b)$ 取得。设在 $\xi$ 取最小值，则 
> $$g'(\xi)=f'(\xi)-\eta=0$$
> $\qquad\quad$ 因此对于任意的 $f'(a)<\eta<f'(b)$，总存在 $\xi$ 使得 $f'(\xi)=\eta$。

在洛必达法则的基础上，即可给出导函数连续的条件：

> **导数极限定理**：若 $f(x)$ 在 $x_0$ 连续，且 $\displaystyle\lim_{x\rightarrow x_0}f'(x)$ 收敛，则 $f'(x_0)$ 存在，且
> $$f'(x_0)=\lim_{x\rightarrow x_0}f'(x)$$
> 证明：由于 $f(x)$ 在 $x_0$ 连续，故 $\displaystyle\lim_{h\rightarrow 0}f(x_0+h)-f(x_0)=0$。  
> $\qquad\quad$ 因此可对导数的定义使用洛必达法则，得
> $$f'(x_0)=\lim_{h\rightarrow 0}\dfrac{f(x_0+h)-f(x_0)}{h}=\lim_{h\rightarrow 0}\dfrac{\dfrac{\mathrm{d}(f(x_0+h)-f(x_0))}{\mathrm{d}h}}{\dfrac{\mathrm{d}h}{\mathrm{d}h}}=\lim_{h\rightarrow 0}f'(x_0+h)=\lim_{x\rightarrow x_0}f'(x)$$
### 微分

微分源自于导数的莱布尼兹记号 $\frac{\mathrm{d}y}{\mathrm{d}x}$ 。该记号相当直观，例如导数的链式法则可用莱布尼兹记号表示为

$$\frac{\mathrm{d}y}{\mathrm{d}x}=\frac{\mathrm{d}y}{\mathrm{d}u}\frac{\mathrm{d}u}{\mathrm{d}x}$$

此时我们依然将 $\frac{\mathrm{d}y}{\mathrm{d}x}$ 视为一个整体。我们希望将其分子和分母拆开使用，即

$$\mathrm{d}y=y'\mathrm{d}x$$

显然，$\mathrm{d}y$ 与 $\Delta y$ 的意义并不相同：后者的意义就是 $y$ 的变化量，而前者的意义由上式可知，  
是将函数在某一点附近的图象 **近似为直线** 后 $y$ 的变化量。

如何理解 “近似为直线”？函数某一点的切线有两种理解方式：

1. 割线的极限，即导数  
  <div align=center> <img src=./d_0.png width=70%></div>

   $$\Downarrow Q\rightarrow P$$
   <div align=center> <img src=./d0.png width=70%></div>
2. 将函数在该点附近的图象无限放大，得到的局部图象越来越接近直线   
   <div align=center> <img src=./d0.png width=70%></div>

   $$\Downarrow\texttt{放大}$$
   <div align=center> <img src=./d1.png width=70%></div>

   $$\Downarrow\texttt{放大}$$
   <div align=center> <img src=./d2.png width=70%></div>

$\quad$这就是对 “近似为直线” 的直观理解，也就是微分的几何意义。

综上，对于变量 $y=f(x)$，若在 $x_0$ 处有

$$\Delta y=f(x_0+\Delta x)-f(x_0)=A\Delta x+\omicron(\Delta x)\quad(\Delta x\rightarrow 0)$$

其中 $A$ 为与 $\Delta x$ 无关的量，则称

$$\mathrm{d}y=A\Delta x\qquad(\Delta x\rightarrow 0)$$

为 $x_0$ 处变量 $y$ 对变量 $x$ 的 **微分**。所有点处的微分构成的关于 $x$ 的函数

$$\mathrm{d}y=A(x)\Delta x$$

称为 变量 $y$ 对变量 $x$ 的微分。

特别地，由定义，自变量对自身的微分 $\mathrm{d}x=\Delta x$。

> 求证：可导与可微等价，且
> $$\mathrm{d}f(x)=f'(x)\mathrm{d}x$$
> 证明：由于 $\Delta x\rightarrow 0$ 的过程中始终非 $0$，故
> $$\lim_{\Delta x\rightarrow 0}\frac{f(x+\Delta x)-f(x)}{\Delta x}=A(x)\Leftrightarrow \lim_{\Delta x\rightarrow 0}\frac{f(x+\Delta x)-f(x)-A(x)\Delta x}{\Delta x}=0$$
> $$\Leftrightarrow f(x+\Delta x)-f(x)-A(x)\Delta x=\omicron(\Delta x)\quad(\Delta x\rightarrow 0)$$
> $$\Leftrightarrow f(x+\Delta x)-f(x)=A(x)\Delta x+\omicron(\Delta x)\quad(\Delta x\rightarrow 0)$$
> $$\Leftrightarrow \mathrm{d}f(x)=A(x)\Delta x\quad(\Delta x\rightarrow 0)$$

导数与微分的等价性说明，二者是对同一对象（切线）的不同表述（割线的极限和函数图象的局部近似）。   
导数的运算法则和基本初等函数的导数均可迁移到微分。

> 求证：导数的链式法则，即
> $$\frac{\mathrm{d}y}{\mathrm{d}x}=\frac{\mathrm{d}y}{\mathrm{d}u}\frac{\mathrm{d}u}{\mathrm{d}x}$$
> 证明：一个显然的想法是
> $$\lim_{\Delta x\rightarrow 0}\frac{\Delta y}{\Delta x}=\lim_{\Delta x\rightarrow 0}\frac{\Delta y}{\Delta u}\frac{\Delta u}{\Delta x}$$
> $\qquad$ 遗憾地是，$\Delta x\rightarrow 0$ 时 $\Delta u$ 可能等于 $0$，不能作为分母。  
> $\qquad$ 不过渐进没有这方面的顾虑，于是我们使用微分。设
> $$\Delta y=A\Delta u+\omicron(\Delta u)\qquad(\Delta u\rightarrow 0)$$
> $$\Delta u=B\Delta x+\omicron(\Delta x)\qquad(\Delta x\rightarrow 0)$$
> $\qquad$ 则
> $$\Delta y=A(B\Delta x+\omicron(\Delta x))+\omicron(B\Delta x+\omicron(\Delta x))$$
> $$=AB\Delta x+A\omicron(\Delta x)+\omicron(B\Delta x+\omicron(\Delta x))$$
> $$=AB\Delta x+\omicron(A\Delta x)+\omicron(B\Delta x)$$
> $$=AB\Delta x+\omicron(\Delta x)\qquad(\Delta x\rightarrow 0)$$
> $\qquad$ 因此
> $$\frac{\mathrm{d}y}{\mathrm{d}u}=A,\frac{\mathrm{d}u}{\mathrm{d}x}=B\Rightarrow \frac{\mathrm{d}y}{\mathrm{d}x}=AB$$

导数的链式法则在微分中等价于

$$\mathrm{d}(f(g(x)))=f'(g(x))\mathrm{d}g(x)$$

也就是说，复合函数不论对哪个中间变量作微分，微分在形式上是不变的，这称为 **微分的形式不变性**。  
因此，我们可以直接称 $\mathrm{d}y$ 为变量 $y$ 的微分，而无须说明是对哪个自变量的微分。

#### 参数方程求导

对于参数方程

$$\begin{cases}
	x=x(t)\\
	y=y(t)\\
\end{cases}$$

我们将 $y$ 对 $x$ 的导数直接看成两个微分相除，即

$$\frac{\mathrm{d}y}{\mathrm{d}x}=\frac{\mathrm{d}y(t)}{\mathrm{d}x(t)}=\frac{y'(t)\mathrm{d}t}{x'(t)\mathrm{d}t}=\frac{y'(t)}{x'(t)}$$

从复合函数和反函数的求导法则来看，这是显然的。

> 思考：如何通过参数方程求解 $\dfrac{\mathrm{d}^2y}{\mathrm{d}x^2}$？（提示：$\dfrac{\mathrm{d}^2y}{\mathrm{d}x^2}=\dfrac{\mathrm{d}\left(\dfrac{\mathrm{d}y}{\mathrm{d}x}\right)}{\mathrm{d}x}$）

> 注：我们一般不考虑与高阶导数相对应的“高阶微分”概念。其不满足形式不变性，且与  
>	【分析】（5）多元微积分 中的 “外微分” 运算相矛盾。

## 积分学

### 定积分

欲求 $f(x)$ 在区间 $[a,b]$ 上图象与直线 $x=a,x=b$ 和 $x$ 轴围成图形的 **有向面积**。  
所谓有向面积，指的是 $f(x)>0$ 时面积取正值，$f(x)<0$ 时面积取负值。  
我们称其为 $f(x)$ 在 $[a,b]$ 上的 **定积分**，记为

$$\int_a^bf(x)\mathrm{d}x$$

--------------------------------------
定积分的一个常用定义是 **黎曼积分**。  
考虑区间 $[a,b]$ 的一个分割 $x_0,x_1,\cdots,x_n$，将其扩展为 **取样分割** $x_0,x_1,\cdots,x_n,t_0,t_1,\cdots,t_n$

$$a=x_0<t_1<x_1<t_2<\cdots<x_{n-1}<t_n<x_n=b$$

我们称取样分割 $y_0,y_1,\cdots,y_m,s_1,s_2,\cdots,s_m$ 比 $x_0,x_1,\cdots,x_n,t_0,t_1,\cdots,t_n$ **更精细**，  
是指分割 $y_0,s_1,y_1,s_2,\cdots,y_{m-1},s_m,y_m$ 比分割 $x_0,t_1,x_1,t_2,\cdots,x_{n-1},t_n,x_n$ 更精细。

定义 $f(x)$ 在取样分割 $x_0,x_1,\cdots,x_n,t_0,t_1,\cdots,t_n$ 下的 **黎曼和**

$$\sum_{k=1}^nf(t_k)(x_k-x_{k-1})$$

记 $\lambda=\displaystyle\max_{k=1}^n(x_k-x_{k-1})$，
若不论将 $[a,b]$ 如何分割，只要 $\lambda\rightarrow 0$，黎曼和的极限收敛且相等，  
则称该极限为 $f(x)$ 在 $[a,b]$ 上的 **黎曼积分**，即

$$\int_a^bf(x)\mathrm{d}x=\lim_{\lambda\rightarrow 0}\sum_{k=1}^nf(t_k)(x_k-x_{k-1})$$

显然，黎曼积分的定义可以推广到 $f(x)$ 在区间上有有限个点无定义（称为 **瑕积分**）  
和积分上下限为 $\pm\infty$ 的情形（称为 **反常积分**）。

#### 定积分的性质

为方便定积分的运算，记

$$\int_a^af(x)\mathrm{d}x=0,\int_b^af(x)\mathrm{d}x=-\int_a^bf(x)\mathrm{d}x$$

1. $\displaystyle a\int_l^rf(x)\mathrm{d}x+b\int_l^rg(x)\mathrm{d}x=\int_l^r(af(x)+bg(x))\mathrm{d}x$
2. $\displaystyle \int_a^b f(x)\mathrm{d}x+\int_b^c f(x)\mathrm{d}x=\int_{a_{}}^c f(x)\mathrm{d}x$
3. 若 $f(x)$ 在 $[a,b]$ 上可积，则 $\displaystyle\int_a^{x^{}} f(t)\mathrm{d}t$ 在 $[a,b]$ 上连续。
4. > **微积分基本定理（牛顿-莱布尼兹公式）**：若 $f'(x)$ 在 $[a,b]$ 上连续，则 $f'(x)$ 在 $[a,b]$ 上可积，且
   > $$\int_a^b f'(x)\mathrm{d}x=f(b)-f(a)$$
   > 证明：首先我们有 **积分中值定理**：若 $f(x)$ 在 $[a,b]$ 连续，则  $\displaystyle\int_a^bf(x)\mathrm{d}x$ 存在，且
   > $$\exists\varepsilon\in[a,b],\int_a^bf(x)\mathrm{d}x=f(\varepsilon)(b-a)$$
   > $\qquad$ 其在几何上是显然的，证明从略。  
   > $\qquad$ 接下来，考虑 $\displaystyle\int_a^x f(t)\mathrm{d}t$ 的导数，得
   > $$\left(\int_a^x f(t)\mathrm{d}t\right)'=\lim_{h\rightarrow 0}\frac {\displaystyle\int_a^{x+h} f(t)\mathrm{d}t-\int_a^{x} f(t)\mathrm{d}t}{h}=\lim_{h\rightarrow 0}\frac{\displaystyle\int_x^{x+h} f(t)\mathrm{d}t}{h}$$
   > $$=\lim_{h\rightarrow 0}\frac{f(\varepsilon)h}{h}=\lim_{h\rightarrow 0}f(\varepsilon)=f(x)$$
   > $\qquad\displaystyle\int_a^x f'(t)\mathrm{d}t$ 与 $f(x)$ 导数相同，因此二者只相差一个常数，这就证明了微积分基本定理。

微积分基本定理给出了连续函数定积分的计算方法。结合性质 3.，即可计算瑕积分和反常积分。

#### 定积分的近似计算

对于 $[l,r]$ 上的二次函数 $f(x)=ax^2+bx+c$ 的定积分，有 **辛普森公式**

$$\int_l^rf(x)\mathrm{d}x=\dfrac{r-l}{6}\left(f(l)+f(r)+4f\left(\dfrac{l+r}{2}\right)\right)$$

推导过程从略。也就是说，只需要用二次函数在区间端点处和中点处的取值即可轻松计算其定积分。

对于一般函数定积分的近似计算，一个简单的想法是将其等距分割后，  
每一段近似为二次函数计算定积分，这就是近似计算定积分的 **辛普森法**。

显然，分割得越精细，每一段越接近二次函数，辛普森公式计算所得误差就越小。  
为了减少计算量，我们可以采取自适应的方法：对于某一段，尝试将其二等分成左右两段，  
若两段用辛普森公式求得定积分之和与原来段用辛普森公式求得的定积分的差值不够小，  
则递归下去对左右两段继续二等分，直到这个差值足够小。这就是 **自适应辛普森法**。



### 不定积分

为方便定积分的计算，定义所谓不定积分为定积分的逆运算

$$\int f'(x)\mathrm{d}x=f(x)+C$$

其中 $C$ 为任意常数，且 **对于 $f(x)$ 的各个连续区间相互独立**。

> 注意：定积分与不定积分对于瑕点的行为是不同的。  
> $\qquad$ 例如对于 $\frac{x}{|x|}$，定积分会分别计算各个连续区间内的积分然后相加
> $$\int_{-1}^1\frac{x}{|x|}\mathrm{d}x=\int_{-1}^0\frac{x}{|x|}\mathrm{d}x+\int_{0}^1\frac{x}{|x|}\mathrm{d}x=-1+1=0$$
> $\qquad$ 而不定积分会赋予每个连续区间以独立的常数
> $$\int\frac{x}{|x|}\mathrm{d}x=\begin{cases}x+C_1&(x>0)\\ -x+C_2&(x<0)\end{cases}$$
> $\qquad$ 因此微积分基本定理仅适用于连续函数的积分。

#### 换元积分法

换元积分法即使用导数的链式法则来计算不定积分。考虑活用不定积分符号中的 $\mathrm{d}x$，得

$$\int f(g(x))g'(x)\mathrm{d}x=\int f(g)\mathrm{d}g$$

这称为 **第一类换元法**，又称 **凑微分法**。

---------------------------------------------------------------------------

**第二类换元法** 则是拆微分，设 $x=x(t),x(t)$ 单调可导，且 $x'(t)\not =0$，则

$$\int f(x)\mathrm{d}x=\int f(x(t))x'(t)\mathrm{d}t$$

> 练习：试计算以下不定积分：
> 1. $\displaystyle\int\frac{\mathrm{d}x}{(x-a)^k}\quad(k\ge 1)$
> 2. $\displaystyle\int\frac{\mathrm{d}x}{x^2+a^2}$
> 3. $\displaystyle\int\frac{ax+b}{x^2+px+q}\mathrm{d}x\quad(p^2-4q<0)$


#### 分部积分法

分部积分法即使用导数的乘法法则来计算不定积分。由

$$\mathrm{d}(uv)=u\mathrm{d}v+v\mathrm{d}u$$

得

$$u\mathrm{d}v=\mathrm{d}(uv)-v\mathrm{d}u$$

$$\int u\mathrm{d}v=uv-\int v\mathrm{d}u$$

分部积分法具有 “一升一降” 的效果。对于两个基本初等函数相乘的情况，一般采取 “反对幂三指” 的原则，  
即选取 $u$ 的优先级为 反三角函数 $>$ 对数函数 $>$ 幂函数（多项式） $>$ 三角函数 $>$ 指数函数。 

> 练习：试计算以下不定积分：  
> 4. $\displaystyle\int(x^2+x+1)e^x\mathrm{d}x$  
> 5. $\displaystyle\int e^x\sin x\mathrm{d}x$  
> 6. $\displaystyle\int \ln x\mathrm{d}x$  
> 7. $\displaystyle\int\cos^n x\mathrm{d}x\quad(n\in\N_+)$

#### 有理函数积分

在阅读该小节前请阅读完【环】（2）多项式环 。

对于初等函数，其定义区间上的积分一定存在。但是，其未必能用初等函数表示，例如

- 指数积分 $\displaystyle\mathrm{Ei}(x)=\int_{-\infty_{}}^x\frac{e^t}{t}\mathrm{d}t$（对数积分 $\displaystyle\mathrm{li}(x)=\int_0^x\frac{\mathrm{d}t}{\ln t}$）
- 正弦积分 $\displaystyle\mathrm{Si}(x)=\int_0^{x^{}}\frac{\sin x}{x}\mathrm{d}x$（余弦积分 $\displaystyle\mathrm{Ci}(x)=\int_{\infty}^x\frac{\cos x}{x}\mathrm{d}x$）
- 误差函数 $=\displaystyle\mathrm{erf}(x)=\frac{1}{\sqrt{\pi}}\int_{-x}^x e^{-t^2}\mathrm{d}t$（正态分布的概率密度）
- 第一类椭圆积分 $\displaystyle\int\frac{\mathrm{d}x}{\sqrt{a^2\sin^2 x+b^2\cos^2 x}}\quad(a,b\not =0,a^2\not=b^2)$（$\displaystyle\int\frac{\mathrm{d}x}{\sqrt{1+x^4}}$）

那么，如何判断任意初等函数的不定积分是否有初等函数解？若有初等函数解，是否有特定的算法能求出该解？  

对于这两个问题，我们已经有了对应的 [Risch 算法](https://mathmu.github.io/MTCAS/doc/Integration.html)。  
但该算法过于复杂，此处仅讨论这一问题的一个子集——有理函数的积分

$$\int\frac{P(x)}{Q(x)}\mathrm{d}x$$

我们已经知道，复数域上，其可通过有理分式分解化为积分

$$\int\frac{f(x)}{(x-a)^k}\mathrm{d}x\quad(k\in\N_+)$$

其中 $f(x)$ 为多项式。然后将 $f(x)$ 不断除以 $(x-a)$，从而化为多项式的积分和积分

$$\int\frac{\mathrm{d}x}{(x-a)^k}\quad(k\in\N_+)$$

这是在练习 1. 中已经解决的问题。这样就解决了复数域上任意有理函数的积分。

实数域上是类似的，此时我们还需要处理积分

$$\int\frac{ax+b}{(x^2+px+q)^k}\quad(k\in\N_+,p^2-4q<0)$$

对该积分的处理详见练习 2.、3.、7. 。

-------------------------------------

许多函数的积分可化为有理函数的积分，这往往是求积分时优先考虑的方法。例如对于形如

$$\int f(\sin x,\cos x,\tan x)\mathrm{d}x$$

的积分，其中 $f$ 为关于 $\sin x,\cos x,\tan x$ 的有理函数，可用万能公式

$$\sin x=\frac{2\tan\frac{x}{2}}{1+\tan^2\frac{x}{2}},\cos x=\frac{1-\tan^2\frac{x}{2}}{1+\tan^2\frac{x}{2}},\tan x=\frac{2\tan\frac{x}{2}}{1-\tan^2\frac{x}{2}}$$

化为关于 $\tan \frac{x}{2}$ 的有理函数的积分，然后设 $\tan\frac{x}{2}=t$，用第二类换元法化为有理函数的积分。

又比如对于形如

$$\int f(x,g(x))\mathrm{d}x$$

的积分，其中 $f$ 为关于 $x$ 和 $g(x)=\sqrt[n]{ax+b}$ 或 $\sqrt[n]{\frac{ax+b}{cx+d}}$ 的有理函数。

由于 $g(x)$ 的反函数为有理函数，可设 $t=g(x)$，用第二类换元法化为有理函数的积分。