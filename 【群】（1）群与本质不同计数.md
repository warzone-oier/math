## 群
**群** 是一个集合 $G$ 和 $G$ 上的二元运算 $*:G\times G\rightarrow G$ 组成的代数结构 $(G,*)$，满足如下四条性质：

1. 封闭性：$\forall a,b\in G,a*b\in G$
2. 结合律：$\forall a,b,c\in G,(a*b)*c=a*(b*c)$  
   满足结合律的运算可以略去括号。
3. 单位元（也称幺元）：$\exist e\in G,\forall g\in G,e*g=g*e=g$
4. 逆元：$\forall g\in G,\exist g^{-1}\in G,g*g^{-1}=g^{-1}*g=e$

- 满足性质 1. 称为 **原群**。
- 满足性质 1. 2. 称为 **半群**。
- 满足性质 1. 2. 3. 称为 **幺半群**。
- 满足以上所有性质称为 **群**。

特别的，如果群还满足

5. 交换律：$\forall a,b\in G,a*b=b*a$  

则称其为 **交换群**（也称 **阿贝尔群**），此时一般记群乘法 $*$ 的符号为 $+$，  
$a$ 的逆元记为 $-a$，交换半群同理。

无歧义时，群 $(G,*)$ 可以简记为 $G$，其单位元记为 $1_G$（群乘法符号为 $+$ 时记为 $0_G$）。  

有限群 $(G,*)$ 的 **阶** 就是集合 $G$ 的大小，即 $|G|$。

----------------------------
群公理既足够简单，又足够丰富，它有以下几个性质：

1. > 群的单位元是唯一的。  
   > 证明：设群有两个单位元 $l,r$，则由单位元的定义得  
   > $$l=l*r=r$$
2. > 群中任意元素的逆元是唯一的。  
   > 证明：设 $g\in G$ 有两个逆元 $a,b$，则  
   > $\qquad\quad a=a*e$（单位元的定义）  
   > $\qquad\qquad=a*(g*b)$（逆元的定义）  
   > $\qquad\qquad=(a*g)*b$（结合律）  
   > $\qquad\qquad=e*b$（逆元的定义）  
   > $\qquad\qquad=e$（单位元的定义）  
   > $\qquad$ 由此证明二者相等。
3. > 群性质 3. 4. 可以弱化为：
   > - 左单位元：$\exist e\in G,\forall g\in G,e*g=g$
   > - 左逆元：$\forall g\in G,\exist g^{-1}\in G,g^{-1}*g=e$  
   > 
   > 证明：首先证明逆元是双侧的：
   > $$g*g^{-1}=e*(g*g^{-1})=((g^{-1})^{-1}*g^{-1})*(g*g^{-1})$$
   > $$=(g^{-1})^{-1}*((g^{-1}*g)*g^{-1})=(g^{-1})^{-1}*(e*g^{-1})$$
   > $$=(g^{-1})^{-1}*g^{-1}=e$$
   > $\qquad$ 然后证明单位元是双侧的：
   > $$g*e=g*(g^{-1}*g)=(g*g^{-1})*g=e*g=g$$
   >
   > 练习：试指出以上证明运用了群的哪些性质。
4. $(g^{-1})^{-1}=g$
5. $(a*b)^{-1}=b^{-1}*a^{-1}$
6. 群的除法：$y=g*x\Leftrightarrow g^{-1}*y=x$
7. 群的消去律：$g*x=g*y\Leftarrow x=y$  
   需要注意的是，除法和消去律均有左右之分。

## 置换群

群有何直观意义？下面我们引入一个具体的群：置换群。

### 置换
对于集合 $S$，其上的双射 $f:S\rightarrow S$ 称为 **置换**。

例如，一次函数 $f(x)=kx+b\ (k\not=0)$ 就是实数集 $\R$ 上的一个置换。

显然，置换可以用函数的表示法来表示。此外，对于 $S=\{a,b,c,\cdots\}$，我们还可以用

$$f=\begin{pmatrix}a&b&c&\cdots\\f(a)&f(b)&f(c)&\cdots\end{pmatrix}$$

来表示置换。

### 置换乘法、单位置换、逆置换

所谓 **置换乘法** 就是映射的复合：

$$\forall x\in S,(f\circ g)(x)=f(g(x))$$

显然置换乘法满足结合律，不满足交换律。

**单位置换** $\varepsilon$ 就是

$$\forall x\in S,\varepsilon(x)=x$$

置换 $f$ 的 **逆置换** $f^{-1}$ 就是

$$f^{-1}\circ f=f\circ f^{-1}=\varepsilon$$

### 置换群、Cayley 定理

总上所述，设 $G$ 为 $S$ 上的若干置换组成的集合，若 $G$ 满足

- 封闭性：$\forall f,g\in G,f\circ g\in G$
- 单位元：$\exist\varepsilon\in G,\forall f\in G,\varepsilon\circ f=f\circ\varepsilon=f$
- 逆元：$\forall f\in G,\exist f^{-1}\in G,f\circ f^{-1}=f^{-1}\circ f=\varepsilon$

则 $G$ 构成群，称为 **置换群**。

置换群和一般群有何关系？首先我们引入 **同构** 这一概念：

> 对于群 $(G,*)$ 和 $(H,\cdot)$，若存在双射 $\phi:G\rightarrow H$，使得
> $$\forall a,b\in G,\phi(a*b)=\varphi(a)\cdot\varphi(b)$$
> 则称这两个群是 **同构** 的，记为 $G\cong H$，并称 $\phi$ 为 **同构映射**。   
> 练习：试证明同构的群的单位元、逆元均相等。

然后，我们有如下定理：

> **Cayley 定理**：任何一群都同构于一置换群。  
> 证明：对于群 $(G,*)$ 和元素 $a\in G$，定义映射 $(a*):G\rightarrow G$ 为
> $$\forall x\in G,(a*)(x)=a*x$$
> 设 $(G*)=\{(g*)|g\in G\}$，显然 $(G*)$ 构成群：
> - 封闭性：$((a*)\circ(b*))(x)=((a*b)*)(x)$
> - 结合律：$((a*)\circ(b*))\circ(c*)=(a*)\circ((b*)\circ(c*))$
> - 单位元：$(e*)\circ(x*)=((x*e)*)=(x*)$
> - 逆元：$(f*)\circ(f^{-1}*)=(f^{-1}*)\circ(f^*)=(e*)$  
> 
> 且由于 $(f*)$ 的逆映射为 $(f^{-1}*)$，$(f*)$ 为置换，$(G*)$ 为置换群。  
> 显然映射 $\phi(g)=(g*)$ 即为 $G\rightarrow (G*)$ 的同构映射，$G$ 与 $(G*)$ 同构。 

Cayley 定理指出了群的直观意义。    
练习：试给出半群和幺半群的直观意义并证明。

### 循环置换

我们可以将**有限集合**上的置换转化成一个有向图：$\forall a\in S$，$a$ 向 $f(a)$ 连一条边。

> 求证：这张有向图由若干不相交的环组成。  
> 证明：显然这个图上所有点的入度、出度均为 $1$。  
> $\qquad$ 考虑从一个点开始不断沿着出边走，走到起点停止。  
> $\qquad$ 由于所有点的入度、出度均为 $1$，走过的每个点不会再经过，且必然能有点可以继续走。  
> $\qquad$ 又因为是有限集合中的置换，每走过一个点就少一个点可走，最后必然能回到起点，路径形成一个环。  
> $\qquad$ 由此证明，有向图中的所有点均属于且仅属于某个环，即这张有向图由若干不相交的环组成。  

因此，我们将该有向图中的一个环扣出来，得到一个新置换，称为 **循环置换**（或 **轮换**），  
其环长称为该循环置换的 **阶**，记为 $|f|$。

循环置换可以通过依次列出环上的点来表示，例如对于 $S=\{2,3,5\}$，其上循环置换可表示为

$$f=(3,5,2)=\begin{pmatrix}2&3&5\\
3&5&2\end{pmatrix}$$

其阶 $|f|=3$ 。

显然我们有如下定理：**任意有限置换都能被唯一分解为若干不相交轮换的乘积**。


## 本质不同计数问题

我们已经知道了两大计数原理：加法原理和乘法原理，二者可用集合表述为：

- 加法原理：$A\cap B=\varnothing$ 时，$|A\cup B|=|A|+|B|$
- 乘法原理：$|A\times B|=|A||B|$

但是，这两个原理无法解决**本质不同计数问题**。例如：求含有 $n$ 个点的无标号无根树个数。  
我们接下来将逐步解决这一问题。

### 等价关系

首先，我们需要描述什么是“本质相同”，什么是“本质不同”。

对于集合 $S$，若 $R\subseteq S\times S$ 满足

1. 自反性：$\forall a\in S,(a,a)\in R$
2. 对称性：$(a,b)\in R \Leftrightarrow (b,a)\in R$
3. 传递性：$(a,b),(b,c)\in R\Rightarrow (a,c)$

则称 $R$ 是 $S$ 上的 **等价关系**，并将 $(a,b)\in R$ 记为 $aRb$。

等价关系的重要意义在于导出了 **等价类** 的概念。对于 $a\in S$，我们称 $a$ 所属的等价类为

$$[a]_R=\{x\in S|xRa\}$$

即，$S$ 中所有与 $a$ 相等的元素构成的集合。


由 $R$ 的自反性，$S$ 中每个元素都至少属于一个等价类。且

> 求证：$\forall a,b\in S$，要么 $[a]_R=[b]_R$，要么 $[a]_R\cap[b]_R=\varnothing$  
> 证明：若 $[a]_R\cap[b]_R=\varnothing$，显然命题成立。  
> $\qquad\quad$ 否则，设 $c\in[a]_R\cap[b]_R$，则 $\forall x\in [a]_R$，根据 $R$ 的对称性、传递性有
> $$
> \begin{rcases}
> 	\begin{rcases}	
> 	\qquad\quad\ xRa\\
> 	cRa\Leftrightarrow aRc
> 	\end{rcases}\Rightarrow xRc	\\
> 	\qquad\qquad\qquad\quad\ \  cRb
> \end{rcases}\Rightarrow xRb
> $$
> $\qquad\quad$ 因此 $[a]_R\subseteq[b]_R$。同理可得 $[b]_R\subseteq[a]_R$，故 $[a]_R=[b]_R$。  

因此，$R$ 的所有等价类构成了 $S$ 的一组划分，称为 **商集**，记为 $S/R$。即

$$S/R=\{[a]_R|a\in S\}$$

由此可见，等价关系和集合的划分密不可分。  
给定任意一个等价关系，均可通过等价类唯一确定集合的一组划分。  
反之，给定集合 $S$ 的任意一组划分 $T$（$\displaystyle\bigcup_{A\in T}A=S,T$ 中元素两两不交），均可唯一确定一个等价关系

$$aRb\Leftrightarrow\exist A\in T,\{a,b\}\subseteq A$$

使得 $S/R=T$。

------------------------------------------

不过，本质不同计数问题中“本质相同”和一般的等价关系略有区别。

以二叉树为例，我们说二叉树 $T_1$ 和 $T_2$ 本质相同，  
指的是能通过“交换某个节点的左右儿子”这一操作，将 $T_1$ 变为 $T_2$。  
可以发现，操作“交换某个节点的左右儿子”是可逆的，也就是说，它是一个置换。  
由于这样的操作可以重复若干次，我们将其扩充为置换群 $G$。这样，  
群的封闭性保证了 $T_1,T_2$ 只需要一次操作即可互相得到。

综上，对于 $a,b\in S$ 和集合 $S$ 上的置换群 $G$，若存在 $g\in G$ 使得 $g(a)=b$，  
则称 $a,b$ 在 $G$ 作用下 **等价**，记为

$$a\mathop{=}\limits^G b$$

此外，我们将 $a\in S$ 在 $G$ 作用下的等价类

$$G(a)=\{g(a)|g\in G\}$$

称为 $a$ 的 **轨道**。我们要求解的就是所有轨道构成的集合

$$S/G=\{G(a)|a\in S\}$$

的大小 $|S/G|$。

### 子群、陪集和拉格朗日定理

$|S/G|$ 显然和 $G$ 的内部结构有关。在处理 $S$ 在 $G$ 作用下的等价关系之前，  
我们先讨论群 $G$ 自身内部的等价关系。

---------------------------------------------------------

对于群 $(G,*)$ 和 $G$ 的一个子集 $H$，若 $(H,*)$ 也构成群，则称 $H$ 为 $G$ 的 **子群**，记为 $H\le G$。

> 求证：若 $H$ 为 $G$ 子群，则二者的单位元，逆元均相等。  
> 证明：对于 $h\in H$，有
> $$1_H*h=1_G*h=h$$
> $\qquad$ 由 $G$ 的消去律可得 $1_H=1_G$。逆元同理。

注意到 $(H*)$ 是 $G$ 上的置换群，因此其导出了 $G$ 上的一个等价关系。  
具体地，对于 $g_1,g_2\in G$，若存在 $h\in H$ 使得 $h*g_1=g_2$，  
则称 $g_1,g_2$ 在 $\pmod H$ 意义下 **右同余**，记为

$$g_1\equiv_r g_2\pmod{H}$$

右同余这一等价关系对应的等价类

$$H*g=\{h*g|h\in H\}$$

称为 $H$ 关于 $g$ 的 **右陪集**。同样可定义左陪集 $g*H$ 和左同余

$$g_1\equiv_l g_2\pmod{H}\Leftrightarrow\exist h\in H,g_1*h=g_2$$

陪集不仅满足等价类的所有性质，还满足

> 求证：$|g*H|=|H*g|=|H|$  
> 证明：集合论中，$|S|=|T|$ 被定义为存在 $S\rightarrow T$ 的双射。  
> $\qquad\quad$ 显然 $(g*)$ 为 $H\rightarrow g*H$ 的双射，因此 $|g*H|=|H|$。$H*g$ 是同理的。

由此可见，子群 $H$ 不仅将 $G$ 划分为等价类，而且划分出来的等价类大小均为 $|H|$，  
由此自然得到了群论中一条极为重要的定理：

> **拉格朗日定理**：有限群 $G$ 的子群 $H$ 的阶整除 $G$ 的阶，且 $H$ 在 $G$ 中的左（右）陪集个数 $[G:H]=\dfrac{|G|}{|H|}$。 

### Polya 原理

群 $G$ 内部的等价关系形成了拉格朗日定理这一极为优美的结论。  
那么，一般集合 $S$ 在置换群 $G$ 作用下的等价关系是否也是如此呢？

置换群 $G$ 确定了 $S$ 上的一个等价关系 $\mathop{=}\limits^G$ 。反过来，$a\in S$ 也确定了 $G$ 上的一个等价关系 $\mathop{=}\limits^a$：

$$g_1\mathop{=}\limits^ag_2\Leftrightarrow g_1(a)=g_2(a)$$

由这个等价关系，即可得到

> **轨道-稳定子定理**：定义 $a\in S$ 在置换群 $G$ 中的 **稳定子**
> $$G_a=\{g\in G|g(a)=a\}$$
> $\qquad\qquad\qquad\quad$ 则若 $G$ 为有限群，那么 $|G|=|G(a)||G_a|$。  
> 证明：注意到
> $$g_1(a)=a,g_2(a)=a\Rightarrow(g_1\circ g_2)(a)=a$$
> $$\varepsilon(a)=a$$
> $$g(a)=a\Leftrightarrow g^{-1}(a)=a$$
> $\qquad\quad$ 因此 $G_a$ 为 $G$ 子群。又因为
> $$g_1(a)=b,g_2(a)=a\Rightarrow (g_1\circ g_2)(a)=b$$
> $$g_1(a)=g_2(a)\Rightarrow \exist g=g_1^{-1}\circ g_2,g(a)=a,g_1\circ g=g_2$$
> $\qquad\quad$ 因此 $\mathop{=}\limits^a$ 意义下，$g\in G$ 所属的等价类就是左陪集 $g\circ G_a$。也就是说
> $$g_1\mathop{=}\limits^ag_2\Leftrightarrow g_1\equiv_l g_2\pmod{G_a}$$
> $\qquad\quad$ 由于 $\mathop{=}\limits^a$ 意义下的等价类共有 $|G(a)|$ 个，故由拉格朗日定理得
> $$|G|=|G(a)||G_a|$$

在其基础上，我们有 

> **Burnside 引理**：定义 $S$ 关于 $g\in G$ 的 **不动点**
> $$S^g=\{a\in S|g(a)=a\}$$
> $\qquad\qquad$ 则对于有限集合 $S$，$S$ 在 $G$ 作用下的等价类个数等于所有置换不动点的平均值，即
>  $$|S/G|=\displaystyle\frac{1}{|G|}\sum_{g\in G}|S^g|$$  
> 证明$^*$：$\displaystyle|S/G|=\sum_{a\in S}\frac{1}{|G(a)|}$（等价类中每个元素的贡献取平均值）  
> $\qquad\qquad\quad\displaystyle=\sum_{a\in S}\frac{|G_a|}{|G|}=\sum_{a\in S}\frac{1}{|G|}\sum_{g\in G}[g(a)=a]$（轨道-稳定化子定理）  
> $\qquad\qquad\quad\displaystyle=\frac{1}{|G|}\sum_{g\in G}\sum_{a\in S}[g(a)=a]$（调换求和顺序）  
> $\qquad\qquad\quad\displaystyle=\frac{1}{|G|}\sum_{g\in G}|S^g|$

Burnside 引理将等价类计数问题转化为了求不动点。接下来只要找到一个求不动点的方法即可。

> **Polya 定理**：设 $X$ 为 $A\rightarrow B$ 所有映射组成的集合（$A,B$ 均为有限集），$G$ 为 $A$ 上置换群，  
> $\qquad\qquad$ 设 $c(g)$ 为置换 $g$ 能分解成的循环置换个数，则
> $$|X/(G\circ)|=\dfrac{1}{|G|}\sum_{g\in G}|B|^{c(g)}$$
> 证明显然。不动点意味着在 $A$ 中在 $a$ 中的同一个元素，在 $B$ 中对应的元素必相等。

Polya 定理告诉我们，我们可以通过将置换分解为循环置换来求解不动点，这就彻底解决了本质不同计数问题。