## 群
**群** 是一个集合 $G$ 和 $G$ 上的二元运算 $*:G\times G\rightarrow G$ 组成的代数结构 $(G,*)$，满足如下四条性质：

1. 封闭性：$\forall a,b\in G,a*b\in G$
2. 结合律：$\forall a,b,c\in G,(a*b)*c=a*(b*c)$  
   仅满足结合律的运算可以略去括号。
3. 单位元（也称幺元）：$\exist e\in G,\forall g\in G,e*g=g*e=g$
4. 逆元：$\forall g\in G,\exist g^{-1}\in G,g*g^{-1}=g^{-1}*g=e$

- 满足性质 1. 称为 **原群**。
- 满足性质 1. 2. 称为 **半群**。
- 满足性质 1. 2. 3. 称为 **幺半群**。
- 满足以上所有性质称为 **群**。

特别的，如果群还满足

5. 交换律：$\forall a,b\in G,a*b=b*a$  

则称其为 **交换群**（也称 **阿贝尔群**），交换半群同理。

无歧义时，群 $(G,*)$ 可以简记为 $G$，其单位元记为 $1_G$。  

----------------------------
群公理既足够简单，又足够丰富，它有以下几个性质：

1. > 群的单位元是唯一的。  
   > 证明：设群有两个单位元 $l,r$，则由单位元的定义得  
   > $$l=l*r=r$$
2. > 群中任意元素的逆元是唯一的。  
   > 证明：设 $g\in G$ 有两个逆元 $a,b$，则  
   > $\qquad\quad a=a*e$（单位元的定义）  
   > $\qquad\qquad=a*(g*b)$（逆元的定义）  
   > $\qquad\qquad=(a*g)*b$（结合律）  
   > $\qquad\qquad=e*b$（逆元的定义）  
   > $\qquad\qquad=e$（单位元的定义）  
   > $\qquad$ 由此证明二者相等。
3. > 群性质 3. 4. 可以弱化为：
   > - 左单位元：$\exist e\in G,\forall g\in G,e*g=g$
   > - 左逆元：$\forall g\in G,\exist g^{-1}\in G,g^{-1}*g=e$  
   > 
   > 证明：首先证明逆元是双侧的：
   > $$g*g^{-1}=e*(g*g^{-1})=((g^{-1})^{-1}*g^{-1})*(g*g^{-1})$$
   > $$=(g^{-1})^{-1}*((g^{-1}*g)*g^{-1})=(g^{-1})^{-1}*(e*g^{-1})$$
   > $$=(g^{-1})^{-1}*g^{-1}=g$$
   > $\qquad$ 然后证明单位元是双侧的：
   > $$g*e=g*(g^{-1}*g)=(g*g^{-1})*g=e*g=g$$
   >
   > 练习：试指出以上证明运用了群的哪些性质。
4. $(g^{-1})^{-1}=g$
5. $(a*b)^{-1}=b^{-1}*a^{-1}$
6. 群的除法：$y=g*x\Leftrightarrow g^{-1}*y=x$
7. 群的消去律：$g*x=g*y\Leftarrow x=y$  
   需要注意的是，除法和消去律均有左右之分。

------------------------------------------------
## 置换群

群有何直观意义？下面我们引入一个具体的群：置换群。

### 置换
对于集合 $S$，其上的双射 $f:S\rightarrow S$ 称为 **置换**。

例如，一次函数 $f(x)=kx+b\ (k\not=0)$ 就是实数集 $\R$ 上的一个置换。

显然，置换可以用函数的表示法来表示。此外，对于 $S=\{a,b,c,\cdots\}$，我们还可以用

$$f=\begin{pmatrix}a&b&c&\cdots\\f(a)&f(b)&f(c)&\cdots\end{pmatrix}$$

来表示置换。

### 置换乘法、单位置换、逆置换

所谓 **置换乘法** 就是映射的复合：

$$\forall x\in S,(f\circ g)(x)=f(g(x))$$

显然置换乘法满足结合律，不满足交换律。

**单位置换** 就是

$$\forall x\in S,\varepsilon(x)=x$$

置换 $f$ 的 **逆置换** $f^{-1}$ 就是

$$f^{-1}\circ f=f\circ f^{-1}=\varepsilon$$

### 置换群、Cayley 定理

总上所述，设 $G$ 为 $S$ 上的若干置换组成的集合，若 $G$ 满足

- 封闭性：$\forall f,g\in G,f\circ g\in G$
- 单位元：$\exist\varepsilon\in G,\forall f\in G,\varepsilon\circ f=f\circ\varepsilon=f$
- 逆元：$\forall f\in G,\exist f^{-1}\in G,f\circ f^{-1}=f^{-1}\circ f=\varepsilon$

则 $G$ 构成群，称为 **置换群**。

置换群和一般群有何关系？首先我们引入 **同构** 这一概念：

> 对于群 $(G,*)$ 和 $(H,\cdot)$，若存在双射 $\varphi:G\rightarrow H$，使得
> $$\forall a,b\in G,\varphi(a*b)=\varphi(a)\cdot\varphi(b)$$
> 则称这两个群是 **同构** 的，记为 $G\cong H$。   
> 练习：试证明同构的群的单位元、逆元均相等。

然后，我们有如下定理：

> **Cayley 定理**：任何一群都同构于一置换群。  
> 证明：对于群 $(G,*)$ 和元素 $a\in G$，定义映射 $(a*):G\rightarrow G$ 为
> $$\forall x\in G,(a*)(x)=a*x$$
> 设 $(G*)=\{(g*)|g\in G\}$，显然 $(G*)$ 构成群：
> - 封闭性：$((a*)\circ(b*))(x)=((a*b)*)(x)$
> - 结合律：$((a*)\circ(b*))\circ(c*)=(a*)\circ((b*)\circ(c*))$
> - 单位元：$(e*)\circ(x*)=((x*e)*)=(x*)$
> - 逆元：$(f*)\circ(f^{-1}*)=(f^{-1}*)\circ(f^*)=(e*)$  
> 
> 且由于 $(f*)$ 的逆映射为 $(f^{-1}*)$，$(f*)$ 为置换，$(G*)$ 为置换群。  
> 显然映射 $\varphi(g)=(g*)$ 即为 $G$ 和 $(G*)$ 间所需的双射，$G$ 与 $(G*)$ 同构。 

Cayley 定理指出了群的直观意义。    
练习：试给出半群和幺半群的直观意义并证明。

### 循环置换

我们可以将**有限集合**上的置换转化成一个有向图：$\forall a\in S$，$a$ 向 $f(a)$ 连一条边。

> 求证：这张有向图由若干不相交的环组成。  
> 证明：显然这个图上所有点的入度、出度均为 $1$。  
> $\qquad\quad$考虑从一个点开始不断沿着出边走，走到起点停止。  
> $\qquad\quad$由于所有点的入度、出度均为 $1$，走过的每个点不会再经过，且必然能有点可以继续走。  
> $\qquad\quad$又因为是有限集合中的置换，每走过一个点就少一个点可走，最后必然能回到起点，路径形成一个环。
> 1. 若环覆盖了整个置换，命题显然成立。
> 2. 否则，剩下的未走过的点仍满足入度、出度均为 $1$，且点数有限，另选一个起点走。  
>   随着原置换不断被覆盖，点数不断减少，最终必然能到 1. 。

因此，我们将该有向图中的一个环扣出来，得到一个新置换，称为 **循环置换**（或 **轮换**），  
其环长称为该循环置换的 **阶**，记为 $|f|$。

循环置换可以通过依次列出环上的点来表示，例如对于 $S=\{2,3,5\}$，其上循环置换可表示为

$$f=(3,5,2)=\begin{pmatrix}2&3&5\\
3&5&2\end{pmatrix}$$

其阶 $|f|=3$ 。

显然我们有如下定理：**任意置换都能被唯一分解为若干不相交轮换的乘积**。

----------------------------
## Polya 原理

我们已经知道了两大计数原理：加法原理和乘法原理，二者可用集合表述为：

- 加法原理：$A\cap B=\varnothing$ 时，$|A\cup B|=|A|+|B|$
- 乘法原理：$|A\times B|=|A||B|$

但是，这两个原理无法解决**本质不同计数问题**。例如：求含有 $n$ 个点的无标号无根树个数。

之所以说无法解决，是因为存在一些操作（例如交换子树的顺序），使得操作前后的对象是 **本质不同的**。

可以发现，操作其实就是一种置换，我们可以将这一问题统一表述为：

给定有限集合 $S$ 和其上一置换群 $G$ ，$\forall a,b\in S$，若

$$\exist g\in G,g(a)=b$$

则称 $a,b$ 是本质相同的。求 $S$ 中本质不同的元素个数。

### Burnside 引理

让我们把该问题更加形式化一点。$\forall a\in S$，定义 $a$ 关于 $G$ 所属的 **等价类**（或者 **轨道**） 为

$$G(a)=\{g(a)|g\in G\}$$

定义 $S$ 关于 $G$ 的 **等价类集合** 为

$$S/G=\{G(a)|a\in S\}$$

问题即求解 $|S/G|$。

> **轨道-稳定子定理$^*$**：$\forall a\in S$，定义 $a$ 关于 $G$ 的 **稳定子**  
> $$G^a=\{g\in G|g(a)=a\}$$
> $\qquad\qquad$ 则 $|G|=|G(a)||G^a|\displaystyle=|G(a)|\sum_{g\in G}[g(a)=a]$。  
> 证明*：$|G|=\displaystyle\sum_{g\in G}\sum_{b\in S}[g(a)=b]$（枚举元素与 $g(a)$ 匹配）  
> $\qquad\qquad=\displaystyle\sum_{b\in S}\sum_{g\in G}[g(a)=b]$（调换求和顺序）  
> $\qquad\qquad=\displaystyle\sum_{b\in G(a)}\sum_{g\in G}[g(a)=b]$（只有 $G(a)$ 中的会被枚举到）   
> $\qquad\quad$  根据**可数选择公理**，$\forall b\in G(a)$，可以指定一个 $f_b\in G$，使得 $f(a)=b$，于是  
> $\qquad\quad|G|=\displaystyle\sum_{b\in G(a)}\sum_{g\in G}[g(a)=f_b(a)]=\sum_{b\in G(a)}\sum_{g\in G}[(f_b^{-1}\circ g)(a)=a]$  
> $\qquad\quad(f^{-1}_b\circ)$ 是 $G$ 上双射，因此  
> $\qquad\quad|G|=\displaystyle\sum_{b\in G(a)}\sum_{g\in G}[g(a)=a]=|G(a)|\sum_{g\in G}[g(a)=a]$

在其基础上，我们有 

> **Burnside 引理**：$\forall g\in G$，定义 $S$ 关于 $g$ 的 **不动点**
> $$S^g=\{a\in S|g(a)=a\}$$
> $\qquad\qquad$ 则 $S$ 关于 $G$ 的等价类个数等于所有置换不动点的平均值，即
>  $$|S/G|=\displaystyle\frac{1}{|G|}\sum_{g\in G}|S^g|$$  
> 证明$^*$：$\displaystyle|S/G|=\sum_{a\in S}\frac{1}{|G(a)|}$（等价类中每个元素的贡献取平均值）  
> $\qquad\qquad\quad\displaystyle=\sum_{a\in S}\frac{1}{|G|}\sum_{g\in G}[g(a)=a]$（轨道-稳定化子定理）  
> $\qquad\qquad\quad\displaystyle=\frac{1}{|G|}\sum_{g\in G}\sum_{a\in S}[g(a)=a]$（调换求和顺序）  
> $\qquad\qquad\quad\displaystyle=\frac{1}{|G|}\sum_{g\in G}|S^g|$

Burnside 引理将等价类计数问题转化为了求不动点。接下来只要找到一个求不动点的方法即可。

> **Polya 定理**：设 $X$ 为 $A\rightarrow B$ 所有映射组成的集合，$G$ 为 $A$ 上置换群，  
> $\qquad\qquad$ 设 $c(g)$ 为置换 $g$ 能分解成的循环置换个数，则
> $$X^{(G\circ)}=\dfrac{1}{|G|}\sum_{g\in G}|B|^{c(g)}$$
> 证明显然。不动点意味着在 $A$ 中在 $a$ 中的同一个元素，在 $B$ 中对应的元素必相等。

Polya 定理告诉我们，我们可以通过将置换分解为循环置换来求解不动点，这就彻底解决了本质不同计数问题。