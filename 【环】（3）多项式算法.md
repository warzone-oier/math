## FFT 与 NTT

### FFT

**快速傅里叶变换（FFT）** 可在 $\Theta(n\log n)$ 的时间复杂度下计算两个多项式的乘积。  
其基本思想是，将多项式 $f(x)$ 转成一组点值

$$(x_0,f(x_0)),(x_1,f(x_1)),\cdots,(x_n,f(x_n))$$

那么两个多项式相乘相当于对应的点值相乘，是 $\Theta(n)$ 的：

$$(x_0,f(x_0)g(x_0)),(x_1,f(x_1)g(x_1)),\cdots,(x_n,f(x_n)g(x_n))$$

之后，将点值还原成多项式的系数，即可得到相乘的结果。

------------------------------------------------------------

如何选取合适的点值，来实现点值与多项式之间的快速转换？  
在复数域内，可以选取 $n$ 次单位根 $\omega_n^0,\omega_n^1,\cdots,\omega_n^{n-1}$ 来实现。

首先阐述这样做的合理性。$n$ 次单位根的模长均为 $1$，设多项式系数的绝对值不超过 $L$，则

$$|f(\omega_n^k)|=\left|\sum_{i=0}^{n-1}f_i\omega_{n}^{ik}\right|\le nL$$

相比之下，选取模长比 $1$ 大或者比 $1$ 小的点，都会带来精度爆炸的问题。

其次， $n$ 次单位根构成的乘法群与 $n$ 阶循环群同构，这使得系数与点值的快速转换是可行的。

-----------------------------------------------------------

接下来我们正式讨论 **DFT**，即将系数转为点值的过程。

设 $n=2^t\ (t\in\N)$，考虑将原多项式的系数奇偶分段，设

$$f(x)=f_0(x^2)+xf_1(x^2)$$

若我们已经计算出了 $f_0,f_1$ DFT 的结果，即

$$f_0(\omega_\frac{n}{2}^0),f_0(\omega_\frac{n}{2}^1),\cdots,f_0(\omega_\frac{n}{2}^{\frac{n}{2}-1})$$
$$f_1(\omega_\frac{n}{2}^0),f_1(\omega_\frac{n}{2}^1),\cdots,f_1(\omega_\frac{n}{2}^{\frac{n}{2}-1})$$

那么 $k<\dfrac{n}{2}$ 时，有

$$f(\omega_n^k)=f_0(\omega_n^{2k})+\omega_n^kf_1(\omega_n^{2k})=f_0(\omega_\frac{n}{2}^{k})+\omega_n^kf_1(\omega_\frac{n}{2}^{k})$$


$$f(\omega_n^{k+\frac{n}{2}})=f_0(\omega_n^{2k+n})+\omega_n^{k+\frac{n}{2}}f_1(\omega_n^{2k+n})=f_0(\omega_n^{2k})+\omega_n^{\frac{n}{2}}\times\omega_n^kf_1(\omega_n^{2k})$$

$$=f_0(\omega_\frac{n}{2}^{k})-\omega_n^kf_1(\omega_\frac{n}{2}^{k})$$

因此，将系数不断奇偶分段，一步步递归即可 $\Theta(n\log n)$ 计算出 DFT 的结果。

实际实现中，我们可以作如下优化：

1. 非递归写法：奇偶分段就是按照下标二进制的末位分段，  
   因此最终递归到的位置的下标就是原下标二进制翻转的结果。  
   可以预处理好，然后用循环代替向上递归。
2. 蝴蝶变换：DFT 是可以就地进行的。因为
   $$\begin{matrix}
   f_0(\omega_{\frac{n}{2}}^k)&f_1(\omega_{\frac{n}{2}}^k)\\
   \Downarrow&\Downarrow\\
   f(\omega_n^k)=f_0(\omega_\frac{n}{2}^{k})+\omega_n^kf_1(\omega_\frac{n}{2}^{k})&f(\omega_n^{k+\frac{n}{2}})=f_0(\omega_\frac{n}{2}^{k})-\omega_n^kf_1(\omega_\frac{n}{2}^{k})
   \end{matrix}$$
   因此可以将 $f(\omega_n^k),f(\omega_n^{k+\frac{n}{2}})$ 放在原 $f_0(\omega_{\frac{n}{2}}^k),f_1(\omega_{\frac{n}{2}}^k)$ 的位置。

---------------------------------------------------

接下来我们讨论 **IDFT**，即点值转为系数的过程。  
DFT 和 IDFT 都是从长度为 $n$ 的序列到另一个长度为 $n$ 的序列的变换。设 DFT 为

$$F(f)_k=f(\omega_n^k)=\sum_{i=0}^{n-1}f_i\omega_n^{ik}$$

则 IDFT 就是

$$F^{-1}(f)_k=\dfrac{1}{n}f(\omega_n^{-k})=\dfrac{1}{n}\sum_{i=0}^{n-1}f_i\omega_n^{-ik}$$

也就是说，将点值序列视作系数序列，代入单位根的共轭 $\omega_n^{-k}$，  
得到的点值序列除以 $n$ 即原本的系数序列。

> 证明：即证明
> $$F^{-1}(F(f))=f$$
> $\qquad\quad$ 套用 DFT 和 IDFT 的定义，可得
> $$F^{-1}(F(f))_k=\dfrac{1}{n}\sum_{i=0}^{n-1}F(f)_i\omega_{n}^{-ik}=\dfrac{1}{n}\sum_{i=0}^{n-1}\omega_{n}^{-ik}\sum_{j=0}^{n-1}f_j\omega_{n}^{ij}=\dfrac{1}{n}\sum_{j=0}^{n-1}f_j\sum_{i=0}^{n-1}\omega_{n}^{i(j-k)}$$
> $\qquad\quad$ 由单位根反演得
> $$F^{-1}(F(f))_k=\sum_{j=0}^{n-1}f_j[(j-k)\bmod n=0]=f_k$$

---------------------------------------------------------

在 $n$ 足够大的时候，FFT 计算的就是 $f\times g$ 。但在 $n$ 不够大的时候，FFT 还具有以下性质：

> 求证：FFT 计算的是 **循环卷积**。即
> $$F^{-1}(F(f)\cdot F(g))_k=\sum_{i+j\equiv k\pmod{n}}f_ig_j$$
> $\qquad\quad$ 其中 $\cdot$ 表示序列对应位置相乘。  
> 证明：同样套用 DFT 和 IDFT 定义，并由单位根反演得
> $$F^{-1}(F(f)\cdot F(g))_k=\dfrac{1}{n}\sum_{l=0}^{n-1}F(f)_lF(g)_l\omega_{n}^{-lk}=\dfrac{1}{n}\sum_{l=0}^{n-1}\omega_{n}^{-lk}\sum_{i=0}^{n-1}f_i\omega_{n}^{il}\sum_{j=0}^{n-1}g_j\omega_{n}^{jl}$$
> $$=\dfrac{1}{n}\sum_{i=0}^{n-1}\sum_{j=0}^{n-1}f_ig_j\sum_{l=0}^{n-1}\omega_n^{(i+j-k)l}=\sum_{i=0}^{n-1}\sum_{j=0}^{n-1}f_ig_j[(i+j-k)\bmod n=0]=\sum_{i+j\equiv k\pmod{n}}f_ig_j$$
> 

### NTT

对于复数域 $\mathbb{C}$ 上的多项式环 $\mathbb{C}[x]$，我们可以选取 $\omega_n^k$ 进行 DFT 和 IDFT，计算两个多项式的乘积。

而对于 $\Z_p$ 上的多项式环 $\Z_p[x]$，若 $p$ 为素数，且

$$p=r\times 2^s+1\quad(r,s\in\N_+)$$

设模 $p$ 意义下的一个原根为 $g$，我们可以用

$$g^{\frac{p-1}{n}k}$$

代替 $n$ 次单位根 $\omega_n^k$ 进行 DFT 和 IDFT，计算两个多项式的乘积。这一算法称为 **快速数论变换（FFT）** 。

NTT 可以进行的前提是模数 $p$ 满足上述要求。如

$$998244353=119\times 2^{23}+1$$

就是一个符合要求的模数。以下是常用的几个 NTT 模数，它们都以 $3$ 为原根：

| $p$ | $r$ | $s$ |
|:-----:|:-----:|:-----:|
|$998244353$|$119$|$23$|
|$1004535809$|$479$|$21$|
|$167772161$|$5$|$25$|
|$104857601$|$25$|$22$|
|$23068673$|$11$|$21$|

### 任意模数 NTT

在 $p$ 不是符合要求的模数的情况下，任意模数 NTT 依然允许我们求解 $\Z_p[x]$ 上两个多项式的乘积。

任意模数 NTT 的基本思想是拆系数。实战中，$p<2^{30}$，故设

$$f(x)=2^{15}a(x)+b(x),g(x)=2^{15}c(x)+d(x)$$

则

$$f(x)g(x)=2^{30}a(x)c(x)+2^{15}(a(x)d(x)+b(x)c(x))+b(x)d(x)$$

$a(x),b(x),c(x),d(x)$ 的系数均小于 $2^{15}$，故它们两两之积的系数均小于 $n\times 2^{30}$，
且两两之积的点值的值域亦为

$$|a(\omega_n^k)c(\omega_n^k)|< n\times 2^{15}\times n\times 2^{15}=n^2\times 2^{30}$$

实战中 $n$ 一般不超过 $10^6$，因此FFT 计算两两之积的过程始终在 double 的精度可以接受的范围内。

因此，用 FFT 分别计算 $a(x)c(x),a(x)d(x),b(x)c(x),b(x)d(x)$，再在模意义下合并即可得到 $f(x)g(x)$。

------------------------------------

但直接使用上述做法需要 4 次 DFT 和 4 次 IDFT，常数巨大。  
但注意到以上涉及的多项式的系数均为实数，而 FFT 需要用到复数，  
因此可以 **综合利用系数序列的实部和虚部，使得一次 DFT/IDFT 得到原本两次 DFT/IDFT 的结果**。

具体地，对于 DFT，将 $a$ 放在实部，将 $b$ 放在虚部，可得

$$a(\omega_n^k)=\sum_{j=0}^{n-1}a_j\omega_n^{jk}=\sum_{j=0}^{n-1}a_j\left(\cos\dfrac{2\pi jk}{n}+i\sin\dfrac{2\pi jk}{n}\right)$$

$$b(\omega_n^k)=\sum_{j=0}^{n-1}b_j\omega_n^{jk}=\sum_{j=0}^{n-1}b_j\left(\cos\dfrac{2\pi jk}{n}+i\sin\dfrac{2\pi jk}{n}\right)$$

$$(a+ib)(\omega_n^{k})=\sum_{j=0}^{n-1}(a_j+ib_j)\omega_n^{jk}=\sum_{j=0}^{n-1}(a_j+ib_j)\left(\cos\dfrac{2\pi jk}{n}+i\sin\dfrac{2\pi jk}{n}\right)$$
$$=\sum_{j=0}^{n-1}\left(\left(a_j\cos\dfrac{2\pi jk}{n}-b_j\sin\dfrac{2\pi jk}{n}\right)+i\left(b_j\cos\dfrac{2\pi jk}{n}+a_j\sin\dfrac{2\pi jk}{n}\right)\right)$$

$$(a+ib)(\omega_n^{n-k})=(a+ib)(\omega_n^{-k})=\sum_{j=0}^{n-1}(a_j+ib_j)\omega_n^{-jk}=\sum_{j=0}^{n-1}(a_j+ib_j)\left(\cos\dfrac{2\pi jk}{n}-i\sin\dfrac{2\pi jk}{n}\right)$$
$$=\sum_{j=0}^{n-1}\left(\left(a_j\cos\dfrac{2\pi jk}{n}+b_j\sin\dfrac{2\pi jk}{n}\right)+i\left(b_j\cos\dfrac{2\pi jk}{n}-a_j\sin\dfrac{2\pi jk}{n}\right)\right)$$

因此对 $(a+ib)(\omega_n^{k}),(a+ib)(\omega_n^{n-k})$ 的实部和虚部分别加减消元即可得到 $a(\omega_n^k),b(\omega_n^k)$ 的实部和虚部。  
这样，我们就可以通过一次 DFT 计算出 $a,b$ DFT 的结果，$c,d$ 同理。

至于 IDFT，由于 $a(x)c(x),a(x)d(x),b(x)c(x),b(x)d(x)$ 的系数均为实数，而

$$(f+ig)(\omega_n^{-k})=f(\omega_n^{-k})+ig(\omega_n^{-k})$$

故对 $F(a)\cdot F(c)+iF(b)\cdot F(d)$ 作 IDFT，所得序列的实部即 $a(x)c(x)$，虚部即 $b(x)d(x)$。  
$a(x)d(x)$ 和 $b(x)c(x)$ 同理，这样两次 IDFT 就可算出所有所需的乘积。

综上，只需要 2 次 DFT 和 2 次 IDFT 即可计算出 $\Z_p[x]$ 上两个多项式的乘积。

### 差卷积

多项式乘法所对应的序列变换

$$(f\times g)_k=\sum_{i+j=k}f_ig_j=\sum_{i=0}^{+\infty}f_ig_{k-i}$$

称为序列卷积，又称 **和卷积**。

有时候，我们需要计算 **差卷积**，即

$$\sum_{i-j=k}f_ig_j=\sum_{j=0}^{+\infty}f_{k+j}g_{j}$$

这可以通过序列的翻转来实现。设

$$f_R(x)=\sum_{k=0}^{\deg f}f_{\deg f-k}x^k$$

则

$$\sum_{i-j=k}f_ig_j=\sum_{i+(\deg g-j)=\deg g+k}f_ig_j=(f\times g_R)_{\deg g+k}$$

> 练习：对于多项式 $f(x)$，试给出计算 $f(ax+b)$ 的算法。

### Bluestein's Algorithm

FFT 要求 $n$ 次单位根 $\omega_n^k$ 满足 $n=2^t\quad(t\in\N)$，

而 **Bluestein's Algorithm**（亦称 **Chirp-Z 变换**）允许我们通过 FFT 计算任意 $n$ 下的 DFT。

具体地，给定多项式 $f$ 和常数 $c$，Bluestein's Algorithm 需要快速计算

$$f(c^0),f(c^1),\cdots,f(c^{m-1})$$

我们有

$$f(c^i)=\sum_{j}f_jc^{ij}=\sum_jf_jc^{\binom{i+j}{2}-\binom{i}{2}-\binom{j}{2}}=c^{-\binom{i}{2}}\sum_jf_jc^{-\binom{j}{2}}\times c^\binom{i+j}{2}$$

（$\dbinom{i}{2}$ 详见【组合】（1）二项式系数）

这样，我们就把 $f(c^0),f(c^1),\cdots,f(c^{m-1})$ 的求解转化为差卷积。

## 形式幂级数相关算法

### 乘法逆元

形式幂级数乘法逆元的前 $n$ 项即 $\pmod{x^{n+1}}$ 意义下的乘法逆元。  
由于 FFT 计算的是循环卷积，即 $\pmod{(x^{n}-1)}$ 意义下的乘法，  
故不能通过对 FFT 点值求逆的方法求形式幂级数的乘法逆元。

使用倍增法，设

$$F(x)P(x)\equiv 1\pmod{x^n}$$

$$F(x)Q(x)\equiv 1\pmod{x^{2n}}$$

则

$$P(x)\equiv Q(x)\pmod{x^{n}}$$

$$P(x)-Q(x)\equiv 0\pmod{x^n}$$

$$(P(x)-Q(x))^2\equiv 0\pmod{x^{2n}}$$

$$P^2(x)-2P(x)Q(x)+Q^2(x)\equiv 0\pmod{x^{2n}}$$

两边同乘 $F(x)$，由 $F(x)Q(x)\equiv 1\pmod{x^{2n}}$ 得

$$F(x)P^{2}(x)-2P(x)+Q(x)\equiv 0\pmod{x^{2n}}$$

$$Q(x)\equiv (2-F(x)P(x))P(x)\pmod{x^{2n}}$$

该式即 $P(x)$ 向 $Q(x)$ 倍增的公式。$n=1$ 时，显然有

$$F^{-1}_0=\dfrac{1}{F_0}$$

时间复杂度由主定理得

$$T(n)=\Theta(n\log n)+T\left(\dfrac{n}{2}\right)=\Theta(n\log n)$$

该式亦可用于计算科学计数法下某个高精度数的倒数。

### 牛顿迭代

对于代数方程

$$f(x)=0$$

我们有所谓的 **牛顿迭代法**：设已经得到近似解 $x_n$，过 $(x_n,f(x_n))$ 作 $f(x)$ 图象的切线，交 $x$ 轴于 $(x_{n+1},0)$，则

$$x_{n+1}=x_n-\dfrac{f(x_n)}{f'(x_n)}$$

就是一个更精确的近似解。且 **迭代一次，精度翻倍**。

代数方程的牛顿迭代法可推广到形式幂级数。若 $f$ 为未知的形式幂级数，且

$$g(x,f(x))=0$$

设 $f(x)\equiv f_n(x)\pmod{x^n}$，则

$$f_{2n}(x)\equiv f_n(x)-\dfrac{g(x,f_n(x))}{\dfrac{\partial g}{\partial y}(x,f_n(x))}\pmod{x^{2n}}$$

> 证明：将 $g$ 的第二个变量在 $f_n(x)$ 处泰勒展开，得
> $$g(x,y)=\sum_{k=0}^{+\infty}\dfrac{\dfrac{\partial^k g}{\partial y^k}(x,f_n(x))}{k!}(y-f_n(x))^k=0$$
> 由于
> $$f_{2n}(x)\equiv f_n(x)\pmod{x^n}$$
> $$f_{2n}(x)-f_n(x)\equiv 0\pmod{x^n}$$
> $$(f_{2n}(x)-f_n(x))^2\equiv 0\pmod{x^{2n}}$$
> 故
> $$\sum_{k=0}^{+\infty}\dfrac{\dfrac{\partial^k g}{\partial y^k}(x,f_n(x))}{k!}(f_{2n}(x)-f_n(x))^k\equiv\sum_{k=0}^{1}\dfrac{\dfrac{\partial^k g}{\partial y^k}(x,f_n(x))}{k!}(f_{2n}(x)-f_n(x))^k$$
> $$\equiv g(x,f_n(x))+\dfrac{\partial g}{\partial y}(x,f_n(x))(f_{2n}(x)-f_n(x))\equiv 0\pmod{x^{2n}}$$
> $$f_{2n}(x)\equiv f_n(x)-\dfrac{g(x,f_n(x))}{\dfrac{\partial g}{\partial y}(x,f_n(x))}\pmod{x^{2n}}$$

同乘法逆元的求解，牛顿迭代的复杂度为 $\Theta(n\log n)$。

练习：试证明对于一阶微分方程
$$f'(x)=g(x,f(x))$$
$\qquad\quad$ 设
$$a_n(x)=g(x,f_n(x)),b_n(x)=\dfrac{\partial g}{\partial y}(x,f_n(x))$$
$\qquad\quad$ 则有迭代式
$$f_{2n}(x)\equiv e^{\int b_n(x)\mathrm{d}x}\left(\int(a_n(x)-b_n(x)f_n(x))e^{-\int b_n(x)\mathrm{d}x}\mathrm{d}x+C\right)\pmod{x^{2n}}$$

### 左复合初等函数

$\ln f(x)$ 可以通过求导再积分求解

$$\ln f(x)=\int\dfrac{f'(x)}{f(x)}\mathrm{d}x+C$$

$\exp{f(x)}=e^{f(x)}$ 可以利用牛顿迭代解方程

$$\ln g(x)=f(x)$$

来求解。

其他初等函数可通过 $\ln$ 和 $\exp$ 导出，如

$$f^r(x)=e^{r\ln f(x)},\cos f(x)=\dfrac{e^{if(x)}+e^{-if(x)}}{2}$$

### 半在线卷积

若已知数列 $a,b$ 和函数列 $\{g_n\}$，欲求解递推式

$$f_n=\sum_{k=0}^{n-1}a_{n-k}g_k(f_k)+b_n$$

该式是一个卷积式子，但通常不能用关于 $f(x)$ 的方程表示，也就无法牛顿迭代求解。

但是我们依然可以用 FFT 优化其递推过程。 具体地，我们有一个 cdq 分治的思路：  
先计算低次项，再计算低次项对高次项的贡献，最后计算高次项。  

事实上，根据二进制拆分，从 $1$ 到 $n$ 枚举 $k$，依次通过 FFT 计算

$$f_{k-\operatorname{lowbit}(k)},f_{k-\operatorname{lowbit}(k)+1},\cdots,f_{k-1}$$

到

$$f_k,f_{k+1},\cdots,f_{k+\operatorname{lowbit(k)-1}}$$

的贡献即可。

该算法称为 **半在线卷积**，时间复杂度由主定理得

$$T(n)=\Theta(n\log n)+2T\left(\dfrac{n}{2}\right)=\Theta(n\log^2 n)$$

相比于牛顿迭代，半在线卷积可以求解更一般的幂级数方程，如

$$f(x)=g(x)f(2x)+h(x)$$

$$f(x)=g(x)(f\cdot h)(x)$$

其中 $\cdot$ 表示系数对应位置相乘。

### Bostan-Mori Algorithm

欲求解

$$[x^n]\dfrac{f(x)}{g(x)}$$

其中 $f,g$ 均为 $m$ 次多项式。分子分母同乘 $g(-x)$，得

$$[x^n]\dfrac{f(x)}{g(x)}=[x^n]\dfrac{f(x)g(-x)}{g(x)g(-x)}$$

注意到 $g(x)g(-x)$ 在奇数次项的系数为 $0$。故设

$$f(x)g(-x)=f_0(x^2)+xf_1(x),g(x)g(-x)=g_0(x^2)$$

则

$$[x^n]\dfrac{f(x)}{g(x)}=[x^n]\dfrac{f_0(x^2)}{g_0(x^2)}+[x^n]x\dfrac{f_1(x^2)}{g_0(x^2)}$$

根据 $n$ 的奇偶性选取一侧递归即可。

$n$ 远大于 $m$ 时，时间复杂度为 $\Theta(m\log m\log n)$。$n=m$ 时，时间复杂度由主定理得为 $\Theta(n\log n)$。

我们将在【组合】（3）一维递推关系中阐述另一个求 $[x^n]\dfrac{f(x)}{g(x)}$ 的算法。但值得注意的是，  
Bostan-Mori Algorithm 可以推广到二元的情形

$$[x^n]\dfrac{f(x,y)}{g(x,y)}=[x^n]\dfrac{f(x,y)g(-x,y)}{g(x,y)g(-x,y)}=[x^n]\dfrac{f_0(x^2,y)}{g_0(x^2,y)}+[x^n]x\dfrac{f_1(x^2,y)}{g_0(x^2,y)}$$

设 $f,g$ 中 $y$ 的次数均为 $m$。每次递归 $x$ 的次数减半的同时，$y$ 的次数翻倍。故时间复杂度为

$$\Theta(nm\log nm\log n)$$

### 复合和复合逆

对于复合逆，根据拉格朗日反演，有

$$
n[x^n]f^k(x)=k[x^{n-k}]\left(\dfrac{x}{f^{\left<-1\right>}(x)}\right)^n\\
[x^{n-k}]\left(\dfrac{x}{f^{\left<-1\right>}(x)}\right)^n=\dfrac{n}{k}[x^n]f^k(x)\quad(k\in[0,n)\cap\N)\\
\left(\dfrac{x}{f^{\left<-1\right>}(x)}\right)^n\equiv\sum_{k=1}^n x^{n-k}\dfrac{n}{k}[x^n]f^k(x)\pmod{x^n}\\
\dfrac{f^{\left<-1\right>}(x)}{x}\equiv\left(\sum_{k=1}^n x^{n-k}\dfrac{n}{k}[x^n]f^k(x)\right)^{-\frac{1}{n}}\pmod{x^n}\\
f^{\left<-1\right>}(x)\equiv x\left(\sum_{k=1}^n x^{n-k}\dfrac{n}{k}[x^n]f^k(x)\right)^{-\frac{1}{n}}\pmod{x^{n+1}}
$$

问题转化为对 $k=1,2,\cdots,n$ 求解 $[x^n]f^k(x)$，也即求解

$$\sum_k [x^n]f^k(x)y^k=[x^n]\dfrac{1}{1-f(x)y}$$

使用 Bostan-Mori Algorithm $\Theta(n\log^2 n)$ 求得 $[x^n]\dfrac{1}{1-f(x)y}$ 后，  
即可 $\Theta(n\log n)$ 开根求得 $f^{\left<-1\right>}(x)$。总时间复杂度 $\Theta(n\log^2 n)$ 。

--------

对于复合，设 $f^R_k=f_{n-k}$，则

$$[x^m](f\circ g)(x)=\sum_kf_k[x^m]g^k(x)=\sum_{k}f^R_{n-k}[x^my^k]\dfrac{1}{1-g(x)y}=[x^my^n]\dfrac{f^R(y)}{1-g(x)y}$$

其不能直接使用  Bostan-Mori Algorithm 求解。使用 [转置原理](https://www.luogu.com.cn/blog/wangrx/tellegen-s--principle)，考虑求解

$$\sum_{k}f_k[x^ky^m]\dfrac{1}{1-g(x)y}$$

则

$$\sum_{k}f_k[x^ky^m]\dfrac{1}{1-g(x)y}=\sum_{k}f^R_{n-k}[x^ky^m]\dfrac{1}{1-g(x)y}=[x^ny^m]\dfrac{f^R(x)}{1-g(x)y}$$

这是可以直接使用 Bostan-Mori Algorithm $\Theta(n\log^2 n)$ 求解的，转置该算法即可。


## 多项式相关算法

### 带余除法

对于高精度整数 $a,b$，求解 $\left\lfloor\dfrac{a}{b}\right\rfloor$ 的过程分为以下四步：

1. 将 $a,b$ 转成科学计数法，如
   $$3\times 10^3+5\times 10^{2}+7\times 10+9=10^3\times(3+5\times 10^{-1}+7\times 10^{-2}\times 9\times 10^{-3})$$
2. 计算 $b$ 在科学计数法下的乘法逆元 $b^{-1}$
3. 计算 $a\times b^{-1}$ 。
4. 将 $a\times b^{-1}$ 转回整数，微调后即为 $\left\lfloor\dfrac{a}{b}\right\rfloor$。

多项式的带余除法沿用这一思路。设

$$\deg A=n,\deg B=m,P(x)=\left\lfloor\frac{A(x)}{B(x)}\right\rfloor,Q(x)=A(x)\bmod B(x)$$

则

$$A(x)=B(x)P(x)+Q(x)\ (\deg P=n-m,\deg Q<m\texttt{ 或 } Q(x)=0)\quad(1)$$

整数转科学计数法对应多项式将系数翻转，对于多项式 $f$ 系数翻转后得到的多项式 $f_R$，有

$$f(x^{-1})=x^{-\deg f}f_R(x)$$

故由 $(1)$ 得

$$A(x^{-1})=B(x^{-1})P(x^{-1})+Q(x^{-1})$$

$$x^{-n}A_R(x)=x^{-m}B_R(x)x^{m-n}P_R(x)+x^{-\deg Q}Q_R(x)$$

$$A_R(x)=B_R(x)P_R(x)+x^{n-\deg Q}Q_R(x)$$

$\deg Q<m$ 或 $Q(x)=0$，故 $n-\deg Q>n-m$ 或 $Q(x)=0$，因此

$$A_R(x)\equiv B_R(x)P_R(x)\pmod{x^{n-m+1}}$$

这就是计算商 $P(x)$ 的表达式。算出后代回 $(1)$ 即可求出余数 $Q(x)$。

### 分治 FFT


> 练习：给出以下问题的 $\Theta(n\operatorname{polylog}(n))$ 级别的算法：  
> 1. 给定 $m$ 次多项式 $f(x)$，计算 $f(e^x)$ 的前 $m$ 项 $\ (m<n)$。
> 2. 给定 $a_1,a_2,\cdots,a_k$ 和 $c_1,c_2,\cdots,c_k$（$a_1,a_2,\cdots,a_k$ 互不相同，$\displaystyle\sum_{i}c_i=n$），  
> $\forall i\in[1,n]\cap\N$，计算 $\displaystyle\prod_{j\not =i}(x-a_j)^{c_j}\bmod(x-a_i)^{c_i}$

### 欧几里得

### 有理分式分解

对于分母已经因式分解的有理分式

$$\dfrac{P(x)}{\displaystyle\prod_i(x-a_i)^{c_i}}$$

欲将其分解为

$$\dfrac{P(x)}{\displaystyle\prod_i(x-a_i)^{c_i}}=Q(x)+\sum_i\dfrac{R_i(x)}{(x-a_i)^{c_i}}\quad(\deg R_i<c_i)$$

的形式。显然其可用 多项式欧几里得+分治 FFT 解决。  

-------------------------------------------------------------
但有不使用多项式欧几里得的更为简洁、常数更小的做法，因为上式等价于

$$P(x)=Q(x)\prod_i(x-a_i)^{c_i}+\sum_iR_i(x)\prod_{j\not =i}(x-a_j)^{c_j}\quad(\deg R_i<c_i)$$

由此得

$$P(x)\equiv R_i(x)\prod_{j\not=i}(x-a_j)^{c_j}\pmod{(x-a_i)^{c_i}}$$

可以用 分治 FFT+多项式除法 $\Theta(n\log n)$ 求出 $f_i(x)=P(x)\bmod(x-a_i)^{c_i}$ 和 $\displaystyle g_i(x)=\prod_{j\not =i}(x-a_j)^{c_j}$，  
之后换元得

$$f_i(x)\equiv R_i(x)g_i(x)\pmod{(x-a_i)^{c_i}}$$
$$f_i(x+a_i)\equiv R_i(x+a_i)g_i(x+a_i)\pmod{x^{c_i}}$$

换元可以通过 $\Theta(c_i\log c_i)$ 的差卷积实现。  
计算 $g_i(x+a_i)$ 在 $\pmod{x^{c_i}}$ 意义下的乘法逆元，即可得到 $R_i(x+a_i)$，换元回去即可得到 $R_i(x)$  。

前半段 分治 FFT 的时间复杂度 $\Theta(n\log^2 n)$，后半段时间复杂度在 $\Omega(n)$ 到 $\Omicron(n\log n)$ 之间，  
总的时间复杂度为 $\Theta(n\log^2 n)$。


### 连续点值平移

### 多点求值

欲求解多项式 $f(x)$ 多个位置的点值 $x_1,x_2,\cdots,x_n$

设 $g_k(x)=f(x+x_k)$，则

$$f(x_k)=g_k(0)=g_k(x)\bmod x=g_k(x-x_k)\bmod(x-x_k)=f(x)\bmod(x-x_k)$$

因此原问题等价于求解 $f(x)$ 分别对 $x-x_1,x-x_2,\cdots,x-x_n$ 取模的结果。

由于 $f(x)\bmod g(x)=(f(x)\bmod g(x)h(x))\bmod g(x)$，故可以分治 FFT 作多项式取模来求解。

另一个做法是 [转置原理](https://www.luogu.com.cn/blog/wangrx/tellegen-s--principle)。
两种做法的时间复杂度均为 $\Theta(n\log^2 n)$。

### 快速插值

我们希望快速地由 $n$ 个不同位置的点值 $(x_0,y_0),(x_1,y_1),\cdots,(x_{n-1},y_{n-1})$   
得到由其确定的 $n-1$ 次多项式 $f(x)$。由拉格朗日插值公式

$$f(x)=\sum_{i}y_i\prod_{j\not =i}\dfrac{x-x_j}{x_i-x_j}$$ 
$$=\sum_{i}\left(\prod_{j<i}(x-x_j)\right)\dfrac{y_i}{\displaystyle\prod_{j\not =i}(x_i-x_j)}\left(\prod_{j>i}(x-x_j)\right)$$

可知，若我们能快速地求出 $\displaystyle\prod_{j\not =i}(x_j-x_i)$，就可以用分治 FFT 求解 $f(x)$ 。

设 $g(x)=\displaystyle\prod_{j}(x-x_j)$，由导数的乘法法则得

$$g'(x)=\sum_i\prod_{j\not =i}(x-x_j)$$

因此

$$g'(x_i)=\prod_{j\not =i}(x_i-x_j)$$

分治 FFT 求出 $g(x)$，然后 $\Theta(n)$ 求导后即可多点求值求出 $\displaystyle\prod_{j\not= i}(x_i-x_j)$。

总时间复杂度 $\Theta(n\log^2 n)$。