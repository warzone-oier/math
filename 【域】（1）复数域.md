## 复数

实数域 $\R$ 内，复数的平方根是不存在的，于是引入虚数单位

$$\sqrt{-1}=i$$

于是任意实数加、减、乘、除、开根后均可表示成 $a+bi\quad(a,b\in\R)$ 的形式。  
它们构成了复数域 $\mathbb{C}$。

### 复数的加减

复数的加减与向量的加减相同。即

$$(a+bi)+(c+di)=(a+c)+(b+d)i\quad(a,b,c,d\in\R)$$

$$(a+bi)-(c+di)=(a-c)+(b-d)i\quad(a,b,c,d\in\R)$$

### 模长和辐角

复数的 **模长**（或 **模**）就是其所对应的向量的长度。即

$$|a+bi|=\sqrt{a^2+b^2}\quad(a,b\in\R)$$

对于复数 $a+bi$，其 **辐角** 就是向量 $(|a+bi|,0)$ 逆时针旋转到 $(a,b)$ 所需的角度。 

显然，同一个复数 $z$ 的辐角有无数个，它们的差值为 $2\pi$ 的整数倍。  
我们称其中在 $[0,2\pi)$ 范围内的辐角为 **辐角的主值**，记为 $\arg z$ 。

在此基础上，任意复数 $z$ 均可表示为

$$r(\cos\theta+i\sin\theta)$$

的形式。其中 $r$ 为 $z$ 的模长，$\theta$ 为 $z$ 的辐角。这称为复数的 **辐角表示**。

### 复数的乘除

复数乘法的计算方法是非常显然的：

$$(a+bi)(c+di)=(ac-bd)+(ad+bc)i\quad(a,b,c,d\in\R)$$

但除法的计算方法不是那么显然。定义复数的 **共轭** 为

$$\overline{a+bi}=a-bi\quad(a,b\in\R)$$

在此基础上，发现对于任意复数 $z$，有

$$z\overline{z}=|z|^2$$

于是复数的除法即可这样计算：

$$\dfrac{z_1}{z_2}=\dfrac{z_1\overline{z_2}}{z_2\overline{z_2}}=\dfrac{z_1\overline{z_2}}{|z_2|^2}\quad(z_1,z_2\in\mathbb{C})$$


复数乘法的几何意义是什么？对于复数的辐角表示，我们有

$$r_1(cos
\alpha+i\sin\alpha)\times r_2(\cos\beta+i\sin\beta)$$

$$=r_1r_2((\cos\alpha\cos\beta-\sin\alpha\sin\beta)+i(\cos\alpha\sin\beta+\cos\beta\sin\alpha))$$
$$=r_1r_2(\cos(\alpha+\beta)+i\sin(\alpha+\beta))$$

也就是说，**复数相乘，模长相乘，辐角相加**。

练习：试指出复数的除法、乘方和开根有何几何意义。

### 欧拉公式

实数域内，指数 $e^x$ 和对数 $\ln$ 实现了乘法和加法的转化。即

$$e^{a+b}=e^a\times e^b,\ln(ab)=\ln a+\ln b$$

由复数乘法 “模长相乘、辐角相加” 的几何意义，复数的指数和对数应该和辐角有关。

具体地，欧拉发现了所谓的 **欧拉公式**

$$e^{ix}=\cos x+i\sin x$$

其可用泰勒展开证明（详见【环】（2）多项式环）。

欧拉公式的第一个重要意义在于给出了复指数和复对数的计算方法。对于复指数，有

$$e^{r+i\theta}=e^r(\cos\theta+i\sin\theta)$$

对于复对数，由于辐角的取值有多个，复对数的取值相应地也有多个，  
像复对数这样有多个取值的“函数”称为 **多值函数**。

欧拉公式的第二个重要意义在于其给出了三角函数在复数域下的定义。  
代入 $-x$ 到欧拉公式中，得到

$$e^{-ix}=\cos x-i\sin x$$

该式与原公式加减消元，可得

$$\cos x=\dfrac{e^{ix}+e^{-ix}}{2},\sin x=\dfrac{e^{ix}-e^{-ix}}{2i},\tan x=\dfrac{\sin x}{\cos x}=\dfrac{e^{ix}-e^{-ix}}{i(e^{ix}+e^{-ix})}$$

这就是复变三角函数的定义。其与双曲函数的定义

$$\cosh x=\dfrac{e^x+e^{-x}}{2},\sinh x=\dfrac{e^x-e^{-x}}{2},\tanh x=\dfrac{e^x-e^{-x}}{e^x+e^{-x}}$$

是一致的。这就是欧拉公式的第三个重要意义：在复数域下揭示三角函数与双曲函数间的联系。

欧拉公式的第四个重要意义在于统一了各种三角恒等变换公式。由于

$$\cos(\alpha+\beta)+i\sin(\alpha+\beta)=e^{i(\alpha+\beta)}$$
$$=e^{i\alpha}\times e^{i\beta}=(\cos\alpha+i\sin\alpha)(\cos\beta+i\sin\beta)$$
$$=\cos(\alpha+\beta)+i\sin(\alpha+\beta)$$

我们可以用复指数来记忆和计算两角和与差的三角函数。

### 单位根反演

实数域 $\R$ 内，方程 $1$ 的 $n$ 次方根至多有 $1$ 和 $-1$ 两个。  
但在复数域内远不止这两个。将单位元 $n$ 等分，设 

$$\omega_n=e^{i\frac{2\pi}{n}}=\cos\dfrac{2\pi}{n}+i\sin\dfrac{2\pi}{n}$$

则 

$$\omega_n^k=e^{i\frac{2k\pi}{n}}=\cos\dfrac{2k\pi }{n}+i\sin\dfrac{2k\pi}{n}=\quad(k\in\{0,1,\cdots,n-1\})$$ 

都是 $x^n=1$ 的解。它们称为 **$n$ 次单位根**。

显然，$n$ 次单位根与复数乘法构成 $n$ 阶循环群。

单位根的重要用途之一是解决与周期数列相关的问题。我们有

>**单位根反演**：$\dfrac{1}{n}\displaystyle\sum_{k=0}^{n-1}\omega_n^{kj}=[n\bmod k=0]=\begin{cases}1&(j\bmod n=0)\\0&(j\bmod n\not=0)\end{cases}$  
>证明：$j\bmod n=0$ 时，$\omega_n^{k}=1$，故
>$$\dfrac{1}{n}\displaystyle\sum_{k=0}^{n-1}\omega_n^{kj}=\dfrac{1}{n}\displaystyle\sum_{k=0}^{n-1}1=\dfrac{n}{n}=1$$
>$\qquad\quad$ 否则，$\omega_n^0,\omega_n^{k},\cdots,\omega_{n}^{(n-1)k}$ 构成等比数列，公比为 $\omega_n^k\notin\{0,1\}$，故
>$$\dfrac{1}{n}\displaystyle\sum_{k=0}^{n-1}\omega_n^{kj}=\dfrac{1}{n}\times\dfrac{\omega^{nk}_n-1}{\omega_n-1}=\dfrac{1}{n}\times\dfrac{1-1}{\omega_n-1}=0$$

有了单位根反演，我们可以将周期数列转换为复数域下的等比数列，    
进而用等比数列的方法处理周期数列。