# 数论函数
$\N_+\rightarrow\mathbb{C}$ 的函数称为数论函数。

我们发现，如果将质数 $2,3,5,7,\cdots$ 变为元 $x_2,x_3,x_5,x_7,\cdots$，  
则每个数论函数都可视为一无穷元生成函数

$$f(x_2,x_3,x_5,x_7,\cdots)=f(1)+f(2)x_2+f(3)x_3+f(4)x_2^2+f(5)x_5+f(6)x_2x_3+f(7)x_7+f(8)x_2^3+\cdots$$

## 狄利克雷卷积

在以上讨论的基础上，定义数论函数的 **狄利克雷卷积** 为

$$(f*g)(n)=\sum_{ab=n}f(a)g(b)=\sum_{d|n}f(d)g\left(\dfrac{n}{d}\right)$$

可以发现，狄利克雷卷积即数论函数所对应的无穷元生成函数的乘积。

因此狄利克雷卷积 $*$ 满足以下性质：

- 交换律：$f*g=g*h$
- 结合律：$(f*g)*h=f*(g*h)$
- 与数乘的相容性：$c(f*g)=(cf)*g=f*(cg)$
- 分配律：$(f+g)*h=f*h+g*h,f*(g+h)=f*h+g*h$
- 单位元：设 $\varepsilon(n)=[n=1]$，则 $\varepsilon *f=f*\varepsilon=f$
- 逆元：若 $f(1)\not =0$，则存在 $f^{-1}:\N_{+}\rightarrow\mathbb{C},f\times f^{-1}=f^{-1}\times f=\varepsilon$
  > 证明：$n=1$ 时，$f^{-1}(1)f(1)=1$，可得 $f^{-1}(1)=\dfrac{1}{f(1)}$。  
  > $\qquad\quad n\not =1$ 时，有
  > $$\sum_{d|n}f^{-1}(d)f\left(\dfrac{n}{d}\right)=0$$
  > $$f^{-1}(n)f(1)+\sum_{d|n,d\not=n}f^{-1}(d)f\left(\dfrac{n}{d}\right)=0$$
  > $$f^{-1}(n)=-\dfrac{1}{f(1)}\sum_{d|n,d\not=n}f^{-1}(d)f\left(\dfrac{n}{d}\right)$$
  > $\qquad\quad$ 由此得到狄利克雷卷积逆的递推式。

计算狄利克雷卷积和卷积逆的前 $n$ 项均需要枚举前 $n$ 个数的约数，也即枚举前 $n$ 个数 $\le n$ 的倍数。  
数 $k$ 的 $\le n$ 的倍数有 $\left\lfloor\dfrac{n}{k}\right\rfloor$ 个，故由调和级数（详见【分析】（2）有限微积分）可知，  
计算狄利克雷卷积和卷积逆前 $n$ 项的时间复杂度为

$$\sum_{k=1}^n\left\lfloor\dfrac{n}{k}\right\rfloor\le n\sum_{k=1}^n\dfrac{1}{k}=nH_n=\Theta(n\ln n)$$

## 积性函数和贝尔级数

对于数论函数，我们着重研究的是其中的 **积性函数**。数论函数 $f$ 为 积性函数当且仅当

$$\forall\gcd(a,b)=1,f(ab)=f(a)f(b)$$

故 $f$ 为积性函数时，将 $n$ 质因数分解，设 $n=p_1^{c_1}p_2^{c_2}\ (p_1,p_2,\cdots\in\mathbb{P})$，则

$$f(n)=f(p_1^{c_1})f(p_2^{c_2})\cdots$$

也就是说，积性函数的值由 $p^k\ (p\in\mathbb{P})$ 处的值唯一确定。

故定义数论函数的 **贝尔级数** 为

$$f_p(x)=\sum_{k=0}^{+\infty}f(p^k)x^k$$

则 $f$ 为积性函数当且仅当其对应的无穷元生成函数可以唯一因式分解为

$$f(x_2,x_3,x_5,x_7,\cdots)=f_2(x_2)f_3(x_3)f_5(x_5)f_7(x_7)\cdots$$

的形式。因此积性函数的以下性质是显然的：

- 两个积性函数的狄利克雷卷积仍是积性函数，其贝尔级数即两个积性函数贝尔级数的乘积。
- 积性函数的狄利克雷卷积逆仍是积性函数，其贝尔级数即原积性函数贝尔级数的倒数。

此外，积性函数还有如下性质：

- > 若 $f$ 为积性函数，则 $f(1)=1$ 或 $f=0$ 。  
  > 证明：若 $f(1)\not=0$，由积性函数的定义得到 $f(1)=f(1)f(1)$，故 $f(1)=1$ 。  
  > $\qquad\quad$ 否则，亦由积性函数定义得 $f(n)=f(n)f(1)=0$，即 $f=0$ 。
- 若 $f(x)$ 为积性函数，则 $f(x^k)$ 也是积性函数。
- 若 $f(x),g(x)$ 为积性函数，则 $f(x)g(x)$ 也是积性函数，其贝尔级数为 $(f\cdot g)(x),\cdot$ 表示系数对应位置相乘。
  - 特别地，若 $f(x)$ 为积性函数，则 $x^kf(x)$ 为积性函数，其贝尔级数为 $f_p(p^kx)$。

还有所谓的 **完全积性函数**。$f$ 为完全积性函数当且仅当

$$\forall a,b\in\N_+,f(ab)=f(a)f(b)$$

这相当于 $f$ 为积性函数，且

$$f_p(x)=\dfrac{1}{1-f(p)x}$$

与贝尔级数等价的另一个理解积性函数的方式是狄利克雷生成函数，在此不作讨论。

### 常见的积性函数及其贝尔级数

- $\varepsilon:$ 狄利克雷卷积的单位元，完全积性。$\varepsilon_p(x)=1$ 。
- $\mathrm{id}_k:$ 幂函数 $\mathrm{id}_k(x)=x^k$，完全积性。$(\mathrm{id}_k)_p(x)=\dfrac{1}{1-p^kx}$ 。
  - $I=\mathrm{id}_0:$ 常值函数 $I(x)=1$，完全积性。$I_p(x)=\dfrac{1}{1-x}$ 。
  - $\mathrm{id}=\mathrm{id}_1:$ 恒等函数 $\mathrm{id}(x)=x$，完全积性。$\mathrm{id}_p(x)=\dfrac{1}{1-px}$ 。 
- $\mu=I^{-1}:$ 莫比乌斯函数，$I$ 的狄利克雷卷积逆。$\mu_p(x)=1-x$ 。
- $\varphi:$ 欧拉函数，$\varphi(n)$ 即 $1,2,\cdots,n$ 中与 $n$ 互质的整数个数。
  > 求证：$\varphi$ 为积性函数，且 $\varphi_p(x)=\dfrac{1-x}{1-px}$ 。  
  > 证明：即证明
  >$$I*\varphi=\mathrm{id}$$
  > $\qquad\quad$ 设 $f_n(a)=\displaystyle\sum_{k=1}^n[\gcd(n,k)=a]$，则显然有 $n=\displaystyle\sum_{k=1}^nf_n(k)$ 。注意到   
  > $$[\gcd(n,k)=a]={\begin{cases}0&(a\nmid n\texttt{ 或 }a\nmid k)\\{[\gcd\left(\dfrac{n}{a},\dfrac{k}{a}\right)=1]}&(x\mid n\texttt{ 且 }x\mid k)\end{cases}}$$  
  > $\qquad\quad$ 故 
  > $$f_n(a)={\begin{cases}0&(a\nmid n)\\{\displaystyle\sum_{k|x\texttt{且}k\in[1,n]}[\gcd\left(\dfrac{n}{a},\dfrac{k}{a}\right)=1]}&(a\mid n)\end{cases}}$$  
  > $\qquad\quad$ 于是 $a\mid n$ 时，$f_n(a)\displaystyle=\sum_{k=1}^\frac{n}{a}[\gcd\left(\dfrac{n}{a},k\right)=1]=\varphi\left(\frac{n}{a}\right)$，  
  > $\qquad\quad$ 由此可得 $\displaystyle n=\sum_{k=1}^nf_n(k)=\sum_{k|n}f_n(k)=\sum_{k|n}\varphi\left(\frac{n}{k}\right)$，即  
  > $$\mathrm{id}=I*\varphi$$  
- $\sigma_k=I*\mathrm{id}_k$：除数函数，即所有约数的 $k$ 次方之和。$(\sigma_k)_p=\dfrac{1}{(1-x)(1-p^rx)}$ 
  - $d=\sigma_0=I*I$：约数个数。$d_p(x)=\dfrac{1}{(1-x)^2}$ 。  
    对于 $d$ 的增长率，我们有以下估计：
	$$d(n)<2\sqrt{n},d(n)<n^{\frac{1.066}{\ln\ln n}}$$
  - $\sigma=\sigma_1=I*\mathrm{id}$：约数之和。$\sigma_p(x)=\dfrac{1}{(1-x)(1-px)}$  
    对于 $\sigma$ 的增长率，当黎曼猜想成立时，有
	$$\sigma(x)<e^{\gamma}x\ln\ln x\ (x>5040)$$
	（$\gamma$ 为欧拉-马歇罗尼常数，详见【分析】（2）有限微积分与无限微积分）

有了积性函数的贝尔级数，就可以 $\Theta(n)$ 线性筛求得其前 $n$ 项。


## 整除分块


带余除法具有以下性质：

1. > $\left\lfloor\dfrac{\lfloor\frac{n}{a}\rfloor}{b}\right\rfloor=\left\lfloor\dfrac{n}{ab}\right\rfloor$  
   > 证明：设 $n=sab+ta+r\quad(s,t,r\in\N,t<b,r<a)$，则 $n$ 总有 $(s,t,r)$ 与之唯一对应，故
   >$$\left\lfloor\frac{\lfloor\frac{n}{a}\rfloor}{b}\right\rfloor=\left\lfloor\dfrac{\lfloor sb+t+\frac{r}{a}\rfloor}{b}\right\rfloor=\left\lfloor\dfrac{sb+t}{b}\right\rfloor=\left\lfloor s+\dfrac{t}{b}\right\rfloor=s$$
   >$$ta+r<ab,\left\lfloor\dfrac{n}{ab}\right\rfloor=\left\lfloor s+\dfrac{ta+r}{ab}\right\rfloor=s$$
   > $$\left\lfloor\dfrac{\lfloor\frac{n}{a}\rfloor}{b}\right\rfloor=\left\lfloor\dfrac{n}{ab}\right\rfloor=s$$
2. > $\forall k\in\N_+,\left\lfloor\dfrac{n}{k}\right\rfloor$ 只有 $\Theta(\sqrt{n})$ 种取值。  
   > 证明：$k\le\sqrt{n}$ 时，$k$ 有约 $\sqrt{n}$ 种取值，故 $\left\lfloor\dfrac{n}{k}\right\rfloor$ 也有约 $\sqrt{n}$ 种取值。  
   > $\qquad\ \ k>\sqrt{n}$ 时，$\dfrac{n}{k}<\sqrt{n}$，故 $\left\lfloor\dfrac{n}{k}\right\rfloor$ 依然只有约 $\sqrt{n}$ 种取值。  
   > $\qquad\quad$ 总上，$\left\lfloor\dfrac{n}{k}\right\rfloor$ 只有约 $2\sqrt{n}=\Theta(\sqrt{n})$ 种取值。  
   > 进一步地，设 $f_n=\left|\left\{\left\lfloor\dfrac{n}{k}\right\rfloor|k\in\N_+\right\}\right|$，则 $f_n=\lfloor\sqrt{n}\rfloor+\left\lfloor\dfrac{n}{\lfloor\sqrt{n}\rfloor+1}\right\rfloor$，且 $\forall m\in\N_+$，
   > $$f_n=2m-1\Leftrightarrow n\in[m^2,m(m+1))$$
   > $$f_n=2m\Leftrightarrow n\in[m(m+1),(m+1)^2)$$
在性质 2. 的基础上，对于形如

$$\sum_{k=1}^nf(k)g\left(\left\lfloor\dfrac{n}{k}\right\rfloor\right)$$

的求和，设 $\left\lfloor\dfrac{n}{k}\right\rfloor=i$，则

$$\left\lfloor\dfrac{n}{k}\right\rfloor=i\Leftrightarrow i\le \dfrac{n}{k}<i+1\Leftrightarrow \dfrac{n}{i+1}<k\le\dfrac{n}{i}\Leftrightarrow \left\lfloor\dfrac{n}{i+1}\right\rfloor<k\le\left\lfloor\dfrac{n}{i}\right\rfloor$$

因此

$$\sum_{k=1}^nf(k)g\left(\left\lfloor\dfrac{n}{k}\right\rfloor\right)=\sum_{i=1}^{\lfloor\sqrt{n}\rfloor}\left(S_f\left(\left\lfloor\dfrac{n}{i}\right\rfloor\right)-S_f\left(\left\lfloor\dfrac{n}{i+1}\right\rfloor\right)\right)g(i)+\sum_{k=1}^{\left\lfloor\frac{n}{\lfloor\sqrt{n}\rfloor+1}\right\rfloor}f(k)g\left(\left\lfloor\dfrac{n}{k}\right\rfloor\right)$$

由此将原求和分成了 $\Theta(\sqrt{n})$ 次 $f$ 的求和和 $g$ 的求点值。该做法称为 **整除分块**。

整除分块是积性函数相关算法的基础。 

## 积性函数求和

在得到了贝尔级数的情况下，可以用 $\Theta(n)$ 的线性筛求出积性函数 $f$ 的前 $n$ 项。

然而，对于积性函数的前 $n$ 项和

$$S_f(n)=\sum_{k=1}^nf(k)$$

其求解的时间复杂度可以低于 $\Theta(n)$ 。例如，  
幂函数的前 $n$ 项和有对应的通项公式（详见【分析】有限微积分与无限微积分）。

积性函数求和问题的核心是通过狄利克雷卷积，将原积性函数的求和转化为幂函数的求和，下面将讨论这一问题。

### 整除分块（求解 $S_{f*g}$）

对于数论函数 $f,g$，整除分块允许我们从 $S_f,S_g$ 导出 $S_{f*g}$：  

$$S_{f*g}(n)=\sum_{k=1}^n(f*g)(n)=\sum_{k=1}^n\sum_{ij=k}f(i)g(j)$$
$$=\sum_{i=1}^nf(i)\sum_{j=1}^{\left\lfloor\frac{n}{i}\right\rfloor}g(j)=\sum_{i=1}^nf(i)S_g\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

由此解决了 $\sigma_k$ 的求和问题：

$$\sigma_k=\mathrm{id}_k*I,S_{\sigma_k}(n)=\sum_{i=1}^n \mathrm{id}_k(i)S_I\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=\sum_{i=1}^n i^k\left\lfloor\frac{n}{i}\right\rfloor$$

对于多个数论函数的狄利克雷卷积的前缀和，由带余除法的性质 1. 可知，  
计算过程中始终只涉及前缀和在 $\left\lfloor\dfrac{n}{i}\right\rfloor$ 处的取值，故**可用哈希表记忆化来优化计算**。      


> 求证：若求解 $S_f(n),S_g(n),S_{h}(n)$ 的复杂度均为 $\Theta(1)$，则记忆化下整除分块求解 $S_{f*g*h}(n)$ 的复杂度为 $\Theta(n^{\frac{3}{4}})$ 。  
> 证明*：我们需要用哈希表记忆化
> $$S_{f*g}\left(\left\lfloor\dfrac{n}{1}\right\rfloor\right),S_{f*g}\left(\left\lfloor\dfrac{n}{2}\right\rfloor\right),\cdots,S_{f*g}\left(\left\lfloor\dfrac{n}{\lfloor\sqrt{n}\rfloor}\right\rfloor\right),$$
> $$S_{f*g}(\lfloor\sqrt{n}\rfloor),S_{f*g}(\lfloor\sqrt{n}\rfloor-1),\cdots,S_{f*g}(1)$$
> $\qquad\quad$ 其中 $S_{f*g}(t)$ 的求解枚举了 $S_f$ 和 $S_g$ 的 $2\lfloor\sqrt{t}\rfloor$ 个位置的值。故总的时间复杂度为
> $$\sum_{i=1}^{\lfloor\sqrt{n}\rfloor}\left(\Theta\left(\sqrt{i}\right)+\Theta\left(\sqrt{\dfrac{n}{i}}\right)\right)=\Theta\left(\sum_{i=1}^{\lfloor\sqrt{n}\rfloor}\sqrt{i}+\sqrt{n}\sum_{k=1}^{\lfloor\sqrt{n}\rfloor}\dfrac{1}{\sqrt{i}}\right)$$
> $\qquad\quad$ 用积分估算求和，时间复杂度即
> $$\Theta\left(\int_0^{\sqrt{n}}\sqrt{x}\mathrm{d}x+\sqrt{n}\int_0^{\sqrt{n}}\dfrac{\mathrm{d}x}{\sqrt{x}}\right)=\Theta\left(\dfrac{2}{3}(\sqrt{n})^{\frac{3}{2}}+\sqrt{n}\times2(\sqrt{n})^{\frac{1}{2}}\right)=\Theta(n^{\frac{3}{4}})$$

记忆化保证了整除分块在此情形下的亚线性时间复杂度。但我们还可以作如下的**折半优化**：  
分成 $x\le k$ 和 $x\ge k$ 两段，$x\le k$ 部分的 $S_{f*g*h}(x)$ 直接线性筛求解，  
$x\ge k$ 部分的 $S_{f*g*h}(x)$ 再使用整除分块求解。

> 求证：在使用记忆化和折半优化的情况下，若求解 $S_f(n),S_g(n),S_{h}(n)$ 的复杂度均为 $\Theta(1)$，  
> $\qquad\quad$ 则 $k=n^{\frac{2}{3}}$ 时，计算 $S_{f*g*h}$ 的最优复杂度为 $\Theta(n^{\frac{2}{3}})$ 。  
> 证明*：由于使用了折半优化，只要计算 $k\le\left\lfloor\dfrac{n}{i}\right\rfloor\le n$ 的 $S_{f*g}\left(\left\lfloor\dfrac{n}{i}\right\rfloor\right)$。  
> $\qquad\quad$ 由仅使用记忆化下时间复杂度的证明可知 $k\le\sqrt{n}$ 时复杂度没有优化，$k>\sqrt{n}$ 时复杂度变为
> $$\Theta(k)+\sum_{i=1}^{\lfloor\frac{n}{k}\rfloor}\Theta\left(\sqrt{\dfrac{n}{i}}\right)=\Theta(k)+\Theta\left(\sqrt{n}\int_{0}^{\frac{n}{k}}\dfrac{\mathrm{d}x}{\sqrt{x}}\right)$$
> $$=\Theta(k)+\Theta\left(\sqrt{n}\times2\sqrt{\dfrac{n}{k}}\right)=\Theta\left(k+\dfrac{n}{\sqrt{k}}\right)$$
> $\qquad\quad$ 故 $k=\dfrac{n}{\sqrt{k}}$ 时，即 $k=n^{\frac{2}{3}}$ 时，取最优复杂度 $\Theta(n^{\frac{2}{3}})$ 。

三个数论函数的情况显然可以推广到多个数论函数，故省略。

### 杜教筛

整除分块允许我们从 $S_f,S_g$ 导出 $S_{f*g}$，在此基础上，**杜教筛** 允许我们从 $S_f,S_g$ 导出 $S_{f^{-1}*g}$ 。

首先，由 $g=f*(f^{-1}*g)$ 得

$$S_g(n)=\sum_{i=1}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=f(1)S_{f^{-1}*g}(n)+\sum_{i=2}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

$f^{-1}$ 存在，故 $f(1)\not =0$，于是 

$$S_{f^{-1}*g}(n)=\dfrac{1}{f(1)}\left(S_g(n)-\sum_{i=2}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)\right)$$

该式是一个 $S_{f^{-1}*g}(n)$ 向 $S_{f^{-1}*g}\left(\left\lfloor\dfrac{n}{i}\right\rfloor\right)$ 递归的式子，  
同样可用记忆化和折半优化来优化计算。因此：

- 在仅使用记忆化的情况下，杜教筛的复杂度为 $\Theta(n^{\frac{3}{4}})$
- 在同时使用记忆化和折半优化的情况下，杜教筛的复杂度为 $\Theta(n^{\frac{2}{3}})$

-----------------------------------------------------
若 $f$ 为积性函数，则 $f(1)=1$，上式即

$$S_{f^{-1}*g}(n)=S_g(n)-\sum_{i=2}^n f(i)S_{f^{-1}*g}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

由此解决了 $\mu,\varphi$ 的求和问题：

$$S_{\mu}(n)=S_\varepsilon(n)-\sum_{i=2}^n I(i)S_{\mu}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=1-\sum_{i=2}^nS_{\mu}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

$$S_{\varphi}(n)=S_\mathrm{id}(n)-\sum_{i=2}^n I(i)S_{\varphi}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)=\dfrac{n(n+1)}{2}-\sum_{i=2}^n S_{\varphi}\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

使用整除分块和杜教筛求积性函数前缀和时，我们需要根据贝尔级数，将积性函数分解成

$$\mathrm{id}_0^{c_0}*\mathrm{id}_1^{c_1}*\mathrm{id}_2^{c_2}*\cdots\quad(c_0,c_1,c_2,\cdots\in\Z)$$

的形式，正次项和负次项分别用整除分块合并，再用杜教筛合并二者。

### Powerful Number 筛

并不是所有的积性函数都可分解成 $\mathrm{id}_i^{c_i}$，但当积性函数 $f$ 满足 $f(p)$ 为关于 $p$ 的低次多项式时，  
**Powerful Number 筛**（简称 **PN 筛**）允许我们将其前缀和的求解转化为该情况。

首先另找一积性函数 $g$，使得 $g(p)=f(p)$  。  
设 $h=f*g^{-1}$，则 $h$ 为积性函数，且 $f=h*g$，可得
$$f(p)=h(p)g(1)+h(1)g(p)=h(p)+g(p)$$
$$h(p)=f(p)-g(p)=0$$

故 $h$ 在质数处取值为 $0$ 。  

> 求证：设满足 $h(n)\not =0$ 的 $n$ 为 **Powerful Number**，则其一定可以表示成 $a^2b^3\ (a,b\in\N_+)$ 的形式。  
> 证明：将 $n$ 质因数分解，设 
> $$n=p_1^{c_1}p_2^{c_2}\cdots p_k^{c_k} \quad(p_1,p_2,\cdots,p_k\in\mathbb{P},c_1,c_2,\cdots,c_k\in\N_+)$$
> $\qquad\quad$ 则由积性函数定义得
> $$h(n)=h(p_1^{c_1})h(p_2^{c_2})\cdots h(p_k^{c_k})$$
> $\qquad\quad h$ 在质数处取值为 $0$，而 $h(n)\not=0$，故
> $$c_1,c_2,\cdots,c_k\ge 2$$ 
> $\qquad\quad$ 若 $c_k$ 为偶数，设 $c_k=2d_k$，则 $p_k^{c_k}=(p_k^{d_k})^2$；  
> $\qquad\quad$ 否则，设 $c_k=2d_k+1$，则 $p_k^{c_k}=\left(p_k^{d_k-1}\right)^2p_k^3$ 。  
> $\qquad\quad$ 总上可得，$p_k^{c_k}$ 总可以表示成 $a^2b^3$ 的形式，故 $n$ 亦同样可以表示成 $a^2b^3$ 的形式。

由以上讨论可得，$n$ 以下 Powerful Number 的个数为

$$\sum_{a=1}^{\sqrt{n}}\sqrt[3]{\frac{n}{a^2}}=\Theta\left(\sqrt[3]{n}\int_{0}^{\sqrt{n}}x^{-\frac{2}{3}}\mathrm{d}x\right)=\Theta(\sqrt{n})$$

又由整除分块可知

$$S_f(n)=S_{h*g}(n)=\sum_{i=1}^n h(i)S_g\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

$$=\sum_{\substack{i\in[1,n]\\i\texttt{ is Powerful Number}} } h(i)S_g\left(\left\lfloor\frac{n}{i}\right\rfloor\right)$$

由此将 $S_f$ 的求解转化为 $S_g$ 的求解和 $h(i)$ 的求解。对于 $h(i)$ 的求解，可以直接根据其贝尔级数，  
也可同狄利克雷卷积逆一样递推，即

$$h(p^k)=f(p^k)-\sum_{i=0}^{k-1}h(p^i)g(p^{k-i})$$

Powerful Number 的质因子小于 $\sqrt{n}$，因此用后一种方式求解 $h(i)$ 所需总时间有一个很宽松的上界

$$\Omicron\left(\frac{\sqrt{n}}{\ln\sqrt{n}}\log^2 n\right)=\Omicron(\sqrt{n}\log n)$$

--------------------------

如何根据 $f$ 来选取合适的 $g$？首先，在 $\pmod{x^2}$ 意义下，有

$$\dfrac{1}{1-p^kx}\equiv 1+p^kx\pmod{x^2}$$

$$(1+ax)(1+bx)\equiv 1+(a+b)x\pmod{x^2}$$

故若 $f(p)=\displaystyle\sum_{k=0}^{m}a_kp^k$，则

$$f_p(x)\equiv 1+\left(\sum_{k=0}^{m}a_kp^k\right)x\equiv\left(\dfrac{1}{1-x}\right)^{a_0}\left(\dfrac{1}{1-px}\right)^{a_1}\cdots\left(\dfrac{1}{1-p^mx}\right)^{a_m}\pmod{x^2}$$

因此所要选取的 $g$ 为

$$\mathrm{id}^{a_0}*\mathrm{id}^{a_1}*\cdots*\mathrm{id}_m^{a_m}$$

综合整除分块、杜教筛和 PN 筛 所得的所有讨论，所得的筛法又称 **冷群筛**。

- 若不使用折半优化，总复杂度为 $\Theta(n^{\frac{3}{4}}\sum_i\log c_i)$ 。
- 若使用折半优化，由于需要线性筛 $n^{\frac{2}{3}}$ 以内的项，而在 $n$ 以下，素数的个数约为 $\dfrac{n}{\ln n}$，  
  $p^k\ (p\in\mathbb{P},k\ge 2)$ 均为 Powerful Number，个数小于 $\Theta(\sqrt{n})$，故总复杂度为 
  $$\Theta\left(n^{\frac{2}{3}}\left(1+\dfrac{\sum_i\log c_i}{\ln n}\right)\right)$$ 

------------------------------------------------------------
### Min_25 筛

冷群筛在求解复杂的积性函数时常数巨大。
