
## 二项式系数

从 $n$ 个物品中选出 $m$ 个物品，或者说，对于大小为 $n$ 的集合选出其一个大小为 $m$ 的子集，

其方案数 $\dbinom{n}{m}$ 称为 **组合数** 或 **二项式系数**，读作 “$n$ 选 $m$”。

$\dbinom{n}{m}$ 原始的组合意义仅包括 $n,m\in\N$ 的情况，实际上其定义可以拓展。

最常用的定义是 $n\in\mathbb{C},m\in\Z$ 的情况：

$$\dbinom{n}{m}=\begin{cases}
	\dfrac{n^{\underline{m}}}{m!}&(m\ge 0)\\
	0&(m<0)
\end{cases}$$

即先从 $n$ 个物品中选出 $m$ 个物品排成一列（即排列数 $n^{\underline{m}}$），  
再消除 $m$ 个物品排列顺序的影响（即除以 $m!$）。

### 对称恒等式

$$\dbinom{n}{m}=\dbinom{n}{n-m}\quad(n\in\N,m\in\Z)$$

其组合意义是：从 $n$ 个物品中选出 $m$ 个物品，相当于从 $n$ 个物品中选出 $n-m$ 个物品。

**这是组合恒等式中唯一一个仅在 $n\in\N$ 成立的恒等式**。使用时一定要注意上指标的范围。


### 吸收恒等式

$$\dbinom{n}{m}=\dfrac{n}{m}\dbinom{n-1}{m-1}\quad(m\not =0)$$

### 二项式定理

$$(1+x)^r=\sum_{k=0}^{+\infty}\dbinom{r}{k}x^k\quad(|x|<1\texttt{ 或 }r\in\N)$$

对 $(1+x)^r$ 作泰勒展开可证。

$r\in\N$ 的情况也可用组合意义说明：$[x^k](1+x)^r$ 即从 $r$ 个 $(1+x)$ 中选出 $k$ 个 $x$，方案数即 $\dbinom{r}{k}$ 。

该式为组合数的生成函数基础，组合数的最常用定义便来源于该式。

### 递推式

$$\dbinom{n}{m}=\dbinom{n-1}{m}+\dbinom{n-1}{m-1}$$

其组合意义是：从 $n$ 个物品中选出 $m$ 个物品，最后一个物品可以选，也可以不选。

证明有两种，一是根据组合数定义，通过有限微积分，得到上指标的差分

$$\Delta\dbinom{x}{n}=\dbinom{x}{n-1}$$

二是根据二项式定理，可得

$$[x^m](1+x)^n=[x^m](1+x)(1+x)^{n-1}=[x^m](1+x)^{n-1}+[x^m]x(1+x)^{n-1}$$
$$=[x^m](1+x)^{n-1}+[x^{m-1}](1+x)^{n-1}$$

### 上指标求和

$$\sum_{k=0}^n\dbinom{k}{m}=\dbinom{n+1}{m+1}$$

其组合意义是：从 $n+1$ 个物品中选出 $m+1$ 个物品，可以先将 $n+1$ 个物品排成一列，  
然后选出在 $m+1$ 个物品中最靠后的那一个物品（在 $n+1$ 个物品中标号为 $k+1$），  
再在前 $k$ 个物品里选出剩余的 $m$ 个物品。

从有限微积分的角度看该式是显然的，我们在递推式的证明中已经得到了上指标的差分。

从生成函数的角度也可以证明。由等比数列求和得

$$\sum_{k=0}^n[x^m](1+x)^k=[x^m]\sum_{k=0}^n(1+x)^k$$
$$=[x^m]\dfrac{(1+x)^{n+1}-1}{1+x-1}=[x^m]\dfrac{(1+x)^{n+1}-1}{x}=[x^{m+1}](1+x)^{n+1}$$

### 范特蒙德卷积

$$\sum_{k=0}^{+\infty}\dbinom{n}{i}\dbinom{m}{k-i}=\dbinom{n+m}{k}$$

其组合意义是：从 $n+m$ 个物品中选 $k$ 个物品，可以先从前 $n$ 个物品里选 $i$ 个，再从后 $m$ 个物品里选 $k-i$ 个。

从生成函数的角度看，这是显然的，因为

$$(1+x)^n(1+x)^m=(1+x)^{n+m}$$

### 三项式版恒等式

与二项式系数类似的，可以定义所谓 **多项式系数**

$$\dbinom{n}{a_1,a_2,\cdots,a_k}=\dfrac{n^{\underline{a_1+a_2+\cdots+a_k}}}{a_1!a_2!\cdots a_k!}$$

其组合意义是：先从 $n$ 个物品中选出 $a_1$ 个物品，再从剩下的物品里选出 $a_2$ 个物品 $\cdots$ 以此类推。

在此基础上，我们有所谓的 **多项式定理**

$$(1+x_1+x_2+\cdots+x_k)^n=\sum_{a_1,a_2,\cdots,a_k}\dbinom{n}{a_1,a_2,\cdots,a_k}x_1^{a_1}x_2^{a_2}\cdots x_n^{a_n}$$

以及二项式系数的 **三项式版恒等式**

$$\dbinom{n}{m}\dbinom{m}{k}=\dbinom{n}{k}\dbinom{n-k}{m-k}=\dbinom{n}{m-k,k}$$

### 上指标翻转

$$\dbinom{n}{k}=\dbinom{k-n-1}{k}(-1)^k$$

其给出了上指标为负时的组合数与上指标非负的组合数之间的转换。

用组合数的定义可以直接证明。

### 平行求和法

$$\sum_{k=0}^n\dbinom{n+k}{k}=\dbinom{n+m+1}{m}$$

这实际上就是二项式定理与上指标翻转的结合。因为

$$\dfrac{1}{(1-x)^{n+1}}=\sum_{k=0}^{+\infty}\dbinom{-n-1}{k}(-x)^k=\sum_{k=0}^{+\infty}\dbinom{n+k}{k}x^k$$

$$\sum_{n=0}^{+\infty}x^n\sum_{k=0}^n\dbinom{n+k}{k}=\dfrac{1}{1-x}\times\dfrac{1}{(1-x)^{n+1}}=\dfrac{1}{(1-x)^{n+2}}$$

### 与斐波那契数列的关系

$$\sum_{k=0}^n\dbinom{n-k}{k}=\mathrm{Fib}_n$$

其中 $\mathrm{Fib}$ 为斐波那契数列。即

$$\mathrm{Fib}_0=\mathrm{Fib}_1=1,\mathrm{Fib}_n=\mathrm{Fib}_{n-1}+\mathrm{Fib}_{n-2}$$

证明的话我们可以求得等式左边的生成函数

$$\sum_{n}x^n\sum_{k}\dbinom{n-k}{k}=\sum_{n,k}\dbinom{n-k}{k}x^n=\sum_{n,k}\dbinom{n-k}{k}x^{n-k}x^k$$

$$=\sum_{n,k}\dbinom{n}{k}x^nx^k=\sum_n x^n\sum_{k}\dbinom{n}{k}x^k=\sum_{n}x^n(1+x)^n$$

$$=\dfrac{1}{1-(1+x)x}=\dfrac{1}{1-x-x^2}$$

而斐波那契数列的生成函数由递推式得

$$\mathrm{Fib}(x)=x\mathrm{Fib}(x)+x^2\mathrm{Fib}(x)+1$$

$$\mathrm{Fib}(x)=\dfrac{1}{1-x-x^2}$$

从而得到等式两边的生成函数是一致的。