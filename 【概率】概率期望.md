## 概率

现代的概率定义源于测度论，即对长度、面积、体积等概念的一般化。  

对于集合 $\Omega$，若集合 $\mathcal{F}\subseteq\Omega$ 满足

1. $\varnothing\in\mathcal{F}$
2. 若 $S\in\mathcal{F}$，则 $\overline{S}=\complement_\Omega S\in\mathcal{F}$
3. 可数可并：若 $S_1,S_2,\cdots \in\mathcal{F}$，则 $\displaystyle\bigcup_{k=1}^{+\infty} S_k\in\mathcal{F}$

则称 $\mathcal{F}$ 为（$\Omega$ 上的）**$\sigma$ 代数**，$(\Omega,\mathcal{F})$ 称为 **可测空间**。若映射 $P:\mathcal{F}\rightarrow[0,+\infty]$ 满足

1. $P(\varnothing)=0$
2. 可数可加性：若 $S_1,S_2,\cdots \in\mathcal{F}$ 两两不交，则 $\displaystyle P\left(\bigcup_{k=1}^{+\infty} S_k\right)=\sum_{k=1}^{+\infty}P(S_k)$

则称 $P$ 为可测空间 $(\Omega,\mathcal{F})$ 上的一个 **测度**。  
测度可以形象地理解为集合的“大小“（比如元素个数、长度、面积、体积）。特别地，若测度 $P$ 满足

$$P(\Omega)=1$$

则称 $(\Omega,\mathcal{F},P)$ 为 **概率空间**，测度 $P$ 称为 **概率**，$\Omega$ 称为 **样本空间**，$\mathcal{F}$ 称为 **事件空间**，  
$S\in\mathcal{F}$ 称为 **事件**，$\varnothing$ 和 $\Omega$ 分别称为 **必然事件** 和 **不可能事件**。

概率空间定义的核心在于，通过事件空间 $\mathcal{F}$ 使得只有 $\Omega$ 的特定子集能定义概率。

读者不难验证可测空间满足

1. $\Omega=\overline{\varnothing}\in\mathcal{F}$
2. 若 $S_1,S_2,\cdots \in\mathcal{F}$，则 $\displaystyle\bigcap_{k=1}^{+\infty} S_k=\overline{\bigcup_{k=1}^{+\infty}\overline{S_k}}\in\mathcal{F}$

测度满足

1. 单调性：$A,B\in\mathcal{F},A\subseteq B\Rightarrow P(A)\le P(B)$   
2. 容斥原理：若 $S\subseteq\mathcal{F}$ 且 $|S|$ 有限，则
   $$P\left(\bigcup_{A\in S}A\right)=\sum_{T\subseteq S}(-1)^{|T|-1}P\left(\bigcap_{A\in T}A\right)$$

概率满足

1. $\forall S\in\mathcal{F},P(S)\in[0,1]$
2. $P(\overline{S})=1-P(S)$

### 条件概率与独立性

定义事件 $A$ 在事件 $B$ 发生的条件下的概率

$$P(A|B)=\dfrac{P(AB)}{P(B)}$$

显然，条件概率的实质，是将样本空间从 $\omega$ 缩小为 $B$（对应事件发生），  
然后在新的事件空间下计算概率。

由此定义，对于若干个事件 $A_1,A_2,\cdots,A_n$，若

$$\forall S\subseteq\{1,2,\cdots,n\},k\in S,P\left(A_k|\bigcup_{i\in\complement_S\{k\}}A_i\right)=P(A_k)$$

即

$$\forall S\subseteq\{1,2,\cdots,n\},P\left(\bigcup_{k\in S}A_k\right)=\prod_{k\in S}P(A_k)$$

则称事件 $A_1,A_2,\cdots,A_n$ **相互独立**。

---------------------------------------------

事件空间不断缩小的过程，恰好对应现实世界中的决策过程：

1. 设当前的样本空间为 $S$，目标是让事件 $T$ 发生。
2. 将当前可做的决策以 $1,2,\cdot,n$ 标号，我们需要根据每个决策下 $T$ 发生的概率
   $$P(T|S\cap A_1),P(T|S\cap A_2),\cdots,P(T|S\cap A_n)$$
   确定选取某个决策的概率
   $$P(A_1|S),P(A_2|S),\cdots,P(A_n|S)$$
   从而保证 $P(T|S)$ 最大化。
3. 当作出决策 $k$ 后，发生了一系列不可控的事件导致样本空间缩小为 $S'$，相应地对
   $$P(T|S'\cap A_1),P(T|S'\cap A_2),\cdots,P(T|S'\cap A_n)$$
   作出修正。

这一决策过程的关键在于对概率作出修正，根据条件概率的定义自然有 **贝叶斯公式**

$$P(A|B)=\dfrac{P(A)P(B|A)}{P(B)}$$

也就是说，设当前状态下发生了事件 $B$，我们能通过事件 $A$ 发生的前提下事件 $B$ 发生的概率，  
校正得到当前状态下事件 $A$ 发生的概率。

## 随机变量与数学期望

除了研究随机事件发生的概率，在应用中我们还需要研究随机事件的数字特征。

在概率空间的基础上，我们将 **（一元）随机变量** $X:\Omega\rightarrow\R$ 定义为从样本空间 $\Omega$ 的映射。  
这样一来，我们便可以研究随机变量取某个值的概率

$$P(X=k)$$

多元随机变量 $(X,Y)$ 则相应地定义为一元随机变量所组成的向量。  
特别地，定义随机变量相互独立当且仅当取值的概率相互独立，即

$$P(X=i,Y=j,Z=k)=P(X=i)P(Y=j)P(Z=k)$$

随机变量的核心特征是它的 **数学期望**。其定义为

$$E(X)=\sum_k kP(X=k)$$

显然期望满足以下性质：

1. 对于 $f:\R\rightarrow\R,\displaystyle E(f(X))=\sum_kf(k)P(X=k)$
2. 期望的线性性：$E(X+Y)=E(X)+E(Y),E(\lambda X)=\lambda E(X)\quad(\lambda\in\R)$
   > 证明：$E(X+Y)=\displaystyle\sum_{k}kP(X+Y=k)=\sum_{i,j}(i+j)P(X=i,Y=j)$
   > $$=\sum_i i\sum_j P(X=i,Y=j)+\sum_j j\sum_iP(X=i,Y=j)$$
   > $$=\sum_iiP(X=i)+\sum_jjP(Y=j)=E(X)+E(Y)$$
   > $$E(\lambda X)=\sum_k\lambda kP(X=k)=\lambda\sum_k kP(X=k)=\lambda E(X)$$
   > 由以上证明可知，**期望的线性性与 $X,Y$ 是否相互独立无关**。
3. $X,Y$ **相互独立** 时，$P(XY)=P(X)P(Y)$
   > 证明：$P(XY)=\displaystyle\sum_{k}kP(XY=k)=\sum_{i,j}ijP(X=i,Y=j)$
   > $$=\sum_{i,j}ijP(X=i)P(Y=j)=\sum_iiP(X=i)\sum_jjP(Y=j)=P(X)P(Y)$$

-----------------------------------

随机变量的另外两个常用特征是它的 **方差** 和 **协方差**，它们的定义分别为

$$D(X)=E((X-E(X))^2)$$

$$\mathrm{Cov}(X,Y)=E((X-E(X))(Y-E(Y)))$$

则显然二者均可以通过期望计算

$$D(X)=E(X^2)-(E(X))^2$$

$$\mathrm{Cov}(X,Y)=E(XY)-E(X)E(Y)$$

如果将随机变量视为向量，那么，协方差 $\mathrm{Cov}$ 就可以一定程度上视为这一线性空间上的内积，  
因为其显然满足

- 对称性：$\mathrm{Cov}(X,Y)=\mathrm{Cov}(Y,X)$
- 线性性：$\mathrm{Cov}(X+Y,Z)=\mathrm{Cov}(X,Z)+\mathrm{Cov}(Y,Z)$  
  $\qquad\quad \ \mathrm{Cov}(\lambda X,Y)=\lambda\mathrm{Cov}(X,Y)\quad(\lambda\in\R)$
- 正定性：$\mathrm{Cov}(X,X)=D(X)\ge 0$，当且仅当 $X=E(X)$ 时取等号。

由此可定义两个随机变量的 **相关系数** 为二者夹角的余弦值

$$\rho_{X,Y}=\dfrac{\mathrm{Cov}(X,Y)}{\sqrt{D(X)D(Y)}}$$

方差可以用于衡量随机变量取值的离散程度，而协方差则可用于衡量两个随机变量的独立性。  
显然，当 $X,Y$ 相互独立时 $\mathrm{Cov}(X,Y)=0$。

### 离散型随机变量

只有有限个或者可数个取值的随机变量称为 **离散型随机变量**。

处理离散型随机变量的核心工具是 **概率生成函数**，其定义为

$$f_X(x)=\sum_kP(X=k)x^k$$

显然，概率生成函数可以用于计算离散型随机变量的数学期望

$$f'_X(1)=\sum_kP(X=k)k=E(X)$$

进一步地，对于任意下降幂多项式 $\displaystyle\sum_k g_kx^{\underline{k}}$，均有

$$E\left(\sum_k g_kX^{\underline{k}}\right)=\sum_kg_kf_X^{(k)}(1)$$

此外，概率生成函数还满足如下性质：

- $f_{\lambda X}(x)=f_X(x^\lambda)\ (\lambda\in \R)$
- 当 $f_X$ 为形式洛朗级数时，有
  $$\dfrac{f_X(x)}{1-x}=\sum_kP(X\le k)x^k$$
  进一步地，若 $X,Y$ 相互独立，$f_Y$ 亦为形式洛朗级数时，设 $Z=\max(X,Y)$，则
  $$\dfrac{f_Z(z)}{1-z}=\left(\dfrac{f_X(z)}{1-z}\right)\cdot\left(\dfrac{f_Y(z)}{1-z}\right)$$
  其中 $\cdot$ 表示系数对应位置相乘。

---------------------------------------------------

相应地，定义 **多元概率生成函数** 为

$$f_{X,Y}(x,y)=\sum_{i,j}P(X=i,Y=j)x^iy^j$$

多元概率生成函数用于处理一元概率生成函数。我们有

- $f_X(x)=f_{X,Y}(x,1),f_Y(y)=f_{X,Y}(1,y)$
- $X,Y$ 相互独立当且仅当 $f_{X,Y}(x,y)=f_X(x)f_Y(y)$
- 设 $Z=X+Y$，则
  $$f_Z(z)=f_{X,Y}(z,z)$$
  特别地，当 $X,Y$ 相互独立时，有
  $$f_Z(z)=f_X(z)f_Y(z)$$
  也就是说，相互独立的离散型随机变量的和对应概率生成函数的积。

### 连续型随机变量

对于任意随机变量 $X$，我们均可以定义其 **概率分布函数**

$$F_X(x)=P(X\le x)$$

对于非离散型随机变量 $X$，我们定义若存在非负可积函数 $f_X(x)$ 使得

$$F_X(x)=\int_{-\infty}^xf_X(t)\mathrm{d}t$$

则称 $X$ 为 **连续型随机变量**，这样的函数 $f_X(x)$ 称为 $X$ 的 **概率密度**。也就是说

$$f_X(x)=F'_X(x)$$

对于连续型随机变量，严格意义上讲其任意一点的概率 $P(X=t)$ 均为 $0$。  
但我们可以近似认为

$$P(X=t)=\mathrm{d}P(X\le t)=f_X(t)\mathrm{d}t$$

在这个意义上，连续型随机变量的数学期望为

$$E(X)=\int_{-\infty}^{+\infty}xf_X(x)\mathrm{d}x$$

进一步地，对于 $g:\R\rightarrow\R$，有

$$E(g(X))=\int_{-\infty}^{+\infty}g(x)f_X(x)\mathrm{d}x$$

> 练习：
> 1. 试证明，若 $g:\R\rightarrow\R$ 单调递增，则
> $$F_{g(X)}(x)=F_X(g^{\left<-1\right>}(x)),f_{g(X)}(x)=\dfrac{f_X(g^{\left<-1\right>}(x))}{g'(g^{\left<-1\right>}(x))}$$
> 2. 试证明当 $\lambda>0$ 时
>    $$F_{\lambda X}(x)=F_X\left(\dfrac{x}{\lambda}\right),f_{\lambda X}(x)=\dfrac{1}{\lambda}f_X\left(\dfrac{x}{\lambda}\right)$$
>    当 $\lambda<0$ 时
>    $$F_{\lambda X}(x)=1-F_X\left(\dfrac{x}{\lambda}\right),f_{\lambda X}(x)=-\dfrac{1}{\lambda}f_X\left(\dfrac{x}{\lambda}\right)$$

--------------------------------------------

相应地，对于多个随机变量，可以定义 **联合分布函数**

$$F_{X,Y}(x,y)=P(X\le x,Y\le y)$$

和 **联合概率密度**

$$F_{X,Y}(x,y)=\int_{-\infty}^y\int_{-\infty}^xf_{X,Y}(s,t)\mathrm{d}s\mathrm{d}t$$

联合分布和联合概率密度同样用于处理一元概率分布和概率密度：

- 边缘分布：$\displaystyle f_X(x)=\int_{-\infty}^{+\infty}f_{X,Y}(x,y)\mathrm{d}y,f_Y(y)=\int_{=\infty}^{+\infty}f_{X,Y}(x,y)\mathrm{d}x$
- $F_{X,Y}(x,y)=F_X(x)F_Y(y)\Leftrightarrow X,Y$ 相互独立 $\Leftrightarrow f_{X,Y}(x,y)=f_X(x)f_Y(y)$
- 定义 **条件概率分布**
  $$F_{X|Y}(x|y)=P(X\le x|Y=y)$$
  则其导数 **条件概率密度**
  $$f_{X|Y}(x|y)=\dfrac{f_{X,Y}(x,y)}{f_{Y}(y)}$$
- 和的概率密度：设 $Z=X+Y$，则
  $$f_Z(z)=\int_{-\infty}^{+\infty}f_{X,Y}(t,z-t)\mathrm{d}t$$
  特别地，当 $X,Y$ 相互独立时，有
  $$f_Z(z)=\int_{-\infty}^{+\infty}f_X(t)f_Y(z-t)\mathrm{d}t$$
  也就是说，相互独立的连续型随机变量的和对应概率密度的卷积。
- $\max$ 的概率分布：设 $Z=\max(X,Y)$，则  
  $$F_Z(z)=F_{X,Y}(z,z)$$
  特别地，当 $X,Y$ 相互独立时，有
  $$F_Z(z)=F_X(z)F_Y(z)$$

> 练习：试证明，对于连续型随机变量 $X,Y$，设 $Z=XY$，则
> $$f_Z(z)=\int_{-\infty}^{+\infty}\dfrac{1}{t}f_{X,Y}\left(t,\dfrac{z}{t}\right)\mathrm{d}t$$

### 矩母函数

不论是离散型随机变量还是连续型随机变量，我们都可以定义其 **矩母函数**

$$M_X(x)=\sum_{k=0}^{+\infty}\dfrac{E(X^k)}{k!}x^k$$

则对于离散型随机变量，矩母函数对应 $f_X(e^x)$

$$f_X(e^x)=\displaystyle\sum_i P(X=i)e^{ix}=\sum_iP(X=i)\sum_{k=0}^{+\infty}\dfrac{i^k}{k!}x^k$$

$$=\sum_{k=0}^{+\infty}\dfrac{x^k}{k!}\sum_iP(X=i)i^k=\sum_{k=0}^{+\infty}\dfrac{x^k}{k!}E(X^k)=M_X(x)$$

对于连续型随机变量，矩母函数对应拉普拉斯变换

$$M_X(x)=\sum_{k=0}^{+\infty}\dfrac{E(X^k)}{k!}x^k=\sum_{k=0}^{+\infty}\dfrac{x^k}{k!}\int_{-\infty}^{+\infty}t^kf_X(t)\mathrm{d}t$$

$$=\int_{-\infty}^{+\infty}\left(\sum_{k=0}^{+\infty}\dfrac{(xt)^k}{k!}\right)f_X(t)\mathrm{d}t=\int_{-\infty}^{+\infty}e^{xt}f_X(t)\mathrm{d}t=(\mathcal{L}^{-1}(f_X))(x)$$

可见，只要确定了随机变量的类型（离散型或连续型），  
矩母函数和随机变量的分布总是一一对应的。

由于矩母函数的系数为 $E(X^k)$，其能有效解决复杂的期望问题。