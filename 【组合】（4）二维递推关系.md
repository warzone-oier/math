# 斯特林数

我们已经知道了，下降幂多项式可用多项式乘法转化为普通幂多项式，  
普通幂多项式可用多项式除法转化为下降幂多项式。

两类多项式的转化导出了所谓 **斯特林数**。它是一种常见的二维递推数列。

## 第一类斯特林数

$$ x^{\underline{n}}=\sum_{k=0}^{+\infty}{n\brack k}(-1)^{n-k}x^k\qquad(1)$$

其组合意义是：可以分解成 $k$ 个轮换的 $n$ 元置换个数。

### 递推式

$${n\brack k}={n-1\brack k-1}+(n-1){n-1\brack k}$$

其组合意义是：$n$ 个元素成 $k$ 个轮换，既可以最后一个元素单独成一个轮换（即 $\displaystyle{n\brack k}$），  
也可以前 $n-1$ 个元素已经成了 $k$ 个轮换，然后最后一个元素插入到这 $k$ 个轮换中  
（长度为 $c$ 的轮换有 $c$ 个空隙可以插入，因此总的空隙数为 $n-1$，方案数即 $\displaystyle(n-1){n-1\brack k}$）

递推式和 $(1)$ 式是等价的，因为

$$x^{\underline{n}}=x^{\underline{n-1}}(x-n+1)$$

$${n\brack k}=(-1)^{n-k}[x^k]x^{\underline{n}}=(-1)^{n-k}[x^k]x^{\underline{n-1}}(x-n+1)$$
$$=(-1)^{n-k}[x^{k-1}]x^{\underline{n-1}}-(-1)^{n-k}(n-1)[x^k]x^{\underline{n}}={n-1\brack k-1}+(n-1){n-1\brack k}$$

### 与上升幂的关系

同下降幂，定义 **上升幂**

$$x^{\overline{n}}=\begin{cases}
	x(x+1)\cdots(x+n-1)&(n>0)\\
	1&(n=0)\\
	\dfrac{1}{(x-1)(x-2)\cdots(x-(-n))}&(n<0)
\end{cases}$$

则

$$(-x)^{\underline{n}}=(-1)^nx^{\overline{n}}$$

因此

$$x^{\overline{n}}=(-1)^n(-x)^{\underline{n}}=(-1)^n\sum_{k=0}^{+\infty}{n\brack k}(-1)^{n-k}(-x)^k$$

$$x^{\overline{n}}=\sum_{k=0}^{+\infty}{n\brack k}x^k\qquad(2)$$

这就是第一类斯特林数与上升幂的关系。

### 求一列或一行

一行第一类斯特林数 $\displaystyle{n\brack 0},{n\brack 1},\cdots,{n\brack n}$ 的生成函数由 $(2)$ 式可得就是 $x^{\overline{n}}$。  
显然可以 $\Theta(n\log^2 n)$ 分治 FFT 求解，也可以考虑倍增 $\Theta(n\log n)$ 求解：

$$x^{\overline{2n}}=x^{\overline{n}}(x+n)^{\overline{n}}$$

$$f(x+c)=\sum_{k}f_k(x+c)^k=\sum_{k}f_k\sum_i\dbinom{k}{i}c^{k-i}x^i=\sum_i\dfrac{x^i}{i!}\sum_k k!f_k\dfrac{c^{k-i}}{(k-i)!}$$
$$=\sum_i\dfrac{x^i}{i!}\sum_k(k+i)!f_{k+i}\dfrac{c^k}{k!}$$

对于一列第一类斯特林数，$n$ 个元素划分为 $k$ 个轮换的方案数为 $\displaystyle{n\brack k}$，而轮换的指数生成函数为 $-\ln(1-x)$，因此

$$\sum_{i=0}^{+\infty}{i\brack k}\dfrac{x^i}{i!}=(-\ln(1-x))^k$$

于是可以用多项式快速幂 $\Theta(n\log n)$ 求得一列。

## 第二类斯特林数

$$x^n=\sum_{k=0}^{+\infty}{n\brace k}x^{\underline{k}}\qquad(3)$$

其组合意义是：将 $n$ 个元素划分为 $k$ 个无标号非空集合的方案数。  

$(3)$ 式可以用组合意义说明：将 $n$ 个球放到 $x$ 个盒子里（即 $x^n$），  
相当于先将 $n$ 个球划分为 $k$ 个无标号非空集合（即 $\displaystyle{n\brace k}$）,  
再将这些非空集合一一分配给盒子，每个盒子至多分配一个集合（即 $x^{\underline{k}}$）

### 递推式

$${n\brace k}={n-1\brace k-1}+k{n-1\brace k}$$

其组合意义是：$n$ 个元素成 $k$ 个无标号非空集合，最后一个元素可以单独成一个集合（即 $\displaystyle{n-1\brace k-1}$），  
也可以插入到已有的集合中（即 $\displaystyle k{n-1\brace k}$）。

### 通项公式

$(2)$ 式等价于

$$n^m=\sum_{k=0}^{+\infty}{m\brace k}\dbinom{n}{k}k! =\sum_{k=0}^{n}\dbinom{n}{k}{m\brace k}k!$$

根据二项式反演（详见【组合】（5）反演变换）可得

$${m\brace n}n! =\sum_{k=0}^{n}\dbinom{n}{k}(-1)^{n-k}k^m$$

$${n\brace m}=\dfrac{1}{m!}\sum_{k=0}^m\dbinom{m}{k}(-1)^{m-k}k^n$$

这就是第二类斯特林数的通项公式。

我们也可通过对递推式建立迭代列得到相同的结果。

### 求一列或一行

对于一行第二类斯特林数，由通项公式得

$${n\brace m}=\sum_{k=0}^m\dfrac{(-1)^{m-k}}{(m-k)!}\times\dfrac{k^n}{k!}$$

因此可以一次 $\Theta(n\log n)$ 卷积求得 $\displaystyle {n\brace 0},{n\brace 1},\cdots,{n\brace n}。$

对于一列第二类斯特林数，$n$ 个元素划分为 $k$ 个无标号非空集合的方案为 $\displaystyle{n\brace k}$，  
而有标号非空集合的指数生成函数为 $e^x-1$，因此

$$\sum_{i=0}^{+\infty}{i\brack k}\dfrac{x^i}{i!}=\dfrac{1}{k!}(e^x-1)^k$$

于是亦可以用多项式快速幂 $\Theta(n\log n)$ 求得一列。

# 二维递推关系的求解

与一维递推关系不同，二维乃至更高维的递推关系的求解方式更为深刻且复杂。  
处理方式有以下三种：

1. 建立关于二元生成函数的函数方程（乃至微分方程）。
2. 建立关于一元幂级数的递推关系（即迭代列）并求解。
3. 使用线性代数建模，这引出了转置原理。

## 二元生成函数

## 迭代列的求解
### 基变换换元法

形如 

$$f_n(x)=axf_{n-1}'(x)$$

的递推是我们已经解决的问题：

$$f_{n,k}=akf_{n-1,k}=(ak)^nf_{0,k}$$

而对于形如

$$f_n(x)=A(x)f_{n-1}'(x)\quad(A_0=0,A_1\not =0)$$

的递推，则可以使用所谓的 **基变量换元法**：设

$$f_n(x)=g_n(u(x))$$

其中 $u(x)$ 为满足 $u_0=0$ 且 $u_1\not=0$ 的形式幂级数。则

$$g_n(u(x))=A(x)(g_{n-1}(u(x)))'=A(x)g'_{n-1}(u(x))u'(x)$$

$$=A(x)\dfrac{u'(x)}{u(x)}\times u(x)g'_{n-1}(u(x))$$

解微分方程 $A(x)\dfrac{u'(x)}{u(x)}=A_1$，得

$$\large u(x)=Ce^{\int\frac{A_1\mathrm{d}x}{A(x)}}$$

由于 $A_0=0$ 且 $A_1\not =0,\dfrac{A_1}{A(x)}$ 满足

$$\dfrac{A_1}{A(x)}=\dfrac{1}{x}+B(x)$$

的形式，其中 $B(x)$ 为形式幂级数。因此

$$u(x)=Ce^{\ln x+\int B(x)\mathrm{d}x}=Cxe^{\int B(x)\mathrm{d}x}$$

于是令 $u(x)=xe^{\int B(x)\mathrm{d}x}$，则

$$g_0(u(x))=f_0(x),g_n(u)=A_1ug'_{n-1}(u)$$

扩展拉格朗日反演求出 $g_0(x)$，递推出 $g_n(x)$ 后重新复合上 $u(x)$ 即可得到 $f_n(x)$。

基变换换元法也可推广到如下递推式

$$f_n(x)=A(x)f'_{n-1}(x)+b(x)\quad(A_0=0,A_1\not =0)\Rightarrow g_n(x)=axg'_{n-1}(x)+c(x)$$

$$f_n(x)=A(x)f'_{n-1}(x)\quad(A_0\not =0)\Rightarrow g_n(x)=ag'_{n-1}(x)$$

$$f_n(x)=x^tA(x)f'_{n-1}(x)\quad(A_0\not=0,t\in\N)\Rightarrow g_n(x)=x^tg_{n-1}'(x)$$

### 附加因子法

附加因子法用于解决递推式中同时含有 $f'_{n-1}(x)$ 和 $f_{n-1}(x)$ 的情形，如

$$f_n(x)=A(x)f_{n-1}'(x)+B(x)f_n(x)+b(x)$$

在一阶线性微分方程的常数变易法的基础上，设

$$f_n(x)=g_n(x)e^{-\int\frac{B(x)}{A(x)}\mathrm{d}x}$$

则

$$g_n(x)e^{-\int\frac{B(x)}{A(x)}\mathrm{d}x}=A(x)g'_{n-1}e^{-\int\frac{B(x)}{A(x)}\mathrm{d}x}-B(x)g_{n-1}(x)e^{-\int\frac{B(x)}{A(x)}\mathrm{d}x}+B(x)g_{n-1}(x)e^{-\int\frac{B(x)}{A(x)}\mathrm{d}x}+b(x)$$

$$g_n(x)e^{-\int\frac{B(x)}{A(x)}\mathrm{d}x}=A(x)g'_{n-1}e^{-\int\frac{B(x)}{A(x)}\mathrm{d}x}+b(x)$$

$$g_n(x)=A(x)g'_{n-1}(x)+b(x)e^{\int\frac{B(x)}{A(x)}\mathrm{d}x}$$

这样就化简为可以用基变换换元处理的递推。

## 线性代数建模与转置原理


