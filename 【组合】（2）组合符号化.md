# 普通生成函数（无标号体系）

**组合类** $(\mathcal{A},|\cdot|)$ 是一个集合 $\mathcal{A}$ 配备了一个映射 $|\cdot|:\mathcal{A}\rightarrow\N$（称为 **大小函数**），满足对于任意非负整数 $n$，  

$$A_n=|\mathcal{A}_n|=|\{\alpha\in\mathcal{A}||\alpha|=n\}|$$

均有限。组合类的元素称为 **组合对象**。

显然，$A_0,A_1,A_2,\cdots$ 构成一个序列，我们定义组合类 $\mathcal{A}$ 的（普通）生成函数  
就是这个序列的（普通）生成函数

$$\mathcal{A}(x)=A(x)=\sum_{\alpha\in\mathcal{A}}x^{|\alpha|}$$

组合类的生成函数与组合类的构造方式息息相关。获取生成函数的第一种方法，  
就是通过组合类的构造方式直接得到其生成函数，即 **组合符号化**。其有两种含义：

- 生成函数的基本运算（加、乘、复合）存在组合意义，可以直接对应到组合对象的构造上。
- 组合对象的一些构造方式不与生成函数的基本运算对应，但可籍此定义对生成函数的特殊变换，  
  这些特殊变换依然可以使用多项式算法（牛顿迭代，半在线卷积）在 $\Theta(n\operatorname{polylog}(n))$ 时间内计算。

------------------------------

最基本的组合对象就是大小为 $0$ 或 $1$ 的组合对象。

大小为 $0$ 的组合对象称为 **中性对象**，一般用 $\epsilon$ 表示（存在多个时可以用 $\epsilon_1,\epsilon_2,\cdots$ 表示），  
大小为 $1$ 的组合对象称为 **原子对象**，一般用 $\circ$ 或 $\bullet$ 表示。

相应地，$\mathcal{E}=\{\epsilon\}$ 称为 **中性类**，$\mathcal{Z}_\circ=\{\circ\}$ 和 $\mathcal{Z}_\bullet=\{\bullet\}$ 称为 **原子类**。显然有

$$\mathcal{E}(x)=1,\mathcal{Z}_\circ(x)=\mathcal{Z}_{\bullet}(x)=x$$

构造同一个组合类可以有多种不同的方法，但得到的生成函数是相同的。  
我们定义两个组合类同构当且仅当二者具有相等的生成函数，即

$$\mathcal{A}\cong\mathcal{B}\Leftrightarrow\mathcal{A}(x)=\mathcal{B}(x)$$

## 基本运算

### 乘法（笛卡尔积）

定义组合对象 $\alpha\in\mathcal{A},\beta\in\mathcal{B}$ 配成的有序对 $(\alpha,\beta)$ 的大小

$$|(\alpha,\beta)|=|\alpha|+|\beta|$$

则 $\mathcal{A\times B}$ 也是组合类，且

$$(\mathcal{A\times B})(x)=\mathcal{A}(x)\times\mathcal{B}(x)$$

即，普通生成函数的乘法的组合意义是两个组合类的笛卡尔积。
这一结论显然可以推广到多个组合类的情形

$$(\mathcal{A_1\times A_2\times\cdots\times A}_n)(x)=\prod_{k=1}^n\mathcal{A}_k(x)$$


### 加法（（不相交）并构造）

对于组合类 $\mathcal{A},\mathcal{B}$，当 $\mathcal{A}\cap\mathcal{B}=\varnothing$ 时，显然有

$$(\mathcal{A}\cup\mathcal{B})(x)=\mathcal{A}(x)+\mathcal{B}(x)$$

也即，普通生成函数的加法的组合意义是两个不相交组合类的并。

为了推广到 $\mathcal{A\cap B}\not=\varnothing$ 的情况，设置两个不同的中性类 $\mathcal{E_0,E_1}$，并定义组合类的加法

$$\mathcal{A+B=(E_0\times A)\cup(E_1\times B)}$$

即，将 $\mathcal{A}$ 中的元素染成红色，将 $\mathcal{B}$ 中的元素染成蓝色。则同样有

$$(\mathcal{A}+\mathcal{B})(x)=\mathcal{A}(x)+\mathcal{B}(x)$$

### 左复合 $\dfrac{1}{1-x}$（Sequence 构造）

有时，我们想从组合类 $\mathcal{A}$ 中选取元素排成不定长度的序列，这引出了组合类的 Sequence 构造。

若组合类 $\mathcal{A}$ 满足 $\mathcal{A}_0=\varnothing$（即不存在大小为 $0$ 的对象），则定义其 **Sequence 构造**

$$\mathrm{SEQ}(\mathcal{A})=\{(\alpha_1,\alpha_2,\cdots,\alpha_n)|n\in\N,\alpha_1,\alpha_2,\cdots,\alpha_n\in\mathcal{A}\}$$

也就是说

$$\mathrm{SEQ}(\mathcal{A})=\mathcal{E+A+A\times A+A\times A\times A+\cdots}$$

$$
(\mathrm{SEQ}(\mathcal{A}))(x)=1+\mathcal{A}(x)+(\mathcal{A}(x))^2+\cdots=\dfrac{1}{1-\mathcal{A}(x)}
$$

即，普通生成函数左复合 $\dfrac{1}{1-x}$ 的组合意义就是 Sequence 构造。

## 特殊变换

### Pólya 指数·改（Powerset 构造）

除了从 $\mathcal{A}$ 中选取元素排成序列，  
我们还需要支持从 $\mathcal{A}$ 中选取元素组成集合。

显然，集合就是元素按固定顺序排好的序列。我们无需再额外定义 $\mathcal{A}$ 的子集的大小，  
就可以直接定义组合类 $\mathcal{A}$ 的 **Powerset 构造**

$$\mathrm{PSET}(\mathcal{A})=\prod_{\alpha\in\mathcal{A}}\{\epsilon,\alpha\}$$

于是

$$(\mathrm{PSET}(\mathcal{A}))(x)=\prod_{\alpha\in\mathcal{A}}(1+x^{|\alpha|})$$

故定义变换 **Pólya 指数·改**

$$(\operatorname{\overline{Exp}}f)(x)=\prod_k(1+x^k)^{f_k}$$


则 Pólya 指数·改 的组合意义就是 Powerset 变换，即

$$(\mathrm{PSET}(\mathcal{A}))(x)=(\operatorname{\overline{Exp}} A)(x)$$

Pólya 指数·改不方便直接计算，不妨设 $f_0=0$，等式两边同时取对数

$$\ln (\operatorname{\overline{Exp}}f)(x)=\sum_k f_k\ln(1+x^k)=\sum_kf_k\sum_i\dfrac{(-1)^{i-1}x^{ki}}{i}$$

$$=\sum_i\dfrac{(-1)^{i-1}}{i}\sum_kf_kx^{ki}=\sum_i\dfrac{(-1)^{i-1}}{i}f(x^i)$$

不难看出 Pólya 指数·改 是一个可以 $\Theta(n\log n)$ 计算的可逆变换，因为

$$[x^n]\ln (\overline{\mathrm{Exp}} f)(x)=\sum_{i|n}\dfrac{(-1)^{i-1}}{i}f_{\frac{n}{i}}$$

就是一个狄利克雷卷积，且 $\dfrac{(-1)^{n-1}}{n}$ 是积性函数。

### Pólya 指数/Euler 变换（Multiset 构造）

同 Powerset 构造，当 $\mathcal{A}_0=\varnothing$ 时，从 $\mathcal{A}$ 中选取元素组成可重集，得到的是组合类 $\mathcal{A}$ 的 **Multiset 构造**

$$\mathrm{MSET}(\mathcal A)=\prod_{\alpha\in\mathcal{A}}\mathrm{SEQ}(\{\alpha\})$$

其对应的生成函数变换称为 **Pólya 指数**，又称 **Euler 变换**

$$(\operatorname{Exp}f)(x)=\prod_k\dfrac{1}{(1-x^k)^{f_k}}$$

同理，等式两边同时取对数，即可发现 Pólya 指数同样是可以 $\Theta(n\log n)$ 计算的可逆变换

$$\ln (\operatorname{Exp}f)(x)=-\sum_k f_k\ln(1-x^k)=\sum_k f_k\sum_i \dfrac{x^{ki}}{i}$$

$$=\sum_i\dfrac{1}{i}\sum_kf_kx^{ki}=\sum_i\dfrac{f(x^i)}{i}$$

$$[x^n]\ln(\operatorname{Exp}f)(x)=\sum_{i|n}\dfrac{f_{\frac{n}{i}}}{i}$$


> 练习：
> 1. 试给出 Euler 逆变换的表达式。
> 2. 若 $\mathcal{A}_0=\varnothing$，定义组合类 $\mathcal{A}$ 的 **Amplification
构造**
>    $$\mathrm{AMP}(\mathcal{A})=\bigcup_{\alpha\in\mathcal{A}}\mathrm{SEQ}(\{\alpha\})$$
>    试证明
>    $$(\mathrm{AMP}(\mathcal{A}))(x)=\sum_{k=1}^{+\infty}\mathcal{A}(x^k)$$

---------------------------

接下来关注一个具体的计数问题——整数分拆问题。

给定正整数 $n\in\N_+$，欲求有多少种方案将 $n$ 拆成若干正整数  
（可以相等，但无序，即组成可重集）的和。即，我们希望求解

$$\left|\left\{(c_1,c_2,\cdots)|c_1,c_2,\cdots\in\N,\sum_{k=1}^{+\infty}kc_k=n\right\}\right|$$

显然设 $\mathcal{A}_n$ 为正整数 $n$ 的所有分拆方案构成的集合，  
令 $n\in\N_+$ 的大小就是 $n$ 本身，则由 Multiset 构造得

$$\mathcal{A}=\mathrm{MSET}(\N_+)$$

$$\mathcal{A}(x)=\operatorname{Exp} \dfrac{x}{1-x}=\prod_{k=1}^{+\infty}\dfrac{1}{1-x^k}$$

如何在 $\Theta(n\operatorname{polylog}(n))$ 的时间内求解其前 $n$ 项？我们有

> **五边形数定理**：$\displaystyle\dfrac{1}{\mathcal{A}(x)}=\prod_{k=1}^{+\infty}(1-x^k)=\sum_{k=-\infty}^{+\infty}(-1)^kx^{\frac{3k^2-k}{2}}$

也就是说，整数分拆的生成函数的倒数中，次数 $\le n$ 的非零项只有 $\Theta(\sqrt{n})$ 个。

### Pólya 对数（Cycle 构造）

注意到，含 $n$ 个元素的可重集可视作长度为 $n$ 的序列在 $n$ 元对称群 $S_n$ 作用下的等价类。  
也就是说，Pólya 指数可以通过对 Sequence 构造使用 Pólya 定理得到。这就是 Pólya 指数名字的来历。

这一做法比直接使用 Multiset 构造的定义要复杂得多，但具有一定的启发性。  

$\mathcal{A}_0=\varnothing$ 时，考虑从 $\mathcal{A}$ 中选取元素串成有向环。定义置换 $\tau:\complement_{\mathrm{SEQ}(\mathcal{A})}\mathcal{E}\rightarrow\complement_{\mathrm{SEQ}(\mathcal{A})}\mathcal{E}$ 满足

$$\tau((\alpha_1,\alpha_2,\cdots,\alpha_{n-1},\alpha_n))=(\alpha_2,\alpha_3,\cdots,\alpha_n,\alpha_1)$$

则 $\left<\tau\right>$ 是 $\complement_{\mathrm{SEQ}(\mathcal{A})}\mathcal{E}$ 上的置换群，且该置换群作用下等价的序列具有相同的大小，  
于是将这个大小定义为整个等价类的大小，并定义 $\mathcal{A}$ 的 **Cycle 构造**

$$\mathrm{CYC}(\mathcal{A})=(\complement_{\mathrm{SEQ}(\mathcal{A})}\mathcal{E})/\left<\tau\right>$$

显然，枚举环长 $m$ 和轮换的步长 $k$，则由 Pólya 定理得

$$(\mathrm{CYC}(\mathcal{A}))(x)=\sum_m\dfrac{1}{m}\sum_{k=1}^m(\mathcal{A}(x^{\frac{m}{\gcd(k,m)}}))^{\gcd(k,m)}=\sum_m\dfrac{1}{m}\sum_{i|m}(\mathcal{A}(x^{\frac{m}{i}}))^{i}\sum_{k=1}^{\frac{m}{i}}[\gcd\left(k,\dfrac{m}{i}\right)=1]$$

$$=\sum_m\dfrac{1}{m}\sum_{i|m}(\mathcal{A}(x^{\frac{m}{i}}))^{i}\varphi\left(\frac{m}{i}\right)=\sum_{i,j}\dfrac{1}{ij}(\mathcal{A}(x^j))^{i}\varphi(j)$$

$$=\sum_j\dfrac{\varphi(j)}{j}\sum_i(\mathcal{A}(x^j))^{i}=\sum_j\dfrac{-\varphi(j)}{j}\ln(1-\mathcal{A}(x^j))$$

故定义 **Pólya 对数**

$$(\operatorname{Ln} f)(x)=\sum_k\dfrac{-\varphi(k)}{k}\ln(1-f(x^k))$$

则 Pólya 对数的组合意义就是 Cycle 构造，且能在 $\Theta(n\log n)$ 的时间内计算 Pólya 对数

$$[x^n](\operatorname{Ln} f)(x)=\sum_{k|n}\dfrac{\varphi(k)}{k}[x^{\frac{n}{k}}]\ln(1-f(x))$$

### 有限制的构造

许多时候，我们希望对以上所讨论的构造加以限制，如

- 使用 $\mathcal{A}$ 中元素组成长度为 $m$ 的有向环，即 $\mathrm{CYC}_m(\mathcal{A})$
- 使用 $\mathcal{A}$ 中元素组成含 $m$ 个元素（重复按重数计）的可重集，即 $\mathrm{MSET}_m(\mathcal{A})$

有些带限制的构造是可以根据组合意义直接表示出其生成函数的，如

$$(\mathrm{CYC}_m(\mathcal{A}))(x)=\dfrac{1}{m}\sum_{k=1}^m(\mathcal{A}(x^{\frac{m}{\gcd(k,m)}}))^{\gcd(k,m)}=\dfrac{1}{m}\sum_{i|m}\varphi(i)(\mathcal{A}(x^i))^{\frac{m}{i}}$$

另一些些带限制的构造，如 $\mathrm{MSET}_m(\mathcal{A})$，则不能根据组合意义直接表示出其生成函数。  
不妨考虑使用二元生成函数，附加一个元 $y$ 统计可重集的元素个数，得

$$(\mathrm{MSET}_m(\mathcal{A}))(x)=[y^m]\prod_{\alpha\in\mathcal{A}}\dfrac{1}{(1-x^{|\alpha|}y)^{|\alpha|}}=[y^m]\prod_k\dfrac{1}{(1-x^ky)^{A_k}}$$

$$=[y^m]\exp\left(-\sum_kA_k\ln(1-x^ky)\right)=[y^m]\exp\sum_{k,i}\dfrac{A_kx^{ki}y^i}{i}=[y^m]\exp\sum_i\dfrac{y^i}{i}A(x^i)$$

> 练习：
> 1. 使用一元生成函数表示 $(\mathrm{SEQ}_m(\mathcal{A}))(x)$
> 2. 使用二元生成函数表示 $(\mathrm{PSET}_m(\mathcal{A}))(x)$

## 指数生成函数（有标号体系）

使用普通生成函数处理的是无标号体系。所谓无标号体系，  
指的是用于构建组合类的基本对象是全同的，只能在大小、位置、颜色等方面加以区分。例如

- 在无标号有根树中，除根结点以外的其他结点没有区别。
- 对于整数分拆
  $$11=1+3+3+4$$
  前一个 $3$ 和后一个 $3$ 没有区别。

虽然这使我们得以解决基本对象全同带来的对称性，但在实际情况中，  
我们还需要处理有标号的情况，即

1. 大小为 $n$ 的组合对象由 $n$ 个原子对象构成。
2. 原子对象通过唯一的编号（从 $1$ 到 $n$）加以区分。

例如，有标号无根树是一个典型的有标号组合对象。

一个自然的想法是，如果将编号用颜色加以取代，例如，将 $1$ 号点染成绿色，将 $2$ 号点染成红色，  
将 $3$ 号点染成蓝色，$4$ 号点又染成红色 $\cdots$，染色完成后，抹去所有结点的编号，就能将有标号对象转换成无标号对象。  
由于每个点被染成哪种颜色是不确定的，最终得到的有标号对象也是不确定的。 

由此定义，给定原子对象 $\bullet_1,\bullet_2,\bullet_3,\cdots$，若对于任意的 $\bullet_{p_1},\bullet_{p_2},\cdots,\bullet_{p_n}$，  
我们总能使用 $\bullet_{p_1},\bullet_{p_2},\cdots,\bullet_{p_n}$ 构建出组合类

$$\mathcal{S}(\bullet_{p_1},\bullet_{p_2},\cdots,\bullet_{p_n})$$

满足 $\mathcal{S}_n=\mathcal{S}$，则定义一个大小为 $n$ 的有标号组合对象为映射 

$$\alpha(x_1,x_2,\cdots,x_n)$$

即，使用原子对象 $x_1,x_2,\cdots,x_n$，构建出一个 $\mathcal{S}(x_1,x_2,\cdots,x_n)$ 中的无标号对象。且这个映射需要满足

$$\{a_1,a_2,\cdots,a_n\}\not =\{b_1,b_2,\cdots,b_n\}\Rightarrow\alpha(a_1,a_2,\cdots,a_n)\not=\alpha(b_1,b_2,\cdots,b_n)$$

即，使用不同的原子对象，构建出的无标号对象应当是不同的。

有标号组合对象组成的组合类称为 **有标号组合类**。

-------------------------------------

为了处理有标号组合类，定义序列 $f_0,f_1,f_2,\cdots$ 的 **指数生成函数**

$$\hat{f}(x)=\sum_{k}\dfrac{f_k}{k!}x^k$$

相应地，定义有标号组合类 $\mathcal{A}$ 的（指数）生成函数

$$\mathcal{A}(x)=\hat{A}(x)=\sum_{\alpha\in\mathcal{A}}\dfrac{x^{|\alpha|}}{|\alpha|!}$$

使用该定义的原因在于指数生成函数的乘法，将在下文中给出。

### 乘法（二项卷积）

指数生成函数加法的组合意义与普通生成函数相同，而指数生成函数的乘法又称二项卷积

$$\hat{h}=\hat{f}\times\hat{g}\Leftrightarrow h_n=\sum_k\dbinom{n}{k}f_kg_{n-k}$$

上式表明，**指数生成函数实质上是一种特殊的集合幂级数**。如果令集合幂级数 $F,G,H$ 满足

$$F(S)=f_{|S|},G(S)=g_{|S|},H(S)=h_{|S|}$$

那么二项卷积对应子集卷积

$$\hat{h}=\hat{f}\times\hat{g}\Leftrightarrow H=F\times G$$

这样一来，二项卷积的组合意义便呼之欲出。定义两个有标号组合类 $\mathcal{A},\mathcal{B}$ 的 **有标号乘法**

$$\mathcal{AB}=\{(\alpha(x_{p_1},x_{p_2},\cdots,x_{p_n}),\beta(x_{q_1},x_{q_2},\cdots,x_{q_m}))|\alpha\in\mathcal{A},\beta\in\mathcal{B},\\
p_1<p_2<\cdots<p_n,q_1<q_2<\cdots<q_m,\{p_1,p_2,\cdots,p_n,q_1,q_2,\cdots,q_m\}=\{1,2,\cdots,n+m\}\}$$

也就是说

1. 将原子对象 $x_1,x_2,\cdots,x_{n+m}$ 划分成两个 **子序列** $x_{p_1},x_{p_2},\cdots,x_{p_n}$ 和 $x_{q_1},x_{q_2},\cdots,x_{q_m}$。
2. 分别使用两个子序列构建组合对象 $\alpha(x_{p_1},x_{p_2},\cdots,x_{p_n}),\beta(x_{q_1},x_{q_2},\cdots,x_{q_m})$，
   然后配成有序对 $(\alpha,\beta)$。

那么，二项卷积的组合意义就是有标号乘法，即

$$(\mathcal{AB})(x)=\mathcal{A}(x)\mathcal{B}(x)$$

### 指数（Set 构造）和对数（Cycle 构造）

指数生成函数 $\exp$ 的组合意义也与集合幂级数相同

$$n![x^n]e^{\hat{f}(x)}=\sum_m\dfrac{1}{m!}\sum_{\sum_{k=1}^ma_k=n}\dbinom{n}{a_1,a_2,\cdots,a_m}\prod_{k=1}^mf_{a_k}$$

由于标号的存在，有标号体系中 Powerset 构造和 Multiset 构造被统一为构建集合的 **Set 构造** $\mathrm{SET}(\mathcal{A})$，即

1. 将原子对象 $x_1,x_2,\cdots,x_n$ 划分成 $m$ 个无标号非空子序列。
2. 每个子序列各用来构建一个 $\mathcal{A}$ 中的组合对象。由于子序列是无标号的，得到的组合对象自然构成集合。

于是有标号组合类 $\mathcal{A}$ 中选取 $m$ 个元素组成集合的构造

$$(\mathrm{SET}_m(\mathcal{A}))(x)=\dfrac{\mathcal{A}^m(x)}{m!}$$ 

那么

$$(\mathrm{SET}(\mathcal{A}))(x)=\sum_m(\mathrm{SET}_m(\mathcal{A})(x))=\sum_m\dfrac{\mathcal{A}^m(x)}{m!}=\exp\mathcal{A}(x)$$

即指数生成函数 $\exp$ 的组合意义就是 Set 构造。

同样类比集合幂级数和无标号体系，  
指数生成函数 $\ln$ 的组合意义就是在有标号体系中构建有向环的 Cycle 构造

$$(\mathrm{CYC}(\mathcal{A}))(x)=\sum_m\dfrac{1}{m}\mathcal{A}^m(x)=-\ln(1-\mathcal{A}(x))$$

> 练习：试证明，有标号体系中左复合 $\dfrac{1}{1-x}$ 的组合意义仍为构建序列的 Sequence 构造
> $$(\mathrm{SEQ}(\mathcal{A}))(x)=\dfrac{1}{1-\mathcal{A}(x)}$$
> 并同时从组合意义和生成函数角度说明为什么
> $$\mathrm{SET(CYC}\mathcal{(A)})=\mathrm{SEQ}(\mathcal{A})$$

### 复合（Subtitution 构造）

左复合 $\exp,-\ln(1-x),\dfrac{1}{1-x}$ 都是复合的特殊情况。

如果对于任意的 $\alpha(x_1,x_2,\cdots,x_k)\in\mathcal{A}$，  
$x_1,x_2,\cdots,x_k$ 的取值并不局限于原子对象，而是也可以取 $\mathcal{B}$ 中的任意对象 $\beta\in\mathcal{B}$，且

$$|\alpha(\beta_1,\beta_2,\cdots,\beta_k)|=|\beta_1|+|\beta_2|+\cdots+|\beta_k|$$

则定义有标号组合类 $\mathcal{A,B}$ 的 **Subtitution 构造**

$$\mathcal{A\circ B}=\{\alpha(\beta_1,\beta_2,\cdots,\beta_k)|\alpha\in\mathcal{A},(\beta_1,\beta_2,\cdots,\beta_k)\in\mathrm{SET}_k(\mathcal{B})\}$$

即，使用 $\mathcal{B}$ 中的对象作为基本对象构建 $\mathcal{A}$ 中的对象，且 $\beta_1,\beta_2,\cdots,\beta_k$ 的顺序由其被分配的原子对象所唯一确定，  
即，$\beta_1$ 所分配的编号最小的原子对象最小，$\beta_1$ 所分配的编号最小的原子对象次小 $\cdots$ 以此类推。那么

$$(\mathcal{A\circ B})(x)=\sum_k|\mathcal{A}_k|(\mathrm{SET}_k(\mathcal{B}))(x)=\sum_k|\mathcal{A}_k|\dfrac{\mathcal{B}^k(x)}{k!}=\mathcal{A}(\mathcal{B}(x))$$

显然，组合意义上 Set 构造，Cycle 构造和 Sequence 构造也同样都是一种特殊的 Subtitution 构造。

### 求导（Pointing 构造）和积分（Add 构造）

图论中，树的计数不仅有有标号和无标号之分，还有有根和无根之分。

考虑有标号有根树和有标号无根树之间的转换。一棵有标号无根树，只要选择一个点作为根，  
就能转化为有标号有根树，由此得到有标号组合类的 **Pointing 构造**

$$\mathrm{PNT}(\mathcal{A})=\{[\bullet_{x_k},\alpha(x_1,x_2,\cdots,x_n)]|\alpha\in\mathcal{A},k\in\{1,2,\cdots,n\}\}$$

即，将 $n$ 个原子对象中的某一个单独标记出来。其对应指数生成函数求导

$$(\mathrm{PNT}(\mathcal{A}))(x)=\sum_{\alpha\in\mathcal{A}}\dfrac{|\alpha|x^{|\alpha|}}{|\alpha|!}=x\dfrac{\mathrm{d}\mathcal{A}(x)}{\mathrm{d}x}$$

有标号有根树计数就是强制选择 $n$ 号点为根的有标号无根树计数，由此得到有标号组合类的 **Add 构造**

$$(\mathrm{ADD}(\mathcal{A}))(x)=\{(\alpha(x_1,x_2,\cdots,x_{n-1}),\bullet_{x_n})|\alpha\in\mathcal{A}\}$$

即，将 $n$ 号点单独拎出来，剩下的再进行计数。其对应指数生成函数积分

$$(\mathrm{ADD}(\mathcal{A}))(x)=\sum_{\alpha\in\mathcal{A}}\dfrac{x^{|\alpha|+1}}{(|\alpha|+1)!}=\int_0^x\mathcal{A}(t)\mathrm{d}t$$