## 排列、逆序数

集合 $\{1,2,\cdots,n\}$ 上的置换称为 **排列** 。$n$ 称为排列的 **长度**。

排列通常用数列而非函数的方式表示。例如，排列

$$p=\begin{pmatrix}
	1&2&\cdots&n\\
	p_1&p_2&\cdots&p_n
\end{pmatrix}$$

一般表示为

$$p=p_1p_2\cdots p_n$$

对于排列，一个非常重要的指标是它的 **逆序数**。  
若 $i<j$ 且 $p_i>p_j$，则称 $(i,j)$ 为排列 $p$ 的一个 **逆序对**。  
逆序数就是逆序对的个数，即

$$\tau(p)=\sum_{i<j}[p_i>p_j]$$

### 逆序数的性质

1. > $\tau(p)=$ 对排列 $p$ 作冒泡排序的交换次数。    
   > 证明：$\tau(\varepsilon)=0$，且冒泡排序中每一次交换都会刚好使 $\tau(p)$ 减 $1$，命题得证。

2. 将 $p$ 转化为二分图：$p_i$ 相当于 $i\rightarrow p_i$。   
   显然，若 $i<j$ 且 $p_i>p_j$，边 $i\rightarrow p_i$ 和 $j\rightarrow p_j$ 会产生一个交点。    
   因此，**$\tau(p)$ 就是二分图的交点总数。**

3. > $\tau(p)=\tau(p^{-1})$。  
   > 证明：考虑将 $p$ 转化为二分图，则 $p$ 到 $p^{-1}$ 相当于交换左右两边（对称过来）。  
   > $\qquad\quad$ 显然这一过程不改变二分图交点总数，命题得证。

4. > 逆序数为奇数的排列称为 **奇排列**，反之则称为 **偶排列**。交换两个元素的置换称为 **对换**，  
   $\qquad\quad$则（排列复合上）对换改变排列的奇偶性。   
   > 证明：对于相邻对换，交换一定会使 $\tau(p)\pm 1$，命题显然成立。  
   > $\qquad\quad$ 不相邻的对换可以分解为 $2j-2i-1$ 奇数次相邻对换：
   > $$p_i\cdots p_j\ ^{\underrightarrow{(j-i-1) 次相邻对换}} \cdots p_ip_j\ ^{\underrightarrow{1 次相邻对换}}\cdots p_jp_i\ ^{\underrightarrow{(j-i-1) 次相邻对换}}p_j\cdots p_i$$
   > $\qquad\quad$ 总共改变了奇数次 $p$ 的奇偶性，$-1\equiv 1\pmod{2}$，相当于只改变了一次，命题得证。

## 逆序数列


将不易计数的对象与易计数的对象间建立双射，是一种重要的计数方法。  
逆序数及其相关的计数问题与逆序数列密不可分。排列 $p$ 的**逆序数列** $t$ 定义为

$$t_i=\sum_{j<i}[p_j>p_i]$$

即，以 $i$ 为第二个数的逆序对数。显然 $t_i\in\{0,1,\cdots,i-1\}$。

排列与其逆序数列之间是可以相互转换的：

- 排列转换为逆序数列：使用树状数组维护集合 $S$，初始 $S$ 为空。  
  从左往右扫排列，每次先将 $p_i$ 放入 $S$，然后询问 $S$ 中有多少个数 $\le p_i$，结果即 $i-t_i$  。  
- 逆序数列转换为排列：同样使用权值线段树维护集合 $S$，初始 $S=\{1,2,\cdots,n\}$，  
  从右往左扫逆序数列，每次取出 $S$ 中第 $i-t_i$ 小的数，这个数就是 $p_i$。  

二者的时间复杂度均为 $\Theta(n\log n)$。

在此基础上，我们有以下定理：

> 任意一个 $t_i\in\{0,1,\cdots,i-1\}$ 的数列 $t$ 都可作为逆序数列唯一对应一个排列 $p$。  
> 证明：考虑逆序数列转换为排列的过程。显然扫到 $t_i$ 且未确定 $p_i$ 时 $S=\{p_1,p_2,\cdots,p_i\}$。  
> $\qquad\quad$ 因为 $|S|=i,i-t_i\in\{1,2,\cdots,i\}$，故每次总能成功地从 $S$ 中取出第 $i-t_i$ 小的数。  
> $\qquad\quad$ 又因为初始 $S=\{1,2,\cdots,n\}$，且每次取出的数互不相同，故最后得到的一定是一个长度为 $n$ 的排列。

于是，我们建立了排列 $p$ 与 $t_i\in\{0,1,\cdots,i-1\}$ 的数列 $t$ 之间的双射，  
这有助于我们解决逆序数相关的计数问题。如：

- 康托展开：排列 $p$ 在所有长度为 $n$ 的排列中的字典序（从 $0$ 开始标号）。其即
$$\sum_{i=1}^n (i-1)!t_i$$

- 逆序数的期望：转为逆序数列 $t$ 元素的期望，即
  $$E(\tau)=\sum_{i=1}^nE(t_i)=\sum_{i=1}^n\dfrac{\dfrac{n!}{i}\displaystyle\sum_{k=0}^{i-1}k}{n!}=\sum_{i=1}^n\dfrac{1}{i}\times\dfrac{i(i-1)}{2}$$
  $$=\sum_{i=1}^n\dfrac{i-1}{2}=\dfrac{1}{2}\sum_{i=0}^{n-1}i=\dfrac{1}{2}\times\dfrac{n(n-1)}{2}=\dfrac{n(n-1)}{4}$$
