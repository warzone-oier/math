## 求和符号（大型运算符）

使用省略号表示求和和求积会显得表达式非常冗长。欧拉引入了 $\sum$ 和 $\prod$ 表示求和和求积

$$\sum_{k=l}^rf(k)=f(l)+f(l+1)+\cdots+f(r)$$

$$\prod_{k=l}^rf(k)=f(l)f(l+1)\cdots f(r)$$

其中 $l,r$ 可以取整数或 $\pm\infty$。  
作为一种简写，$\sum$ 的优先级与乘法同级，例如多个元素的乘法分配律可以写成

$$a\sum_{k=l}^r(f(k)+g(k))=a\sum_{k=l}^r f(k)+a\sum_{k=l}^r g(k)$$

显然，求和的上下限 $l,r$ 一般满足 $l\le r$。但我们也可以将符号推广到 $l>r$ 的情形

$$\sum_{k=l}^{l-1}f(k)=0,\prod_{k=l}^{l-1}f(k)=1$$

$$\sum_{k=r}^l f(k)=-\sum_{k=l+1}^{r-1}f(k),\prod_{k=r}^lf(k)=\dfrac{1}{\displaystyle\prod_{k=l+1}^{r-1}f(k)}$$

-------------------------------------------------------------

通过上下限 $l,r$ 来定义求和的一大问题在于 **求和的元素必须是有序的**。  
在加法交换律和加法结合律的基础上，我们可以引入 **广义求和**：  
$\displaystyle\sum_{P(a)}f(a)$ 表示对所有满足性质 $P$ 的元素 $a$，求 $f(a)$ 的和。例如

$$\sum_{k\texttt{是整数且}1\le k\le 5}2^k=2^1+2^2+2^3+2^4+2^5$$

**以上所有讨论适用于一切满足交换律和结合律的运算**。例如，在定义了并集 $\cup$ 的基础上，可以定义

$$\bigcup_{k=l}^rA_k=A_l\cup A_{l+1}\cup\cdots\cup A_r$$

## 逻辑基础

为了表示分段函数，我们通常用中括号将一个命题括起来组成一个函数，  
表示如果这个命题为真则函数值为 $1$，否则函数值为 $0$，如

$$[x<5]=\begin{cases}1&(x<5)\\0&(x\ge 5)\end{cases}$$

在此基础上，便有只涉及 $0$（假）、$1$（真）的逻辑代数，称为 **布尔代数** 或 **命题逻辑**。

在布尔代数中，我们有以下逻辑运算：

- $\land$：逻辑与（合取），当且仅当 $a=b=1$ 时 $a\land b=1$，否则 $a\land b=0$
- $\lor$：逻辑或（析取），当且仅当 $a=b=0$ 时 $a\land b=0$，否则 $a\land b=1$  
  显然，$\land$ 和 $\lor$ 满足
  - 交换律：$a\land b=b\land a,a\lor b=b\lor a$
  - 结合律：$(a\land b)\land c=a\land(b\land c)=a\land b\land c$  
    $\qquad\quad\ (a\lor b)\lor c=a\lor(b\lor c)=a\lor b\lor c$
  - 分配律：$(a\land b)\lor c=(a\lor c)\land(b\lor c)$  
    $\qquad\quad\ (a\lor b)\land c=(a\land c)\lor (b\land c)$
- $\lnot$：逻辑非，$\lnot 1=0,\lnot 0=1$。逻辑非满足
  - $\lnot(\lnot a)=a$
  - **德摩根律**：$\lnot(a\land b)=(\lnot a)\lor(\lnot b),\lnot(a\lor b)=(\lnot a)\land(\lnot b)$
  
- $\oplus$：异或，当且仅当 $a\not=b$ 时 $a\oplus b=1$，否则 $a\oplus b=0$。该运算可以用与、或、非表示
  $$a\oplus b=(a\lor b)\land\lnot(a\land b)$$
  $\qquad$ 异或可以视为不进位的加法，逻辑与可以视为不进位的乘法。这意味着异或满足
  - 交换律：$a\oplus b=b\oplus a$
  - 结合律：$(a\oplus b)\oplus c=a\oplus (b\oplus c)=a\oplus b\oplus c$
  - 分配律：$(a\oplus b)\land c=(a\land c)\oplus (b\land c)$
- $\rightarrow$：蕴含运算，当 $a=0$ 时不论 $b$ 为何值 $a\rightarrow b$ 均为 $1$（“太阳从西边出来”），  
  否则只有 $b=1$ 时 $a\rightarrow b=1$。该运算同样可以用与、或、非表示  
  $$a\rightarrow b=(\lnot a)\lor b$$
  蕴含满足的一条核心性质是
  $$p\rightarrow q=(\lnot q)\rightarrow(\lnot p)$$
  其实现了一个命题与其逆否命题的转换。
- $\leftrightarrow$：等价运算。$p\leftrightarrow q=(p\rightarrow q)\land(q\rightarrow p)$


### 主析取范式
根据布尔代数的运算律，布尔代数的表达式可以只由 $\land,\lor,\lnot$ 三种逻辑运算组成。  
我们需要解决以下两个问题：

1. 给定两个布尔表达式 $P(x_1,x_2,\cdots,x_n),Q(x_1,x_2,\cdots,x_n)$，如何把它们化简成相同的形式？
2. 已知含 $n$ 个变量 $x_1,x_2,\cdots,x_n$ 的布尔表达式 $P(x_1,x_2,\cdots,x_n)$ 的所有 $2^n$ 种函数值，  
   如何用 $\land,\lor,\lnot$ 把这个表达式表示出来？

显然，对于问题 2.，其答案为

$$P(x_1,x_2,\cdots,x_n)=\mathop{\huge\lor}\limits_{P(c_1,c_2,\cdots,c_n)=1}\bigwedge_{k=1}^n({c_k}\oplus x_k)$$

该式称为 $P(x_1,x_2,\cdots,x_n)$ 的 **主析取范式**。其中

$$0\oplus x_k=x_k,1\oplus x_k=\lnot x_k$$

显然，主析取范式与逻辑表达式的取值一一对应，且任意逻辑表达式都可以由以下步骤转换为主析取范式：

1. 将 $\oplus,\rightarrow,\leftrightarrow$ 用 $\land,\lor,\lnot$ 表示。
2. 使用德摩根律，保证 $\lnot$ 只能作用在单个变量 $x_k$ 上。
3. 使用 $\land$ 关于 $\lor$ 的分配律，保证 $\lor$ 只出现在表达式的最外层。
4. 检查所有 $\bigwedge$，如果某个 $\bigwedge$ 中没有出现 $x_k$，则添加 $(x_k\lor \lnot x_k)$。
5. 使用 $\land$ 关于 $\lor$ 的分配律将 $\lor$ 拆到表达式最外层，即可得到主析取范式。

### 谓词逻辑

命题逻辑只能处理有限个对象。为了处理无限个对象（如自然数）的命题，需要引入 **谓词逻辑**。
要引入谓词逻辑，首先要确定变量的取值范围，称为 **论域**。在此基础上，即可引入

- $\forall$：全程量词，$\forall xP(x)$ 表示对于论域中任意个体 $x$，命题 $P(x)$ 都成立
- $\exists$：存在量词，$\exists xP(x)$ 表示存在论域中某一个体 $x$，满足命题 $P(x)$

全称量词欲存在量词互为否命题。即

$$\lnot\forall xP(x)\Leftrightarrow \exists x\lnot P(x)$$

集合论中经常出现在使用量词时临时限定论域为某个集合的情况，  
这总是可以改写为论域为整个集合论宇宙的情况

$$\forall A\in S,P(A)\Leftrightarrow \forall A((A\in S)\rightarrow P(A))$$

$$\exists A\in S,P(A)\Leftrightarrow \exist A((A\in S)\land P(A))$$

谓词逻辑又称 **一阶逻辑**，这是因为命题的参数只能是个体。在此基础上，如果允许“命题的命题”存在，  
则称为 **二阶逻辑**，允许 “命题的命题的命题”存在则成为三阶逻辑 $\cdots$，以此类推。

数学归纳法是一个典型的二阶逻辑命题：

$$\forall P(x)(\left(P(0)\land\forall n(((n\in\N)\land P(n))\rightarrow P(n+1))\right)\rightarrow\forall n(n\in \N\rightarrow P(n)))$$

## 映射与基数

定义集合的 **笛卡尔积** 为将集合中元素配成有序对

$$A\times B=\{(a,b)|a\in A,b\in B\}$$

$A\times B$ 的子集称为 $A,B$ 间的 **关系**。在此基础上，就可以定义 **映射**  $f:A\rightarrow B$ 为一一对应的关系

$$f(a)=b\Leftrightarrow \{y\in B|(a,y)\in f\}=\{b\}$$

进一步地，对于映射 $f:A\rightarrow B$，

- 如果 $B$ 中每个元素至多对应 $A$ 中一个元素，则称 $f$ 为 **单射**；  
- 如果 $B$ 中每个元素至少对应 $A$ 中一个元素，则称 $f$ 为 **满射**；
- 如果 $B$ 中每个元素恰好对应 $A$ 中一个元素，则称 $f$ 为 **双射**；

以下几个术语是映射的同义词，经常混用：

- 函数：一般指数到数的映射
- 泛函：一般指函数到数的映射
- 算子：一般指函数到函数的映射
- 变换：集合到自身的映射

所有映射都具备 **复合** 这一运算。我们定义 $A\times B$ 上关系 $f$ 和 $B\times C$ 上的关系 $g$ 的复合为

$$f\circ g=\{(a,c)\in A\times C|\exist b\in B,((a,b)\in f)\land((b,c)\in g)\}$$

映射的复合就是关系的复合在映射上的情形

$$(f\circ g)(x)=f(g(x))$$

显然，**映射的复合满足结合律，不满足交换律**。  
以后的讲解将指出一切满足结合律的运算本质上都是某种映射的复合，包括关系的复合本身。

---------------------------------

接下来我们讨论一个重要的问题：如何比较两个集合的大小。

这个问题的关键在于需要给出集合大小的定义。  
定义 $|A|\le |B|$ 表示集合 $A$ 的大小小于等于 $B$，则这一关系需要满足

- 自反性：$|A|\le |A|$
- 传递性：$|A|\le |B|,|B|\le |C|\rightarrow |A|\le |C|$

我们发现，单射的存在性恰好满足以上两条性质。  
于是我们定义，$|A|\le |B|$ 当且仅当存在 $A\rightarrow B$ 的单射，称为 **$A$ 的基数小于等于 $B$**。

> 练习：试证明
> 1. 试证明奇数集、偶数集、整数集 $\Z$ 的基数均与自然数集 $\N$ 相等。
> 2. 试证明有理数集 $\mathbb{Q}$ 的基数与自然数集 $\N$ 相等。（提示：唯一分解定理）

于是定义自然数集 $\N$ 的基数为 **阿列夫零**，记为 $\aleph_0$。  
我们称一个集合 **可数** 当且仅当其基数与 $\N$ 相等。

> 求证：实数集的基数 $\R$ 的基数严格大于自然数集 $\N$。  
> 证明：首先通过函数 $\dfrac{1}{2}+\dfrac{1}{\pi}\arctan x$，可知实数集 $\R$ 的基数与 $(0,1)$ 相等，  
> $\qquad$ 通过提取出 $[0,1]$ 中的可数子集（如 $\{2^{-n}|n\in\N\}$），可知 $(0,1)$ 的基数与 $[0,1]$ 相等。  
> $\qquad$ 假设存在 $\N\rightarrow [0,1]$ 的双射，其中 $n\in\N$ 对应 $[0,1]$ 中的实数 $a_n$。  
> $\qquad$ 显然 $a_n$ 可以用二进制小数表示
> $$a_n=0.a_{n,0}a_{n,1}a_{n,2}\cdots$$
> $\qquad$ 则我们可以构造一个新实数 $b=0.b_0b_1b_2\cdots$ 满足
> $$\forall n\in\N,b_n=1-a_{n,n}$$
> $\qquad$ 显然这个实数是一个在 $[0,1]$ 中，与 $a_0,a_1,a_2,\cdots$ 均不相等的实数，  
> $\qquad$ 这与 $a$ 是双射矛盾。这就是著名的 **对角论证法**。

由以上定理，我们称实数集的基数为 **连续统**。

我们定义一个集合 $S$ 的幂集为 $S$ 的所有子集组成的集合

$$2^S=\{T|T\subseteq S\}$$

则同样可以用对角论证法证明对于任意集合 $S$ 均有 $|S|<|2^S|$。  
于是在阿列夫零的基础上可以定义阿列夫一、阿列夫二$\cdots$

$$\aleph_1=2^{\aleph_0},\aleph_2=2^{\aleph_1},\cdots$$

于是一个自然的假设是，连续统是否就是阿列夫一？这一假设称为 **连续统假设**。

## 公理化集合论

朴素集合论存在罗素悖论这一著名的逻辑错误。

> **罗素悖论**：在朴素集合论中，满足任意一种性质的所有对象都可组成集合。即集合总是能定义为  
> $$\{x|P(x)\}$$
> $\qquad\quad P$ 为某一种性质。这被称为概括原则。  
> $\qquad\quad$ 然而，我们可以构造集合
> $$S=\{x|x\not\in S\}$$
> $\qquad\quad$ 现在的问题是，$S$ 是否属于它自己？答案是两者都不对。  
> $\qquad\quad$ 根据逻辑的排中律这一更基本的公理，集合之间要么属于，要么不属于，  
> $\qquad\quad$ 因此，这种集合是不存在的，即概括原则是错的。 

为克服这一逻辑错误，人们构建了形形色色的公理化集合论。  

### ZF 公理系统
ZF 公理系统是最为早，最著名的集合论公理系统。  
该公理系统的特点是**集合的元素还是集合**。    
其包括下面的 7 条公理：

1. 外延公理：集合 $A,B$ 相等当且仅当其元素相等。  
   也即，我们只关心“集合”这一对象是否具有给定元素，而不关心其一切其他性质。
2. 空集公理：存在空集 $\varnothing$，其没有元素。  
   空集是一切集合结构的起点。
3. 分离公理：任意集合 $A$ 和性质 $P$ 都对应一个集合
   $$B=\{x\in A|P(x)\}$$
   分离公理允许我们求集合的某个子集（比如补集），并对概括原则作出了第一步约束：  
   绝大部分情况下，只有性质得不到集合，还需要一个已有的集合才行。    

接下来的 3 个公理描述了直接用某些特定的性质得到集合的情况：

4. 并集公理：对于集合的集合 $M$，存在并集
   $$\cup M=\bigcup_{S\in M}S=\{x\in S|S\in M\}$$
   结合并集公理和分离公理，可定义集合 $M$ 的交集
   $$\cap M=\bigcup_{S\in M}S=\{x\in\cup M|\forall S((S\in M)\rightarrow (x\in S))\}$$
5. 配对公理：对于任意集合 $X,Y$，存在集合 $Z=\begin{cases}
	\{X,Y\}&(X\not =Y)\\
	\{X\}&(X=Y)
\end{cases}$。  
   配对公理给出了无序对，在此基础上，我们可以定义有序对
   $$(X,Y)=\begin{cases}\{\{X\},\{X,Y\}\}&(X\not=Y)\\\{\{X\}\}&(X=Y)\end{cases}$$
   结合配对公理和并集、交集，就可以给出有限个集合的并、交、对称差
   $$A\cap B=\cap\{A,B\},A\cup B=\cup\{A,B\}$$
   $$A\Delta B=\complement_{A\cup B}(A\cap B)$$
   以及有限个集合组成的集合
   $$\{S_1,S_2,\cdots,S_n\}=\cup\{\{S_1,S_2,\cdots,S_{n-1}\},\{S_n\}\}$$
6. 幂集公理：对于集合 $S$，存在幂集 $2^{S}=\{T|T\subseteq S\}$  
   结合分离公理，幂集公理和有序对，就可以引入笛卡尔积
   $$A\times B=\{p\in2^{2^{X\cup Y}}|p=(x,y)\land(x\in A)\land(y\in B)\}$$

有了以上几条公理，罗素悖论就被彻底规避了。  
接下来的正则公理进一步限定了集合的来源：

7. 正则公理：对于任意非空集合 $X$，存在 $Y\in X$ 使得 $X\cap Y=\varnothing$ 。  
   正则公理的本质意义是：所有集合归根结底都是从空集构造出来的。    
   例如，可以推出不存在集合 $S$ 使得 $S\in S$，因为如果存在这样的集合 $S$，  
   就可以用分离公理在 $S$ 中提取出 $\{S\}$，但 $\{S\}$ 仅有
   $$S\in\{S\},S\cap\{S\}=\{S\}\not=\varnothing$$
   这与正则公理相矛盾。  
   另外，正则公理从某种意义上来说不是必须的，  
   去掉它只会让我们得到一些奇奇怪怪的结构，但是不会让这个结构本身是不一致的。
   
接下来公理给出了自然数集的构造：

8. 无穷公理：设集合 $X$ 的后继 $X^{++}=X\cup\{X\}$，若集合 $S$ 满足
   - $\varnothing\in S$
   - $\forall X\in S,X^{++}\in S$  
  
   则称 $S$ 为归纳集。归纳集一定存在。  
   有了无穷公理，就可以将自然数集定义为最小归纳集  
   （分离公理作用于某一归纳集得到）：
   $$\N=\{\varnothing,\{\varnothing\},\{\varnothing,\{\varnothing\}\},\cdots\}$$
   其元素就是我们用符号 $0,1,2,\cdots$ 表示的自然数。

最后还有一条公理，在构建超现实数时会用到：

9. 替换公理：若
    $$\forall x\in X,\exist\text{唯一的}y,P(x,y)\text{成立}$$
	则存在集合
	$$\{y|\exist x\in X,P(x,y)\text{成立}\}$$

### 选择公理

选择公理是要解决这样一个问题：如何从无限多双袜子中选择一双？  
也就是说，给定无限多个非空集合组成的集合 $M$。是否存在 **选择函数** $f:M\rightarrow \cup M$，满足

$$\forall X\in M,f(X)\in X$$

如果是无限多双鞋子，存在具体的选择方法：选择左脚的。  
这个时候通过分离公理即可得到选择函数。

如果是“无限多双袜子”这样的情况，不存在具体的选择方法。
选择公理断言，这样的选择函数依然存在，即我们可以“随便选一个”。

选择公理 AC 与 ZF 公理系统相独立，二者可共同构成最常用的集合论公理系统 ZFC。

> 注：以下情况用不到选择公理：  
> 1. $M$ 只有有限个元素：这个时候可以直接用存在量词 $\exist$ 做选择。
> 2. 存在具体的选择方法：直接使用分离公理。

选择公理与以下命题等价：

1. 无限多个非空集合的笛卡尔积非空
2. 基数比较的连接性：对于任意两个集合，要么存在 $A\rightarrow B$ 的单射，  
   要么存在 $B\rightarrow A$ 的单射
3. 任意线性空间都存在基

但是选择公理可以导出

> 巴拿赫-塔斯基分球怪论：可以通过有限次切割、旋转和平移，将一个三维实心球分成两个一模一样的球。

故相当一部分数学家不承认选择公理。

本讲义默认 $M$ 为可数集时选择公理成立，即 **可数选择公理**。